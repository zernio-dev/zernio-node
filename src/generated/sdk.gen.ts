// This file is auto-generated by @hey-api/openapi-ts

import { createClient, createConfig, type OptionsLegacyParser, formDataBodySerializer } from '@hey-api/client-fetch';
import type { ValidatePostLengthData, ValidatePostLengthError, ValidatePostLengthResponse, ValidatePostData, ValidatePostError, ValidatePostResponse, ValidateMediaData, ValidateMediaError, ValidateMediaResponse, ValidateSubredditData, ValidateSubredditError, ValidateSubredditResponse, GetAnalyticsData, GetAnalyticsError, GetAnalyticsResponse, GetYouTubeChannelInsightsData, GetYouTubeChannelInsightsError, GetYouTubeChannelInsightsResponse, GetLinkedInOrgAggregateAnalyticsData, GetLinkedInOrgAggregateAnalyticsError, GetLinkedInOrgAggregateAnalyticsResponse, GetTikTokAccountInsightsData, GetTikTokAccountInsightsError, GetTikTokAccountInsightsResponse, GetYouTubeDailyViewsData, GetYouTubeDailyViewsError, GetYouTubeDailyViewsResponse, GetYouTubeVideoRetentionData, GetYouTubeVideoRetentionError, GetYouTubeVideoRetentionResponse, GetFacebookPageInsightsData, GetFacebookPageInsightsError, GetFacebookPageInsightsResponse, GetFacebookPostEarningsData, GetFacebookPostEarningsError, GetFacebookPostEarningsResponse, GetInstagramAccountInsightsData, GetInstagramAccountInsightsError, GetInstagramAccountInsightsResponse, GetInstagramFollowerHistoryData, GetInstagramFollowerHistoryError, GetInstagramFollowerHistoryResponse, GetInstagramDemographicsData, GetInstagramDemographicsError, GetInstagramDemographicsResponse, GetYouTubeDemographicsData, GetYouTubeDemographicsError, GetYouTubeDemographicsResponse, GetDailyMetricsData, GetDailyMetricsError, GetDailyMetricsResponse, GetBestTimeToPostData, GetBestTimeToPostError, GetBestTimeToPostResponse, GetContentDecayData, GetContentDecayError, GetContentDecayResponse, GetPostingFrequencyData, GetPostingFrequencyError, GetPostingFrequencyResponse, GetPostTimelineData, GetPostTimelineError, GetPostTimelineResponse, GetGoogleBusinessPerformanceData, GetGoogleBusinessPerformanceError, GetGoogleBusinessPerformanceResponse, GetGoogleBusinessSearchKeywordsData, GetGoogleBusinessSearchKeywordsError, GetGoogleBusinessSearchKeywordsResponse, GetInboxVolumeData, GetInboxVolumeError, GetInboxVolumeResponse, GetInboxHeatmapData, GetInboxHeatmapError, GetInboxHeatmapResponse, GetInboxSourceBreakdownData, GetInboxSourceBreakdownError, GetInboxSourceBreakdownResponse, GetInboxResponseTimeData, GetInboxResponseTimeError, GetInboxResponseTimeResponse, GetInboxTopAccountsData, GetInboxTopAccountsError, GetInboxTopAccountsResponse, ListInboxConversationAnalyticsData, ListInboxConversationAnalyticsError, ListInboxConversationAnalyticsResponse, GetInboxConversationAnalyticsData, GetInboxConversationAnalyticsError, GetInboxConversationAnalyticsResponse, ListAccountGroupsError, ListAccountGroupsResponse, CreateAccountGroupData, CreateAccountGroupError, CreateAccountGroupResponse, UpdateAccountGroupData, UpdateAccountGroupError, UpdateAccountGroupResponse, DeleteAccountGroupData, DeleteAccountGroupError, DeleteAccountGroupResponse, GetMediaPresignedUrlData, GetMediaPresignedUrlError, GetMediaPresignedUrlResponse, SearchRedditData, SearchRedditError, SearchRedditResponse, GetRedditFeedData, GetRedditFeedError, GetRedditFeedResponse, GetBillingError, GetBillingResponse, GetXApiPricingError, GetXApiPricingResponse, GetUsageData, GetUsageError, GetUsageResponse, GetUsageStatsData, GetUsageStatsError, GetUsageStatsResponse, GetCallsUsageData, GetCallsUsageError, GetCallsUsageResponse, GetSmsUsageData, GetSmsUsageError, GetSmsUsageResponse, ListPostsData, ListPostsError, ListPostsResponse, CreatePostData, CreatePostError, CreatePostResponse, SyncExternalPostsData, SyncExternalPostsError, SyncExternalPostsResponse, GetPostData, GetPostError, GetPostResponse, UpdatePostData, UpdatePostError, UpdatePostResponse, DeletePostData, DeletePostError, DeletePostResponse, BulkUploadPostsData, BulkUploadPostsError, BulkUploadPostsResponse, RetryPostData, RetryPostError, RetryPostResponse, UnpublishPostData, UnpublishPostError, UnpublishPostResponse, EditPostData, EditPostError, EditPostResponse, UpdatePostMetadataData, UpdatePostMetadataError, UpdatePostMetadataResponse, ListUsersError, ListUsersResponse, GetUserData, GetUserError, GetUserResponse, ListProfilesData, ListProfilesError, ListProfilesResponse, CreateProfileData, CreateProfileError, CreateProfileResponse, GetProfileData, GetProfileError, GetProfileResponse, UpdateProfileData, UpdateProfileError, UpdateProfileResponse, DeleteProfileData, DeleteProfileError, DeleteProfileResponse, ListAccountsData, ListAccountsError, ListAccountsResponse, GetFollowerStatsData, GetFollowerStatsError, GetFollowerStatsResponse, UpdateAccountData, UpdateAccountError, UpdateAccountResponse, MoveAccountToProfileData, MoveAccountToProfileError, MoveAccountToProfileResponse, DeleteAccountData, DeleteAccountError, DeleteAccountResponse, GetAllAccountsHealthData, GetAllAccountsHealthError, GetAllAccountsHealthResponse, RegisterWhatsAppNumberData, RegisterWhatsAppNumberError, RegisterWhatsAppNumberResponse, GetAccountHealthData, GetAccountHealthError, GetAccountHealthResponse, GetInstagramFollowStatusData, GetInstagramFollowStatusError, GetInstagramFollowStatusResponse, GetTikTokCreatorInfoData, GetTikTokCreatorInfoError, GetTikTokCreatorInfoResponse, VerifyCredentialError, VerifyCredentialResponse, ListApiKeysError, ListApiKeysResponse, CreateApiKeyData, CreateApiKeyError, CreateApiKeyResponse, DeleteApiKeyData, DeleteApiKeyError, DeleteApiKeyResponse, ListConnectedAppsError, ListConnectedAppsResponse, RevokeConnectedAppData, RevokeConnectedAppError, RevokeConnectedAppResponse, CreateInviteTokenData, CreateInviteTokenError, CreateInviteTokenResponse, GetConnectUrlData, GetConnectUrlError, GetConnectUrlResponse, HandleOAuthCallbackData, HandleOAuthCallbackError, HandleOAuthCallbackResponse, ConnectAdsData, ConnectAdsError, ConnectAdsResponse, GetShopifyConnectUrlData, GetShopifyConnectUrlError, GetShopifyConnectUrlResponse, ConfigureTikTokAdsBrandIdentityData, ConfigureTikTokAdsBrandIdentityError, ConfigureTikTokAdsBrandIdentityResponse, ListFacebookPagesData, ListFacebookPagesError, ListFacebookPagesResponse, SelectFacebookPageData, SelectFacebookPageError, SelectFacebookPageResponse, ListInstagramPagesData, ListInstagramPagesError, ListInstagramPagesResponse, SelectInstagramAccountData, SelectInstagramAccountError, SelectInstagramAccountResponse, ListGoogleBusinessLocationsData, ListGoogleBusinessLocationsError, ListGoogleBusinessLocationsResponse, SelectGoogleBusinessLocationData, SelectGoogleBusinessLocationError, SelectGoogleBusinessLocationResponse, GetGoogleBusinessReviewsData, GetGoogleBusinessReviewsError, GetGoogleBusinessReviewsResponse, GetGoogleBusinessVerificationsData, GetGoogleBusinessVerificationsError, GetGoogleBusinessVerificationsResponse, StartGoogleBusinessVerificationData, StartGoogleBusinessVerificationError, StartGoogleBusinessVerificationResponse, FetchGoogleBusinessVerificationOptionsData, FetchGoogleBusinessVerificationOptionsError, FetchGoogleBusinessVerificationOptionsResponse, CompleteGoogleBusinessVerificationData, CompleteGoogleBusinessVerificationError, CompleteGoogleBusinessVerificationResponse, GetGoogleBusinessFoodMenusData, GetGoogleBusinessFoodMenusError, GetGoogleBusinessFoodMenusResponse, UpdateGoogleBusinessFoodMenusData, UpdateGoogleBusinessFoodMenusError, UpdateGoogleBusinessFoodMenusResponse, GetGoogleBusinessLocationDetailsData, GetGoogleBusinessLocationDetailsError, GetGoogleBusinessLocationDetailsResponse, UpdateGoogleBusinessLocationDetailsData, UpdateGoogleBusinessLocationDetailsError, UpdateGoogleBusinessLocationDetailsResponse, ListGoogleBusinessMediaData, ListGoogleBusinessMediaError, ListGoogleBusinessMediaResponse, CreateGoogleBusinessMediaData, CreateGoogleBusinessMediaError, CreateGoogleBusinessMediaResponse, DeleteGoogleBusinessMediaData, DeleteGoogleBusinessMediaError, DeleteGoogleBusinessMediaResponse, GetGmbAttributeMetadataData, GetGmbAttributeMetadataError, GetGmbAttributeMetadataResponse, GetGoogleBusinessAttributesData, GetGoogleBusinessAttributesError, GetGoogleBusinessAttributesResponse, UpdateGoogleBusinessAttributesData, UpdateGoogleBusinessAttributesError, UpdateGoogleBusinessAttributesResponse, ListGoogleBusinessPlaceActionsData, ListGoogleBusinessPlaceActionsError, ListGoogleBusinessPlaceActionsResponse, CreateGoogleBusinessPlaceActionData, CreateGoogleBusinessPlaceActionError, CreateGoogleBusinessPlaceActionResponse, DeleteGoogleBusinessPlaceActionData, DeleteGoogleBusinessPlaceActionError, DeleteGoogleBusinessPlaceActionResponse, UpdateGoogleBusinessPlaceActionData, UpdateGoogleBusinessPlaceActionError, UpdateGoogleBusinessPlaceActionResponse, GetGoogleBusinessServicesData, GetGoogleBusinessServicesError, GetGoogleBusinessServicesResponse, UpdateGoogleBusinessServicesData, UpdateGoogleBusinessServicesError, UpdateGoogleBusinessServicesResponse, BatchGetGoogleBusinessReviewsData, BatchGetGoogleBusinessReviewsError, BatchGetGoogleBusinessReviewsResponse, ReplyToGoogleBusinessReviewData, ReplyToGoogleBusinessReviewError, ReplyToGoogleBusinessReviewResponse, DeleteGoogleBusinessReviewReplyData, DeleteGoogleBusinessReviewReplyError, DeleteGoogleBusinessReviewReplyResponse, GetPendingOAuthDataData, GetPendingOAuthDataError, GetPendingOAuthDataResponse, ListLinkedInOrganizationsData, ListLinkedInOrganizationsError, ListLinkedInOrganizationsResponse, SelectLinkedInOrganizationData, SelectLinkedInOrganizationError, SelectLinkedInOrganizationResponse, ListPinterestBoardsForSelectionData, ListPinterestBoardsForSelectionError, ListPinterestBoardsForSelectionResponse, SelectPinterestBoardData, SelectPinterestBoardError, SelectPinterestBoardResponse, ListSnapchatProfilesData, ListSnapchatProfilesError, ListSnapchatProfilesResponse, SelectSnapchatProfileData, SelectSnapchatProfileError, SelectSnapchatProfileResponse, ConnectBlueskyCredentialsData, ConnectBlueskyCredentialsError, ConnectBlueskyCredentialsResponse, ConnectOpenAiAdsCredentialsData, ConnectOpenAiAdsCredentialsError, ConnectOpenAiAdsCredentialsResponse, ConnectWhatsAppCredentialsData, ConnectWhatsAppCredentialsError, ConnectWhatsAppCredentialsResponse, ListWhatsAppPhoneNumbersData, ListWhatsAppPhoneNumbersError, ListWhatsAppPhoneNumbersResponse, CompleteWhatsAppPhoneSelectionData, CompleteWhatsAppPhoneSelectionError, CompleteWhatsAppPhoneSelectionResponse, GetTelegramConnectStatusData, GetTelegramConnectStatusError, GetTelegramConnectStatusResponse, InitiateTelegramConnectData, InitiateTelegramConnectError, InitiateTelegramConnectResponse, CompleteTelegramConnectData, CompleteTelegramConnectError, CompleteTelegramConnectResponse, GetFacebookPagesData, GetFacebookPagesError, GetFacebookPagesResponse, UpdateFacebookPageData, UpdateFacebookPageError, UpdateFacebookPageResponse, GetLinkedInOrganizationsData, GetLinkedInOrganizationsError, GetLinkedInOrganizationsResponse, GetLinkedInAggregateAnalyticsData, GetLinkedInAggregateAnalyticsError, GetLinkedInAggregateAnalyticsResponse, GetLinkedInPostAnalyticsData, GetLinkedInPostAnalyticsError, GetLinkedInPostAnalyticsResponse, GetLinkedInPostReactionsData, GetLinkedInPostReactionsError, GetLinkedInPostReactionsResponse, UpdateLinkedInOrganizationData, UpdateLinkedInOrganizationError, UpdateLinkedInOrganizationResponse, GetLinkedInMentionsData, GetLinkedInMentionsError, GetLinkedInMentionsResponse, ListInstagramStoriesData, ListInstagramStoriesError, ListInstagramStoriesResponse, GetInstagramPublishingLimitData, GetInstagramPublishingLimitError, GetInstagramPublishingLimitResponse, SearchInstagramAudioData, SearchInstagramAudioError, SearchInstagramAudioResponse, GetInstagramAudioData, GetInstagramAudioError, GetInstagramAudioResponse, GetInstagramStoryInsightsData, GetInstagramStoryInsightsError, GetInstagramStoryInsightsResponse, GetPinterestBoardsData, GetPinterestBoardsError, GetPinterestBoardsResponse, UpdatePinterestBoardsData, UpdatePinterestBoardsError, UpdatePinterestBoardsResponse, CreatePinterestBoardData, CreatePinterestBoardError, CreatePinterestBoardResponse, GetYoutubePlaylistsData, GetYoutubePlaylistsError, GetYoutubePlaylistsResponse, UpdateYoutubeDefaultPlaylistData, UpdateYoutubeDefaultPlaylistError, UpdateYoutubeDefaultPlaylistResponse, GetGmbLocationsData, GetGmbLocationsError, GetGmbLocationsResponse, UpdateGmbLocationData, UpdateGmbLocationError, UpdateGmbLocationResponse, AssignGoogleBusinessLocationData, AssignGoogleBusinessLocationError, AssignGoogleBusinessLocationResponse, GetFacebookPostReactionsData, GetFacebookPostReactionsError, GetFacebookPostReactionsResponse, GetRedditSubredditsData, GetRedditSubredditsError, GetRedditSubredditsResponse, UpdateRedditSubredditsData, UpdateRedditSubredditsError, UpdateRedditSubredditsResponse, GetSubredditRulesData, GetSubredditRulesError, GetSubredditRulesResponse, VoteRedditThingData, VoteRedditThingError, VoteRedditThingResponse, GetRedditFlairsData, GetRedditFlairsError, GetRedditFlairsResponse, SetRedditPostFlairData, SetRedditPostFlairError, SetRedditPostFlairResponse, GetSlackSettingsData, GetSlackSettingsError, GetSlackSettingsResponse, UpdateSlackSettingsData, UpdateSlackSettingsError, UpdateSlackSettingsResponse, GetDiscordSettingsData, GetDiscordSettingsError, GetDiscordSettingsResponse, UpdateDiscordSettingsData, UpdateDiscordSettingsError, UpdateDiscordSettingsResponse, GetDiscordChannelsData, GetDiscordChannelsError, GetDiscordChannelsResponse, ListSlackMembersData, ListSlackMembersError, ListSlackMembersResponse, SendDiscordDirectMessageData, SendDiscordDirectMessageError, SendDiscordDirectMessageResponse, ListDiscordGuildRolesData, ListDiscordGuildRolesError, ListDiscordGuildRolesResponse, CreateDiscordGuildRoleData, CreateDiscordGuildRoleError, CreateDiscordGuildRoleResponse, EditDiscordGuildRoleData, EditDiscordGuildRoleError, EditDiscordGuildRoleResponse, DeleteDiscordGuildRoleData, DeleteDiscordGuildRoleError, DeleteDiscordGuildRoleResponse, ListDiscordGuildMembersData, ListDiscordGuildMembersError, ListDiscordGuildMembersResponse, SearchDiscordGuildMembersData, SearchDiscordGuildMembersError, SearchDiscordGuildMembersResponse, GetDiscordGuildMemberData, GetDiscordGuildMemberError, GetDiscordGuildMemberResponse, AddDiscordMemberRoleData, AddDiscordMemberRoleError, AddDiscordMemberRoleResponse, RemoveDiscordMemberRoleData, RemoveDiscordMemberRoleError, RemoveDiscordMemberRoleResponse, DeleteDiscordMessageData, DeleteDiscordMessageError, DeleteDiscordMessageResponse, CrosspostDiscordMessageData, CrosspostDiscordMessageError, CrosspostDiscordMessageResponse, CreateDiscordThreadData, CreateDiscordThreadError, CreateDiscordThreadResponse, ListDiscordPinnedMessagesData, ListDiscordPinnedMessagesError, ListDiscordPinnedMessagesResponse, PinDiscordMessageData, PinDiscordMessageError, PinDiscordMessageResponse, UnpinDiscordMessageData, UnpinDiscordMessageError, UnpinDiscordMessageResponse, ListDiscordScheduledEventsData, ListDiscordScheduledEventsError, ListDiscordScheduledEventsResponse, CreateDiscordScheduledEventData, CreateDiscordScheduledEventError, CreateDiscordScheduledEventResponse, GetDiscordScheduledEventData, GetDiscordScheduledEventError, GetDiscordScheduledEventResponse, UpdateDiscordScheduledEventData, UpdateDiscordScheduledEventError, UpdateDiscordScheduledEventResponse, DeleteDiscordScheduledEventData, DeleteDiscordScheduledEventError, DeleteDiscordScheduledEventResponse, ListQueueSlotsData, ListQueueSlotsError, ListQueueSlotsResponse, CreateQueueSlotData, CreateQueueSlotError, CreateQueueSlotResponse, UpdateQueueSlotData, UpdateQueueSlotError, UpdateQueueSlotResponse, DeleteQueueSlotData, DeleteQueueSlotError, DeleteQueueSlotResponse, PreviewQueueData, PreviewQueueError, PreviewQueueResponse, GetNextQueueSlotData, GetNextQueueSlotError, GetNextQueueSlotResponse, GetWebhookSettingsError, GetWebhookSettingsResponse, CreateWebhookSettingsData, CreateWebhookSettingsError, CreateWebhookSettingsResponse, UpdateWebhookSettingsData, UpdateWebhookSettingsError, UpdateWebhookSettingsResponse, DeleteWebhookSettingsData, DeleteWebhookSettingsError, DeleteWebhookSettingsResponse, GetWebhookLogsData, GetWebhookLogsError, GetWebhookLogsResponse, TestWebhookData, TestWebhookError, TestWebhookResponse, ListLogsData, ListLogsError, ListLogsResponse, ListInboxConversationsData, ListInboxConversationsError, ListInboxConversationsResponse, CreateInboxConversationData, CreateInboxConversationError, CreateInboxConversationResponse, SearchInboxConversationsData, SearchInboxConversationsError, SearchInboxConversationsResponse, GetInboxConversationData, GetInboxConversationError, GetInboxConversationResponse, UpdateInboxConversationData, UpdateInboxConversationError, UpdateInboxConversationResponse, GetInboxConversationMessagesData, GetInboxConversationMessagesError, GetInboxConversationMessagesResponse, SendInboxMessageData, SendInboxMessageError, SendInboxMessageResponse, GetWhatsAppMediaData, GetWhatsAppMediaError, GetWhatsAppMediaResponse, EditInboxMessageData, EditInboxMessageError, EditInboxMessageResponse, DeleteInboxMessageData, DeleteInboxMessageError, DeleteInboxMessageResponse, SendTypingIndicatorData, SendTypingIndicatorError, SendTypingIndicatorResponse, MarkConversationReadData, MarkConversationReadError, MarkConversationReadResponse, AddMessageReactionData, AddMessageReactionError, AddMessageReactionResponse, RemoveMessageReactionData, RemoveMessageReactionError, RemoveMessageReactionResponse, UploadMediaDirectData, UploadMediaDirectError, UploadMediaDirectResponse, GetMessengerMenuData, GetMessengerMenuError, GetMessengerMenuResponse, SetMessengerMenuData, SetMessengerMenuError, SetMessengerMenuResponse, DeleteMessengerMenuData, DeleteMessengerMenuError, DeleteMessengerMenuResponse, GetInstagramIceBreakersData, GetInstagramIceBreakersError, GetInstagramIceBreakersResponse, SetInstagramIceBreakersData, SetInstagramIceBreakersError, SetInstagramIceBreakersResponse, DeleteInstagramIceBreakersData, DeleteInstagramIceBreakersError, DeleteInstagramIceBreakersResponse, GetTelegramCommandsData, GetTelegramCommandsError, GetTelegramCommandsResponse, SetTelegramCommandsData, SetTelegramCommandsError, SetTelegramCommandsResponse, DeleteTelegramCommandsData, DeleteTelegramCommandsError, DeleteTelegramCommandsResponse, GetMessageAttachmentData, GetMessageAttachmentError, GetMessageAttachmentResponse, ListInboxCommentsData, ListInboxCommentsError, ListInboxCommentsResponse, GetInboxPostCommentsData, GetInboxPostCommentsError, GetInboxPostCommentsResponse, ReplyToInboxPostData, ReplyToInboxPostError, ReplyToInboxPostResponse, DeleteInboxCommentData, DeleteInboxCommentError, DeleteInboxCommentResponse, EditInboxCommentData, EditInboxCommentError, EditInboxCommentResponse, SetCommentModerationData, SetCommentModerationError, SetCommentModerationResponse, HideInboxCommentData, HideInboxCommentError, HideInboxCommentResponse, UnhideInboxCommentData, UnhideInboxCommentError, UnhideInboxCommentResponse, LikeInboxCommentData, LikeInboxCommentError, LikeInboxCommentResponse, UnlikeInboxCommentData, UnlikeInboxCommentError, UnlikeInboxCommentResponse, LikePostData, LikePostError, LikePostResponse, UnlikePostData, UnlikePostError, UnlikePostResponse, SendPrivateReplyToCommentData, SendPrivateReplyToCommentError, SendPrivateReplyToCommentResponse, RetweetPostData, RetweetPostError, RetweetPostResponse, UndoRetweetData, UndoRetweetError, UndoRetweetResponse, BookmarkPostData, BookmarkPostError, BookmarkPostResponse, RemoveBookmarkData, RemoveBookmarkError, RemoveBookmarkResponse, FollowUserData, FollowUserError, FollowUserResponse, UnfollowUserData, UnfollowUserError, UnfollowUserResponse, SearchTweetsData, SearchTweetsError, SearchTweetsResponse, GetTweetData, GetTweetError, GetTweetResponse, ListInboxMentionsData, ListInboxMentionsError, ListInboxMentionsResponse, ReplyToMentionData, ReplyToMentionError, ReplyToMentionResponse, ListInboxReviewsData, ListInboxReviewsError, ListInboxReviewsResponse, ReplyToInboxReviewData, ReplyToInboxReviewError, ReplyToInboxReviewResponse, DeleteInboxReviewReplyData, DeleteInboxReviewReplyError, DeleteInboxReviewReplyResponse, GetWhatsAppTemplatesData, GetWhatsAppTemplatesError, GetWhatsAppTemplatesResponse, CreateWhatsAppTemplateData, CreateWhatsAppTemplateError, CreateWhatsAppTemplateResponse, GetWhatsAppTemplateData, GetWhatsAppTemplateError, GetWhatsAppTemplateResponse, UpdateWhatsAppTemplateData, UpdateWhatsAppTemplateError, UpdateWhatsAppTemplateResponse, DeleteWhatsAppTemplateData, DeleteWhatsAppTemplateError, DeleteWhatsAppTemplateResponse, GetWhatsAppCallingConfigData, GetWhatsAppCallingConfigError, GetWhatsAppCallingConfigResponse, EnableWhatsAppCallingLegacyData, EnableWhatsAppCallingLegacyError, EnableWhatsAppCallingLegacyResponse, UpdateWhatsAppCallingLegacyData, UpdateWhatsAppCallingLegacyError, UpdateWhatsAppCallingLegacyResponse, DisableWhatsAppCallingLegacyData, DisableWhatsAppCallingLegacyError, DisableWhatsAppCallingLegacyResponse, GetWhatsAppCallPermissionsData, GetWhatsAppCallPermissionsError, GetWhatsAppCallPermissionsResponse, InitiateWhatsAppCallData, InitiateWhatsAppCallError, InitiateWhatsAppCallResponse, ListWhatsAppCallsData, ListWhatsAppCallsError, ListWhatsAppCallsResponse, GetWhatsAppCallData, GetWhatsAppCallError, GetWhatsAppCallResponse, GetWhatsAppCallRecordingData, GetWhatsAppCallRecordingError, GetWhatsAppCallRecordingResponse, GetWhatsAppCallEstimateData, GetWhatsAppCallEstimateError, GetWhatsAppCallEstimateResponse, ListCallsData, ListCallsError, ListCallsResponse, GetCallData, GetCallError, GetCallResponse, GetCallRecordingData, GetCallRecordingError, GetCallRecordingResponse, CreateVoiceCallData, CreateVoiceCallError, CreateVoiceCallResponse, ListVoiceCallsData, ListVoiceCallsError, ListVoiceCallsResponse, GetVoiceCallData, GetVoiceCallError, GetVoiceCallResponse, EndVoiceCallData, EndVoiceCallError, EndVoiceCallResponse, GetVoiceCallRecordingData, GetVoiceCallRecordingError, GetVoiceCallRecordingResponse, TransferVoiceCallData, TransferVoiceCallError, TransferVoiceCallResponse, GetVoiceCallEstimateData, GetVoiceCallEstimateError, GetVoiceCallEstimateResponse, CreateVoiceWebSessionError, CreateVoiceWebSessionResponse, DialVoiceWebCallData, DialVoiceWebCallError, DialVoiceWebCallResponse, SendSmsData, SendSmsError, SendSmsResponse, LookupSmsNumberData, LookupSmsNumberError, LookupSmsNumberResponse, ListSmsOptOutsData, ListSmsOptOutsError, ListSmsOptOutsResponse, CreateSmsSenderIdData, CreateSmsSenderIdError, CreateSmsSenderIdResponse, ListSmsSenderIdsError, ListSmsSenderIdsResponse, RequestSmsSenderIdLimitIncreaseData, RequestSmsSenderIdLimitIncreaseError, RequestSmsSenderIdLimitIncreaseResponse, DeleteSmsSenderIdData, DeleteSmsSenderIdError, DeleteSmsSenderIdResponse, StartSmsRegistrationData, StartSmsRegistrationError, StartSmsRegistrationResponse, ListSmsRegistrationsData, ListSmsRegistrationsError, ListSmsRegistrationsResponse, PreflightSmsRegistrationData, PreflightSmsRegistrationError, PreflightSmsRegistrationResponse, DeactivateSmsRegistrationData, DeactivateSmsRegistrationError, DeactivateSmsRegistrationResponse, GetSmsRegistrationData, GetSmsRegistrationError, GetSmsRegistrationResponse, VerifySmsRegistrationOtpData, VerifySmsRegistrationOtpError, VerifySmsRegistrationOtpResponse, ResendSmsRegistrationOtpData, ResendSmsRegistrationOtpError, ResendSmsRegistrationOtpResponse, AppealSmsRegistrationData, AppealSmsRegistrationError, AppealSmsRegistrationResponse, RespondToSmsRegistrationReviewData, RespondToSmsRegistrationReviewError, RespondToSmsRegistrationReviewResponse, UploadSmsOptInProofFileData, UploadSmsOptInProofFileError, UploadSmsOptInProofFileResponse, UploadSmsOptInProofData, UploadSmsOptInProofError, UploadSmsOptInProofResponse, ShareSmsRegistrationData, ShareSmsRegistrationError, ShareSmsRegistrationResponse, GetWhatsAppLibraryTemplateData, GetWhatsAppLibraryTemplateError, GetWhatsAppLibraryTemplateResponse, GetWhatsAppBusinessProfileData, GetWhatsAppBusinessProfileError, GetWhatsAppBusinessProfileResponse, UpdateWhatsAppBusinessProfileData, UpdateWhatsAppBusinessProfileError, UpdateWhatsAppBusinessProfileResponse, UploadWhatsAppProfilePhotoData, UploadWhatsAppProfilePhotoError, UploadWhatsAppProfilePhotoResponse, GetWhatsAppDisplayNameData, GetWhatsAppDisplayNameError, GetWhatsAppDisplayNameResponse, UpdateWhatsAppDisplayNameData, UpdateWhatsAppDisplayNameError, UpdateWhatsAppDisplayNameResponse, GetWhatsappBusinessUsernameData, GetWhatsappBusinessUsernameError, GetWhatsappBusinessUsernameResponse, SetWhatsappBusinessUsernameData, SetWhatsappBusinessUsernameError, SetWhatsappBusinessUsernameResponse, DeleteWhatsappBusinessUsernameData, DeleteWhatsappBusinessUsernameError, DeleteWhatsappBusinessUsernameResponse, GetWhatsappBusinessUsernameSuggestionsData, GetWhatsappBusinessUsernameSuggestionsError, GetWhatsappBusinessUsernameSuggestionsResponse, GetWhatsAppNumberInfoData, GetWhatsAppNumberInfoError, GetWhatsAppNumberInfoResponse, GetWhatsAppBlockStatusData, GetWhatsAppBlockStatusError, GetWhatsAppBlockStatusResponse, GetWhatsAppBlockedUsersData, GetWhatsAppBlockedUsersError, GetWhatsAppBlockedUsersResponse, BlockWhatsAppUsersData, BlockWhatsAppUsersError, BlockWhatsAppUsersResponse, UnblockWhatsAppUsersData, UnblockWhatsAppUsersError, UnblockWhatsAppUsersResponse, GetWhatsAppDatasetData, GetWhatsAppDatasetError, GetWhatsAppDatasetResponse, CreateWhatsAppDatasetData, CreateWhatsAppDatasetError, CreateWhatsAppDatasetResponse, ListPhoneNumbersData, ListPhoneNumbersError, ListPhoneNumbersResponse, GetPhoneNumberData, GetPhoneNumberError, GetPhoneNumberResponse, ReleasePhoneNumberData, ReleasePhoneNumberError, ReleasePhoneNumberResponse, PurchasePhoneNumberData, PurchasePhoneNumberError, PurchasePhoneNumberResponse, ListPhoneNumberCountriesError, ListPhoneNumberCountriesResponse, SearchAvailablePhoneNumbersData, SearchAvailablePhoneNumbersError, SearchAvailablePhoneNumbersResponse, CheckPhoneNumberAvailabilityData, CheckPhoneNumberAvailabilityError, CheckPhoneNumberAvailabilityResponse, GetWhatsAppPhoneNumbersData, GetWhatsAppPhoneNumbersError, GetWhatsAppPhoneNumbersResponse, PurchaseWhatsAppPhoneNumberData, PurchaseWhatsAppPhoneNumberError, PurchaseWhatsAppPhoneNumberResponse, ListWhatsAppNumberCountriesError, ListWhatsAppNumberCountriesResponse, SearchAvailableWhatsAppNumbersData, SearchAvailableWhatsAppNumbersError, SearchAvailableWhatsAppNumbersResponse, CheckWhatsAppNumberAvailabilityData, CheckWhatsAppNumberAvailabilityError, CheckWhatsAppNumberAvailabilityResponse, GetPhoneNumberKycFormData, GetPhoneNumberKycFormError, GetPhoneNumberKycFormResponse, SubmitPhoneNumberKycData, SubmitPhoneNumberKycError, SubmitPhoneNumberKycResponse, ViewPhoneNumberKycDocumentData, ViewPhoneNumberKycDocumentError, ViewPhoneNumberKycDocumentResponse, UploadPhoneNumberKycDocumentData, UploadPhoneNumberKycDocumentError, UploadPhoneNumberKycDocumentResponse, ValidatePhoneNumberKycAddressData, ValidatePhoneNumberKycAddressError, ValidatePhoneNumberKycAddressResponse, CreatePhoneNumberKycLinkData, CreatePhoneNumberKycLinkError, CreatePhoneNumberKycLinkResponse, CreatePhoneNumberPortInData, CreatePhoneNumberPortInError, CreatePhoneNumberPortInResponse, ListPhoneNumberPortInsError, ListPhoneNumberPortInsResponse, CheckPhoneNumberPortabilityData, CheckPhoneNumberPortabilityError, CheckPhoneNumberPortabilityResponse, UploadPhoneNumberPortInDocumentData, UploadPhoneNumberPortInDocumentError, UploadPhoneNumberPortInDocumentResponse, GetPhoneNumberPortInRequirementsData, GetPhoneNumberPortInRequirementsError, GetPhoneNumberPortInRequirementsResponse, GetPhoneNumberPortInOrderRequirementsData, GetPhoneNumberPortInOrderRequirementsError, GetPhoneNumberPortInOrderRequirementsResponse, CancelPhoneNumberPortInData, CancelPhoneNumberPortInError, CancelPhoneNumberPortInResponse, ReviewPhoneNumberKycPacketData, ReviewPhoneNumberKycPacketError, ReviewPhoneNumberKycPacketResponse, GetPhoneNumberRemediationData, GetPhoneNumberRemediationError, GetPhoneNumberRemediationResponse, RemediatePhoneNumberData, RemediatePhoneNumberError, RemediatePhoneNumberResponse, ReplyToPhoneNumberReviewerData, ReplyToPhoneNumberReviewerError, ReplyToPhoneNumberReviewerResponse, RespondToPhoneNumberReviewerData, RespondToPhoneNumberReviewerError, RespondToPhoneNumberReviewerResponse, GetWhatsAppNumberKycFormData, GetWhatsAppNumberKycFormError, GetWhatsAppNumberKycFormResponse, SubmitWhatsAppNumberKycData, SubmitWhatsAppNumberKycError, SubmitWhatsAppNumberKycResponse, UploadWhatsAppNumberKycDocumentData, UploadWhatsAppNumberKycDocumentError, UploadWhatsAppNumberKycDocumentResponse, ValidateWhatsAppNumberKycAddressData, ValidateWhatsAppNumberKycAddressError, ValidateWhatsAppNumberKycAddressResponse, CreateWhatsAppNumberKycLinkData, CreateWhatsAppNumberKycLinkError, CreateWhatsAppNumberKycLinkResponse, MoveWhatsAppNumberToProfileData, MoveWhatsAppNumberToProfileError, MoveWhatsAppNumberToProfileResponse, GetWhatsAppNumberRemediationData, GetWhatsAppNumberRemediationError, GetWhatsAppNumberRemediationResponse, RemediateWhatsAppNumberData, RemediateWhatsAppNumberError, RemediateWhatsAppNumberResponse, EnableVoiceOnNumberData, EnableVoiceOnNumberError, EnableVoiceOnNumberResponse, DisableVoiceOnNumberData, DisableVoiceOnNumberError, DisableVoiceOnNumberResponse, EnableSmsOnNumberData, EnableSmsOnNumberError, EnableSmsOnNumberResponse, DisableSmsOnNumberData, DisableSmsOnNumberError, DisableSmsOnNumberResponse, ReuseSmsRegistrationForNumberData, ReuseSmsRegistrationForNumberError, ReuseSmsRegistrationForNumberResponse, GetWhatsAppCallingData, GetWhatsAppCallingError, GetWhatsAppCallingResponse, EnableWhatsAppCallingData, EnableWhatsAppCallingError, EnableWhatsAppCallingResponse, UpdateWhatsAppCallingData, UpdateWhatsAppCallingError, UpdateWhatsAppCallingResponse, DisableWhatsAppCallingData, DisableWhatsAppCallingError, DisableWhatsAppCallingResponse, StartWhatsAppCallerIdVerificationData, StartWhatsAppCallerIdVerificationError, StartWhatsAppCallerIdVerificationResponse, VerifyWhatsAppCallerIdData, VerifyWhatsAppCallerIdError, VerifyWhatsAppCallerIdResponse, GetWhatsAppPhoneNumberData, GetWhatsAppPhoneNumberError, GetWhatsAppPhoneNumberResponse, ReleaseWhatsAppPhoneNumberData, ReleaseWhatsAppPhoneNumberError, ReleaseWhatsAppPhoneNumberResponse, ListWhatsAppSandboxSessionsError, ListWhatsAppSandboxSessionsResponse, CreateWhatsAppSandboxSessionData, CreateWhatsAppSandboxSessionError, CreateWhatsAppSandboxSessionResponse, DeleteWhatsAppSandboxSessionData, DeleteWhatsAppSandboxSessionError, DeleteWhatsAppSandboxSessionResponse, ListWhatsAppGroupChatsData, ListWhatsAppGroupChatsError, ListWhatsAppGroupChatsResponse, CreateWhatsAppGroupChatData, CreateWhatsAppGroupChatError, CreateWhatsAppGroupChatResponse, GetWhatsAppGroupChatData, GetWhatsAppGroupChatError, GetWhatsAppGroupChatResponse, UpdateWhatsAppGroupChatData, UpdateWhatsAppGroupChatError, UpdateWhatsAppGroupChatResponse, DeleteWhatsAppGroupChatData, DeleteWhatsAppGroupChatError, DeleteWhatsAppGroupChatResponse, AddWhatsAppGroupParticipantsData, AddWhatsAppGroupParticipantsError, AddWhatsAppGroupParticipantsResponse, RemoveWhatsAppGroupParticipantsData, RemoveWhatsAppGroupParticipantsError, RemoveWhatsAppGroupParticipantsResponse, CreateWhatsAppGroupInviteLinkData, CreateWhatsAppGroupInviteLinkError, CreateWhatsAppGroupInviteLinkResponse, ListWhatsAppGroupJoinRequestsData, ListWhatsAppGroupJoinRequestsError, ListWhatsAppGroupJoinRequestsResponse, ApproveWhatsAppGroupJoinRequestsData, ApproveWhatsAppGroupJoinRequestsError, ApproveWhatsAppGroupJoinRequestsResponse, RejectWhatsAppGroupJoinRequestsData, RejectWhatsAppGroupJoinRequestsError, RejectWhatsAppGroupJoinRequestsResponse, ListWhatsAppFlowsData, ListWhatsAppFlowsError, ListWhatsAppFlowsResponse, CreateWhatsAppFlowData, CreateWhatsAppFlowError, CreateWhatsAppFlowResponse, GetWhatsAppFlowData, GetWhatsAppFlowError, GetWhatsAppFlowResponse, UpdateWhatsAppFlowData, UpdateWhatsAppFlowError, UpdateWhatsAppFlowResponse, DeleteWhatsAppFlowData, DeleteWhatsAppFlowError, DeleteWhatsAppFlowResponse, GetWhatsAppFlowJsonData, GetWhatsAppFlowJsonError, GetWhatsAppFlowJsonResponse, UploadWhatsAppFlowJsonData, UploadWhatsAppFlowJsonError, UploadWhatsAppFlowJsonResponse, GetWhatsAppFlowPreviewData, GetWhatsAppFlowPreviewError, GetWhatsAppFlowPreviewResponse, ListWhatsAppFlowVersionsData, ListWhatsAppFlowVersionsError, ListWhatsAppFlowVersionsResponse, PublishWhatsAppFlowData, PublishWhatsAppFlowError, PublishWhatsAppFlowResponse, DeprecateWhatsAppFlowData, DeprecateWhatsAppFlowError, DeprecateWhatsAppFlowResponse, SendWhatsAppFlowMessageData, SendWhatsAppFlowMessageError, SendWhatsAppFlowMessageResponse, ListWhatsAppFlowResponsesData, ListWhatsAppFlowResponsesError, ListWhatsAppFlowResponsesResponse, ListContactsData, ListContactsError, ListContactsResponse, CreateContactData, CreateContactError, CreateContactResponse, GetContactData, GetContactError, GetContactResponse, UpdateContactData, UpdateContactError, UpdateContactResponse, DeleteContactData, DeleteContactError, DeleteContactResponse, GetContactChannelsData, GetContactChannelsError, GetContactChannelsResponse, BulkCreateContactsData, BulkCreateContactsError, BulkCreateContactsResponse, SetContactFieldValueData, SetContactFieldValueError, SetContactFieldValueResponse, ClearContactFieldValueData, ClearContactFieldValueError, ClearContactFieldValueResponse, ListCustomFieldsData, ListCustomFieldsError, ListCustomFieldsResponse, CreateCustomFieldData, CreateCustomFieldError, CreateCustomFieldResponse, UpdateCustomFieldData, UpdateCustomFieldError, UpdateCustomFieldResponse, DeleteCustomFieldData, DeleteCustomFieldError, DeleteCustomFieldResponse, ListBroadcastsData, ListBroadcastsError, ListBroadcastsResponse, CreateBroadcastData, CreateBroadcastError, CreateBroadcastResponse, GetBroadcastData, GetBroadcastError, GetBroadcastResponse, UpdateBroadcastData, UpdateBroadcastError, UpdateBroadcastResponse, DeleteBroadcastData, DeleteBroadcastError, DeleteBroadcastResponse, SendBroadcastData, SendBroadcastError, SendBroadcastResponse, ScheduleBroadcastData, ScheduleBroadcastError, ScheduleBroadcastResponse, CancelBroadcastData, CancelBroadcastError, CancelBroadcastResponse, ListBroadcastRecipientsData, ListBroadcastRecipientsError, ListBroadcastRecipientsResponse, AddBroadcastRecipientsData, AddBroadcastRecipientsError, AddBroadcastRecipientsResponse, ListWorkflowsData, ListWorkflowsError, ListWorkflowsResponse, CreateWorkflowData, CreateWorkflowError, CreateWorkflowResponse, GetWorkflowData, GetWorkflowError, GetWorkflowResponse, UpdateWorkflowData, UpdateWorkflowError, UpdateWorkflowResponse, DeleteWorkflowData, DeleteWorkflowError, DeleteWorkflowResponse, ActivateWorkflowData, ActivateWorkflowError, ActivateWorkflowResponse, PauseWorkflowData, PauseWorkflowError, PauseWorkflowResponse, ListWorkflowExecutionsData, ListWorkflowExecutionsError, ListWorkflowExecutionsResponse, TriggerWorkflowData, TriggerWorkflowError, TriggerWorkflowResponse, ListWorkflowExecutionEventsData, ListWorkflowExecutionEventsError, ListWorkflowExecutionEventsResponse, DuplicateWorkflowData, DuplicateWorkflowError, DuplicateWorkflowResponse, ListWorkflowVersionsData, ListWorkflowVersionsError, ListWorkflowVersionsResponse, GetWorkflowVersionData, GetWorkflowVersionError, GetWorkflowVersionResponse, RestoreWorkflowVersionData, RestoreWorkflowVersionError, RestoreWorkflowVersionResponse, ListSequencesData, ListSequencesError, ListSequencesResponse, CreateSequenceData, CreateSequenceError, CreateSequenceResponse, GetSequenceData, GetSequenceError, GetSequenceResponse, UpdateSequenceData, UpdateSequenceError, UpdateSequenceResponse, DeleteSequenceData, DeleteSequenceError, DeleteSequenceResponse, ActivateSequenceData, ActivateSequenceError, ActivateSequenceResponse, PauseSequenceData, PauseSequenceError, PauseSequenceResponse, EnrollContactsData, EnrollContactsError, EnrollContactsResponse, UnenrollContactData, UnenrollContactError, UnenrollContactResponse, ListSequenceEnrollmentsData, ListSequenceEnrollmentsError, ListSequenceEnrollmentsResponse, ListCommentAutomationsData, ListCommentAutomationsError, ListCommentAutomationsResponse, CreateCommentAutomationData, CreateCommentAutomationError, CreateCommentAutomationResponse, GetCommentAutomationData, GetCommentAutomationError, GetCommentAutomationResponse, UpdateCommentAutomationData, UpdateCommentAutomationError, UpdateCommentAutomationResponse, DeleteCommentAutomationData, DeleteCommentAutomationError, DeleteCommentAutomationResponse, ListCommentAutomationLogsData, ListCommentAutomationLogsError, ListCommentAutomationLogsResponse, ListAdsData, ListAdsError, ListAdsResponse, GetAdsSearchTermsData, GetAdsSearchTermsError, GetAdsSearchTermsResponse, ListLocalServicesLeadsData, ListLocalServicesLeadsError, ListLocalServicesLeadsResponse, ListLocalServicesLeadConversationsData, ListLocalServicesLeadConversationsError, ListLocalServicesLeadConversationsResponse, ListAdKeywordsData, ListAdKeywordsError, ListAdKeywordsResponse, ListAdCampaignsData, ListAdCampaignsError, ListAdCampaignsResponse, CreateAdCampaignData, CreateAdCampaignError, CreateAdCampaignResponse, UpdateAdCampaignStatusData, UpdateAdCampaignStatusError, UpdateAdCampaignStatusResponse, UpdateAdCampaignData, UpdateAdCampaignError, UpdateAdCampaignResponse, DeleteAdCampaignData, DeleteAdCampaignError, DeleteAdCampaignResponse, BulkUpdateAdCampaignStatusData, BulkUpdateAdCampaignStatusError, BulkUpdateAdCampaignStatusResponse, DuplicateAdCampaignData, DuplicateAdCampaignError, DuplicateAdCampaignResponse, DuplicateAdSetData, DuplicateAdSetError, DuplicateAdSetResponse, DuplicateAdData, DuplicateAdError, DuplicateAdResponse, GetAdSetDetailsData, GetAdSetDetailsError, GetAdSetDetailsResponse, UpdateAdSetData, UpdateAdSetError, UpdateAdSetResponse, UpdateAdSetStatusData, UpdateAdSetStatusError, UpdateAdSetStatusResponse, GetAdTreeData, GetAdTreeError, GetAdTreeResponse, GetAdsTimelineData, GetAdsTimelineError, GetAdsTimelineResponse, GetAdData, GetAdError, GetAdResponse, UpdateAdData, UpdateAdError, UpdateAdResponse, DeleteAdData, DeleteAdError, DeleteAdResponse, UpdateAdStatusData, UpdateAdStatusError, UpdateAdStatusResponse, GetCampaignAnalyticsData, GetCampaignAnalyticsError, GetCampaignAnalyticsResponse, GenerateAdPreviewsData, GenerateAdPreviewsError, GenerateAdPreviewsResponse, GetAdPreviewsData, GetAdPreviewsError, GetAdPreviewsResponse, GenerateKeywordIdeasData, GenerateKeywordIdeasError, GenerateKeywordIdeasResponse, GenerateKeywordHistoricalMetricsData, GenerateKeywordHistoricalMetricsError, GenerateKeywordHistoricalMetricsResponse, QueryAdInsightsData, QueryAdInsightsError, QueryAdInsightsResponse, CreateAdInsightsReportData, CreateAdInsightsReportError, CreateAdInsightsReportResponse, GetAdInsightsReportData, GetAdInsightsReportError, GetAdInsightsReportResponse, GetAdAnalyticsData, GetAdAnalyticsError, GetAdAnalyticsResponse, GetAdTrackingTagsData, GetAdTrackingTagsError, GetAdTrackingTagsResponse, UpdateAdTrackingTagsData, UpdateAdTrackingTagsError, UpdateAdTrackingTagsResponse, GetAdCommentsData, GetAdCommentsError, GetAdCommentsResponse, ListAdsBusinessCentersData, ListAdsBusinessCentersError, ListAdsBusinessCentersResponse, GetAdsActivityLogData, GetAdsActivityLogError, GetAdsActivityLogResponse, CreateRfPredictionData, CreateRfPredictionError, CreateRfPredictionResponse, GetRfPredictionData, GetRfPredictionError, GetRfPredictionResponse, CancelRfReservationData, CancelRfReservationError, CancelRfReservationResponse, ReserveRfPredictionData, ReserveRfPredictionError, ReserveRfPredictionResponse, ListAdStudiesData, ListAdStudiesError, ListAdStudiesResponse, ListMetaBusinessesData, ListMetaBusinessesError, ListMetaBusinessesResponse, ListAdLabelsData, ListAdLabelsError, ListAdLabelsResponse, ListHighDemandPeriodsData, ListHighDemandPeriodsError, ListHighDemandPeriodsResponse, CreateHighDemandPeriodData, CreateHighDemandPeriodError, CreateHighDemandPeriodResponse, ListAdCreativesData, ListAdCreativesError, ListAdCreativesResponse, CreateAdCreativeData, CreateAdCreativeError, CreateAdCreativeResponse, GetAdCreativeData, GetAdCreativeError, GetAdCreativeResponse, UpdateAdCreativeData, UpdateAdCreativeError, UpdateAdCreativeResponse, DeleteAdCreativeData, DeleteAdCreativeError, DeleteAdCreativeResponse, ListValueRuleSetsData, ListValueRuleSetsError, ListValueRuleSetsResponse, CreateValueRuleSetData, CreateValueRuleSetError, CreateValueRuleSetResponse, GetValueRuleSetData, GetValueRuleSetError, GetValueRuleSetResponse, UpdateValueRuleSetData, UpdateValueRuleSetError, UpdateValueRuleSetResponse, DeleteValueRuleSetData, DeleteValueRuleSetError, DeleteValueRuleSetResponse, GetAdAccountFinanceData, GetAdAccountFinanceError, GetAdAccountFinanceResponse, ListAdAccountsData, ListAdAccountsError, ListAdAccountsResponse, UpdateAdAccountData, UpdateAdAccountError, UpdateAdAccountResponse, GetDsaDefaultsData, GetDsaDefaultsError, GetDsaDefaultsResponse, GetDsaRecommendationsData, GetDsaRecommendationsError, GetDsaRecommendationsResponse, BoostPostData, BoostPostError, BoostPostResponse, CreateStandaloneAdData, CreateStandaloneAdError, CreateStandaloneAdResponse, ListLeadsData, ListLeadsError, ListLeadsResponse, ListLeadFormsData, ListLeadFormsError, ListLeadFormsResponse, CreateLeadFormData, CreateLeadFormError, CreateLeadFormResponse, GetLeadFormData, GetLeadFormError, GetLeadFormResponse, ArchiveLeadFormData, ArchiveLeadFormError, ArchiveLeadFormResponse, ListFormLeadsData, ListFormLeadsError, ListFormLeadsResponse, CreateTestLeadData, CreateTestLeadError, CreateTestLeadResponse, UploadAdImageData, UploadAdImageError, UploadAdImageResponse, ListAdImagesData, ListAdImagesError, ListAdImagesResponse, SearchAdInterestsData, SearchAdInterestsError, SearchAdInterestsResponse, SearchAdTargetingData, SearchAdTargetingError, SearchAdTargetingResponse, EstimateAdReachData, EstimateAdReachError, EstimateAdReachResponse, GetLinkedInBidPricingData, GetLinkedInBidPricingError, GetLinkedInBidPricingResponse, GetLinkedInSupplyForecastData, GetLinkedInSupplyForecastError, GetLinkedInSupplyForecastResponse, ListAdCatalogsData, ListAdCatalogsError, ListAdCatalogsResponse, ListAdCatalogProductSetsData, ListAdCatalogProductSetsError, ListAdCatalogProductSetsResponse, ListAdAudiencesData, ListAdAudiencesError, ListAdAudiencesResponse, CreateAdAudienceData, CreateAdAudienceError, CreateAdAudienceResponse, GetAdAudienceData, GetAdAudienceError, GetAdAudienceResponse, UpdateAdAudienceData, UpdateAdAudienceError, UpdateAdAudienceResponse, DeleteAdAudienceData, DeleteAdAudienceError, DeleteAdAudienceResponse, AddUsersToAdAudienceData, AddUsersToAdAudienceError, AddUsersToAdAudienceResponse, GetConversionsQualityData, GetConversionsQualityError, GetConversionsQualityResponse, SendConversionsData, SendConversionsError, SendConversionsResponse, AdjustConversionsData, AdjustConversionsError, AdjustConversionsResponse, ListConversionDestinationsData, ListConversionDestinationsError, ListConversionDestinationsResponse, CreateConversionDestinationData, CreateConversionDestinationError, CreateConversionDestinationResponse, GetConversionDestinationData, GetConversionDestinationError, GetConversionDestinationResponse, UpdateConversionDestinationData, UpdateConversionDestinationError, UpdateConversionDestinationResponse, DeleteConversionDestinationData, DeleteConversionDestinationError, DeleteConversionDestinationResponse, ListConversionAssociationsData, ListConversionAssociationsError, ListConversionAssociationsResponse, AddConversionAssociationsData, AddConversionAssociationsError, AddConversionAssociationsResponse, RemoveConversionAssociationsData, RemoveConversionAssociationsError, RemoveConversionAssociationsResponse, GetConversionMetricsData, GetConversionMetricsError, GetConversionMetricsResponse, ListWhatsAppConversionsData, ListWhatsAppConversionsError, ListWhatsAppConversionsResponse, SendWhatsAppConversionData, SendWhatsAppConversionError, SendWhatsAppConversionResponse, CreateMessagingAdData, CreateMessagingAdError, CreateMessagingAdResponse, CreateCallAdData, CreateCallAdError, CreateCallAdResponse, CreateCtwaAdData, CreateCtwaAdError, CreateCtwaAdResponse, ListCustomConversionsData, ListCustomConversionsError, ListCustomConversionsResponse, CreateCustomConversionData, CreateCustomConversionError, CreateCustomConversionResponse, ListTrackingTagsData, ListTrackingTagsError, ListTrackingTagsResponse, CreateTrackingTagData, CreateTrackingTagError, CreateTrackingTagResponse, GetTrackingTagData, GetTrackingTagError, GetTrackingTagResponse, UpdateTrackingTagData, UpdateTrackingTagError, UpdateTrackingTagResponse, ListTrackingTagSharedAccountsData, ListTrackingTagSharedAccountsError, ListTrackingTagSharedAccountsResponse, AddTrackingTagSharedAccountData, AddTrackingTagSharedAccountError, AddTrackingTagSharedAccountResponse, RemoveTrackingTagSharedAccountData, RemoveTrackingTagSharedAccountError, RemoveTrackingTagSharedAccountResponse, GetTrackingTagStatsData, GetTrackingTagStatsError, GetTrackingTagStatsResponse, ListBlogsData, ListBlogsError, ListBlogsResponse, CreateBlogData, CreateBlogError, CreateBlogResponse, GetBlogData, GetBlogError, GetBlogResponse, UpdateBlogData, UpdateBlogError, UpdateBlogResponse, DeleteBlogData, DeleteBlogError, DeleteBlogResponse, ListBlogArticlesData, ListBlogArticlesError, ListBlogArticlesResponse, CreateBlogArticleData, CreateBlogArticleError, CreateBlogArticleResponse, GetBlogArticleData, GetBlogArticleError, GetBlogArticleResponse, UpdateBlogArticleData, UpdateBlogArticleError, UpdateBlogArticleResponse, DeleteBlogArticleData, DeleteBlogArticleError, DeleteBlogArticleResponse, CreateVerificationData, CreateVerificationError, CreateVerificationResponse, GetVerificationData, GetVerificationError, GetVerificationResponse, CheckVerificationData, CheckVerificationError, CheckVerificationResponse } from './types.gen';

export const client = createClient(createConfig());

/**
 * Validate character count
 * Check weighted character count per platform and whether the text is within each platform's limit.
 *
 * Twitter/X uses weighted counting (URLs = 23 chars via t.co, emojis = 2 chars). All other platforms use plain character length.
 *
 * Returns counts and limits for all 15 supported platform variants.
 *
 */
export const validatePostLength = <ThrowOnError extends boolean = false>(options: OptionsLegacyParser<ValidatePostLengthData, ThrowOnError>) => {
    return (options?.client ?? client).post<ValidatePostLengthResponse, ValidatePostLengthError, ThrowOnError>({
        ...options,
        url: '/v1/tools/validate/post-length'
    });
};

/**
 * Validate post content
 * Dry-run the full post validation pipeline without publishing. Catches issues like missing media for Instagram/TikTok/YouTube, hashtag limits, invalid thread formats, Facebook Reel requirements, and character limit violations.
 *
 * Accepts the same body as POST /v1/posts. Does NOT validate accounts, process media, or track usage. Account lookups are limit-only: a twitter accountId is resolved, scoped to the caller, only to pick the 280 vs 25000 character limit. Missing, foreign, or invalid ids fall back to 280 and never error.
 *
 * Returns errors for failures and warnings for near-limit content (>90% of character limit).
 *
 */
export const validatePost = <ThrowOnError extends boolean = false>(options: OptionsLegacyParser<ValidatePostData, ThrowOnError>) => {
    return (options?.client ?? client).post<ValidatePostResponse, ValidatePostError, ThrowOnError>({
        ...options,
        url: '/v1/tools/validate/post'
    });
};

/**
 * Validate media URL
 * Check if a media URL is accessible and return metadata (content type, file size) plus per-platform size limit comparisons.
 *
 * Performs a HEAD request (with GET fallback) to detect content type and size. Rejects private/localhost URLs for SSRF protection.
 *
 * Platform limits are sourced from each platform's actual upload constraints.
 *
 */
export const validateMedia = <ThrowOnError extends boolean = false>(options: OptionsLegacyParser<ValidateMediaData, ThrowOnError>) => {
    return (options?.client ?? client).post<ValidateMediaResponse, ValidateMediaError, ThrowOnError>({
        ...options,
        url: '/v1/tools/validate/media'
    });
};

/**
 * Check subreddit existence
 * Check if a subreddit exists and return basic info (title, subscriber count, NSFW status, post types allowed).
 *
 * When accountId is provided, uses authenticated Reddit OAuth API with automatic token refresh (recommended). Falls back to Reddit's public JSON API, which may be unreliable from server IPs. Returns exists: false for private, banned, or nonexistent subreddits.
 *
 */
export const validateSubreddit = <ThrowOnError extends boolean = false>(options: OptionsLegacyParser<ValidateSubredditData, ThrowOnError>) => {
    return (options?.client ?? client).get<ValidateSubredditResponse, ValidateSubredditError, ThrowOnError>({
        ...options,
        url: '/v1/tools/validate/subreddit'
    });
};

/**
 * Get post analytics
 * Returns analytics for posts. With postId, returns a single post. Without it, returns a paginated list with overview stats.
 * Accepts both Zernio Post IDs and External Post IDs (auto-resolved). fromDate defaults to 90 days ago if omitted, max range 366 days.
 * Single post lookups may return 202 (sync pending) or 424 (all platforms failed). For follower stats, use /v1/accounts/follower-stats.
 *
 * LinkedIn personal accounts: Analytics are only available for posts published through Zernio. LinkedIn's API only returns metrics for posts authored by the authenticated user. Organization/company page analytics work for all posts.
 *
 */
export const getAnalytics = <ThrowOnError extends boolean = false>(options?: OptionsLegacyParser<GetAnalyticsData, ThrowOnError>) => {
    return (options?.client ?? client).get<GetAnalyticsResponse, GetAnalyticsError, ThrowOnError>({
        ...options,
        url: '/v1/analytics'
    });
};

/**
 * Get YouTube channel insights
 * Returns channel-scoped aggregate metrics from YouTube Analytics API v2. Saves you
 * from looping /v1/analytics/youtube/daily-views over every video when you only need
 * channel totals.
 *
 * Response shape matches /v1/analytics/instagram/account-insights so the same client
 * handling works. Requires yt-analytics.readonly scope (412 with reauthorizeUrl if
 * missing). Data has a 2-3 day delay (endDate is clamped accordingly). Max 89 days,
 * defaults to last 30 days. Requires the Analytics add-on.
 *
 * NOT exposed: impressions (Studio thumbnail impressions) and impressionsClickThroughRate.
 * YouTube Analytics API v2 does not expose these for any principal type, not channel
 * owners, not Partner Program channels, not content owners with CMS access. The only way
 * to get them is Studio CSV export. This is a Google-side limitation.
 *
 */
export const getYouTubeChannelInsights = <ThrowOnError extends boolean = false>(options: OptionsLegacyParser<GetYouTubeChannelInsightsData, ThrowOnError>) => {
    return (options?.client ?? client).get<GetYouTubeChannelInsightsResponse, GetYouTubeChannelInsightsError, ThrowOnError>({
        ...options,
        url: '/v1/analytics/youtube/channel-insights'
    });
};

/**
 * Get LinkedIn org analytics
 * Returns aggregate analytics for a LinkedIn organization page. Parallel to
 * /v1/accounts/{id}/linkedin-aggregate-analytics (which handles personal accounts only).
 * Backed by LinkedIn's organizationalEntityShareStatistics,
 * organizationalEntityFollowerStatistics, and organizationPageStatistics endpoints.
 *
 * Response shape matches /v1/analytics/instagram/account-insights. Max 89 days,
 * defaults to last 30 days. Requires the Analytics add-on.
 *
 * Scope requirements: r_organization_social, r_organization_followers, and
 * r_organization_admin must all be present on the account. Accounts connected before
 * these scopes were included in the OAuth flow will return 412 with a reauth hint.
 *
 * Enforced by this endpoint:
 * - Page-view metrics accept only metricType=total_value (LinkedIn omits per-day
 * segmentation even when the API is called with DAY granularity, so a time-series
 * response would be meaningless).
 * - Date range capped at 89 days.
 *
 * LinkedIn-side platform limits (not re-enforced here, but worth knowing for larger
 * ranges in a future release):
 * - Follower stats: rolling 12-month window, end must be no later than 2 days ago.
 * - Share stats: rolling 12-month window.
 *
 */
export const getLinkedInOrgAggregateAnalytics = <ThrowOnError extends boolean = false>(options: OptionsLegacyParser<GetLinkedInOrgAggregateAnalyticsData, ThrowOnError>) => {
    return (options?.client ?? client).get<GetLinkedInOrgAggregateAnalyticsResponse, GetLinkedInOrgAggregateAnalyticsError, ThrowOnError>({
        ...options,
        url: '/v1/analytics/linkedin/org-aggregate-analytics'
    });
};

/**
 * Get TikTok account-level insights
 * Returns account-level TikTok insights from /v2/user/info/ (live) plus historical
 * time series joined from Zernio's daily snapshotter (AccountStats).
 *
 * Response shape matches /v1/analytics/instagram/account-insights. Max 89 days,
 * defaults to last 30 days. Requires the Analytics add-on and the user.info.stats
 * scope on the account (412 if missing).
 *
 * Scope intentionally narrow. TikTok's public API exposes only the four counter
 * metrics below. The deep metrics that live in TikTok Studio are NOT available on any
 * public TikTok API, even for Business accounts:
 * - profile_views
 * - account-level impressions / reach
 * - follower inflow / outflow breakdown
 * - video watch time, average watch time, full-watched rate
 * - impression_sources (FYP / Following / Hashtag / Search / Personal profile)
 *
 * TikTok's Research API doesn't expose those fields either, and is restricted to
 * non-commercial academic use per TikTok's eligibility policy. There is no public
 * API workaround. Post-level metrics (views, likes, comments, shares per video) are
 * available via /v1/analytics?postId=... from TikTok's /v2/video/query/.
 *
 */
export const getTikTokAccountInsights = <ThrowOnError extends boolean = false>(options: OptionsLegacyParser<GetTikTokAccountInsightsData, ThrowOnError>) => {
    return (options?.client ?? client).get<GetTikTokAccountInsightsResponse, GetTikTokAccountInsightsError, ThrowOnError>({
        ...options,
        url: '/v1/analytics/tiktok/account-insights'
    });
};

/**
 * Get YouTube daily views
 * Returns daily view counts for a YouTube video including views, watch time, and subscriber changes.
 * Requires yt-analytics.readonly scope (re-authorization may be needed). YouTube finalizes analytics
 * with a ~3-day delay; by default only finalized days are returned, and an explicit endDate can reach
 * into the delay window (see the endDate parameter). Max 90 days, defaults to last 30 days.
 *
 */
export const getYouTubeDailyViews = <ThrowOnError extends boolean = false>(options: OptionsLegacyParser<GetYouTubeDailyViewsData, ThrowOnError>) => {
    return (options?.client ?? client).get<GetYouTubeDailyViewsResponse, GetYouTubeDailyViewsError, ThrowOnError>({
        ...options,
        url: '/v1/analytics/youtube/daily-views'
    });
};

/**
 * Get YouTube video retention curve
 * Returns the audience retention curve for a single YouTube video, plus the video's
 * duration for rendering the curve on a time axis. The curve has up to 100 points
 * (elapsedVideoTimeRatio 0.01-1.0) aggregated over the whole date range; YouTube does
 * not support per-day retention breakdowns.
 *
 * audienceWatchRatio is the absolute share of viewers watching at that point in the
 * video and can exceed 1 (rewinds and looping, common on Shorts). relativeRetentionPerformance
 * compares against videos of similar length (0 = worst, 0.5 = median, 1 = best).
 * YouTube returns an empty curve for videos with very few views or before analytics
 * processing completes (2-3 day delay).
 *
 * Requires yt-analytics.readonly scope (re-authorization may be needed).
 *
 */
export const getYouTubeVideoRetention = <ThrowOnError extends boolean = false>(options: OptionsLegacyParser<GetYouTubeVideoRetentionData, ThrowOnError>) => {
    return (options?.client ?? client).get<GetYouTubeVideoRetentionResponse, GetYouTubeVideoRetentionError, ThrowOnError>({
        ...options,
        url: '/v1/analytics/youtube/video-retention'
    });
};

/**
 * Get Facebook Page insights
 * Returns page-level Facebook insights (media views, views, post engagements, video metrics,
 * follower counts). Response shape matches /v1/analytics/instagram/account-insights so the
 * same client handling works across platforms.
 *
 * Metric names track the current (post-November 2025) Meta Graph API. The legacy
 * page_impressions / page_fans / page_fan_adds / page_fan_removes metrics were deprecated
 * by Meta on November 15, 2025 and are NOT accepted by this endpoint. Use the replacements
 * below. Because Meta did not provide direct adds/removes replacements, Zernio synthesizes
 * followers_gained / followers_lost from the daily follower snapshotter.
 *
 * Max 89 days, defaults to last 30 days. Requires the Analytics add-on.
 *
 */
export const getFacebookPageInsights = <ThrowOnError extends boolean = false>(options: OptionsLegacyParser<GetFacebookPageInsightsData, ThrowOnError>) => {
    return (options?.client ?? client).get<GetFacebookPageInsightsResponse, GetFacebookPageInsightsError, ThrowOnError>({
        ...options,
        url: '/v1/analytics/facebook/page-insights'
    });
};

/**
 * Get Facebook post monetization earnings
 * Returns lifetime monetization earnings for ONE Facebook post, read live from Meta on every
 * request. Requires the Analytics add-on.
 *
 * Earnings are CUMULATIVE since the post was published, not earnings within a date range, so
 * this endpoint takes no since/until and the totals must not be summed across dates or across
 * posts. Page-level daily earnings live on /v1/analytics/facebook/page-insights.
 *
 * A post on a Page that is not enrolled in monetization, or that earned nothing, returns
 * "total": 0 rather than an error: Meta does not distinguish the two. A metric Meta returned no
 * bucket for at all is reported in "unavailableMetrics" and omitted from "metrics", never as a 0.
 *
 * Amounts are the platform's raw numbers in the stated "unit" and are never rescaled by Zernio.
 * Breakdown dimensions are not exposed and a "breakdown" param is rejected with 400. So are
 * "since", "until", "period", and "metricType": scoping this endpoint to a window is not
 * possible, and silently returning the lifetime total for one would let a caller sum a year of
 * weekly requests into a figure ~52x the post's real earnings.
 *
 */
export const getFacebookPostEarnings = <ThrowOnError extends boolean = false>(options: OptionsLegacyParser<GetFacebookPostEarningsData, ThrowOnError>) => {
    return (options?.client ?? client).get<GetFacebookPostEarningsResponse, GetFacebookPostEarningsError, ThrowOnError>({
        ...options,
        url: '/v1/analytics/facebook/post-earnings'
    });
};

/**
 * Get Instagram insights
 * Returns account-level Instagram insights such as reach, views, accounts engaged, and total interactions.
 * These metrics reflect the entire account's performance across all content surfaces (feed, stories, explore, profile),
 * and are fundamentally different from post-level metrics. Data may be delayed up to 48 hours.
 * Max 90 days, defaults to last 30 days. Requires the Analytics add-on.
 *
 */
export const getInstagramAccountInsights = <ThrowOnError extends boolean = false>(options: OptionsLegacyParser<GetInstagramAccountInsightsData, ThrowOnError>) => {
    return (options?.client ?? client).get<GetInstagramAccountInsightsResponse, GetInstagramAccountInsightsError, ThrowOnError>({
        ...options,
        url: '/v1/analytics/instagram/account-insights'
    });
};

/**
 * Get Instagram follower history
 * Returns a daily running Instagram follower count time series, served from Zernio's
 * cross-platform daily snapshotter. Exists because Meta removed follower_count from
 * the /insights endpoint in Graph API v22+ and never exposed a historical daily series
 * via any public API.
 *
 * Response envelope matches /v1/analytics/instagram/account-insights so the same client
 * handling works. Max 89 days, defaults to last 30 days. Requires the Analytics add-on.
 *
 */
export const getInstagramFollowerHistory = <ThrowOnError extends boolean = false>(options: OptionsLegacyParser<GetInstagramFollowerHistoryData, ThrowOnError>) => {
    return (options?.client ?? client).get<GetInstagramFollowerHistoryResponse, GetInstagramFollowerHistoryError, ThrowOnError>({
        ...options,
        url: '/v1/analytics/instagram/follower-history'
    });
};

/**
 * Get Instagram demographics
 * Returns audience demographic insights for an Instagram account, broken down by age, city, country, and/or gender.
 * Requires at least 100 followers. Returns top 45 entries per dimension.
 * Data may be delayed up to 48 hours. Requires the Analytics add-on.
 *
 */
export const getInstagramDemographics = <ThrowOnError extends boolean = false>(options: OptionsLegacyParser<GetInstagramDemographicsData, ThrowOnError>) => {
    return (options?.client ?? client).get<GetInstagramDemographicsResponse, GetInstagramDemographicsError, ThrowOnError>({
        ...options,
        url: '/v1/analytics/instagram/demographics'
    });
};

/**
 * Get YouTube demographics
 * Returns audience demographic insights for a YouTube channel, broken down by age, gender, and/or country.
 * Pass videoId to get the audience profile of a single video instead of the whole channel.
 * Age and gender values are viewer percentages (0-100). Country values are view counts.
 * Data is based on signed-in viewers only, with a 2-3 day delay. YouTube suppresses demographics
 * for videos with too few signed-in views, so low-traffic videos can return empty breakdowns.
 * Requires the Analytics add-on.
 *
 */
export const getYouTubeDemographics = <ThrowOnError extends boolean = false>(options: OptionsLegacyParser<GetYouTubeDemographicsData, ThrowOnError>) => {
    return (options?.client ?? client).get<GetYouTubeDemographicsResponse, GetYouTubeDemographicsError, ThrowOnError>({
        ...options,
        url: '/v1/analytics/youtube/demographics'
    });
};

/**
 * Get daily aggregated metrics
 * Returns daily aggregated analytics metrics and a per-platform breakdown.
 * Each day includes post count, platform distribution, and summed metrics (impressions, reach, likes, comments, shares, saves, clicks, views).
 * Defaults to the last 180 days. Requires the Analytics add-on.
 *
 */
export const getDailyMetrics = <ThrowOnError extends boolean = false>(options?: OptionsLegacyParser<GetDailyMetricsData, ThrowOnError>) => {
    return (options?.client ?? client).get<GetDailyMetricsResponse, GetDailyMetricsError, ThrowOnError>({
        ...options,
        url: '/v1/analytics/daily-metrics'
    });
};

/**
 * Get best times to post
 * Returns the best times to post based on historical engagement data.
 * Groups all published posts by day of week and hour (UTC), calculating average engagement per slot.
 * Use this to auto-schedule posts at optimal times. Requires the Analytics add-on.
 *
 */
export const getBestTimeToPost = <ThrowOnError extends boolean = false>(options?: OptionsLegacyParser<GetBestTimeToPostData, ThrowOnError>) => {
    return (options?.client ?? client).get<GetBestTimeToPostResponse, GetBestTimeToPostError, ThrowOnError>({
        ...options,
        url: '/v1/analytics/best-time'
    });
};

/**
 * Get content performance decay
 * Returns how engagement accumulates over time after a post is published.
 * Each bucket shows what percentage of the post's total engagement had been reached by that time window.
 * Useful for understanding content lifespan (e.g. "posts reach 78% of total engagement within 24 hours").
 * Requires the Analytics add-on.
 *
 */
export const getContentDecay = <ThrowOnError extends boolean = false>(options?: OptionsLegacyParser<GetContentDecayData, ThrowOnError>) => {
    return (options?.client ?? client).get<GetContentDecayResponse, GetContentDecayError, ThrowOnError>({
        ...options,
        url: '/v1/analytics/content-decay'
    });
};

/**
 * Get frequency vs engagement
 * Returns the correlation between posting frequency (posts per week) and engagement rate, broken down by platform.
 * Helps find the optimal posting cadence for each platform. Each row represents a specific (platform, posts_per_week) combination
 * with the average engagement rate observed across all weeks matching that frequency.
 * Requires the Analytics add-on.
 *
 */
export const getPostingFrequency = <ThrowOnError extends boolean = false>(options?: OptionsLegacyParser<GetPostingFrequencyData, ThrowOnError>) => {
    return (options?.client ?? client).get<GetPostingFrequencyResponse, GetPostingFrequencyError, ThrowOnError>({
        ...options,
        url: '/v1/analytics/posting-frequency'
    });
};

/**
 * Get post analytics timeline
 * Returns a daily timeline of analytics metrics for a specific post, showing how impressions, likes,
 * and other metrics evolved day-by-day since publishing. Each row represents one day of data per platform.
 * For multi-platform Zernio posts, returns separate rows for each platform. Requires the Analytics add-on.
 *
 */
export const getPostTimeline = <ThrowOnError extends boolean = false>(options: OptionsLegacyParser<GetPostTimelineData, ThrowOnError>) => {
    return (options?.client ?? client).get<GetPostTimelineResponse, GetPostTimelineError, ThrowOnError>({
        ...options,
        url: '/v1/analytics/post-timeline'
    });
};

/**
 * Get GBP performance metrics
 * Returns daily performance metrics for a Google Business Profile location.
 * Metrics include impressions (Maps/Search, desktop/mobile), website clicks,
 * call clicks, direction requests, conversations, bookings, and food orders.
 * Data may be delayed 2-3 days. Max 18 months of historical data.
 * Requires the Analytics add-on.
 *
 */
export const getGoogleBusinessPerformance = <ThrowOnError extends boolean = false>(options: OptionsLegacyParser<GetGoogleBusinessPerformanceData, ThrowOnError>) => {
    return (options?.client ?? client).get<GetGoogleBusinessPerformanceResponse, GetGoogleBusinessPerformanceError, ThrowOnError>({
        ...options,
        url: '/v1/analytics/googlebusiness/performance'
    });
};

/**
 * Get GBP search keywords
 * Returns search keywords that triggered impressions for a Google Business Profile location.
 * Data is aggregated monthly. Keywords below a minimum impression threshold set by Google are excluded.
 * Max 18 months of historical data. Requires the Analytics add-on.
 *
 */
export const getGoogleBusinessSearchKeywords = <ThrowOnError extends boolean = false>(options: OptionsLegacyParser<GetGoogleBusinessSearchKeywordsData, ThrowOnError>) => {
    return (options?.client ?? client).get<GetGoogleBusinessSearchKeywordsResponse, GetGoogleBusinessSearchKeywordsError, ThrowOnError>({
        ...options,
        url: '/v1/analytics/googlebusiness/search-keywords'
    });
};

/**
 * Get inbox messaging volume
 * Daily inbox messaging volume + breakdowns. Folds the raw messaging
 * events into three projections so the client can render the volume
 * chart, KPI strip, and per-platform stacked bar from a single call.
 * Max date range is 365 days.
 *
 */
export const getInboxVolume = <ThrowOnError extends boolean = false>(options: OptionsLegacyParser<GetInboxVolumeData, ThrowOnError>) => {
    return (options?.client ?? client).get<GetInboxVolumeResponse, GetInboxVolumeError, ThrowOnError>({
        ...options,
        url: '/v1/analytics/inbox/volume'
    });
};

/**
 * Get day × hour heatmap
 * Day-of-week × hour-of-day breakdown of inbox messages. Buckets are
 * sparse — only cells with at least one event are returned; clients
 * zero-fill the rest to render the full 7×24 grid. The `dow` field
 * follows ClickHouse's `toDayOfWeek` convention (1 = Monday … 7 =
 * Sunday). Max date range is 365 days.
 *
 */
export const getInboxHeatmap = <ThrowOnError extends boolean = false>(options: OptionsLegacyParser<GetInboxHeatmapData, ThrowOnError>) => {
    return (options?.client ?? client).get<GetInboxHeatmapResponse, GetInboxHeatmapError, ThrowOnError>({
        ...options,
        url: '/v1/analytics/inbox/heatmap'
    });
};

/**
 * Get inbox source breakdown
 * Breakdown of inbox messages by their lineage source (the
 * `metadata.source` field set at ingest time: human / workflow /
 * sequence / broadcast / comment_automation / api / contact /
 * platform). Each source row also carries a per-platform sub-split.
 * Max date range is 365 days.
 *
 */
export const getInboxSourceBreakdown = <ThrowOnError extends boolean = false>(options: OptionsLegacyParser<GetInboxSourceBreakdownData, ThrowOnError>) => {
    return (options?.client ?? client).get<GetInboxSourceBreakdownResponse, GetInboxSourceBreakdownError, ThrowOnError>({
        ...options,
        url: '/v1/analytics/inbox/source-breakdown'
    });
};

/**
 * Get inbox response-time stats
 * Time-to-first-response stats. Pairs each received message with the
 * next sent message in the same conversation and reports the delta
 * as both summary statistics and a fixed-bucket histogram suited
 * for the analytics page's TTR chart.
 *
 * `sampleSize` reflects only conversations that received AND got a
 * reply in the window — received-but-never-answered conversations
 * are excluded. Compare against /v1/analytics/inbox/volume's
 * `summary.received` to compute reply rate.
 *
 * Max date range is 365 days.
 *
 */
export const getInboxResponseTime = <ThrowOnError extends boolean = false>(options: OptionsLegacyParser<GetInboxResponseTimeData, ThrowOnError>) => {
    return (options?.client ?? client).get<GetInboxResponseTimeResponse, GetInboxResponseTimeError, ThrowOnError>({
        ...options,
        url: '/v1/analytics/inbox/response-time'
    });
};

/**
 * Get top accounts by inbox volume
 * Leaderboard of social accounts by inbox message volume. Decorates
 * each row with display labels from the live SocialAccount record
 * (so the UI shows username + displayName, not just an ID). Accounts
 * that no longer map to a SocialAccount surface as "(disconnected)"
 * so the row stays visible. Max date range is 365 days.
 *
 */
export const getInboxTopAccounts = <ThrowOnError extends boolean = false>(options: OptionsLegacyParser<GetInboxTopAccountsData, ThrowOnError>) => {
    return (options?.client ?? client).get<GetInboxTopAccountsResponse, GetInboxTopAccountsError, ThrowOnError>({
        ...options,
        url: '/v1/analytics/inbox/top-accounts'
    });
};

/**
 * List conversation analytics
 * Per-conversation listing with per-row totals + first/last message
 * timestamps. The inbox analog of GET /v1/analytics (posts listing) —
 * same filter shape, same pagination, same sort/order semantics.
 * Use as the entry point for the per-conversation analytics drawer
 * at /v1/analytics/inbox/conversations/{conversationId}.
 *
 * Rows are enriched with the conversation's participant info
 * (`participantName`, `participantUsername`, `participantPicture`)
 * and last-message preview by joining the Conversation document
 * scoped to the caller's team. Max date range is 365 days.
 *
 */
export const listInboxConversationAnalytics = <ThrowOnError extends boolean = false>(options: OptionsLegacyParser<ListInboxConversationAnalyticsData, ThrowOnError>) => {
    return (options?.client ?? client).get<ListInboxConversationAnalyticsResponse, ListInboxConversationAnalyticsError, ThrowOnError>({
        ...options,
        url: '/v1/analytics/inbox/conversations'
    });
};

/**
 * Get conversation analytics
 * Per-conversation inbox analytics. The inbox analog of
 * /v1/analytics/post-timeline — one conversation, daily totals,
 * source mix.
 *
 * The {conversationId} path param accepts EITHER the Mongo `_id` of
 * the Conversation document OR its `platformConversationId` (the
 * same identity used by metadata.conversationId at ingest time).
 * Ownership is verified in MongoDB against the caller's team
 * before the Tinybird query fires.
 *
 * Max date range is 365 days.
 *
 */
export const getInboxConversationAnalytics = <ThrowOnError extends boolean = false>(options: OptionsLegacyParser<GetInboxConversationAnalyticsData, ThrowOnError>) => {
    return (options?.client ?? client).get<GetInboxConversationAnalyticsResponse, GetInboxConversationAnalyticsError, ThrowOnError>({
        ...options,
        url: '/v1/analytics/inbox/conversations/{conversationId}'
    });
};

/**
 * List groups
 * Returns all account groups visible to the authenticated user. Groups can
 * contain accounts from multiple profiles. For API keys scoped to specific
 * profiles, only groups whose accounts all live in allowed profiles are
 * returned.
 *
 */
export const listAccountGroups = <ThrowOnError extends boolean = false>(options?: OptionsLegacyParser<unknown, ThrowOnError>) => {
    return (options?.client ?? client).get<ListAccountGroupsResponse, ListAccountGroupsError, ThrowOnError>({
        ...options,
        url: '/v1/account-groups'
    });
};

/**
 * Create group
 * Creates a new account group with a name and a list of social account IDs.
 * Accounts can belong to different profiles; the caller must have access to
 * every account's profile. Group names must be unique per user.
 *
 */
export const createAccountGroup = <ThrowOnError extends boolean = false>(options: OptionsLegacyParser<CreateAccountGroupData, ThrowOnError>) => {
    return (options?.client ?? client).post<CreateAccountGroupResponse, CreateAccountGroupError, ThrowOnError>({
        ...options,
        url: '/v1/account-groups'
    });
};

/**
 * Update group
 * Updates the name or account list of an existing group. You can rename the group, change its accounts, or both.
 */
export const updateAccountGroup = <ThrowOnError extends boolean = false>(options: OptionsLegacyParser<UpdateAccountGroupData, ThrowOnError>) => {
    return (options?.client ?? client).put<UpdateAccountGroupResponse, UpdateAccountGroupError, ThrowOnError>({
        ...options,
        url: '/v1/account-groups/{groupId}'
    });
};

/**
 * Delete group
 * Permanently deletes an account group. The accounts themselves are not affected.
 */
export const deleteAccountGroup = <ThrowOnError extends boolean = false>(options: OptionsLegacyParser<DeleteAccountGroupData, ThrowOnError>) => {
    return (options?.client ?? client).delete<DeleteAccountGroupResponse, DeleteAccountGroupError, ThrowOnError>({
        ...options,
        url: '/v1/account-groups/{groupId}'
    });
};

/**
 * Get upload URL
 * Get a presigned URL to upload files directly to cloud storage (up to 5GB). Returns an uploadUrl and publicUrl. PUT your file to the uploadUrl, then use the publicUrl in your posts.
 */
export const getMediaPresignedUrl = <ThrowOnError extends boolean = false>(options: OptionsLegacyParser<GetMediaPresignedUrlData, ThrowOnError>) => {
    return (options?.client ?? client).post<GetMediaPresignedUrlResponse, GetMediaPresignedUrlError, ThrowOnError>({
        ...options,
        url: '/v1/media/presign'
    });
};

/**
 * Search posts
 * Search Reddit posts using a connected account. Optionally scope to a specific subreddit.
 */
export const searchReddit = <ThrowOnError extends boolean = false>(options: OptionsLegacyParser<SearchRedditData, ThrowOnError>) => {
    return (options?.client ?? client).get<SearchRedditResponse, SearchRedditError, ThrowOnError>({
        ...options,
        url: '/v1/reddit/search'
    });
};

/**
 * Get subreddit feed
 * Fetch posts from a subreddit feed. Supports sorting, time filtering, and cursor-based pagination.
 */
export const getRedditFeed = <ThrowOnError extends boolean = false>(options: OptionsLegacyParser<GetRedditFeedData, ThrowOnError>) => {
    return (options?.client ?? client).get<GetRedditFeedResponse, GetRedditFeedError, ThrowOnError>({
        ...options,
        url: '/v1/reddit/feed'
    });
};

/**
 * Account billing snapshot (plan, cycle, balance, caps, status)
 * The billing "wallet/statement" view: current plan, billing cycle,
 * accrued balance + remaining credits this period, spend caps, and
 * payment / access status. This is the billing half of the legacy
 * `/v1/usage-stats` snapshot — the per-product consumption half is metering
 * and lives on `GET /v1/usage`.
 *
 * Usage-based (Metronome) accounts get a populated `balance`; legacy Stripe
 * accounts get `balance: null` plus a deprecated `legacy.limits` block and,
 * when payment-blocked, `status.openInvoiceUrl` / `status.declineReason`.
 *
 */
export const getBilling = <ThrowOnError extends boolean = false>(options?: OptionsLegacyParser<unknown, ThrowOnError>) => {
    return (options?.client ?? client).get<GetBillingResponse, GetBillingError, ThrowOnError>({
        ...options,
        url: '/v1/billing'
    });
};

/**
 * Get X/Twitter API pricing table
 * Returns Zernio's canonical X/Twitter API pricing table. Each X action has its
 * own Metronome product and its own rate, and Zernio passes X API costs through
 * at exact rates with zero markup.
 *
 * The response is identical for every authenticated user (pricing is universal),
 * so it is safe to cache on the client for the duration of a billing period.
 *
 * To compute your own per-operation spend, pair this endpoint with
 * `GET /v1/usage-stats` — that endpoint returns `usage.xApiCallsByOperation`
 * keyed by the same `operation` field you get here.
 *
 */
export const getXApiPricing = <ThrowOnError extends boolean = false>(options?: OptionsLegacyParser<unknown, ThrowOnError>) => {
    return (options?.client ?? client).get<GetXApiPricingResponse, GetXApiPricingError, ThrowOnError>({
        ...options,
        url: '/v1/billing/x-pricing'
    });
};

/**
 * Usage snapshot (default) or billed-spend metering (with params)
 * Dual-mode endpoint, selected by query params — fully backward
 * compatible:
 *
 * **Without metering params (the default):** the plan / quota / usage
 * snapshot — plan name, billing period, limits, usage counts, access
 * state. Identical to `GET /v1/usage-stats`. Existing integrations keep
 * working unchanged.
 *
 * **With `range`, `granularity`, `from`, or `to`:** usage METERING —
 * billed spend (USD) by product family (`accounts`, `numbers`, `calls`,
 * `sms`, `dlc`, `xApi`, `credits`, `other`) over the window, at
 * `day` / `month` / `total` granularity, from Metronome's invoice
 * breakdown (the CHARGE view — always reconciles with what gets billed).
 * Also served at `GET /v1/usage/daily`. Usage-based accounts only —
 * legacy Stripe accounts get `{ "supported": false, "days": [] }`.
 *
 * For per-domain consumption *volumes* use `GET /v1/usage/calls` and
 * `GET /v1/usage/sms`. For the billing statement (balance, credits,
 * caps, payment status) use `GET /v1/billing`.
 *
 */
export const getUsage = <ThrowOnError extends boolean = false>(options?: OptionsLegacyParser<GetUsageData, ThrowOnError>) => {
    return (options?.client ?? client).get<GetUsageResponse, GetUsageError, ThrowOnError>({
        ...options,
        url: '/v1/usage'
    });
};

/**
 * @deprecated
 * Get plan and usage snapshot (plan, limits, payment status)
 * The plan / quota / payment-status snapshot: current plan name, billing
 * period, plan limits, usage counts, and access state. Identical to a
 * bare `GET /v1/usage` call (this path is its deprecated alias). For
 * billed spend by product, call `GET /v1/usage` with `range` /
 * `granularity` params. The statement view (balance, credits, caps,
 * payment status) lives at `GET /v1/billing`.
 *
 * The response shape depends on the account's `billingSystem`:
 * * Stripe users: per-period `usage.uploads` / `usage.profiles` counters.
 * * Metronome (usage-based) users: `usage.connectedAccounts`,
 * `usage.xApiCallsByOperation` (per-operation X API call counts —
 * resolve keys via `GET /v1/billing/x-pricing`), plus a `spend`
 * block with `currentPeriodCents`, `xSpendCents`, and
 * `xSpendLimitCents`. The legacy `usage.xApiCalls` 3-tier
 * aggregate is still emitted for back-compat but excludes the
 * $0.200 URL tier and any future tiers — new clients should
 * consume `xApiCallsByOperation` only.
 *
 */
export const getUsageStats = <ThrowOnError extends boolean = false>(options?: OptionsLegacyParser<GetUsageStatsData, ThrowOnError>) => {
    return (options?.client ?? client).get<GetUsageStatsResponse, GetUsageStatsError, ThrowOnError>({
        ...options,
        url: '/v1/usage-stats'
    });
};

/**
 * Calling usage and cost
 * Aggregated calling usage across your numbers, both channels
 * (WhatsApp Business Calling + regular phone/PSTN): call counts,
 * answered counts, minutes, and cost. Use it for cost visibility or to
 * rebill your own customers per number.
 *
 * Costs come from each call's billing snapshot, so this endpoint always
 * agrees with the invoice: `billableUSD` is what Zernio bills;
 * `metaUSD` is the WhatsApp per-minute charge Meta bills directly to
 * your WABA (display only, never billed by Zernio).
 *
 * Optional `groupBy` returns a breakdown by UTC day, by your number, or
 * by channel. Defaults to the last 30 days.
 *
 */
export const getCallsUsage = <ThrowOnError extends boolean = false>(options?: OptionsLegacyParser<GetCallsUsageData, ThrowOnError>) => {
    return (options?.client ?? client).get<GetCallsUsageResponse, GetCallsUsageError, ThrowOnError>({
        ...options,
        url: '/v1/usage/calls'
    });
};

/**
 * SMS usage (volumes)
 * Aggregated SMS/MMS volumes across your numbers: sent, received, and
 * total message counts, with an optional breakdown by UTC day or by
 * number. Defaults to the last 30 days.
 *
 * Volumes only, deliberately: SMS cost is carrier-rated asynchronously
 * and billed to your invoice, so per-message cost is not available here.
 * Calling usage (GET /v1/usage/calls) does include billable cost.
 *
 */
export const getSmsUsage = <ThrowOnError extends boolean = false>(options?: OptionsLegacyParser<GetSmsUsageData, ThrowOnError>) => {
    return (options?.client ?? client).get<GetSmsUsageResponse, GetSmsUsageError, ThrowOnError>({
        ...options,
        url: '/v1/usage/sms'
    });
};

/**
 * List posts
 * Returns a paginated list of posts. Published posts include platformPostUrl with the public URL on each platform.
 */
export const listPosts = <ThrowOnError extends boolean = false>(options?: OptionsLegacyParser<ListPostsData, ThrowOnError>) => {
    return (options?.client ?? client).get<ListPostsResponse, ListPostsError, ThrowOnError>({
        ...options,
        url: '/v1/posts'
    });
};

/**
 * Create post
 * Create and optionally publish a post. Immediate posts (`publishNow: true`) include `platformPostUrl` in the response.
 * Content is optional when media is attached, all platforms have `customContent`, or every platform entry is a LinkedIn plain repost (`platformSpecificData.reshareUrl` with no text). See each platform's schema for media constraints.
 *
 * ## Idempotency
 *
 * Two layers of duplicate-protection apply, so safe-to-retry callers (network blips, n8n / Zapier retries, etc.) don't accidentally double-post.
 *
 * **1. Same-request idempotency (5-minute window).**
 * Pass an `x-request-id` header to mark a logical request. If a second request arrives with the same `x-request-id` while the first is in-flight (or within ~5 minutes of completion), we return **HTTP 200** with the original post in the `existingPost` field — no new post is created. The official Zernio SDKs auto-generate a unique `x-request-id` per call. If you're using a generic HTTP client (curl, n8n's HTTP node, Zapier, custom code), either:
 * - Set a unique `x-request-id` per logical call (recommended — UUIDv4 is fine)
 * - Or simply omit the header — we'll treat each request as new
 *
 * **Common pitfall**: if your workflow tool uses a single execution-level request ID and reuses it across multiple HTTP nodes (e.g. one ID for the whole run, shared across 6 different platform calls), every call after the first will look like a retry of the first and return its post. Generate a fresh ID per node.
 *
 * **2. Content-hash dedup (24-hour window).**
 * Independently, we hash `(platform, accountId, content + media URLs)` and reject duplicates within 24 hours with **HTTP 409**. This catches genuine "same content posted twice to the same account" cases regardless of `x-request-id`. Returns `error`, `accountId`, `platform`, and `existingPostId` so you can find the original. To intentionally re-post identical content within 24h, change something (the caption, the media, the account) — the dedup is keyed on the full content fingerprint.
 *
 * Order: same-`x-request-id` retries (200) are checked first; if no idempotency match, the content-hash dedup (409) runs.
 *
 */
export const createPost = <ThrowOnError extends boolean = false>(options: OptionsLegacyParser<CreatePostData, ThrowOnError>) => {
    return (options?.client ?? client).post<CreatePostResponse, CreatePostError, ThrowOnError>({
        ...options,
        url: '/v1/posts'
    });
};

/**
 * Sync an external post
 * Fetch an account's latest external posts (published directly on the platform, not through Zernio) on demand, so a just-published post is retrievable within seconds instead of waiting for the background sync (which refreshes each account at most every ~90 minutes).
 *
 * Primary use case: verifying a submitted post. When a user publishes on the platform and immediately pastes the post URL into your app, call this with `accountId` plus `url` (or `postId`) to confirm the post exists and return its metadata.
 *
 * Behavior:
 * - We check our stored copy first and return immediately if the post is already known (no platform call).
 * - Otherwise we fetch the account's latest posts live from the platform, then match and return the submitted post.
 * - Requests are debounced per account (~15s): if the account was just synced, the live fetch is skipped.
 *
 * `accountId` is required — a post URL or id alone cannot be resolved to an account, and the account must be connected to Zernio (we use its token to read the platform). Supported for every platform with a listing API (Instagram, Facebook, TikTok, YouTube, X, Threads, Pinterest, Reddit, Bluesky, Google Business, and LinkedIn organization accounts).
 *
 * LinkedIn personal profiles: LinkedIn has no listing API for personal profiles, so a `url` is REQUIRED and imports that single post. Pass any LinkedIn post URL (`linkedin.com/posts/…`, `linkedin.com/feed/update/urn:li:activity:…`) or a `urn:li:share:…` / `urn:li:ugcPost:…` URN. Works for posts published outside Zernio and before the account was connected, any age; the post must be authored by the connected member. Imported posts return full analytics (impressions, reach, reactions, comments, reshares, saves) and keep refreshing on the background analytics cycle, but carry no content/media (LinkedIn does not expose them for personal profiles).
 *
 * `url` accepts any format the platform uses (e.g. `instagram.com/p/…`, `instagram.com/reel/…`, `youtu.be/…`, `youtube.com/shorts/…`, `tiktok.com/@user/video/…`, and `vm.tiktok.com` short links). Pass `postId` (the platform media/video id) as an alternative locator.
 *
 * Note: post-level analytics (reach, impressions) still carry the platform's own delay (e.g. ~24h on Instagram). This endpoint confirms the post exists and returns its metadata plus basic engagement (likes, comments), not delayed insights.
 *
 */
export const syncExternalPosts = <ThrowOnError extends boolean = false>(options: OptionsLegacyParser<SyncExternalPostsData, ThrowOnError>) => {
    return (options?.client ?? client).post<SyncExternalPostsResponse, SyncExternalPostsError, ThrowOnError>({
        ...options,
        url: '/v1/posts/sync-external'
    });
};

/**
 * Get post
 * Fetch a single post by ID. For published posts, this returns platformPostUrl for each platform.
 *
 */
export const getPost = <ThrowOnError extends boolean = false>(options: OptionsLegacyParser<GetPostData, ThrowOnError>) => {
    return (options?.client ?? client).get<GetPostResponse, GetPostError, ThrowOnError>({
        ...options,
        url: '/v1/posts/{postId}'
    });
};

/**
 * Update post
 * Update an existing post. Draft, scheduled, failed, partial, and cancelled posts can be edited.
 * Published posts can only have their recycling config updated.
 *
 * To promote a draft to scheduled, send `isDraft: false` together with `scheduledFor` (or `publishNow: true`,
 * or `queuedFromProfile`). If `isDraft` is omitted the post keeps its current draft status, so sending only
 * `scheduledFor` to a draft returns 200 but the post remains a draft.
 *
 * Non-draft updates run the same per-platform validation as post creation (media requirements, platform-specific
 * field rules, etc.) against the resulting platforms, returning 400 on failure.
 *
 */
export const updatePost = <ThrowOnError extends boolean = false>(options: OptionsLegacyParser<UpdatePostData, ThrowOnError>) => {
    return (options?.client ?? client).put<UpdatePostResponse, UpdatePostError, ThrowOnError>({
        ...options,
        url: '/v1/posts/{postId}'
    });
};

/**
 * Delete post
 * Delete a draft or scheduled post from Zernio. Published posts cannot be deleted; use the Unpublish endpoint instead. Upload quota is automatically refunded.
 */
export const deletePost = <ThrowOnError extends boolean = false>(options: OptionsLegacyParser<DeletePostData, ThrowOnError>) => {
    return (options?.client ?? client).delete<DeletePostResponse, DeletePostError, ThrowOnError>({
        ...options,
        url: '/v1/posts/{postId}'
    });
};

/**
 * Bulk upload from CSV
 * Create multiple posts by uploading a CSV file. Use dryRun=true to validate without creating posts.
 */
export const bulkUploadPosts = <ThrowOnError extends boolean = false>(options: OptionsLegacyParser<BulkUploadPostsData, ThrowOnError>) => {
    return (options?.client ?? client).post<BulkUploadPostsResponse, BulkUploadPostsError, ThrowOnError>({
        ...options,
        ...formDataBodySerializer,
        headers: {
            'Content-Type': null,
            ...options?.headers
        },
        url: '/v1/posts/bulk-upload'
    });
};

/**
 * Retry failed post
 * Immediately retries publishing a failed post. Returns the updated post with its new status.
 */
export const retryPost = <ThrowOnError extends boolean = false>(options: OptionsLegacyParser<RetryPostData, ThrowOnError>) => {
    return (options?.client ?? client).post<RetryPostResponse, RetryPostError, ThrowOnError>({
        ...options,
        url: '/v1/posts/{postId}/retry'
    });
};

/**
 * Unpublish post
 * Deletes a published post from the specified platform. The post record in Zernio is kept but its status is updated to cancelled.
 * Not supported on Instagram, TikTok, or Snapchat. Threaded posts delete all items. YouTube deletion is permanent.
 *
 */
export const unpublishPost = <ThrowOnError extends boolean = false>(options: OptionsLegacyParser<UnpublishPostData, ThrowOnError>) => {
    return (options?.client ?? client).post<UnpublishPostResponse, UnpublishPostError, ThrowOnError>({
        ...options,
        url: '/v1/posts/{postId}/unpublish'
    });
};

/**
 * Edit published post
 * Edit the text of an already-published post. Supported on X (Twitter), Discord,
 * Facebook, and Reddit. Each platform enforces its own rules:
 *
 * **X (Twitter)**
 * - Connected X account must have an active X Premium subscription
 * - Must be within 1 hour of original publish time
 * - Maximum 5 edits per tweet (enforced by X)
 * - Threads cannot be edited, only single tweets
 * - X assigns a NEW post ID on edit, returned as `id`
 *
 * **Discord**
 * - No time limit and no premium requirement
 * - The message ID is unchanged after the edit
 *
 * **Facebook**
 * - Graph only permits editing a post that the same app created, so this works on
 * posts published through Zernio and is rejected for posts created in Meta
 * Business Suite / Composer or by another tool
 * - Media cannot be swapped, only the message text
 * - Reactions, comments, and shares are preserved. The post ID is unchanged
 *
 * **Reddit**
 * - Self-posts only. A link post has no editable body and is rejected before the write
 * - Body only. Reddit exposes no API to edit a post title, ever
 * - The post ID is unchanged
 *
 * Media edits are not supported on any platform. The post record in Zernio is updated
 * with the new content and an edit-history entry.
 *
 */
export const editPost = <ThrowOnError extends boolean = false>(options: OptionsLegacyParser<EditPostData, ThrowOnError>) => {
    return (options?.client ?? client).post<EditPostResponse, EditPostError, ThrowOnError>({
        ...options,
        url: '/v1/posts/{postId}/edit'
    });
};

/**
 * Update post metadata
 * Updates metadata of a published video on the specified platform without re-uploading.
 * Currently only supported for YouTube. At least one updatable field is required.
 *
 * Two modes:
 *
 * 1. Post-based (video published through Zernio): pass the Zernio postId in the URL and platform in the body.
 * 2. Direct video ID (video uploaded outside Zernio, e.g. directly to YouTube): use _ as the postId,
 * and pass videoId + accountId + platform in the body. The accountId is the Zernio social account ID
 * for the connected YouTube channel.
 *
 */
export const updatePostMetadata = <ThrowOnError extends boolean = false>(options: OptionsLegacyParser<UpdatePostMetadataData, ThrowOnError>) => {
    return (options?.client ?? client).post<UpdatePostMetadataResponse, UpdatePostMetadataError, ThrowOnError>({
        ...options,
        url: '/v1/posts/{postId}/update-metadata'
    });
};

/**
 * List users
 * Returns all users in the workspace including roles and profile access. Also returns the currentUserId of the caller.
 */
export const listUsers = <ThrowOnError extends boolean = false>(options?: OptionsLegacyParser<unknown, ThrowOnError>) => {
    return (options?.client ?? client).get<ListUsersResponse, ListUsersError, ThrowOnError>({
        ...options,
        url: '/v1/users'
    });
};

/**
 * Get user
 * Returns a single user's details by ID, including name, email, and role.
 */
export const getUser = <ThrowOnError extends boolean = false>(options: OptionsLegacyParser<GetUserData, ThrowOnError>) => {
    return (options?.client ?? client).get<GetUserResponse, GetUserError, ThrowOnError>({
        ...options,
        url: '/v1/users/{userId}'
    });
};

/**
 * List profiles
 * Returns profiles sorted default-first, then by creation date. Filter with name (exact match) and paginate with limit/skip; without those params the full list is returned unchanged. Use includeOverLimit=true to include profiles that exceed the plan limit.
 */
export const listProfiles = <ThrowOnError extends boolean = false>(options?: OptionsLegacyParser<ListProfilesData, ThrowOnError>) => {
    return (options?.client ?? client).get<ListProfilesResponse, ListProfilesError, ThrowOnError>({
        ...options,
        url: '/v1/profiles'
    });
};

/**
 * Create profile
 * Creates a new profile with a name, optional description, and color. Names are unique per workspace: a duplicate returns a 409 whose details.existingProfileId carries the id of the existing profile. Send an Idempotency-Key header to make retries safe: a retried create with the same key and body replays the original 201 (same _id) instead of conflicting.
 */
export const createProfile = <ThrowOnError extends boolean = false>(options: OptionsLegacyParser<CreateProfileData, ThrowOnError>) => {
    return (options?.client ?? client).post<CreateProfileResponse, CreateProfileError, ThrowOnError>({
        ...options,
        url: '/v1/profiles'
    });
};

/**
 * Get profile
 * Returns a single profile by ID, including its name, color, and default status.
 */
export const getProfile = <ThrowOnError extends boolean = false>(options: OptionsLegacyParser<GetProfileData, ThrowOnError>) => {
    return (options?.client ?? client).get<GetProfileResponse, GetProfileError, ThrowOnError>({
        ...options,
        url: '/v1/profiles/{profileId}'
    });
};

/**
 * Update profile
 * Updates a profile's name, description, color, or default status.
 */
export const updateProfile = <ThrowOnError extends boolean = false>(options: OptionsLegacyParser<UpdateProfileData, ThrowOnError>) => {
    return (options?.client ?? client).put<UpdateProfileResponse, UpdateProfileError, ThrowOnError>({
        ...options,
        url: '/v1/profiles/{profileId}'
    });
};

/**
 * Delete profile
 * Permanently deletes a profile. Active connected accounts block deletion (returns 400) - disconnect them first. Any remaining disconnected accounts and provisioned WhatsApp numbers are moved to another of your profiles (a new one is created only if needed), never deleted.
 */
export const deleteProfile = <ThrowOnError extends boolean = false>(options: OptionsLegacyParser<DeleteProfileData, ThrowOnError>) => {
    return (options?.client ?? client).delete<DeleteProfileResponse, DeleteProfileError, ThrowOnError>({
        ...options,
        url: '/v1/profiles/{profileId}'
    });
};

/**
 * List accounts
 * Returns connected social accounts. Only includes accounts within the plan limit by default. Follower data requires analytics add-on.
 * Supports optional server-side pagination via page/limit params. When omitted, returns all accounts (backward-compatible).
 * page and limit must be supplied together; out-of-range page/limit values are rejected with 400 rather than silently clamped.
 *
 */
export const listAccounts = <ThrowOnError extends boolean = false>(options?: OptionsLegacyParser<ListAccountsData, ThrowOnError>) => {
    return (options?.client ?? client).get<ListAccountsResponse, ListAccountsError, ThrowOnError>({
        ...options,
        url: '/v1/accounts'
    });
};

/**
 * Get follower stats
 * Returns follower count history and growth metrics for connected social accounts.
 * Requires analytics add-on subscription. Follower counts are refreshed once per day.
 *
 */
export const getFollowerStats = <ThrowOnError extends boolean = false>(options?: OptionsLegacyParser<GetFollowerStatsData, ThrowOnError>) => {
    return (options?.client ?? client).get<GetFollowerStatsResponse, GetFollowerStatsError, ThrowOnError>({
        ...options,
        url: '/v1/accounts/follower-stats'
    });
};

/**
 * Update account
 * Updates a connected social account's display name or username override.
 *
 * For X/Twitter accounts on usage-based billing, also accepts an `xCapabilities`
 * object to toggle background API operations that incur X API pass-through costs.
 * Both fields are opt-in (default `false`) — when off, no analytics syncs or DM
 * polling are performed for that account, and no API call is metered for those
 * operations. Publishing and deleting posts are always available regardless of
 * these toggles. Setting `xCapabilities` on a non-X account returns 400.
 *
 */
export const updateAccount = <ThrowOnError extends boolean = false>(options: OptionsLegacyParser<UpdateAccountData, ThrowOnError>) => {
    return (options?.client ?? client).put<UpdateAccountResponse, UpdateAccountError, ThrowOnError>({
        ...options,
        url: '/v1/accounts/{accountId}'
    });
};

/**
 * Move account to another profile
 * Moves a connected social account to a different profile owned by the same
 * user. The target profile must belong to the same user as the account.
 *
 * For API keys restricted to specific profiles, BOTH the source account's
 * current profile AND the target profile must be in the key's allowed set.
 * Calls with a target profile outside the key's scope return 403.
 *
 */
export const moveAccountToProfile = <ThrowOnError extends boolean = false>(options: OptionsLegacyParser<MoveAccountToProfileData, ThrowOnError>) => {
    return (options?.client ?? client).patch<MoveAccountToProfileResponse, MoveAccountToProfileError, ThrowOnError>({
        ...options,
        url: '/v1/accounts/{accountId}'
    });
};

/**
 * Disconnect account
 * Disconnects and removes a connected social account.
 */
export const deleteAccount = <ThrowOnError extends boolean = false>(options: OptionsLegacyParser<DeleteAccountData, ThrowOnError>) => {
    return (options?.client ?? client).delete<DeleteAccountResponse, DeleteAccountError, ThrowOnError>({
        ...options,
        url: '/v1/accounts/{accountId}'
    });
};

/**
 * Check accounts health
 * Returns health status of all connected accounts including token validity, permissions, and issues needing attention.
 */
export const getAllAccountsHealth = <ThrowOnError extends boolean = false>(options?: OptionsLegacyParser<GetAllAccountsHealthData, ThrowOnError>) => {
    return (options?.client ?? client).get<GetAllAccountsHealthResponse, GetAllAccountsHealthError, ThrowOnError>({
        ...options,
        url: '/v1/accounts/health'
    });
};

/**
 * Register a connected WhatsApp number on the Cloud API
 * Re-runs Meta's Cloud API registration for a WhatsApp account that is already connected.
 * Use it when the number has its own two-step verification PIN: the connect flows register
 * with a default PIN, Meta rejects that with error 133005, and the number then fails every
 * send with the misleading '(#200) You do not have the necessary permission to send messages'
 * while the account still shows as connected. The PIN is used for this call only and is not stored.
 *
 */
export const registerWhatsAppNumber = <ThrowOnError extends boolean = false>(options: OptionsLegacyParser<RegisterWhatsAppNumberData, ThrowOnError>) => {
    return (options?.client ?? client).post<RegisterWhatsAppNumberResponse, RegisterWhatsAppNumberError, ThrowOnError>({
        ...options,
        url: '/v1/accounts/{accountId}/whatsapp/register'
    });
};

/**
 * Check account health
 * Returns detailed health info for a specific account including token status, permissions, and recommendations.
 *
 * For WhatsApp accounts the response also includes `platformConnection`, a live probe of the
 * Meta link behind the channel (the same read as `GET /v1/whatsapp/number-info`). The OAuth
 * token can be perfectly valid while Meta refuses to serve the phone-number object (for
 * example after a phone-side coexistence disconnect), so `tokenStatus` alone is not a
 * liveness signal for WhatsApp. When the Meta link is dead, `platformConnection.status` is
 * `disconnected` and the overall `status` is `error`.
 *
 */
export const getAccountHealth = <ThrowOnError extends boolean = false>(options: OptionsLegacyParser<GetAccountHealthData, ThrowOnError>) => {
    return (options?.client ?? client).get<GetAccountHealthResponse, GetAccountHealthError, ThrowOnError>({
        ...options,
        url: '/v1/accounts/{accountId}/health'
    });
};

/**
 * Check whether an Instagram user follows the account
 * Resolves the follow relationship between an Instagram user and the connected
 * account, plus their public profile counters.
 *
 * `userId` is the Instagram-scoped id (IGSID) Meta gives you on a webhook:
 * `sender.id` on `message.received`, `comment.author.id` on `comment.received`.
 *
 * **Meta only answers for people who have MESSAGED the account.** Commenting grants
 * no consent, so a commenter who has never DMed you is unresolvable - that is a
 * platform rule, not a limitation of this endpoint. When it cannot be resolved the
 * response is still `200` with `isFollower: null` and an `unavailableReason`, because
 * "unknown" is a normal state to branch on:
 *
 * * `consent_required` - the user has never messaged this account.
 * * `dm_access_disabled` - the account owner turned off Instagram Direct API access.
 * * `not_messageable` - the id is not a messaging-scoped id.
 * * `error` - a transient Graph API failure.
 *
 * To gate a comment automation on this, use the automation's `audience` rules instead
 * of calling this per comment - they run the same lookup only on comments that
 * actually match a keyword, and can ask the commenter to confirm with one tap.
 *
 * Answers are cached briefly per (account, user). Pass `refresh=true` right after
 * asking someone to follow, so a follow from a moment ago is visible.
 *
 */
export const getInstagramFollowStatus = <ThrowOnError extends boolean = false>(options: OptionsLegacyParser<GetInstagramFollowStatusData, ThrowOnError>) => {
    return (options?.client ?? client).get<GetInstagramFollowStatusResponse, GetInstagramFollowStatusError, ThrowOnError>({
        ...options,
        url: '/v1/accounts/{accountId}/follow-status/{userId}'
    });
};

/**
 * Get TikTok creator info
 * Returns TikTok creator details, available privacy levels, posting limits, and commercial content options for a specific TikTok account. Only works with TikTok accounts.
 */
export const getTikTokCreatorInfo = <ThrowOnError extends boolean = false>(options: OptionsLegacyParser<GetTikTokCreatorInfoData, ThrowOnError>) => {
    return (options?.client ?? client).get<GetTikTokCreatorInfoResponse, GetTikTokCreatorInfoError, ThrowOnError>({
        ...options,
        url: '/v1/accounts/{accountId}/tiktok/creator-info'
    });
};

/**
 * Verify credential
 * Checks whether the bearer credential on this request is valid, without reading any data. Accepts an API key or an OAuth access token. Intended for clients that must validate a credential before use (for example an MCP server verifying an incoming token) so they do not have to call a data endpoint to do it.
 */
export const verifyCredential = <ThrowOnError extends boolean = false>(options?: OptionsLegacyParser<unknown, ThrowOnError>) => {
    return (options?.client ?? client).get<VerifyCredentialResponse, VerifyCredentialError, ThrowOnError>({
        ...options,
        url: '/v1/auth/verify'
    });
};

/**
 * List keys
 * Returns all API keys for the authenticated user. Keys are returned with a preview only, not the full key value.
 */
export const listApiKeys = <ThrowOnError extends boolean = false>(options?: OptionsLegacyParser<unknown, ThrowOnError>) => {
    return (options?.client ?? client).get<ListApiKeysResponse, ListApiKeysError, ThrowOnError>({
        ...options,
        url: '/v1/api-keys'
    });
};

/**
 * Create key
 * Creates a new API key with an optional expiry. The full key value is only returned once in the response.
 */
export const createApiKey = <ThrowOnError extends boolean = false>(options: OptionsLegacyParser<CreateApiKeyData, ThrowOnError>) => {
    return (options?.client ?? client).post<CreateApiKeyResponse, CreateApiKeyError, ThrowOnError>({
        ...options,
        url: '/v1/api-keys'
    });
};

/**
 * Delete key
 * Permanently revokes and deletes an API key.
 */
export const deleteApiKey = <ThrowOnError extends boolean = false>(options: OptionsLegacyParser<DeleteApiKeyData, ThrowOnError>) => {
    return (options?.client ?? client).delete<DeleteApiKeyResponse, DeleteApiKeyError, ThrowOnError>({
        ...options,
        url: '/v1/api-keys/{keyId}'
    });
};

/**
 * List connected apps
 * Returns the OAuth clients (AI assistants and MCP connectors) the authenticated
 * user has authorized and that still hold a live token.
 *
 * Requires a session or a full-access API key. A profile-scoped API key, a
 * restricted (zrk_) API key, or an OAuth access token is rejected with 403: an
 * app must not be able to enumerate its sibling authorizations, and connected-app
 * management is admin-plane.
 *
 */
export const listConnectedApps = <ThrowOnError extends boolean = false>(options?: OptionsLegacyParser<unknown, ThrowOnError>) => {
    return (options?.client ?? client).get<ListConnectedAppsResponse, ListConnectedAppsError, ThrowOnError>({
        ...options,
        url: '/v1/me/connected-apps'
    });
};

/**
 * Revoke connected app
 * Ends an app's access: invalidates the client's pending authorization codes and
 * revokes every live token it holds for the authenticated user. Takes effect on
 * the app's next request.
 *
 * Idempotent while the authorization is still on record: revoking an app that
 * was already revoked returns 200 with `revokedTokens: 0`.
 *
 * Requires a session or a full-access API key. A profile-scoped API key, a
 * restricted (zrk_) API key, or an OAuth access token is rejected with 403.
 *
 */
export const revokeConnectedApp = <ThrowOnError extends boolean = false>(options: OptionsLegacyParser<RevokeConnectedAppData, ThrowOnError>) => {
    return (options?.client ?? client).delete<RevokeConnectedAppResponse, RevokeConnectedAppError, ThrowOnError>({
        ...options,
        url: '/v1/me/connected-apps/{clientId}'
    });
};

/**
 * Create invite token
 * Generate a secure invite link to grant team members access to your profiles.
 * Invites expire after 7 days and are single-use.
 *
 * Returns 403 when a requested profile is not found or not owned, or when
 * called with a restricted (zrk_) API key: invite management is admin-plane.
 *
 */
export const createInviteToken = <ThrowOnError extends boolean = false>(options: OptionsLegacyParser<CreateInviteTokenData, ThrowOnError>) => {
    return (options?.client ?? client).post<CreateInviteTokenResponse, CreateInviteTokenError, ThrowOnError>({
        ...options,
        url: '/v1/invite/tokens'
    });
};

/**
 * Get OAuth connect URL
 * Initiate an OAuth connection flow. Returns an authUrl to redirect the user to.
 * Standard flow: Zernio hosts the selection UI, then redirects to your redirect_url. Headless mode (headless=true): user is redirected to your redirect_url with OAuth data for custom UI. Use the platform-specific selection endpoints to complete.
 *
 */
export const getConnectUrl = <ThrowOnError extends boolean = false>(options: OptionsLegacyParser<GetConnectUrlData, ThrowOnError>) => {
    return (options?.client ?? client).get<GetConnectUrlResponse, GetConnectUrlError, ThrowOnError>({
        ...options,
        url: '/v1/connect/{platform}'
    });
};

/**
 * Complete OAuth callback
 * Exchange the OAuth authorization code for tokens and connect the account to the specified profile.
 */
export const handleOAuthCallback = <ThrowOnError extends boolean = false>(options: OptionsLegacyParser<HandleOAuthCallbackData, ThrowOnError>) => {
    return (options?.client ?? client).post<HandleOAuthCallbackResponse, HandleOAuthCallbackError, ThrowOnError>({
        ...options,
        url: '/v1/connect/{platform}'
    });
};

/**
 * Connect ads for a platform
 * Unified ads connection endpoint. Creates a dedicated ads SocialAccount for the specified platform.
 *
 * Same-token platforms (facebook, instagram, linkedin, pinterest): Creates an ads SocialAccount (metaads, linkedinads, pinterestads) with a copied OAuth token from the parent posting account. If the ads account already exists, returns alreadyConnected: true. No extra OAuth needed.
 *
 * Separate-token platforms (tiktok, twitter): Starts the platform-specific marketing API OAuth flow and creates an ads SocialAccount (tiktokads, xads) with its own token. If the ads account already exists, returns alreadyConnected: true.
 * - tiktok: accountId is OPTIONAL. With accountId, the new tiktokads account links to that posting account (parentAccountId set) — Spark Ads + standalone ads using the posting TT_USER identity become available. Without accountId, ads-only mode kicks in: the new tiktokads account has parentAccountId=null and standalone ads use a synthetic CUSTOMIZED_USER ("Brand Identity"); Spark Ads are unavailable because TikTok requires a posting account for them. The Brand Identity is configured separately via PATCH /v1/connect/tiktok-ads (or inline on POST /v1/ads/create via the brandIdentity field).
 * - twitter (X Ads): accountId is REQUIRED. There's no ads-only mode — tweets need to be authored by a real X user.
 *
 * Standalone platforms (googleads): Starts the Google Ads OAuth flow and creates a standalone ads SocialAccount (googleads) with no parent. If the account already exists, returns alreadyConnected: true.
 *
 * Ads accounts appear as regular SocialAccount documents with ads platform values (e.g., metaads, tiktokads) in GET /v1/accounts.
 *
 */
export const connectAds = <ThrowOnError extends boolean = false>(options: OptionsLegacyParser<ConnectAdsData, ThrowOnError>) => {
    return (options?.client ?? client).get<ConnectAdsResponse, ConnectAdsError, ThrowOnError>({
        ...options,
        url: '/v1/connect/{platform}/ads'
    });
};

/**
 * Get Shopify OAuth connect URL
 * Initiate the Shopify OAuth flow for a store. Shopify is a connect-only
 * platform: the connected account does not publish social posts, it powers
 * the Blogs API (`/v1/accounts/{accountId}/blogs`). Returns an `authUrl`
 * to redirect the merchant to; after they approve the install, Shopify
 * redirects their browser to Zernio's callback, the account is created on
 * the profile (platform `shopify`), and the browser is redirected to
 * `redirect_url` (or the Zernio dashboard when omitted). Requested scopes
 * are `read_content` and `write_content` (content only; no customer or
 * order data). Connecting the same profile to a store again refreshes the
 * stored token in place.
 *
 */
export const getShopifyConnectUrl = <ThrowOnError extends boolean = false>(options: OptionsLegacyParser<GetShopifyConnectUrlData, ThrowOnError>) => {
    return (options?.client ?? client).get<GetShopifyConnectUrlResponse, GetShopifyConnectUrlError, ThrowOnError>({
        ...options,
        url: '/v1/connect/shopify'
    });
};

/**
 * Set TikTok brand identity
 * Set or update the Brand Identity (display name + avatar) for a
 * `tiktokads` SocialAccount. TikTok requires every ad to carry an
 * `identity_id + identity_type` pair. The Brand Identity is the
 * CUSTOMIZED_USER alternative to attributing ads to a real @username
 * (TT_USER). This route uploads the supplied image to TikTok, creates
 * the identity via `/v2/identity/create/`, and caches the resulting
 * `identity_id` on the account so subsequent `POST /v1/ads/create`
 * calls can opt into it via `identityType: 'CUSTOMIZED_USER'`.
 *
 * Configurable on every `tiktokads` account, including linked-mode ones
 * (those with a posting account on the same profile). Configuration is
 * idempotent and harmless when posting is also connected: the default
 * ad-create path still prefers TT_USER, and CUSTOMIZED_USER is only used
 * per-ad when the caller explicitly opts in.
 *
 * TikTok identities are immutable post-creation. Re-saving creates a new
 * identity on TikTok and swaps the cached id; the old identity stays
 * orphaned on TikTok's side (harmless, no billing impact).
 *
 * Alternative: pass `brandIdentity` directly on `POST /v1/ads/create` to
 * configure on first ad creation in a single round-trip.
 *
 */
export const configureTikTokAdsBrandIdentity = <ThrowOnError extends boolean = false>(options: OptionsLegacyParser<ConfigureTikTokAdsBrandIdentityData, ThrowOnError>) => {
    return (options?.client ?? client).patch<ConfigureTikTokAdsBrandIdentityResponse, ConfigureTikTokAdsBrandIdentityError, ThrowOnError>({
        ...options,
        url: '/v1/connect/tiktok-ads'
    });
};

/**
 * List Facebook pages
 * Returns the list of Facebook Pages the user can manage after OAuth. Extract tempToken and userProfile from the OAuth redirect params and pass them here. Use the X-Connect-Token header if connecting via API key.
 */
export const listFacebookPages = <ThrowOnError extends boolean = false>(options: OptionsLegacyParser<ListFacebookPagesData, ThrowOnError>) => {
    return (options?.client ?? client).get<ListFacebookPagesResponse, ListFacebookPagesError, ThrowOnError>({
        ...options,
        url: '/v1/connect/facebook/select-page'
    });
};

/**
 * Select Facebook page
 * Complete the headless flow by saving the user's selected Facebook page. Pass the userProfile from the OAuth redirect and use X-Connect-Token if connecting via API key.
 */
export const selectFacebookPage = <ThrowOnError extends boolean = false>(options: OptionsLegacyParser<SelectFacebookPageData, ThrowOnError>) => {
    return (options?.client ?? client).post<SelectFacebookPageResponse, SelectFacebookPageError, ThrowOnError>({
        ...options,
        url: '/v1/connect/facebook/select-page'
    });
};

/**
 * List Pages with a linked Instagram account
 * Completes the `loginMethod=facebook_login` Instagram flow, i.e. "Instagram API with Facebook Login".
 *
 * After the user authorizes on Facebook, extract `tempToken` from the redirect params (headless mode adds `step=select_account`) and pass it here to list the Facebook Pages they manage. Only Pages that have a linked Instagram professional account are returned, so an empty array means the user has no eligible Page. Use the X-Connect-Token header if connecting via API key.
 *
 * Not used by the default `instagram_login` flow, which creates the account without a selection step.
 *
 */
export const listInstagramPages = <ThrowOnError extends boolean = false>(options: OptionsLegacyParser<ListInstagramPagesData, ThrowOnError>) => {
    return (options?.client ?? client).get<ListInstagramPagesResponse, ListInstagramPagesError, ThrowOnError>({
        ...options,
        url: '/v1/connect/instagram/select-account'
    });
};

/**
 * Select the Page whose Instagram account to connect
 * Saves the selected Page as an Instagram account connected via Facebook Login. The Page access token becomes the account's access token, so every Instagram call for it runs against the Facebook Graph host.
 *
 * One Instagram account per profile: if the profile already has an Instagram account, this replaces it, and picking a different Instagram identity purges the previous account's conversations, external posts and stats.
 *
 */
export const selectInstagramAccount = <ThrowOnError extends boolean = false>(options: OptionsLegacyParser<SelectInstagramAccountData, ThrowOnError>) => {
    return (options?.client ?? client).post<SelectInstagramAccountResponse, SelectInstagramAccountError, ThrowOnError>({
        ...options,
        url: '/v1/connect/instagram/select-account'
    });
};

/**
 * List GBP locations
 * For headless flows. Returns the list of GBP locations the user can manage. Use pendingDataToken (from the OAuth callback redirect) to list locations without consuming the token, so it remains available for select-location. Use X-Connect-Token header if connecting via API key.
 *
 */
export const listGoogleBusinessLocations = <ThrowOnError extends boolean = false>(options?: OptionsLegacyParser<ListGoogleBusinessLocationsData, ThrowOnError>) => {
    return (options?.client ?? client).get<ListGoogleBusinessLocationsResponse, ListGoogleBusinessLocationsError, ThrowOnError>({
        ...options,
        url: '/v1/connect/googlebusiness/locations'
    });
};

/**
 * Select GBP location
 * Complete the headless GBP flow by saving the user's selected location. The pendingDataToken is returned in your redirect URL after OAuth completes (step=select_location). Tokens and profile data are stored server-side, so only the pendingDataToken is needed here. Use X-Connect-Token header if connecting via API key.
 *
 */
export const selectGoogleBusinessLocation = <ThrowOnError extends boolean = false>(options: OptionsLegacyParser<SelectGoogleBusinessLocationData, ThrowOnError>) => {
    return (options?.client ?? client).post<SelectGoogleBusinessLocationResponse, SelectGoogleBusinessLocationError, ThrowOnError>({
        ...options,
        url: '/v1/connect/googlebusiness/select-location'
    });
};

/**
 * Get reviews
 * Returns reviews for a GBP account including ratings, comments, and owner replies. Use nextPageToken for pagination.
 */
export const getGoogleBusinessReviews = <ThrowOnError extends boolean = false>(options: OptionsLegacyParser<GetGoogleBusinessReviewsData, ThrowOnError>) => {
    return (options?.client ?? client).get<GetGoogleBusinessReviewsResponse, GetGoogleBusinessReviewsError, ThrowOnError>({
        ...options,
        url: '/v1/accounts/{accountId}/gmb-reviews'
    });
};

/**
 * Get verification state
 * Returns the location's Voice of Merchant state plus its verification history. `voiceOfMerchantState.hasVoiceOfMerchant` tells you whether the listing is verified and published; when it is false, `verify` reports whether a verification is already pending. Each entry in `verifications` has a `state` of PENDING, COMPLETED, or FAILED.
 */
export const getGoogleBusinessVerifications = <ThrowOnError extends boolean = false>(options: OptionsLegacyParser<GetGoogleBusinessVerificationsData, ThrowOnError>) => {
    return (options?.client ?? client).get<GetGoogleBusinessVerificationsResponse, GetGoogleBusinessVerificationsError, ThrowOnError>({
        ...options,
        url: '/v1/accounts/{accountId}/gmb-verifications'
    });
};

/**
 * Start a verification
 * Starts a verification for the location. This is a mutating action: depending on `method`, Google mails a postcard, places a call, or sends an SMS/email to the business. Submit the resulting code with POST /gmb-verifications/{verificationId}/complete. Use POST /gmb-verifications/options first to discover which methods are eligible.
 */
export const startGoogleBusinessVerification = <ThrowOnError extends boolean = false>(options: OptionsLegacyParser<StartGoogleBusinessVerificationData, ThrowOnError>) => {
    return (options?.client ?? client).post<StartGoogleBusinessVerificationResponse, StartGoogleBusinessVerificationError, ThrowOnError>({
        ...options,
        url: '/v1/accounts/{accountId}/gmb-verifications'
    });
};

/**
 * Fetch verification options
 * Reports the verification methods Google currently offers for the location. Non-mutating (nothing is sent to the business). `languageCode` is required; service-area ("CUSTOMER_LOCATION_ONLY") businesses also require `context.address`, otherwise Google returns 400.
 */
export const fetchGoogleBusinessVerificationOptions = <ThrowOnError extends boolean = false>(options: OptionsLegacyParser<FetchGoogleBusinessVerificationOptionsData, ThrowOnError>) => {
    return (options?.client ?? client).post<FetchGoogleBusinessVerificationOptionsResponse, FetchGoogleBusinessVerificationOptionsError, ThrowOnError>({
        ...options,
        url: '/v1/accounts/{accountId}/gmb-verifications/options'
    });
};

/**
 * Complete a verification
 * Completes a PENDING verification by submitting the PIN/code Google sent the business (postcard code, SMS PIN, etc.). On success the verification moves to COMPLETED.
 */
export const completeGoogleBusinessVerification = <ThrowOnError extends boolean = false>(options: OptionsLegacyParser<CompleteGoogleBusinessVerificationData, ThrowOnError>) => {
    return (options?.client ?? client).post<CompleteGoogleBusinessVerificationResponse, CompleteGoogleBusinessVerificationError, ThrowOnError>({
        ...options,
        url: '/v1/accounts/{accountId}/gmb-verifications/{verificationId}/complete'
    });
};

/**
 * Get food menus
 * Returns food menus for a GBP location including sections, items, pricing, and dietary info. Only for locations with food menu support.
 */
export const getGoogleBusinessFoodMenus = <ThrowOnError extends boolean = false>(options: OptionsLegacyParser<GetGoogleBusinessFoodMenusData, ThrowOnError>) => {
    return (options?.client ?? client).get<GetGoogleBusinessFoodMenusResponse, GetGoogleBusinessFoodMenusError, ThrowOnError>({
        ...options,
        url: '/v1/accounts/{accountId}/gmb-food-menus'
    });
};

/**
 * Update food menus
 * Updates food menus for a GBP location. Send the full menus array. Use updateMask for partial updates.
 */
export const updateGoogleBusinessFoodMenus = <ThrowOnError extends boolean = false>(options: OptionsLegacyParser<UpdateGoogleBusinessFoodMenusData, ThrowOnError>) => {
    return (options?.client ?? client).put<UpdateGoogleBusinessFoodMenusResponse, UpdateGoogleBusinessFoodMenusError, ThrowOnError>({
        ...options,
        url: '/v1/accounts/{accountId}/gmb-food-menus'
    });
};

/**
 * Get location details
 * Returns detailed GBP location info (hours, description, phone, website, categories, services). Use readMask to request specific fields.
 */
export const getGoogleBusinessLocationDetails = <ThrowOnError extends boolean = false>(options: OptionsLegacyParser<GetGoogleBusinessLocationDetailsData, ThrowOnError>) => {
    return (options?.client ?? client).get<GetGoogleBusinessLocationDetailsResponse, GetGoogleBusinessLocationDetailsError, ThrowOnError>({
        ...options,
        url: '/v1/accounts/{accountId}/gmb-location-details'
    });
};

/**
 * Update location details
 * Updates GBP location details. The updateMask field is required and specifies which fields to update.
 * This endpoint proxies Google's Business Information API locations.patch, so any valid updateMask field is supported.
 * Common fields: regularHours, specialHours, profile.description, websiteUri, phoneNumbers, categories, serviceItems.
 *
 */
export const updateGoogleBusinessLocationDetails = <ThrowOnError extends boolean = false>(options: OptionsLegacyParser<UpdateGoogleBusinessLocationDetailsData, ThrowOnError>) => {
    return (options?.client ?? client).put<UpdateGoogleBusinessLocationDetailsResponse, UpdateGoogleBusinessLocationDetailsError, ThrowOnError>({
        ...options,
        url: '/v1/accounts/{accountId}/gmb-location-details'
    });
};

/**
 * List media
 * Lists media items (photos) for a Google Business Profile location.
 * Returns photo URLs, descriptions, categories, and metadata.
 *
 */
export const listGoogleBusinessMedia = <ThrowOnError extends boolean = false>(options: OptionsLegacyParser<ListGoogleBusinessMediaData, ThrowOnError>) => {
    return (options?.client ?? client).get<ListGoogleBusinessMediaResponse, ListGoogleBusinessMediaError, ThrowOnError>({
        ...options,
        url: '/v1/accounts/{accountId}/gmb-media'
    });
};

/**
 * Upload photo
 * Creates a media item (photo) for a location from a publicly accessible URL.
 *
 * Categories determine where the photo appears: CATEGORY_UNSPECIFIED, COVER, PROFILE, LOGO, EXTERIOR, INTERIOR, PRODUCT, FOOD_AND_DRINK, MENU, COMMON_AREA, ROOMS, TEAMS, AT_WORK, ADDITIONAL.
 *
 */
export const createGoogleBusinessMedia = <ThrowOnError extends boolean = false>(options: OptionsLegacyParser<CreateGoogleBusinessMediaData, ThrowOnError>) => {
    return (options?.client ?? client).post<CreateGoogleBusinessMediaResponse, CreateGoogleBusinessMediaError, ThrowOnError>({
        ...options,
        url: '/v1/accounts/{accountId}/gmb-media'
    });
};

/**
 * Delete photo
 * Deletes a photo or media item from a GBP location.
 */
export const deleteGoogleBusinessMedia = <ThrowOnError extends boolean = false>(options: OptionsLegacyParser<DeleteGoogleBusinessMediaData, ThrowOnError>) => {
    return (options?.client ?? client).delete<DeleteGoogleBusinessMediaResponse, DeleteGoogleBusinessMediaError, ThrowOnError>({
        ...options,
        url: '/v1/accounts/{accountId}/gmb-media'
    });
};

/**
 * Get attribute metadata
 * Returns metadata about which Google Business Profile attributes are available for
 * a location or business category. Use this endpoint to discover valid attribute names,
 * value types, and allowed enum values before reading or writing via gmb-attributes.
 *
 * Two mutually exclusive query modes:
 *
 * **Location mode**: pass `locationId` (or rely on the account's stored `selectedLocationId`).
 * Google returns attributes valid for that specific location.
 *
 * **Category mode**: pass `categoryName` (must start with `categories/`) and `regionCode`.
 * Google returns attributes valid for that category across the given region.
 * `languageCode` is optional in category mode.
 *
 * Both modes support `pageSize` and `pageToken` for pagination.
 *
 */
export const getGmbAttributeMetadata = <ThrowOnError extends boolean = false>(options: OptionsLegacyParser<GetGmbAttributeMetadataData, ThrowOnError>) => {
    return (options?.client ?? client).get<GetGmbAttributeMetadataResponse, GetGmbAttributeMetadataError, ThrowOnError>({
        ...options,
        url: '/v1/accounts/{accountId}/gmb-attribute-metadata'
    });
};

/**
 * Get attributes
 * Returns GBP location attributes (amenities, services, accessibility, payment types). Available attributes vary by business category.
 */
export const getGoogleBusinessAttributes = <ThrowOnError extends boolean = false>(options: OptionsLegacyParser<GetGoogleBusinessAttributesData, ThrowOnError>) => {
    return (options?.client ?? client).get<GetGoogleBusinessAttributesResponse, GetGoogleBusinessAttributesError, ThrowOnError>({
        ...options,
        url: '/v1/accounts/{accountId}/gmb-attributes'
    });
};

/**
 * Update attributes
 * Updates location attributes (amenities, services, etc.).
 *
 * The attributeMask specifies which attributes to update (comma-separated).
 *
 */
export const updateGoogleBusinessAttributes = <ThrowOnError extends boolean = false>(options: OptionsLegacyParser<UpdateGoogleBusinessAttributesData, ThrowOnError>) => {
    return (options?.client ?? client).put<UpdateGoogleBusinessAttributesResponse, UpdateGoogleBusinessAttributesError, ThrowOnError>({
        ...options,
        url: '/v1/accounts/{accountId}/gmb-attributes'
    });
};

/**
 * List action links
 * Lists place action links for a Google Business Profile location.
 *
 * Place actions are the booking, ordering, and reservation buttons that appear on your listing.
 *
 */
export const listGoogleBusinessPlaceActions = <ThrowOnError extends boolean = false>(options: OptionsLegacyParser<ListGoogleBusinessPlaceActionsData, ThrowOnError>) => {
    return (options?.client ?? client).get<ListGoogleBusinessPlaceActionsResponse, ListGoogleBusinessPlaceActionsError, ThrowOnError>({
        ...options,
        url: '/v1/accounts/{accountId}/gmb-place-actions'
    });
};

/**
 * Create action link
 * Creates a place action link for a location.
 *
 * Available action types: APPOINTMENT, ONLINE_APPOINTMENT, DINING_RESERVATION, FOOD_ORDERING, FOOD_DELIVERY, FOOD_TAKEOUT, SHOP_ONLINE.
 *
 */
export const createGoogleBusinessPlaceAction = <ThrowOnError extends boolean = false>(options: OptionsLegacyParser<CreateGoogleBusinessPlaceActionData, ThrowOnError>) => {
    return (options?.client ?? client).post<CreateGoogleBusinessPlaceActionResponse, CreateGoogleBusinessPlaceActionError, ThrowOnError>({
        ...options,
        url: '/v1/accounts/{accountId}/gmb-place-actions'
    });
};

/**
 * Delete action link
 * Deletes a place action link (e.g. booking or ordering URL) from a GBP location.
 */
export const deleteGoogleBusinessPlaceAction = <ThrowOnError extends boolean = false>(options: OptionsLegacyParser<DeleteGoogleBusinessPlaceActionData, ThrowOnError>) => {
    return (options?.client ?? client).delete<DeleteGoogleBusinessPlaceActionResponse, DeleteGoogleBusinessPlaceActionError, ThrowOnError>({
        ...options,
        url: '/v1/accounts/{accountId}/gmb-place-actions'
    });
};

/**
 * Update action link
 * Updates a place action link (change URL or action type).
 * Only the fields included in the request body will be updated.
 *
 */
export const updateGoogleBusinessPlaceAction = <ThrowOnError extends boolean = false>(options: OptionsLegacyParser<UpdateGoogleBusinessPlaceActionData, ThrowOnError>) => {
    return (options?.client ?? client).patch<UpdateGoogleBusinessPlaceActionResponse, UpdateGoogleBusinessPlaceActionError, ThrowOnError>({
        ...options,
        url: '/v1/accounts/{accountId}/gmb-place-actions'
    });
};

/**
 * Get services
 * Gets the services offered by a Google Business Profile location.
 * Returns an array of service items (structured or free-form with optional price).
 *
 */
export const getGoogleBusinessServices = <ThrowOnError extends boolean = false>(options: OptionsLegacyParser<GetGoogleBusinessServicesData, ThrowOnError>) => {
    return (options?.client ?? client).get<GetGoogleBusinessServicesResponse, GetGoogleBusinessServicesError, ThrowOnError>({
        ...options,
        url: '/v1/accounts/{accountId}/gmb-services'
    });
};

/**
 * Replace services
 * Replaces the entire service list for a location.
 * Google's API requires full replacement; individual item updates are not supported.
 * Each service can be structured (using a predefined serviceTypeId) or free-form (custom label).
 *
 */
export const updateGoogleBusinessServices = <ThrowOnError extends boolean = false>(options: OptionsLegacyParser<UpdateGoogleBusinessServicesData, ThrowOnError>) => {
    return (options?.client ?? client).put<UpdateGoogleBusinessServicesResponse, UpdateGoogleBusinessServicesError, ThrowOnError>({
        ...options,
        url: '/v1/accounts/{accountId}/gmb-services'
    });
};

/**
 * Batch get reviews
 * Fetches reviews across multiple locations in a single request.
 * More efficient than calling GET /gmb-reviews per location for multi-location businesses.
 * Returns a flat locationReviews array (not grouped by location): each item carries
 * the location resource name it belongs to (`name`) plus the review object (`review`),
 * whose identity is `review.reviewId`.
 * Reviews are requested from Google ordered by `orderBy` (default `updateTime desc`,
 * newest first), so callers polling for recent reviews can stop paginating once they
 * cross their date window.
 * Note: this endpoint does not return aggregate metrics (averageRating / totalReviewCount).
 * For those, use the single-location GET /gmb-reviews endpoint.
 *
 */
export const batchGetGoogleBusinessReviews = <ThrowOnError extends boolean = false>(options: OptionsLegacyParser<BatchGetGoogleBusinessReviewsData, ThrowOnError>) => {
    return (options?.client ?? client).post<BatchGetGoogleBusinessReviewsResponse, BatchGetGoogleBusinessReviewsError, ThrowOnError>({
        ...options,
        url: '/v1/accounts/{accountId}/gmb-reviews/batch'
    });
};

/**
 * Reply to a review
 * Posts (or updates) the business owner reply to a Google Business review.
 * The reply is associated with the account's currently selected location (set via /v1/accounts/{accountId}/gmb-locations).
 * Calling this endpoint a second time on the same review overwrites the previous reply (PUT semantics on Google's side).
 *
 */
export const replyToGoogleBusinessReview = <ThrowOnError extends boolean = false>(options: OptionsLegacyParser<ReplyToGoogleBusinessReviewData, ThrowOnError>) => {
    return (options?.client ?? client).post<ReplyToGoogleBusinessReviewResponse, ReplyToGoogleBusinessReviewError, ThrowOnError>({
        ...options,
        url: '/v1/accounts/{accountId}/gmb-reviews/{reviewId}/reply'
    });
};

/**
 * Delete a review reply
 * Removes the business owner reply from a Google Business review. The review itself remains.
 */
export const deleteGoogleBusinessReviewReply = <ThrowOnError extends boolean = false>(options: OptionsLegacyParser<DeleteGoogleBusinessReviewReplyData, ThrowOnError>) => {
    return (options?.client ?? client).delete<DeleteGoogleBusinessReviewReplyResponse, DeleteGoogleBusinessReviewReplyError, ThrowOnError>({
        ...options,
        url: '/v1/accounts/{accountId}/gmb-reviews/{reviewId}/reply'
    });
};

/**
 * Get pending OAuth data
 * Fetch pending OAuth data for headless mode using the pendingDataToken from the redirect URL.
 *
 * **Scope**: This endpoint is used for LinkedIn organizations, Snapchat profiles, and Pinterest boards, where the selection list is too large to fit in URL params. The redirect carries a `pendingDataToken` instead of the full payload; the response includes the corresponding selection array (e.g. `boards` for Pinterest). WhatsApp, Facebook, Google Business and other platforms pass selection state directly via URL query params on the redirect (`profileId`, `tempToken`, `step`), no pending record is created, so this endpoint will return 404 for those flows. Use the platform-specific selection endpoint instead (e.g. `/v1/connect/whatsapp/select-phone-number`).
 *
 * Token is one-time use and expires after 10 minutes. No authentication required.
 *
 */
export const getPendingOAuthData = <ThrowOnError extends boolean = false>(options: OptionsLegacyParser<GetPendingOAuthDataData, ThrowOnError>) => {
    return (options?.client ?? client).get<GetPendingOAuthDataResponse, GetPendingOAuthDataError, ThrowOnError>({
        ...options,
        url: '/v1/connect/pending-data'
    });
};

/**
 * List LinkedIn orgs
 * Fetch full LinkedIn organization details (logos, vanity names, websites) for custom UI. No authentication required, just the tempToken from OAuth.
 */
export const listLinkedInOrganizations = <ThrowOnError extends boolean = false>(options: OptionsLegacyParser<ListLinkedInOrganizationsData, ThrowOnError>) => {
    return (options?.client ?? client).get<ListLinkedInOrganizationsResponse, ListLinkedInOrganizationsError, ThrowOnError>({
        ...options,
        url: '/v1/connect/linkedin/organizations'
    });
};

/**
 * Select LinkedIn org
 * Complete the LinkedIn connection flow. Set accountType to "personal" or "organization" to connect as a company page. Use X-Connect-Token if connecting via API key.
 */
export const selectLinkedInOrganization = <ThrowOnError extends boolean = false>(options: OptionsLegacyParser<SelectLinkedInOrganizationData, ThrowOnError>) => {
    return (options?.client ?? client).post<SelectLinkedInOrganizationResponse, SelectLinkedInOrganizationError, ThrowOnError>({
        ...options,
        url: '/v1/connect/linkedin/select-organization'
    });
};

/**
 * List Pinterest boards
 * For headless flows. Returns Pinterest boards the user can post to. Use X-Connect-Token from the redirect URL.
 */
export const listPinterestBoardsForSelection = <ThrowOnError extends boolean = false>(options: OptionsLegacyParser<ListPinterestBoardsForSelectionData, ThrowOnError>) => {
    return (options?.client ?? client).get<ListPinterestBoardsForSelectionResponse, ListPinterestBoardsForSelectionError, ThrowOnError>({
        ...options,
        url: '/v1/connect/pinterest/select-board'
    });
};

/**
 * Select Pinterest board
 * Complete the Pinterest connection flow. After OAuth, use this endpoint to save the selected board and complete the account connection. Use the X-Connect-Token header if you initiated the connection via API key.
 *
 */
export const selectPinterestBoard = <ThrowOnError extends boolean = false>(options: OptionsLegacyParser<SelectPinterestBoardData, ThrowOnError>) => {
    return (options?.client ?? client).post<SelectPinterestBoardResponse, SelectPinterestBoardError, ThrowOnError>({
        ...options,
        url: '/v1/connect/pinterest/select-board'
    });
};

/**
 * List Snapchat profiles
 * For headless flows. Returns Snapchat Public Profiles the user can post to. Use X-Connect-Token from the redirect URL.
 */
export const listSnapchatProfiles = <ThrowOnError extends boolean = false>(options: OptionsLegacyParser<ListSnapchatProfilesData, ThrowOnError>) => {
    return (options?.client ?? client).get<ListSnapchatProfilesResponse, ListSnapchatProfilesError, ThrowOnError>({
        ...options,
        url: '/v1/connect/snapchat/select-profile'
    });
};

/**
 * Select Snapchat profile
 * Complete the Snapchat connection flow by saving the selected Public Profile. Snapchat requires a Public Profile to publish content. Use X-Connect-Token if connecting via API key.
 */
export const selectSnapchatProfile = <ThrowOnError extends boolean = false>(options: OptionsLegacyParser<SelectSnapchatProfileData, ThrowOnError>) => {
    return (options?.client ?? client).post<SelectSnapchatProfileResponse, SelectSnapchatProfileError, ThrowOnError>({
        ...options,
        url: '/v1/connect/snapchat/select-profile'
    });
};

/**
 * Connect Bluesky account
 * Connect a Bluesky account using identifier (handle or email) and an app password.
 * To get your userId for the state parameter, call GET /v1/users which includes a currentUserId field.
 *
 */
export const connectBlueskyCredentials = <ThrowOnError extends boolean = false>(options: OptionsLegacyParser<ConnectBlueskyCredentialsData, ThrowOnError>) => {
    return (options?.client ?? client).post<ConnectBlueskyCredentialsResponse, ConnectBlueskyCredentialsError, ThrowOnError>({
        ...options,
        url: '/v1/connect/bluesky/credentials'
    });
};

/**
 * Connect an OpenAI Ads account
 * Connect an OpenAI Ads account using an API key from ChatGPT Ads Manager.
 *
 * The key grants full campaign write access on OpenAI's side (OpenAI does
 * not offer a read-only key scope). Zernio uses it to read ads and
 * performance, and to create and manage campaigns you set up through
 * Zernio (create, status, budget, and cancel). Campaigns created
 * directly in ChatGPT Ads Manager can still be managed there.
 *
 */
export const connectOpenAiAdsCredentials = <ThrowOnError extends boolean = false>(options: OptionsLegacyParser<ConnectOpenAiAdsCredentialsData, ThrowOnError>) => {
    return (options?.client ?? client).post<ConnectOpenAiAdsCredentialsResponse, ConnectOpenAiAdsCredentialsError, ThrowOnError>({
        ...options,
        url: '/v1/connect/openai-ads/credentials'
    });
};

/**
 * Connect WhatsApp via credentials
 * Connect a WhatsApp Business Account by providing Meta credentials directly.
 * This is the headless alternative to the Embedded Signup browser flow.
 *
 * To get the required credentials:
 * 1. Go to Meta Business Suite (business.facebook.com)
 * 2. Create or select a WhatsApp Business Account
 * 3. In Business Settings > System Users, create a System User
 * 4. Assign it the whatsapp_business_management and whatsapp_business_messaging permissions
 * 5. Generate a permanent access token
 * 6. Get the WABA ID from WhatsApp Manager > Account Tools > Phone Numbers
 * 7. Get the Phone Number ID from the same page (click on the number)
 *
 */
export const connectWhatsAppCredentials = <ThrowOnError extends boolean = false>(options: OptionsLegacyParser<ConnectWhatsAppCredentialsData, ThrowOnError>) => {
    return (options?.client ?? client).post<ConnectWhatsAppCredentialsResponse, ConnectWhatsAppCredentialsError, ThrowOnError>({
        ...options,
        url: '/v1/connect/whatsapp/credentials'
    });
};

/**
 * List numbers for selection
 * Fetch the WhatsApp phone numbers available across the user's WhatsApp Business Accounts (WABAs) after a headless OAuth flow.
 *
 * WhatsApp OAuth grants access at the WABA level. When a connected WABA has 2 or more phone numbers, you must call this endpoint to list them and then `POST /v1/connect/whatsapp/select-phone-number` to bind one to the Zernio profile. Single-phone WABAs auto-complete during the OAuth callback and never reach this endpoint.
 *
 * Use the `profileId` and `tempToken` returned in the headless redirect (`step=select_phone_number`).
 *
 * Alternative: if you already know `wabaId` and `phoneNumberId` (e.g. from Meta Business Suite), use `connectWhatsAppCredentials` instead, which skips this two-step flow.
 *
 */
export const listWhatsAppPhoneNumbers = <ThrowOnError extends boolean = false>(options: OptionsLegacyParser<ListWhatsAppPhoneNumbersData, ThrowOnError>) => {
    return (options?.client ?? client).get<ListWhatsAppPhoneNumbersResponse, ListWhatsAppPhoneNumbersError, ThrowOnError>({
        ...options,
        url: '/v1/connect/whatsapp/select-phone-number'
    });
};

/**
 * Complete number selection
 * Bind a specific WhatsApp phone number to the Zernio profile after the user picks one from `listWhatsAppPhoneNumbers`. Exchanges the short-lived OAuth token for a long-lived token, subscribes the WABA to webhooks, and creates the SocialAccount.
 *
 */
export const completeWhatsAppPhoneSelection = <ThrowOnError extends boolean = false>(options: OptionsLegacyParser<CompleteWhatsAppPhoneSelectionData, ThrowOnError>) => {
    return (options?.client ?? client).post<CompleteWhatsAppPhoneSelectionResponse, CompleteWhatsAppPhoneSelectionError, ThrowOnError>({
        ...options,
        url: '/v1/connect/whatsapp/select-phone-number'
    });
};

/**
 * Generate Telegram code
 * Generate an access code (valid 15 minutes) for connecting a Telegram channel or group. Add the bot as admin, then send the code + @yourchannel to the bot. Poll PATCH /v1/connect/telegram to check status.
 */
export const getTelegramConnectStatus = <ThrowOnError extends boolean = false>(options: OptionsLegacyParser<GetTelegramConnectStatusData, ThrowOnError>) => {
    return (options?.client ?? client).get<GetTelegramConnectStatusResponse, GetTelegramConnectStatusError, ThrowOnError>({
        ...options,
        url: '/v1/connect/telegram'
    });
};

/**
 * Connect Telegram directly
 * Connect a Telegram channel/group directly using the chat ID. Alternative to the access code flow. The bot must already be an admin in the channel/group.
 */
export const initiateTelegramConnect = <ThrowOnError extends boolean = false>(options: OptionsLegacyParser<InitiateTelegramConnectData, ThrowOnError>) => {
    return (options?.client ?? client).post<InitiateTelegramConnectResponse, InitiateTelegramConnectError, ThrowOnError>({
        ...options,
        url: '/v1/connect/telegram'
    });
};

/**
 * Check Telegram status
 * Poll this endpoint to check if a Telegram access code has been used to connect a channel/group. Recommended polling interval: 3 seconds.
 * Status values: pending (waiting for user), connected (channel/group linked), expired (generate a new code).
 *
 */
export const completeTelegramConnect = <ThrowOnError extends boolean = false>(options: OptionsLegacyParser<CompleteTelegramConnectData, ThrowOnError>) => {
    return (options?.client ?? client).patch<CompleteTelegramConnectResponse, CompleteTelegramConnectError, ThrowOnError>({
        ...options,
        url: '/v1/connect/telegram'
    });
};

/**
 * List Facebook pages
 * Returns all Facebook pages the connected account has access to, including the currently selected page.
 */
export const getFacebookPages = <ThrowOnError extends boolean = false>(options: OptionsLegacyParser<GetFacebookPagesData, ThrowOnError>) => {
    return (options?.client ?? client).get<GetFacebookPagesResponse, GetFacebookPagesError, ThrowOnError>({
        ...options,
        url: '/v1/accounts/{accountId}/facebook-page'
    });
};

/**
 * Update Facebook page
 * Switch which Facebook Page is active for a connected account.
 */
export const updateFacebookPage = <ThrowOnError extends boolean = false>(options: OptionsLegacyParser<UpdateFacebookPageData, ThrowOnError>) => {
    return (options?.client ?? client).put<UpdateFacebookPageResponse, UpdateFacebookPageError, ThrowOnError>({
        ...options,
        url: '/v1/accounts/{accountId}/facebook-page'
    });
};

/**
 * List LinkedIn orgs
 * Returns LinkedIn organizations (company pages) the connected account has admin access to.
 */
export const getLinkedInOrganizations = <ThrowOnError extends boolean = false>(options: OptionsLegacyParser<GetLinkedInOrganizationsData, ThrowOnError>) => {
    return (options?.client ?? client).get<GetLinkedInOrganizationsResponse, GetLinkedInOrganizationsError, ThrowOnError>({
        ...options,
        url: '/v1/accounts/{accountId}/linkedin-organizations'
    });
};

/**
 * Get LinkedIn aggregate stats
 * Returns aggregate analytics across all posts for a LinkedIn personal account. Only includes posts published through Zernio (LinkedIn API limitation). Org accounts should use /v1/analytics instead. Requires r_member_postAnalytics scope. Saves (POST_SAVE) and sends (POST_SEND) are available for personal accounts; organization pages always return 0 for these two metrics because LinkedIn does not expose them on the organization analytics endpoint.
 */
export const getLinkedInAggregateAnalytics = <ThrowOnError extends boolean = false>(options: OptionsLegacyParser<GetLinkedInAggregateAnalyticsData, ThrowOnError>) => {
    return (options?.client ?? client).get<GetLinkedInAggregateAnalyticsResponse, GetLinkedInAggregateAnalyticsError, ThrowOnError>({
        ...options,
        url: '/v1/accounts/{accountId}/linkedin-aggregate-analytics'
    });
};

/**
 * Get LinkedIn post stats
 * Returns analytics for a specific LinkedIn post by URN. Works for both personal and organization accounts. Saves and sends are only populated for personal accounts (LinkedIn does not expose these metrics on the organization analytics endpoint).
 */
export const getLinkedInPostAnalytics = <ThrowOnError extends boolean = false>(options: OptionsLegacyParser<GetLinkedInPostAnalyticsData, ThrowOnError>) => {
    return (options?.client ?? client).get<GetLinkedInPostAnalyticsResponse, GetLinkedInPostAnalyticsError, ThrowOnError>({
        ...options,
        url: '/v1/accounts/{accountId}/linkedin-post-analytics'
    });
};

/**
 * Get LinkedIn post reactions
 * Returns individual reactions for a specific LinkedIn post, including reactor profiles
 * (name, headline/job title, profile picture, profile URL, reaction type).
 * Only works for organization/company page accounts. LinkedIn restricts reaction
 * data for personal profiles (r_member_social_feed is a closed permission).
 *
 */
export const getLinkedInPostReactions = <ThrowOnError extends boolean = false>(options: OptionsLegacyParser<GetLinkedInPostReactionsData, ThrowOnError>) => {
    return (options?.client ?? client).get<GetLinkedInPostReactionsResponse, GetLinkedInPostReactionsError, ThrowOnError>({
        ...options,
        url: '/v1/accounts/{accountId}/linkedin-post-reactions'
    });
};

/**
 * Switch LinkedIn account type
 * Switch a LinkedIn account between personal profile and organization (company page) posting.
 */
export const updateLinkedInOrganization = <ThrowOnError extends boolean = false>(options: OptionsLegacyParser<UpdateLinkedInOrganizationData, ThrowOnError>) => {
    return (options?.client ?? client).put<UpdateLinkedInOrganizationResponse, UpdateLinkedInOrganizationError, ThrowOnError>({
        ...options,
        url: '/v1/accounts/{accountId}/linkedin-organization'
    });
};

/**
 * Resolve LinkedIn mention
 * Converts a LinkedIn profile or company URL to a URN for @mentions in posts.
 *
 * How to use LinkedIn @mentions (2-step workflow):
 *
 * 1. Call this endpoint with the LinkedIn profile/company URL to get the mention URN and format.
 * 2. Embed the returned mentionFormat (e.g. @[Vincent Jong](urn:li:person:xxx)) directly in your post's content field.
 *
 * Example:
 * - Resolve: GET /v1/accounts/{id}/linkedin-mentions?url=linkedin.com/in/vincentjong&displayName=Vincent Jong
 * - Returns: mentionFormat: "@[Vincent Jong](urn:li:person:xxx)"
 * - Use in post content: "Great talk with @[Vincent Jong](urn:li:person:xxx) today!"
 *
 * Important: The mentions array field in POST /v1/posts is stored for reference only and does NOT trigger @mentions on LinkedIn. You must embed the mention format directly in the content text.
 *
 * Requirements:
 * - Person mentions require the LinkedIn account to be admin of at least one organization. This is a LinkedIn API limitation: the only endpoints that resolve profile URLs to member URNs (vanityUrl, peopleTypeahead) are scoped to organization followers. There is no public LinkedIn API to resolve a vanity URL without organization context.
 * - Organization mentions (e.g. @Microsoft) work without this requirement.
 * - For person mentions to be clickable, the displayName parameter must exactly match the name shown on their LinkedIn profile.
 * - Person mentions DO work when published from personal profiles (the URN just needs to be valid). The limitation is only in the resolution step (URL to URN), not in publishing.
 *
 */
export const getLinkedInMentions = <ThrowOnError extends boolean = false>(options: OptionsLegacyParser<GetLinkedInMentionsData, ThrowOnError>) => {
    return (options?.client ?? client).get<GetLinkedInMentionsResponse, GetLinkedInMentionsError, ThrowOnError>({
        ...options,
        url: '/v1/accounts/{accountId}/linkedin-mentions'
    });
};

/**
 * List active Instagram stories
 * Returns the IG Business/Creator account's currently-active stories.
 * Meta keeps stories live for 24h; expired stories are not returned.
 *
 * Limitations propagated from Meta (these are NOT bugs):
 * - 24h window only
 * - Live videos excluded
 * - Reshared stories not returned
 * - `mediaUrl` may be null if Meta flagged the story for copyright
 * - `caption`, `likeCount`, `commentsCount` do not apply to story media
 *
 */
export const listInstagramStories = <ThrowOnError extends boolean = false>(options: OptionsLegacyParser<ListInstagramStoriesData, ThrowOnError>) => {
    return (options?.client ?? client).get<ListInstagramStoriesResponse, ListInstagramStoriesError, ThrowOnError>({
        ...options,
        url: '/v1/accounts/{accountId}/instagram/stories'
    });
};

/**
 * Get Instagram publishing limit
 * Returns the account's remaining content-publishing quota for Instagram's rolling
 * 24-hour window, so you can pace publishing and warn before the cap is reached.
 *
 * `quotaUsage` counts containers published since the start of the window.
 * Always compare against the returned `quotaTotal` rather than hardcoding a number:
 * Meta's prose documentation and the live API disagree on the value, and the live
 * value is authoritative.
 *
 */
export const getInstagramPublishingLimit = <ThrowOnError extends boolean = false>(options: OptionsLegacyParser<GetInstagramPublishingLimitData, ThrowOnError>) => {
    return (options?.client ?? client).get<GetInstagramPublishingLimitResponse, GetInstagramPublishingLimitError, ThrowOnError>({
        ...options,
        url: '/v1/accounts/{accountId}/instagram/publishing-limit'
    });
};

/**
 * Search Instagram audio
 * Search Instagram's audio catalog (licensed music or original sounds),
 * or list what is currently trending by omitting `q`. Returns up to ~30
 * assets; Meta exposes no pagination on this edge.
 *
 * Pass the returned `audioId` as
 * `platformSpecificData.audioConfiguration.audioId` when creating a Reel
 * to publish it with that track.
 *
 * Requires an Instagram account connected via **Facebook Login**. Meta
 * hosts this catalog on graph.facebook.com only, so accounts connected
 * with classic Instagram Login receive a 400
 * (`instagram_audio_requires_facebook_login`) and must be reconnected
 * choosing the Facebook option.
 *
 */
export const searchInstagramAudio = <ThrowOnError extends boolean = false>(options: OptionsLegacyParser<SearchInstagramAudioData, ThrowOnError>) => {
    return (options?.client ?? client).get<SearchInstagramAudioResponse, SearchInstagramAudioError, ThrowOnError>({
        ...options,
        url: '/v1/accounts/{accountId}/instagram/audio'
    });
};

/**
 * Get Instagram audio metadata
 * Fetch one audio asset's metadata by ID. Use it to re-validate a stored
 * `audioId` before a scheduled Reel publishes, or to refresh the preview
 * `downloadUrl` (Meta expires preview URLs after roughly 1.5 days).
 *
 * Same connection requirement as the search endpoint: Facebook-Login
 * Instagram accounts only.
 *
 */
export const getInstagramAudio = <ThrowOnError extends boolean = false>(options: OptionsLegacyParser<GetInstagramAudioData, ThrowOnError>) => {
    return (options?.client ?? client).get<GetInstagramAudioResponse, GetInstagramAudioError, ThrowOnError>({
        ...options,
        url: '/v1/accounts/{accountId}/instagram/audio/{audioId}'
    });
};

/**
 * Get Instagram story insights
 * Returns metrics for a single story. The `source` field discriminates
 * between three states:
 *
 * - `live` — fetched from Meta in real time (story is still active)
 * - `cached` — fetched from a persisted `story_insights` webhook payload
 * (story has expired but we received its final-state metrics from Meta)
 * - `unavailable` — story has expired and we never received its webhook
 * payload (for example, the account connected after the story expired)
 *
 * Meta can report an expired story as an empty successful result rather
 * than an error, so an expired story resolves to `cached` or `unavailable`
 * even though the upstream request itself succeeded.
 *
 * Field semantics follow Meta's API. Counts below 5 may be returned as 0
 * due to Meta's privacy floor on small audiences. The `navigation` field
 * is the sum of `tapsForward + tapsBack + exits + swipesForward`.
 *
 */
export const getInstagramStoryInsights = <ThrowOnError extends boolean = false>(options: OptionsLegacyParser<GetInstagramStoryInsightsData, ThrowOnError>) => {
    return (options?.client ?? client).get<GetInstagramStoryInsightsResponse, GetInstagramStoryInsightsError, ThrowOnError>({
        ...options,
        url: '/v1/accounts/{accountId}/instagram/stories/{storyId}/insights'
    });
};

/**
 * List Pinterest boards
 * Returns the boards available for a connected Pinterest account. Use this to get a board ID when creating a Pinterest post.
 */
export const getPinterestBoards = <ThrowOnError extends boolean = false>(options: OptionsLegacyParser<GetPinterestBoardsData, ThrowOnError>) => {
    return (options?.client ?? client).get<GetPinterestBoardsResponse, GetPinterestBoardsError, ThrowOnError>({
        ...options,
        url: '/v1/accounts/{accountId}/pinterest-boards'
    });
};

/**
 * Set default Pinterest board
 * Sets the default board used when publishing pins for this account.
 */
export const updatePinterestBoards = <ThrowOnError extends boolean = false>(options: OptionsLegacyParser<UpdatePinterestBoardsData, ThrowOnError>) => {
    return (options?.client ?? client).put<UpdatePinterestBoardsResponse, UpdatePinterestBoardsError, ThrowOnError>({
        ...options,
        url: '/v1/accounts/{accountId}/pinterest-boards'
    });
};

/**
 * Create Pinterest board
 * Creates a new board on the connected Pinterest account. The returned board ID can be used immediately as `platformSpecificData.boardId` when creating a Pinterest post.
 */
export const createPinterestBoard = <ThrowOnError extends boolean = false>(options: OptionsLegacyParser<CreatePinterestBoardData, ThrowOnError>) => {
    return (options?.client ?? client).post<CreatePinterestBoardResponse, CreatePinterestBoardError, ThrowOnError>({
        ...options,
        url: '/v1/accounts/{accountId}/pinterest-boards'
    });
};

/**
 * List YouTube playlists
 * Returns the playlists available for a connected YouTube account. Use this to get a playlist ID when creating a YouTube post with the playlistId field.
 */
export const getYoutubePlaylists = <ThrowOnError extends boolean = false>(options: OptionsLegacyParser<GetYoutubePlaylistsData, ThrowOnError>) => {
    return (options?.client ?? client).get<GetYoutubePlaylistsResponse, GetYoutubePlaylistsError, ThrowOnError>({
        ...options,
        url: '/v1/accounts/{accountId}/youtube-playlists'
    });
};

/**
 * Set default YouTube playlist
 * Sets the default playlist used when publishing videos for this account. When a post does not specify a playlistId, the default playlist is not automatically used (it is stored for client-side convenience).
 */
export const updateYoutubeDefaultPlaylist = <ThrowOnError extends boolean = false>(options: OptionsLegacyParser<UpdateYoutubeDefaultPlaylistData, ThrowOnError>) => {
    return (options?.client ?? client).put<UpdateYoutubeDefaultPlaylistResponse, UpdateYoutubeDefaultPlaylistError, ThrowOnError>({
        ...options,
        url: '/v1/accounts/{accountId}/youtube-playlists'
    });
};

/**
 * List GBP locations
 * Returns Google Business Profile locations the connected account can access, plus the currently selected location. The list is bounded (see hasMore); for accounts that own many locations, use the search or filter query params to find a specific one instead of loading them all, or raise limit to enumerate an account with more than 100 locations.
 *
 */
export const getGmbLocations = <ThrowOnError extends boolean = false>(options: OptionsLegacyParser<GetGmbLocationsData, ThrowOnError>) => {
    return (options?.client ?? client).get<GetGmbLocationsResponse, GetGmbLocationsError, ThrowOnError>({
        ...options,
        url: '/v1/accounts/{accountId}/gmb-locations'
    });
};

/**
 * Update GBP location
 * Switch which GBP location is active for a connected account.
 */
export const updateGmbLocation = <ThrowOnError extends boolean = false>(options: OptionsLegacyParser<UpdateGmbLocationData, ThrowOnError>) => {
    return (options?.client ?? client).put<UpdateGmbLocationResponse, UpdateGmbLocationError, ThrowOnError>({
        ...options,
        url: '/v1/accounts/{accountId}/gmb-locations'
    });
};

/**
 * Assign GBP location to another profile
 * Connect a Google Business location onto a DIFFERENT profile by reusing the OAuth grant from an already-connected GBP account — no browser, no re-authorization. Built for agencies whose single Google account has manager access to many client locations and who run one profile per client: connect one location the normal way (browser OAuth), then bulk-assign the rest onto each client's profile via this endpoint. The path `accountId` is a SOURCE connected GBP account (the token holder); the body `profileId` is the TARGET profile. Returns 409 if the target profile already has a Google Business connection (switch its location with PUT gmb-locations instead).
 *
 */
export const assignGoogleBusinessLocation = <ThrowOnError extends boolean = false>(options: OptionsLegacyParser<AssignGoogleBusinessLocationData, ThrowOnError>) => {
    return (options?.client ?? client).post<AssignGoogleBusinessLocationResponse, AssignGoogleBusinessLocationError, ThrowOnError>({
        ...options,
        url: '/v1/accounts/{accountId}/gmb-locations/assign'
    });
};

/**
 * Get Facebook post reactions
 * Returns the reaction breakdown for a Facebook Page post: a count per reaction type
 * plus the overall total.
 *
 * The whole breakdown is fetched in a single Graph call. Note that the post analytics
 * endpoint reports only an aggregate reaction count (surfaced there as `likes`), so use
 * this endpoint when you need per-type counts.
 *
 */
export const getFacebookPostReactions = <ThrowOnError extends boolean = false>(options: OptionsLegacyParser<GetFacebookPostReactionsData, ThrowOnError>) => {
    return (options?.client ?? client).get<GetFacebookPostReactionsResponse, GetFacebookPostReactionsError, ThrowOnError>({
        ...options,
        url: '/v1/accounts/{accountId}/facebook-post-reactions'
    });
};

/**
 * List Reddit subreddits
 * Returns the subreddits the connected Reddit account can post to. Use this to get a subreddit name when creating a Reddit post.
 */
export const getRedditSubreddits = <ThrowOnError extends boolean = false>(options: OptionsLegacyParser<GetRedditSubredditsData, ThrowOnError>) => {
    return (options?.client ?? client).get<GetRedditSubredditsResponse, GetRedditSubredditsError, ThrowOnError>({
        ...options,
        url: '/v1/accounts/{accountId}/reddit-subreddits'
    });
};

/**
 * Set default subreddit
 * Sets the default subreddit used when publishing posts for this Reddit account.
 */
export const updateRedditSubreddits = <ThrowOnError extends boolean = false>(options: OptionsLegacyParser<UpdateRedditSubredditsData, ThrowOnError>) => {
    return (options?.client ?? client).put<UpdateRedditSubredditsResponse, UpdateRedditSubredditsError, ThrowOnError>({
        ...options,
        url: '/v1/accounts/{accountId}/reddit-subreddits'
    });
};

/**
 * Get subreddit rules
 * Returns a subreddit's posting rules plus Reddit's site-wide rules, so you can check
 * them before submitting and avoid a removal.
 *
 * Use this alongside `POST /v1/tools/validate/subreddit`, which only confirms that a
 * subreddit exists and reports its basic posting settings.
 *
 */
export const getSubredditRules = <ThrowOnError extends boolean = false>(options: OptionsLegacyParser<GetSubredditRulesData, ThrowOnError>) => {
    return (options?.client ?? client).get<GetSubredditRulesResponse, GetSubredditRulesError, ThrowOnError>({
        ...options,
        url: '/v1/accounts/{accountId}/reddit-subreddits/{subreddit}/rules'
    });
};

/**
 * Vote on a Reddit post or comment
 * Cast, change, or clear the connected account's vote on a Reddit post or comment.
 *
 * **Reddit requires that votes be cast by humans.** Reddit's API terms permit a client
 * to proxy a human's action one-for-one, and prohibit a bot from deciding how to vote
 * or from amplifying a human's vote. Call this endpoint only in direct response to an
 * explicit action by the account owner. Automated or agent-decided voting is
 * vote manipulation and puts API access at risk.
 *
 */
export const voteRedditThing = <ThrowOnError extends boolean = false>(options: OptionsLegacyParser<VoteRedditThingData, ThrowOnError>) => {
    return (options?.client ?? client).post<VoteRedditThingResponse, VoteRedditThingError, ThrowOnError>({
        ...options,
        url: '/v1/accounts/{accountId}/reddit-vote'
    });
};

/**
 * List subreddit flairs
 * Returns available post flairs for a subreddit. Some subreddits require a flair when posting.
 */
export const getRedditFlairs = <ThrowOnError extends boolean = false>(options: OptionsLegacyParser<GetRedditFlairsData, ThrowOnError>) => {
    return (options?.client ?? client).get<GetRedditFlairsResponse, GetRedditFlairsError, ThrowOnError>({
        ...options,
        url: '/v1/accounts/{accountId}/reddit-flairs'
    });
};

/**
 * Set Reddit post flair
 * Applies a flair to a post the connected account already published. Use the GET on this
 * path to list the available `flairTemplateId` values for the subreddit.
 *
 * Flair can also be set at submit time by passing `flairId` in `platformSpecificData`
 * when creating the post. This endpoint is for changing it afterwards.
 *
 * The subreddit must allow users to select their own post flair. Setting flair on
 * another user's post requires moderator permissions, which Zernio does not request.
 *
 */
export const setRedditPostFlair = <ThrowOnError extends boolean = false>(options: OptionsLegacyParser<SetRedditPostFlairData, ThrowOnError>) => {
    return (options?.client ?? client).post<SetRedditPostFlairResponse, SetRedditPostFlairError, ThrowOnError>({
        ...options,
        url: '/v1/accounts/{accountId}/reddit-flairs'
    });
};

/**
 * Get Slack account settings
 * Returns the connected Slack channel details and the default message identity (name and avatar shown as the author on every post, with Slack's APP badge). The identity applies to messages only; the app's own Slack profile is global and cannot be changed per workspace.
 */
export const getSlackSettings = <ThrowOnError extends boolean = false>(options: OptionsLegacyParser<GetSlackSettingsData, ThrowOnError>) => {
    return (options?.client ?? client).get<GetSlackSettingsResponse, GetSlackSettingsError, ThrowOnError>({
        ...options,
        url: '/v1/accounts/{accountId}/slack-settings'
    });
};

/**
 * Update Slack account settings
 * Set or clear the default message identity for this channel. Empty string clears a field; per-post platformSpecificData.username/iconUrl still override these defaults.
 */
export const updateSlackSettings = <ThrowOnError extends boolean = false>(options: OptionsLegacyParser<UpdateSlackSettingsData, ThrowOnError>) => {
    return (options?.client ?? client).patch<UpdateSlackSettingsResponse, UpdateSlackSettingsError, ThrowOnError>({
        ...options,
        url: '/v1/accounts/{accountId}/slack-settings'
    });
};

/**
 * Get Discord account settings
 * Returns the current Discord account settings including webhook identity (display name and avatar), connected channel, and guild information.
 */
export const getDiscordSettings = <ThrowOnError extends boolean = false>(options: OptionsLegacyParser<GetDiscordSettingsData, ThrowOnError>) => {
    return (options?.client ?? client).get<GetDiscordSettingsResponse, GetDiscordSettingsError, ThrowOnError>({
        ...options,
        url: '/v1/accounts/{accountId}/discord-settings'
    });
};

/**
 * Update Discord settings
 * Update Discord account settings. Supports two operations (can be combined):
 *
 * 1. **Webhook identity** - Set the default display name and avatar that appear as the message author on every post. These are account-level defaults; individual posts can override them via platformSpecificData.webhookUsername / webhookAvatarUrl.
 *
 * 2. **Switch channel** - Move the connection to a different channel in the same guild. A new webhook is automatically created in the target channel.
 *
 */
export const updateDiscordSettings = <ThrowOnError extends boolean = false>(options: OptionsLegacyParser<UpdateDiscordSettingsData, ThrowOnError>) => {
    return (options?.client ?? client).patch<UpdateDiscordSettingsResponse, UpdateDiscordSettingsError, ThrowOnError>({
        ...options,
        url: '/v1/accounts/{accountId}/discord-settings'
    });
};

/**
 * List Discord guild channels
 * Returns the text, announcement, and forum channels in the connected Discord guild. Use this to discover available channels when switching the connected channel via PATCH /v1/accounts/{accountId}/discord-settings.
 */
export const getDiscordChannels = <ThrowOnError extends boolean = false>(options: OptionsLegacyParser<GetDiscordChannelsData, ThrowOnError>) => {
    return (options?.client ?? client).get<GetDiscordChannelsResponse, GetDiscordChannelsError, ThrowOnError>({
        ...options,
        url: '/v1/accounts/{accountId}/discord-channels'
    });
};

/**
 * List Slack workspace members
 * Members of the connected Slack workspace that can receive a direct message, for populating a recipient picker. Bots, deactivated members and Slackbot are excluded. Start a DM by passing a member id as `participantId` to POST /v1/inbox/conversations.
 */
export const listSlackMembers = <ThrowOnError extends boolean = false>(options: OptionsLegacyParser<ListSlackMembersData, ThrowOnError>) => {
    return (options?.client ?? client).get<ListSlackMembersResponse, ListSlackMembersError, ThrowOnError>({
        ...options,
        url: '/v1/accounts/{accountId}/slack-members'
    });
};

/**
 * Send a Discord Direct Message
 * Send a 1:1 Direct Message from the bot to a Discord user (by snowflake ID).
 * Supports the same payload shape as channel posts — content, embeds, media
 * attachments, and TTS.
 *
 * Constraints (Discord platform limits):
 * - The bot can only DM users it shares at least one guild with.
 * - If the recipient has DMs disabled for non-friends, Discord returns 403
 * (surfaces as a 502 platform error).
 * - `content` capped at 2,000 chars.
 * - At least one of `content`, `embeds`, or `attachments` is required.
 * - The recipient must be identified by Discord snowflake ID (not username).
 *
 * This is a dedicated endpoint rather than a `POST /v1/posts` variant because
 * DMs are 1:1 operational messages (onboarding, billing reminders, support
 * pings) with a different lifecycle than scheduled channel posts. DMs are
 * not persisted to `Post` / `ExternalPost` and are always sent immediately.
 *
 */
export const sendDiscordDirectMessage = <ThrowOnError extends boolean = false>(options: OptionsLegacyParser<SendDiscordDirectMessageData, ThrowOnError>) => {
    return (options?.client ?? client).post<SendDiscordDirectMessageResponse, SendDiscordDirectMessageError, ThrowOnError>({
        ...options,
        url: '/v1/discord/dms'
    });
};

/**
 * List Discord guild roles
 * Returns all roles in a Discord guild. Useful for building role-mention
 * pickers, role-permission UIs, or finding the role ID before calling
 * the role-assign endpoint.
 *
 * Roles are returned unordered — sort client-side by `position` if you
 * need Discord's UI ordering.
 *
 * Caller must pass `accountId` of a Discord SocialAccount bound to this
 * guild (route verifies team access + guild match).
 *
 */
export const listDiscordGuildRoles = <ThrowOnError extends boolean = false>(options: OptionsLegacyParser<ListDiscordGuildRolesData, ThrowOnError>) => {
    return (options?.client ?? client).get<ListDiscordGuildRolesResponse, ListDiscordGuildRolesError, ThrowOnError>({
        ...options,
        url: '/v1/discord/guilds/{guildId}/roles'
    });
};

/**
 * Create a Discord guild role
 * Creates a new role in the guild.
 *
 * Requires the bot to hold the Manage Roles permission. Guilds that added the Zernio bot
 * before role management shipped must re-invite it, because Discord applies the
 * permission set at invite time.
 *
 * Discord's role hierarchy applies: the bot cannot create a role positioned at or above
 * its own highest role, and cannot grant permissions it does not itself hold. Either
 * attempt returns a 403 carrying Discord's own error.
 *
 */
export const createDiscordGuildRole = <ThrowOnError extends boolean = false>(options: OptionsLegacyParser<CreateDiscordGuildRoleData, ThrowOnError>) => {
    return (options?.client ?? client).post<CreateDiscordGuildRoleResponse, CreateDiscordGuildRoleError, ThrowOnError>({
        ...options,
        url: '/v1/discord/guilds/{guildId}/roles'
    });
};

/**
 * Edit a Discord guild role
 * Updates a role's name, color, hoist, mentionable flag, or permission bitfield.
 * At least one field must be supplied. Omitted fields are left unchanged.
 *
 * Requires the bot to hold Manage Roles, and the target role must sit below the bot's
 * highest role. See the create-role operation for the re-invite requirement.
 *
 */
export const editDiscordGuildRole = <ThrowOnError extends boolean = false>(options: OptionsLegacyParser<EditDiscordGuildRoleData, ThrowOnError>) => {
    return (options?.client ?? client).patch<EditDiscordGuildRoleResponse, EditDiscordGuildRoleError, ThrowOnError>({
        ...options,
        url: '/v1/discord/guilds/{guildId}/roles/{roleId}'
    });
};

/**
 * Delete a Discord guild role
 * Permanently deletes a role from the guild and removes it from every member.
 * This cannot be undone.
 *
 * Requires the bot to hold Manage Roles, and the target role must sit below the bot's
 * highest role.
 *
 */
export const deleteDiscordGuildRole = <ThrowOnError extends boolean = false>(options: OptionsLegacyParser<DeleteDiscordGuildRoleData, ThrowOnError>) => {
    return (options?.client ?? client).delete<DeleteDiscordGuildRoleResponse, DeleteDiscordGuildRoleError, ThrowOnError>({
        ...options,
        url: '/v1/discord/guilds/{guildId}/roles/{roleId}'
    });
};

/**
 * List Discord guild members
 * Cursor-paginated list of guild members. Returns Discord's raw member
 * objects so callers can build community-ops automation (e.g. "add role
 * to all members joined in the last 7 days") on the actual platform shape.
 *
 * Pagination: pass `after` = the last `user.id` from the previous page.
 * Omit on the first call. Response includes a `nextCursor` and `hasMore`
 * flag so callers don't need to know Discord's pagination shape.
 *
 */
export const listDiscordGuildMembers = <ThrowOnError extends boolean = false>(options: OptionsLegacyParser<ListDiscordGuildMembersData, ThrowOnError>) => {
    return (options?.client ?? client).get<ListDiscordGuildMembersResponse, ListDiscordGuildMembersError, ThrowOnError>({
        ...options,
        url: '/v1/discord/guilds/{guildId}/members'
    });
};

/**
 * Search Discord guild members
 * Search guild members whose username or nickname **starts with** the
 * query (Discord matches prefixes only, not substrings).
 *
 * Cheaper than paginating the full member listing when you already know
 * who you are looking for.
 *
 */
export const searchDiscordGuildMembers = <ThrowOnError extends boolean = false>(options: OptionsLegacyParser<SearchDiscordGuildMembersData, ThrowOnError>) => {
    return (options?.client ?? client).get<SearchDiscordGuildMembersResponse, SearchDiscordGuildMembersError, ThrowOnError>({
        ...options,
        url: '/v1/discord/guilds/{guildId}/members/search'
    });
};

/**
 * Get a Discord guild member
 * Fetch a single guild member by Discord user id.
 *
 * Cheaper than paginating the full member listing when you already know
 * who you are looking for.
 *
 */
export const getDiscordGuildMember = <ThrowOnError extends boolean = false>(options: OptionsLegacyParser<GetDiscordGuildMemberData, ThrowOnError>) => {
    return (options?.client ?? client).get<GetDiscordGuildMemberResponse, GetDiscordGuildMemberError, ThrowOnError>({
        ...options,
        url: '/v1/discord/guilds/{guildId}/members/{userId}'
    });
};

/**
 * Assign a role to a guild member
 * Assign one role to one member. Idempotent on Discord's side — re-running
 * on a member who already has the role is a 204 no-op.
 *
 * Path shape mirrors Discord's own API (`PUT /guilds/{guild}/members/{user}/roles/{role}`)
 * for zero-translation mental mapping.
 *
 * Bot needs MANAGE_ROLES permission in the guild AND its highest role
 * must be above the target role (Discord hierarchy rule). The
 * `@everyone` role (where roleId == guildId) cannot be assigned.
 *
 */
export const addDiscordMemberRole = <ThrowOnError extends boolean = false>(options: OptionsLegacyParser<AddDiscordMemberRoleData, ThrowOnError>) => {
    return (options?.client ?? client).put<AddDiscordMemberRoleResponse, AddDiscordMemberRoleError, ThrowOnError>({
        ...options,
        url: '/v1/discord/guilds/{guildId}/members/{userId}/roles/{roleId}'
    });
};

/**
 * Remove a role from a guild member
 * Remove one role from one member. Idempotent — removing a role the
 * member doesn't have returns 204 no-op.
 *
 * Same permission + hierarchy constraints as the PUT counterpart.
 *
 */
export const removeDiscordMemberRole = <ThrowOnError extends boolean = false>(options: OptionsLegacyParser<RemoveDiscordMemberRoleData, ThrowOnError>) => {
    return (options?.client ?? client).delete<RemoveDiscordMemberRoleResponse, RemoveDiscordMemberRoleError, ThrowOnError>({
        ...options,
        url: '/v1/discord/guilds/{guildId}/members/{userId}/roles/{roleId}'
    });
};

/**
 * Delete a Discord channel message
 * Deletes a message from a channel, for moderation and cleanup. This cannot be undone.
 *
 * Deleting a message the bot did not send requires the bot to hold the Manage Messages
 * permission, which the Zernio bot requests at install time. Deleting the bot's own
 * message needs no extra permission.
 *
 * Ownership is verified by resolving the channel's guild and confirming the caller owns
 * a Discord account bound to it.
 *
 */
export const deleteDiscordMessage = <ThrowOnError extends boolean = false>(options: OptionsLegacyParser<DeleteDiscordMessageData, ThrowOnError>) => {
    return (options?.client ?? client).delete<DeleteDiscordMessageResponse, DeleteDiscordMessageError, ThrowOnError>({
        ...options,
        url: '/v1/discord/channels/{channelId}/messages/{messageId}'
    });
};

/**
 * Crosspost Discord message
 * Publishes a message from an announcement channel so it propagates to every server
 * following that channel.
 *
 * The source channel must be an announcement channel. Calling this on a regular text
 * channel returns a 400 before Discord is contacted, because Discord's own error for
 * this case is opaque.
 *
 */
export const crosspostDiscordMessage = <ThrowOnError extends boolean = false>(options: OptionsLegacyParser<CrosspostDiscordMessageData, ThrowOnError>) => {
    return (options?.client ?? client).post<CrosspostDiscordMessageResponse, CrosspostDiscordMessageError, ThrowOnError>({
        ...options,
        url: '/v1/discord/channels/{channelId}/messages/{messageId}/crosspost'
    });
};

/**
 * Create a Discord public thread
 * Creates a public thread in a channel. Pass `messageId` to start the thread from an
 * existing message, or omit it to create a standalone thread.
 *
 * Threads created here are always public. Requires the bot to hold Create Public
 * Threads, which the Zernio bot requests at install time.
 *
 */
export const createDiscordThread = <ThrowOnError extends boolean = false>(options: OptionsLegacyParser<CreateDiscordThreadData, ThrowOnError>) => {
    return (options?.client ?? client).post<CreateDiscordThreadResponse, CreateDiscordThreadError, ThrowOnError>({
        ...options,
        url: '/v1/discord/channels/{channelId}/threads'
    });
};

/**
 * List pinned messages
 * Returns the channel's pinned messages, sorted most-recently-pinned
 * first. Discord caps a channel at 50 pinned messages and returns the
 * full list unpaginated.
 *
 * Bot needs READ_MESSAGE_HISTORY in the channel (granted by default
 * BOT_PERMISSIONS).
 *
 */
export const listDiscordPinnedMessages = <ThrowOnError extends boolean = false>(options: OptionsLegacyParser<ListDiscordPinnedMessagesData, ThrowOnError>) => {
    return (options?.client ?? client).get<ListDiscordPinnedMessagesResponse, ListDiscordPinnedMessagesError, ThrowOnError>({
        ...options,
        url: '/v1/discord/channels/{channelId}/pins'
    });
};

/**
 * Pin a Discord message
 * Pin a specific message in a channel. Path shape mirrors Discord's own
 * API (`PUT /channels/{cid}/pins/{mid}`).
 *
 * Idempotent — re-pinning an already-pinned message is a 204 no-op.
 *
 * Constraints:
 * - Bot needs MANAGE_MESSAGES in the channel.
 * - 50-pin cap per channel — hitting it returns 400 (Discord-side).
 * Caller should unpin one first.
 *
 */
export const pinDiscordMessage = <ThrowOnError extends boolean = false>(options: OptionsLegacyParser<PinDiscordMessageData, ThrowOnError>) => {
    return (options?.client ?? client).put<PinDiscordMessageResponse, PinDiscordMessageError, ThrowOnError>({
        ...options,
        url: '/v1/discord/channels/{channelId}/pins/{messageId}'
    });
};

/**
 * Unpin a Discord message
 * Unpin a message. Same MANAGE_MESSAGES permission requirement as pin.
 * Idempotent — unpinning a non-pinned message is a 204 no-op.
 *
 */
export const unpinDiscordMessage = <ThrowOnError extends boolean = false>(options: OptionsLegacyParser<UnpinDiscordMessageData, ThrowOnError>) => {
    return (options?.client ?? client).delete<UnpinDiscordMessageResponse, UnpinDiscordMessageError, ThrowOnError>({
        ...options,
        url: '/v1/discord/channels/{channelId}/pins/{messageId}'
    });
};

/**
 * List Discord scheduled events
 * Return all scheduled events in the guild. Events are distinct from
 * messages — they appear in the server's Events panel and Discord
 * auto-notifies interested members ahead of start time.
 *
 * Pass `withUserCount=true` to include `user_count` (number of members
 * who RSVP'd) on each event. Useful for surfacing engagement.
 *
 */
export const listDiscordScheduledEvents = <ThrowOnError extends boolean = false>(options: OptionsLegacyParser<ListDiscordScheduledEventsData, ThrowOnError>) => {
    return (options?.client ?? client).get<ListDiscordScheduledEventsResponse, ListDiscordScheduledEventsError, ThrowOnError>({
        ...options,
        url: '/v1/discord/guilds/{guildId}/events'
    });
};

/**
 * Create a Discord scheduled event
 * Create a guild scheduled event. Three event types, selected via the
 * discriminator on `entity.type`:
 *
 * - `external` — off-platform (Zoom, in-person, livestream). Requires
 * both `location` and `endsAt`. Most common type for scheduler
 * integrations.
 * - `voice` — hosted in a Discord voice channel. Requires `channelId`.
 * - `stage` — hosted in a Discord stage channel. Requires `channelId`.
 *
 * Bot needs MANAGE_EVENTS in the guild. Existing installs (pre-events
 * PR) need a re-invite OR a server admin manually granting the
 * permission — see route header for details.
 *
 */
export const createDiscordScheduledEvent = <ThrowOnError extends boolean = false>(options: OptionsLegacyParser<CreateDiscordScheduledEventData, ThrowOnError>) => {
    return (options?.client ?? client).post<CreateDiscordScheduledEventResponse, CreateDiscordScheduledEventError, ThrowOnError>({
        ...options,
        url: '/v1/discord/guilds/{guildId}/events'
    });
};

/**
 * Get a Discord scheduled event
 */
export const getDiscordScheduledEvent = <ThrowOnError extends boolean = false>(options: OptionsLegacyParser<GetDiscordScheduledEventData, ThrowOnError>) => {
    return (options?.client ?? client).get<GetDiscordScheduledEventResponse, GetDiscordScheduledEventError, ThrowOnError>({
        ...options,
        url: '/v1/discord/guilds/{guildId}/events/{eventId}'
    });
};

/**
 * Update a Discord scheduled event
 * Patch any subset of fields. Passing `status: 'cancelled'` is how you
 * cancel an event — Discord doesn't have a dedicated cancel endpoint,
 * it's a status transition.
 *
 * Most status transitions Discord enforces (you can't go SCHEDULED →
 * COMPLETED directly). The common consumer case is SCHEDULED → CANCELED.
 *
 */
export const updateDiscordScheduledEvent = <ThrowOnError extends boolean = false>(options: OptionsLegacyParser<UpdateDiscordScheduledEventData, ThrowOnError>) => {
    return (options?.client ?? client).patch<UpdateDiscordScheduledEventResponse, UpdateDiscordScheduledEventError, ThrowOnError>({
        ...options,
        url: '/v1/discord/guilds/{guildId}/events/{eventId}'
    });
};

/**
 * Delete a Discord scheduled event
 * Hard-delete an event. Use PATCH with `status: 'cancelled'` instead
 * if you want the event preserved in the guild's history.
 *
 */
export const deleteDiscordScheduledEvent = <ThrowOnError extends boolean = false>(options: OptionsLegacyParser<DeleteDiscordScheduledEventData, ThrowOnError>) => {
    return (options?.client ?? client).delete<DeleteDiscordScheduledEventResponse, DeleteDiscordScheduledEventError, ThrowOnError>({
        ...options,
        url: '/v1/discord/guilds/{guildId}/events/{eventId}'
    });
};

/**
 * List schedules
 * Returns queue schedules for a profile. Use all=true for all queues, or queueId for a specific one. Defaults to the default queue.
 */
export const listQueueSlots = <ThrowOnError extends boolean = false>(options: OptionsLegacyParser<ListQueueSlotsData, ThrowOnError>) => {
    return (options?.client ?? client).get<ListQueueSlotsResponse, ListQueueSlotsError, ThrowOnError>({
        ...options,
        url: '/v1/queue/slots'
    });
};

/**
 * Create schedule
 * Create an additional queue for a profile. The first queue created becomes the default.
 * Subsequent queues are non-default unless explicitly set.
 *
 */
export const createQueueSlot = <ThrowOnError extends boolean = false>(options: OptionsLegacyParser<CreateQueueSlotData, ThrowOnError>) => {
    return (options?.client ?? client).post<CreateQueueSlotResponse, CreateQueueSlotError, ThrowOnError>({
        ...options,
        url: '/v1/queue/slots'
    });
};

/**
 * Update schedule
 * Create a new queue or update an existing one. Without queueId, creates/updates the default queue. With queueId, updates a specific queue. With setAsDefault=true, makes this queue the default for the profile.
 *
 */
export const updateQueueSlot = <ThrowOnError extends boolean = false>(options: OptionsLegacyParser<UpdateQueueSlotData, ThrowOnError>) => {
    return (options?.client ?? client).put<UpdateQueueSlotResponse, UpdateQueueSlotError, ThrowOnError>({
        ...options,
        url: '/v1/queue/slots'
    });
};

/**
 * Delete schedule
 * Delete a queue from a profile. Pass queueId to delete a specific queue;
 * omit it to delete all queues for the profile.
 * If deleting the default queue, another queue will be promoted to default.
 *
 */
export const deleteQueueSlot = <ThrowOnError extends boolean = false>(options: OptionsLegacyParser<DeleteQueueSlotData, ThrowOnError>) => {
    return (options?.client ?? client).delete<DeleteQueueSlotResponse, DeleteQueueSlotError, ThrowOnError>({
        ...options,
        url: '/v1/queue/slots'
    });
};

/**
 * Preview upcoming slots
 * Returns the next N upcoming queue slot times for a profile as ISO datetime strings.
 */
export const previewQueue = <ThrowOnError extends boolean = false>(options: OptionsLegacyParser<PreviewQueueData, ThrowOnError>) => {
    return (options?.client ?? client).get<PreviewQueueResponse, PreviewQueueError, ThrowOnError>({
        ...options,
        url: '/v1/queue/preview'
    });
};

/**
 * Get next available slot
 * Returns the next available queue slot for preview purposes. To create a queue post, use POST /v1/posts with queuedFromProfile instead of scheduledFor.
 */
export const getNextQueueSlot = <ThrowOnError extends boolean = false>(options: OptionsLegacyParser<GetNextQueueSlotData, ThrowOnError>) => {
    return (options?.client ?? client).get<GetNextQueueSlotResponse, GetNextQueueSlotError, ThrowOnError>({
        ...options,
        url: '/v1/queue/next-slot'
    });
};

/**
 * List webhooks
 * Retrieve all configured webhooks for the authenticated user. Supports up to 50 webhooks per user.
 */
export const getWebhookSettings = <ThrowOnError extends boolean = false>(options?: OptionsLegacyParser<unknown, ThrowOnError>) => {
    return (options?.client ?? client).get<GetWebhookSettingsResponse, GetWebhookSettingsError, ThrowOnError>({
        ...options,
        url: '/v1/webhooks/settings'
    });
};

/**
 * Create webhook
 * Create a new webhook configuration. Maximum 50 webhooks per user.
 *
 * `name`, `url` and `events` are required. `url` must be a valid URL and `events` must contain at least one event. Whitespace is trimmed from `url` before validation.
 *
 * Webhooks are automatically disabled after 10 consecutive delivery failures.
 *
 * A restricted (zrk_) API key can only subscribe to events whose resource group
 * the key holds; an event outside the key's groups is rejected with 403, so a
 * restricted key can never create a subscription broader than itself.
 *
 * `disabledResourceGroups` restricts the subscription itself, independently of
 * which key or session later reads it. Events in a disabled group are dropped
 * before delivery to this endpoint, on live delivery and on every replay path
 * (test fire, redelivery, dead-letter requeue), even if they are listed in
 * `events`. Omit it to receive everything in `events`, which is how existing
 * subscriptions behave. A restricted key's own disabled groups are always
 * unioned in.
 *
 */
export const createWebhookSettings = <ThrowOnError extends boolean = false>(options: OptionsLegacyParser<CreateWebhookSettingsData, ThrowOnError>) => {
    return (options?.client ?? client).post<CreateWebhookSettingsResponse, CreateWebhookSettingsError, ThrowOnError>({
        ...options,
        url: '/v1/webhooks/settings'
    });
};

/**
 * Update webhook
 * Update an existing webhook configuration. All fields except `_id` are optional; only provided fields will be updated.
 *
 * When provided, `name` must be 1-50 characters, `url` must be a valid URL, and `events` must contain at least one event. Whitespace is trimmed from `url` before validation.
 *
 * Webhooks are automatically disabled after 10 consecutive delivery failures.
 *
 * A restricted (zrk_) API key can only set `events` to events whose resource
 * group the key holds; an event outside the key's groups is rejected with 403.
 * It also cannot widen an existing subscription past its own groups.
 *
 * `disabledResourceGroups` replaces the subscription's own denylist, which
 * applies to delivery regardless of which key or session created it. Send an
 * empty array to clear it. A restricted key's own disabled groups are unioned
 * into the stored value on every update, so repointing a legacy unrestricted
 * subscription with a restricted key also narrows it.
 *
 * Timing: the new denylist applies to every event emitted after the update.
 * Events already queued for delivery when the update landed were filtered
 * against the previous denylist and can still arrive at your endpoint for up
 * to five minutes after they were enqueued, because the delivery worker
 * trusts a five-minute enqueue-time snapshot before re-checking the
 * subscription. Retries beyond that window, dead-letter replays, test fires,
 * and redeliveries are all checked against the current denylist.
 *
 */
export const updateWebhookSettings = <ThrowOnError extends boolean = false>(options: OptionsLegacyParser<UpdateWebhookSettingsData, ThrowOnError>) => {
    return (options?.client ?? client).put<UpdateWebhookSettingsResponse, UpdateWebhookSettingsError, ThrowOnError>({
        ...options,
        url: '/v1/webhooks/settings'
    });
};

/**
 * Delete webhook
 * Permanently delete a webhook configuration.
 */
export const deleteWebhookSettings = <ThrowOnError extends boolean = false>(options: OptionsLegacyParser<DeleteWebhookSettingsData, ThrowOnError>) => {
    return (options?.client ?? client).delete<DeleteWebhookSettingsResponse, DeleteWebhookSettingsError, ThrowOnError>({
        ...options,
        url: '/v1/webhooks/settings'
    });
};

/**
 * List webhook delivery logs
 * Retrieve recorded webhook delivery attempts for the authenticated user, most recent first.
 * Logs are retained for 30 days. Supports filtering by status, event type, webhook ID, and event ID,
 * plus offset-based pagination.
 *
 * For a restricted (zrk_) API key, rows for events outside the key's resource
 * groups are omitted (`pagination.total` may over-count), and an `event` filter
 * naming such an event is rejected with 403. Events blocked by a subscription's
 * own `disabledResourceGroups` are dropped before delivery, so they produce no
 * log rows for anyone; the exception is the five-minute tail after a denylist
 * change, where an already-queued event can still be delivered and logged.
 *
 */
export const getWebhookLogs = <ThrowOnError extends boolean = false>(options?: OptionsLegacyParser<GetWebhookLogsData, ThrowOnError>) => {
    return (options?.client ?? client).get<GetWebhookLogsResponse, GetWebhookLogsError, ThrowOnError>({
        ...options,
        url: '/v1/webhooks/logs'
    });
};

/**
 * Send test webhook
 * Send a test webhook to verify your endpoint is configured correctly. The test payload includes event: "webhook.test" to distinguish it from real events.
 *
 * `webhook.test` belongs to the `webhooks` resource group, so a key with that
 * group disabled is rejected with 403, as is a test fire on a subscription that
 * lists `webhooks` in its own `disabledResourceGroups` (a 403, not a reported
 * delivery failure). Replays of real events (redelivery, dead-letter requeue) run
 * the same checks as live delivery, against both the key's groups and the
 * subscription's.
 *
 */
export const testWebhook = <ThrowOnError extends boolean = false>(options: OptionsLegacyParser<TestWebhookData, ThrowOnError>) => {
    return (options?.client ?? client).post<TestWebhookResponse, TestWebhookError, ThrowOnError>({
        ...options,
        url: '/v1/webhooks/test'
    });
};

/**
 * List activity logs
 * Unified logs endpoint. Returns logs for publishing, connections, webhooks, and messaging.
 * Filter by type, platform, status, and time range. Logs are retained for 90 days.
 *
 */
export const listLogs = <ThrowOnError extends boolean = false>(options?: OptionsLegacyParser<ListLogsData, ThrowOnError>) => {
    return (options?.client ?? client).get<ListLogsResponse, ListLogsError, ThrowOnError>({
        ...options,
        url: '/v1/logs'
    });
};

/**
 * List conversations
 * Fetch conversations (DMs) from all connected messaging accounts in a single API call. Supports filtering by profile and platform. Results are aggregated and deduplicated.
 * Supported platforms: Facebook, Instagram, Twitter/X, Bluesky, Reddit, Telegram.
 *
 * Twitter/X limitation: X has replaced traditional DMs with encrypted "X Chat" for many accounts. Messages sent or received through encrypted X Chat are not accessible via X's API (the /2/dm_events endpoint only returns legacy unencrypted DMs). This means some Twitter/X conversations may show only outgoing messages or appear empty. This is an X platform limitation that affects all third-party applications. See X's docs on encrypted messaging for more details.
 *
 * Instagram and Facebook pre-connect history: when one of these accounts is connected, Zernio replays the DM history the account already holds on Meta, so conversations that began before the account was connected appear here. Up to 500 conversations per account are replayed. The replay runs in the background and can finish after a listing you have already taken, and replayed conversations keep their original lastMessageAt, so they sort into date order rather than appearing at the top. If you mirror this endpoint into your own store, re-run the sweep rather than relying on a single pass at connect time. Replayed history emits no webhooks and is stored as already read, so it never affects unread counts. Threads that Meta refuses to serve are skipped, and an account whose Instagram "connected tools" message access is turned off is not replayed at all.
 *
 */
export const listInboxConversations = <ThrowOnError extends boolean = false>(options?: OptionsLegacyParser<ListInboxConversationsData, ThrowOnError>) => {
    return (options?.client ?? client).get<ListInboxConversationsResponse, ListInboxConversationsError, ThrowOnError>({
        ...options,
        url: '/v1/inbox/conversations'
    });
};

/**
 * Create conversation
 * Initiate a new direct message conversation with a specified user. If a conversation already exists with the recipient, the message is added to the existing thread.
 *
 * Supported platforms: X/Twitter, Bluesky, Reddit, WhatsApp, SMS, and Slack. Other platforms return PLATFORM_NOT_SUPPORTED.
 *
 * Slack: pass a workspace member id as participantId (list them with GET /v1/accounts/{accountId}/slack-members). Zernio opens the DM channel with that member and sends the message; the thread then behaves like any other Slack conversation in the inbox. The member must belong to the connected workspace.
 *
 * WhatsApp: this is the endpoint for sending an approved template message to a phone number. Provide templateName, templateLanguage, and templateParams (variable values for the text header, body and dynamic URL buttons, in that order), with the recipient phone in participantId. A template is required because WhatsApp does not permit freeform messages to open a conversation; a missing template returns TEMPLATE_REQUIRED. Templates with media headers (image, video, document) are handled automatically: Zernio reads the approved template definition and fills the header at send time with the template's approved sample asset. To send a DIFFERENT asset per message (e.g. a distinct invoice PDF for each recipient), pass the headerMedia field with a public link (or a Meta media id); it overrides the sample for that send. Calling this for a number you already have a thread with simply sends the template into that thread, which also makes it the way to re-engage a contact after the 24-hour customer-service window has closed. Once the recipient replies (opening the 24h window), send freeform messages with the send-message endpoint (POST /v1/inbox/conversations/{conversationId}/messages). Template fields are accepted on the JSON body only, not on multipart requests. Alternatively, WhatsApp Business Accounts eligible for Meta Direct Send can open a conversation with a business-initiated utility text message and no template: pass category: 'utility' together with message (and no templateName). See the category field below.
 *
 * DM eligibility (X/Twitter): Before sending, the endpoint checks if the recipient accepts DMs from your account (via the receives_your_dm field). If not, a 422 error with code DM_NOT_ALLOWED is returned. You can skip this check with skipDmCheck: true if you have already verified eligibility.
 *
 * X API tier requirement: DM write endpoints require X API Pro tier ($5,000/month) or Enterprise access. This applies to BYOK (Bring Your Own Key) users who provide their own X API credentials.
 *
 * Rate limits (X/Twitter only): X's DM API enforces 200 requests per 15 minutes, 1,000 per 24 hours per connected X account, and 15,000 per 24 hours per X developer app (shared across all DM endpoints). These limits do NOT apply to other platforms. WhatsApp sends are governed by Meta's per-number messaging tiers (unique business-initiated conversations per 24 hours) and per-number throughput instead.
 *
 */
export const createInboxConversation = <ThrowOnError extends boolean = false>(options: OptionsLegacyParser<CreateInboxConversationData, ThrowOnError>) => {
    return (options?.client ?? client).post<CreateInboxConversationResponse, CreateInboxConversationError, ThrowOnError>({
        ...options,
        url: '/v1/inbox/conversations'
    });
};

/**
 * Search conversations
 * Search your conversations two ways at once, and get back the matching conversations, most-recent match first:
 *
 * - Message text: matches words inside message bodies. Case-insensitive and accent-insensitive, exact tokens only (no substrings, no stemming). Each hit carries up to 3 most-recent matching messages. With direction=outgoing you can collect examples of how you write to customers, for example to teach an AI agent your tone of voice.
 * - Contact identity: matches the participant's name, username, or phone number as a case-insensitive substring. These hits have matchCount 0 and an empty matches array.
 *
 * A conversation that matches both ways is returned once, carrying its message matches.
 *
 * Only platforms whose messages are stored by Zernio are searchable: WhatsApp, SMS, Telegram, Facebook, Instagram, Twitter/X and Reddit. Bluesky conversations are fetched live from the platform and cannot be searched; those accounts are listed in meta.accountsSkipped.
 *
 */
export const searchInboxConversations = <ThrowOnError extends boolean = false>(options: OptionsLegacyParser<SearchInboxConversationsData, ThrowOnError>) => {
    return (options?.client ?? client).get<SearchInboxConversationsResponse, SearchInboxConversationsError, ThrowOnError>({
        ...options,
        url: '/v1/inbox/conversations/search'
    });
};

/**
 * Get conversation
 * Retrieve details and metadata for a specific conversation. Requires accountId query parameter.
 */
export const getInboxConversation = <ThrowOnError extends boolean = false>(options: OptionsLegacyParser<GetInboxConversationData, ThrowOnError>) => {
    return (options?.client ?? client).get<GetInboxConversationResponse, GetInboxConversationError, ThrowOnError>({
        ...options,
        url: '/v1/inbox/conversations/{conversationId}'
    });
};

/**
 * Update conversation status
 * Archive or activate a conversation. Requires accountId in request body.
 */
export const updateInboxConversation = <ThrowOnError extends boolean = false>(options: OptionsLegacyParser<UpdateInboxConversationData, ThrowOnError>) => {
    return (options?.client ?? client).put<UpdateInboxConversationResponse, UpdateInboxConversationError, ThrowOnError>({
        ...options,
        url: '/v1/inbox/conversations/{conversationId}'
    });
};

/**
 * List messages
 * Fetch messages for a specific conversation, with cursor-based pagination
 * and ordering control.
 *
 * Pagination: pass `pagination.nextCursor` from a prior response back as
 * the `cursor` query param to fetch the next page. The cursor is opaque;
 * do not parse or construct it client-side.
 *
 * Sort order: defaults to `asc` (oldest first, chat style). For the
 * "show me the latest messages" pattern, pass `?sortOrder=desc&limit=N`.
 * Twitter, Instagram, Telegram, WhatsApp and Reddit honor the requested
 * order from the local message store. For Facebook and Bluesky, the
 * upstream APIs only return newest-first and have no order parameter —
 * sort order is best-effort and only reverses items within a single page
 * (pages still walk newest→oldest). The response field `sortOrderApplied`
 * tells you what was actually applied.
 *
 * Reddit threads are paginated client-side because Reddit's API has no
 * per-thread cursor. Very long threads may be upstream-truncated by
 * Reddit's inbox/sent windows (~100 most-recent items each); this is a
 * Reddit platform limitation.
 *
 * Instagram and Facebook conversations include history from before the
 * account was connected, replayed from Meta. That replay covers the 500
 * most recent messages per conversation: a longer thread keeps its newest
 * 500 and older messages are not retrievable. Messages that arrived after
 * the account was connected are unaffected. Replayed messages are stored
 * as already read and emit no webhooks.
 *
 * Twitter/X limitation: X's encrypted "X Chat" messages are not accessible via the API. Conversations where the other participant uses encrypted X Chat may only show your outgoing messages. See the list conversations endpoint for more details.
 *
 * This endpoint is read-only and does NOT mark messages as read or send
 * read receipts. To mark a conversation read (and send WhatsApp blue ticks
 * on eligible accounts), call `POST /v1/inbox/conversations/{conversationId}/read`.
 *
 */
export const getInboxConversationMessages = <ThrowOnError extends boolean = false>(options: OptionsLegacyParser<GetInboxConversationMessagesData, ThrowOnError>) => {
    return (options?.client ?? client).get<GetInboxConversationMessagesResponse, GetInboxConversationMessagesError, ThrowOnError>({
        ...options,
        url: '/v1/inbox/conversations/{conversationId}/messages'
    });
};

/**
 * Send message
 * Send a message in a conversation. Supports text, attachments, quick replies,
 * buttons, templates, and message tags. Attachment and interactive message
 * support varies by platform.
 *
 * WhatsApp template messages: to send an approved template into this
 * conversation (required when the 24-hour customer-service window is
 * closed), use the `template` field with a single element carrying the
 * template reference: `{ "elements": [{ "name": ..., "language": ..., "components": [...] }] }`.
 * See the `template` field below for the exact shape. To send a template
 * to a phone number you have no conversation with yet, use the
 * create-conversation endpoint (POST /v1/inbox/conversations) instead.
 *
 * WhatsApp rich interactive messages (list, CTA URL, Flow, location request)
 * are available via the `interactive` field. Tap events are delivered through
 * the `message.received` webhook with WhatsApp-specific `metadata` fields
 * (`interactiveType`, `interactiveId`, `flowResponseJson`, `flowResponseData`).
 *
 * **Idempotency:** send an `Idempotency-Key` header to make retries safe
 * (e.g. after a client-side timeout where delivery is unknown): same key +
 * same body replays the original response (with `Idempotent-Replayed: true`)
 * instead of sending the message a second time; same key + different body
 * returns 422; a key still in flight returns 409. Works for JSON and
 * multipart (file upload) requests alike. Keys are retained for 24 hours.
 *
 */
export const sendInboxMessage = <ThrowOnError extends boolean = false>(options: OptionsLegacyParser<SendInboxMessageData, ThrowOnError>) => {
    return (options?.client ?? client).post<SendInboxMessageResponse, SendInboxMessageError, ThrowOnError>({
        ...options,
        url: '/v1/inbox/conversations/{conversationId}/messages'
    });
};

/**
 * Download WhatsApp media
 * Streams the binary for a WhatsApp attachment. This is the endpoint the
 * `url` on a WhatsApp `attachments[]` entry points at, in both the
 * `message.received` webhook and the List messages response.
 *
 * **This is an authenticated endpoint, not a public link.** Send
 * `Authorization: Bearer <your API key>` exactly as you would for any other
 * call. Passing the URL straight to a browser, an LLM vision API, or a
 * no-code "download file" step without the header returns `401`. This is
 * the most common integration mistake on this endpoint, and it differs from
 * Instagram, Facebook and Telegram, whose `attachments[].url` is a direct
 * CDN link that needs no header.
 *
 * **Fetch on receipt, not lazily.** WhatsApp media lives in Meta's media
 * store, not ours, and it is removed after a limited retention window
 * (currently 7 days, and Meta has been dropping some inbound media sooner).
 * Once Meta drops it the media is unrecoverable and this endpoint answers
 * `400` permanently, so retrying will never succeed. Download and store the
 * bytes when the webhook arrives.
 *
 */
export const getWhatsAppMedia = <ThrowOnError extends boolean = false>(options: OptionsLegacyParser<GetWhatsAppMediaData, ThrowOnError>) => {
    return (options?.client ?? client).get<GetWhatsAppMediaResponse, GetWhatsAppMediaError, ThrowOnError>({
        ...options,
        url: '/v1/whatsapp/media/{mediaId}'
    });
};

/**
 * Edit message
 * Edit the text and/or reply markup of a previously sent Telegram message.
 * Only supported for Telegram. Returns 400 for other platforms.
 *
 */
export const editInboxMessage = <ThrowOnError extends boolean = false>(options: OptionsLegacyParser<EditInboxMessageData, ThrowOnError>) => {
    return (options?.client ?? client).patch<EditInboxMessageResponse, EditInboxMessageError, ThrowOnError>({
        ...options,
        url: '/v1/inbox/conversations/{conversationId}/messages/{messageId}'
    });
};

/**
 * Delete message
 * Delete a message from a conversation. Platform support varies:
 * - Telegram: Full delete (bot's own messages anytime, others if admin)
 * - X/Twitter: Full delete (own DM events only)
 * - Bluesky: Delete for self only (recipient still sees it)
 * - Reddit: Delete from sender's view only
 * - Facebook, Instagram, WhatsApp: Not supported (returns 400)
 *
 */
export const deleteInboxMessage = <ThrowOnError extends boolean = false>(options: OptionsLegacyParser<DeleteInboxMessageData, ThrowOnError>) => {
    return (options?.client ?? client).delete<DeleteInboxMessageResponse, DeleteInboxMessageError, ThrowOnError>({
        ...options,
        url: '/v1/inbox/conversations/{conversationId}/messages/{messageId}'
    });
};

/**
 * Send typing indicator
 * Show a typing indicator in a conversation. Platform support:
 * - Facebook Messenger: Shows "Page is typing..." for 20 seconds
 * - Instagram: Shows "typing..." to the recipient (works for both Instagram Login and Facebook Login accounts). The recipient must be signed in to Instagram to see it.
 * - Telegram: Shows "Bot is typing..." for 5 seconds
 * - WhatsApp: Shows "typing..." for up to 25 seconds. Requires a recent inbound message in the conversation (Meta references the inbound message id) and also marks that message as read as a side-effect.
 * - All others: Returns 200 but no-op (platform doesn't support it)
 *
 * Typing indicators are best-effort. The endpoint always returns 200 even if the platform call fails; `success` reports whether a typing indicator was actually sent to the platform (`false` on unsupported platforms or when the platform call failed).
 *
 */
export const sendTypingIndicator = <ThrowOnError extends boolean = false>(options: OptionsLegacyParser<SendTypingIndicatorData, ThrowOnError>) => {
    return (options?.client ?? client).post<SendTypingIndicatorResponse, SendTypingIndicatorError, ThrowOnError>({
        ...options,
        url: '/v1/inbox/conversations/{conversationId}/typing'
    });
};

/**
 * Mark a conversation as read
 * Marks all unread incoming messages in the conversation as read.
 *
 * For WhatsApp, this also sends read receipts (blue ticks) to the contact,
 * EXCEPT on coexistence accounts (where the WhatsApp Business app on the
 * customer's phone owns read state and we never override it).
 *
 * This is the explicit, human-driven counterpart to `GET .../messages`,
 * which is side-effect-free and does NOT mark anything read. Call this when
 * a user actually views the conversation.
 *
 */
export const markConversationRead = <ThrowOnError extends boolean = false>(options: OptionsLegacyParser<MarkConversationReadData, ThrowOnError>) => {
    return (options?.client ?? client).post<MarkConversationReadResponse, MarkConversationReadError, ThrowOnError>({
        ...options,
        url: '/v1/inbox/conversations/{conversationId}/read'
    });
};

/**
 * Add reaction
 * Add an emoji reaction to a message. Platform support:
 * - Telegram: Supports a subset of Unicode emoji reactions
 * - WhatsApp: Supports any standard emoji (one reaction per message per sender)
 * - Instagram and Facebook Messenger: Any standard emoji, subject to Meta's 24h messaging window
 * - Slack: The emoji must have a Slack name (e.g. `:thumbsup:`); unnamed characters return 400
 * - All others: Returns 400 (not supported)
 *
 */
export const addMessageReaction = <ThrowOnError extends boolean = false>(options: OptionsLegacyParser<AddMessageReactionData, ThrowOnError>) => {
    return (options?.client ?? client).post<AddMessageReactionResponse, AddMessageReactionError, ThrowOnError>({
        ...options,
        url: '/v1/inbox/conversations/{conversationId}/messages/{messageId}/reactions'
    });
};

/**
 * Remove reaction
 * Remove a reaction from a message. Platform support:
 * - Telegram: Send empty reaction array to clear
 * - WhatsApp: Send empty emoji to remove
 * - Instagram and Facebook Messenger: Sends Meta's `unreact` action; the emoji does not need to be repeated
 * - Slack: Removes the reaction we previously sent on that message
 * - All others: Returns 400 (not supported)
 *
 */
export const removeMessageReaction = <ThrowOnError extends boolean = false>(options: OptionsLegacyParser<RemoveMessageReactionData, ThrowOnError>) => {
    return (options?.client ?? client).delete<RemoveMessageReactionResponse, RemoveMessageReactionError, ThrowOnError>({
        ...options,
        url: '/v1/inbox/conversations/{conversationId}/messages/{messageId}/reactions'
    });
};

/**
 * Upload media file
 * Upload a media file using API key authentication and get back a publicly accessible URL.
 * The URL can be used as attachmentUrl when sending inbox messages.
 *
 * Files are stored in temporary storage and auto-delete after 7 days.
 * Maximum file size is 25MB.
 *
 * Unlike /v1/media/upload (which uses upload tokens for end-user flows),
 * this endpoint uses standard Bearer token authentication for programmatic use.
 *
 */
export const uploadMediaDirect = <ThrowOnError extends boolean = false>(options: OptionsLegacyParser<UploadMediaDirectData, ThrowOnError>) => {
    return (options?.client ?? client).post<UploadMediaDirectResponse, UploadMediaDirectError, ThrowOnError>({
        ...options,
        ...formDataBodySerializer,
        headers: {
            'Content-Type': null,
            ...options?.headers
        },
        url: '/v1/media/upload-direct'
    });
};

/**
 * Get FB persistent menu
 * Get the persistent menu configuration for a Facebook Messenger account.
 */
export const getMessengerMenu = <ThrowOnError extends boolean = false>(options: OptionsLegacyParser<GetMessengerMenuData, ThrowOnError>) => {
    return (options?.client ?? client).get<GetMessengerMenuResponse, GetMessengerMenuError, ThrowOnError>({
        ...options,
        url: '/v1/accounts/{accountId}/messenger-menu'
    });
};

/**
 * Set FB persistent menu
 * Set the persistent menu for a Facebook Messenger account. Max 3 top-level items, max 5 nested items.
 */
export const setMessengerMenu = <ThrowOnError extends boolean = false>(options: OptionsLegacyParser<SetMessengerMenuData, ThrowOnError>) => {
    return (options?.client ?? client).put<SetMessengerMenuResponse, SetMessengerMenuError, ThrowOnError>({
        ...options,
        url: '/v1/accounts/{accountId}/messenger-menu'
    });
};

/**
 * Delete FB persistent menu
 * Removes the persistent menu from Facebook Messenger conversations for this account.
 */
export const deleteMessengerMenu = <ThrowOnError extends boolean = false>(options: OptionsLegacyParser<DeleteMessengerMenuData, ThrowOnError>) => {
    return (options?.client ?? client).delete<DeleteMessengerMenuResponse, DeleteMessengerMenuError, ThrowOnError>({
        ...options,
        url: '/v1/accounts/{accountId}/messenger-menu'
    });
};

/**
 * Get IG ice breakers
 * Get the ice breaker configuration for an Instagram account.
 */
export const getInstagramIceBreakers = <ThrowOnError extends boolean = false>(options: OptionsLegacyParser<GetInstagramIceBreakersData, ThrowOnError>) => {
    return (options?.client ?? client).get<GetInstagramIceBreakersResponse, GetInstagramIceBreakersError, ThrowOnError>({
        ...options,
        url: '/v1/accounts/{accountId}/instagram-ice-breakers'
    });
};

/**
 * Set IG ice breakers
 * Set ice breakers for an Instagram account. Max 4 ice breakers, question max 80 chars.
 */
export const setInstagramIceBreakers = <ThrowOnError extends boolean = false>(options: OptionsLegacyParser<SetInstagramIceBreakersData, ThrowOnError>) => {
    return (options?.client ?? client).put<SetInstagramIceBreakersResponse, SetInstagramIceBreakersError, ThrowOnError>({
        ...options,
        url: '/v1/accounts/{accountId}/instagram-ice-breakers'
    });
};

/**
 * Delete IG ice breakers
 * Removes the ice breaker questions from an Instagram account's Messenger experience.
 */
export const deleteInstagramIceBreakers = <ThrowOnError extends boolean = false>(options: OptionsLegacyParser<DeleteInstagramIceBreakersData, ThrowOnError>) => {
    return (options?.client ?? client).delete<DeleteInstagramIceBreakersResponse, DeleteInstagramIceBreakersError, ThrowOnError>({
        ...options,
        url: '/v1/accounts/{accountId}/instagram-ice-breakers'
    });
};

/**
 * Get TG bot commands
 * Get the bot commands configuration for a Telegram account.
 */
export const getTelegramCommands = <ThrowOnError extends boolean = false>(options: OptionsLegacyParser<GetTelegramCommandsData, ThrowOnError>) => {
    return (options?.client ?? client).get<GetTelegramCommandsResponse, GetTelegramCommandsError, ThrowOnError>({
        ...options,
        url: '/v1/accounts/{accountId}/telegram-commands'
    });
};

/**
 * Set TG bot commands
 * Set bot commands for a Telegram account.
 */
export const setTelegramCommands = <ThrowOnError extends boolean = false>(options: OptionsLegacyParser<SetTelegramCommandsData, ThrowOnError>) => {
    return (options?.client ?? client).put<SetTelegramCommandsResponse, SetTelegramCommandsError, ThrowOnError>({
        ...options,
        url: '/v1/accounts/{accountId}/telegram-commands'
    });
};

/**
 * Delete TG bot commands
 * Clears all bot commands configured for a Telegram bot account.
 */
export const deleteTelegramCommands = <ThrowOnError extends boolean = false>(options: OptionsLegacyParser<DeleteTelegramCommandsData, ThrowOnError>) => {
    return (options?.client ?? client).delete<DeleteTelegramCommandsResponse, DeleteTelegramCommandsError, ThrowOnError>({
        ...options,
        url: '/v1/accounts/{accountId}/telegram-commands'
    });
};

/**
 * Resolve message attachment
 * Resolve one attachment on a message to a media url that works right now.
 *
 * Instagram and Facebook sign DM media urls per request and expire them, so
 * the `url` on a message is a snapshot: it works when you read the message
 * and stops working later. This endpoint checks the stored url and, when it
 * has gone stale, re-mints the message's media from Meta and persists it
 * before answering. The message id never expires, so this URL is the one to
 * store — it is returned on each attachment as `refreshUrl`.
 *
 * By default it responds `302` to the live media url, so it can be used
 * directly as an `<img src>` on a browser session. API-key integrators
 * should pass `?format=json` and read `url` off the body, since a browser
 * cannot attach an Authorization header to an image request.
 *
 * Only Instagram and Facebook media can be re-minted. On other platforms
 * the stored url is returned as-is when it still resolves, and `404`
 * otherwise.
 *
 */
export const getMessageAttachment = <ThrowOnError extends boolean = false>(options: OptionsLegacyParser<GetMessageAttachmentData, ThrowOnError>) => {
    return (options?.client ?? client).get<GetMessageAttachmentResponse, GetMessageAttachmentError, ThrowOnError>({
        ...options,
        url: '/v1/inbox/conversations/{conversationId}/messages/{messageId}/attachments/{index}'
    });
};

/**
 * List commented posts
 * Returns posts with comment counts from all connected accounts. Aggregates data across multiple accounts.
 *
 * For users with the Ads add-on (Metronome plans always qualify), the user's Meta ads
 * (boosted/dark posts) are included too. There's one row per (ad, placement-with-comments):
 * an ad that runs on both Facebook feed and Instagram feed produces up to two rows (the
 * Page dark post and the IG media have separate comment threads), each flagged
 * `isAd: true` with `adId` and `placement` (`id` is `{adId}:{placement}`). Use
 * `?platform=metaads` to return *only* ad rows; passing `facebook`/`instagram` returns
 * *organic* posts only (no ads); omitting `platform` returns both. Fetch a row's thread
 * from GET /v1/ads/{adId}/comments?placement={placement}. Ad comment counts are read with
 * the Marketing API token (Facebook side) or the connected Instagram account's token
 * (Instagram side); a row whose count can't be read is omitted.
 *
 * Pagination walks each account's platform listing. Following `nextCursor` reaches past
 * the first page on Facebook, Instagram, Threads, LinkedIn and YouTube, since they are
 * the platforms that support a server-side date window; on the others the listing stops
 * at its first page. Cursor pagination is only coherent for the default sort
 * (`sortBy=date`, `sortOrder=desc`): with `sortOrder=asc`, or with `sortBy=comments`,
 * the cursor filter does not match the sort order and the second page is unreliable.
 *
 * `nextCursor` is opaque: pass it back verbatim, never construct or parse it, its
 * composition may change without notice. Because each page re-queries a live window,
 * results can still shift between requests, so dedupe by `id` on the client.
 *
 * `commentCount` semantics differ by platform: YouTube's includes replies, Facebook's counts
 * top-level comments only.
 *
 */
export const listInboxComments = <ThrowOnError extends boolean = false>(options?: OptionsLegacyParser<ListInboxCommentsData, ThrowOnError>) => {
    return (options?.client ?? client).get<ListInboxCommentsResponse, ListInboxCommentsError, ThrowOnError>({
        ...options,
        url: '/v1/inbox/comments'
    });
};

/**
 * Get post comments
 * Fetch comments for a specific post. Requires accountId query parameter.
 *
 * On Facebook and Instagram, passing a COMMENT id as `postId` is also supported and
 * returns that comment's replies instead of the post's top-level comments. This is not
 * available on YouTube, where `postId` must be a video id.
 *
 */
export const getInboxPostComments = <ThrowOnError extends boolean = false>(options: OptionsLegacyParser<GetInboxPostCommentsData, ThrowOnError>) => {
    return (options?.client ?? client).get<GetInboxPostCommentsResponse, GetInboxPostCommentsError, ThrowOnError>({
        ...options,
        url: '/v1/inbox/comments/{postId}'
    });
};

/**
 * Reply to comment
 * Post a reply to a post or specific comment. Requires accountId in request body.
 */
export const replyToInboxPost = <ThrowOnError extends boolean = false>(options: OptionsLegacyParser<ReplyToInboxPostData, ThrowOnError>) => {
    return (options?.client ?? client).post<ReplyToInboxPostResponse, ReplyToInboxPostError, ThrowOnError>({
        ...options,
        url: '/v1/inbox/comments/{postId}'
    });
};

/**
 * Delete comment
 * Delete a comment on a post. Supported by Facebook, Instagram, Bluesky, Reddit, YouTube, and LinkedIn.
 * Requires accountId and commentId query parameters.
 *
 */
export const deleteInboxComment = <ThrowOnError extends boolean = false>(options: OptionsLegacyParser<DeleteInboxCommentData, ThrowOnError>) => {
    return (options?.client ?? client).delete<DeleteInboxCommentResponse, DeleteInboxCommentError, ThrowOnError>({
        ...options,
        url: '/v1/inbox/comments/{postId}'
    });
};

/**
 * Edit comment
 * Edit the body of a comment the connected account posted. Supported on Reddit only.
 *
 * Reddit keeps the same comment id after an edit. Reddit exposes no API to edit a post
 * title, and a link post has no editable body. To edit a published post's body, use
 * `POST /v1/posts/{postId}/edit`.
 *
 */
export const editInboxComment = <ThrowOnError extends boolean = false>(options: OptionsLegacyParser<EditInboxCommentData, ThrowOnError>) => {
    return (options?.client ?? client).patch<EditInboxCommentResponse, EditInboxCommentError, ThrowOnError>({
        ...options,
        url: '/v1/inbox/comments/{postId}/{commentId}'
    });
};

/**
 * Set comment moderation status
 * Set a comment's moderation status. Supported on YouTube only.
 *
 * Use this to work a moderation queue: approve a held comment (`published`), reject it
 * (`rejected`), or send it back for review (`heldForReview`).
 *
 * The request must be authorized by the owner of the channel or video the comment
 * belongs to. You cannot moderate comments on videos you do not own.
 *
 * This is distinct from `POST /v1/inbox/comments/{postId}/{commentId}/hide`, which
 * covers Facebook, Instagram, Threads, and X/Twitter and does not apply to YouTube.
 *
 */
export const setCommentModeration = <ThrowOnError extends boolean = false>(options: OptionsLegacyParser<SetCommentModerationData, ThrowOnError>) => {
    return (options?.client ?? client).post<SetCommentModerationResponse, SetCommentModerationError, ThrowOnError>({
        ...options,
        url: '/v1/inbox/comments/{postId}/{commentId}/moderation'
    });
};

/**
 * Hide comment
 * Hide a comment on a post. Supported by Facebook, Instagram, Threads, and X/Twitter.
 * Hidden comments are only visible to the commenter and page admin.
 * For X/Twitter, the reply must belong to a conversation started by the authenticated user.
 *
 */
export const hideInboxComment = <ThrowOnError extends boolean = false>(options: OptionsLegacyParser<HideInboxCommentData, ThrowOnError>) => {
    return (options?.client ?? client).post<HideInboxCommentResponse, HideInboxCommentError, ThrowOnError>({
        ...options,
        url: '/v1/inbox/comments/{postId}/{commentId}/hide'
    });
};

/**
 * Unhide comment
 * Unhide a previously hidden comment. Supported by Facebook, Instagram, Threads, and X/Twitter.
 *
 */
export const unhideInboxComment = <ThrowOnError extends boolean = false>(options: OptionsLegacyParser<UnhideInboxCommentData, ThrowOnError>) => {
    return (options?.client ?? client).delete<UnhideInboxCommentResponse, UnhideInboxCommentError, ThrowOnError>({
        ...options,
        url: '/v1/inbox/comments/{postId}/{commentId}/hide'
    });
};

/**
 * Like comment
 * Like or upvote a comment on a post. Supported platforms: Facebook, Twitter/X,
 * Bluesky, Reddit, LinkedIn. For Bluesky, the cid (content identifier) is required
 * in the request body. For LinkedIn, pass the composite comment URN returned by the
 * comments endpoints as commentId; an optional reactionType picks the reaction
 * (defaults to LIKE), and accounts connected before the social-feed scopes were
 * requested get a 403 with code `linkedin_reconnect_required`.
 *
 */
export const likeInboxComment = <ThrowOnError extends boolean = false>(options: OptionsLegacyParser<LikeInboxCommentData, ThrowOnError>) => {
    return (options?.client ?? client).post<LikeInboxCommentResponse, LikeInboxCommentError, ThrowOnError>({
        ...options,
        url: '/v1/inbox/comments/{postId}/{commentId}/like'
    });
};

/**
 * Unlike comment
 * Remove a like from a comment. Supported platforms: Facebook, Twitter/X, Bluesky,
 * Reddit, LinkedIn. For Bluesky, the likeUri query parameter is required.
 *
 */
export const unlikeInboxComment = <ThrowOnError extends boolean = false>(options: OptionsLegacyParser<UnlikeInboxCommentData, ThrowOnError>) => {
    return (options?.client ?? client).delete<UnlikeInboxCommentResponse, UnlikeInboxCommentError, ThrowOnError>({
        ...options,
        url: '/v1/inbox/comments/{postId}/{commentId}/like'
    });
};

/**
 * Like post
 * Like (or react to) a post as a connected account. Supported platforms: LinkedIn,
 * Twitter/X, Facebook, YouTube, Bluesky. Instagram, Threads, TikTok and Pinterest
 * expose no like endpoint in their APIs and return 400. Reddit returns 400 too,
 * pointing at `POST /v1/accounts/{accountId}/reddit-vote`, which covers upvote,
 * downvote and clear on both posts and comments.
 *
 * The account does not have to be the one that published the post, which is what
 * makes executive engagement possible: pass an exec's `accountId` and the brand
 * post's ID. `postId` accepts either a Zernio post ID or the platform's native post
 * ID. A Zernio post ID resolves to the entry for `accountId`, falling back to the
 * post's single entry on the same platform (two entries on that platform is a 400,
 * so pass the native ID).
 *
 * LinkedIn requires the `w_member_social_feed` / `w_organization_social_feed`
 * scopes, which are not retroactive: accounts connected before those were requested
 * get a 403 with code `linkedin_reconnect_required` until the user reconnects the
 * account. YouTube spends 50 quota units per call.
 *
 */
export const likePost = <ThrowOnError extends boolean = false>(options: OptionsLegacyParser<LikePostData, ThrowOnError>) => {
    return (options?.client ?? client).post<LikePostResponse, LikePostError, ThrowOnError>({
        ...options,
        url: '/v1/inbox/posts/{postId}/like'
    });
};

/**
 * Unlike post
 * Remove this account's like from a post. Supported platforms: LinkedIn, Twitter/X,
 * Facebook, YouTube, Bluesky. On YouTube this clears the rating. For Bluesky,
 * `likeUri` (returned when the post was liked) is required. Reddit uses
 * `POST /v1/accounts/{accountId}/reddit-vote` with `direction: 0`.
 *
 */
export const unlikePost = <ThrowOnError extends boolean = false>(options: OptionsLegacyParser<UnlikePostData, ThrowOnError>) => {
    return (options?.client ?? client).delete<UnlikePostResponse, UnlikePostError, ThrowOnError>({
        ...options,
        url: '/v1/inbox/posts/{postId}/like'
    });
};

/**
 * Send private reply
 * Send a private message to the author of a comment. Supported on Instagram and Facebook only.
 * One reply per comment, must be sent within 7 days. Optionally attach interactive elements:
 * `quickReplies` (chips above the keyboard, max 13) or `buttons` (1-3 inline postback/url
 * buttons rendered in the same bubble via Meta's button_template). Buttons are recommended
 * for cold reach since chips do not render in the Instagram Message Requests folder.
 * `quickReplies` and `buttons` are mutually exclusive.
 *
 */
export const sendPrivateReplyToComment = <ThrowOnError extends boolean = false>(options: OptionsLegacyParser<SendPrivateReplyToCommentData, ThrowOnError>) => {
    return (options?.client ?? client).post<SendPrivateReplyToCommentResponse, SendPrivateReplyToCommentError, ThrowOnError>({
        ...options,
        url: '/v1/inbox/comments/{postId}/{commentId}/private-reply'
    });
};

/**
 * Retweet a post
 * Retweet (repost) a tweet by ID.
 * Rate limit: 50 requests per 15-min window. Shares the 300/3hr creation limit with tweet creation.
 *
 */
export const retweetPost = <ThrowOnError extends boolean = false>(options: OptionsLegacyParser<RetweetPostData, ThrowOnError>) => {
    return (options?.client ?? client).post<RetweetPostResponse, RetweetPostError, ThrowOnError>({
        ...options,
        url: '/v1/twitter/retweet'
    });
};

/**
 * Undo retweet
 * Undo a retweet (un-repost a tweet).
 *
 */
export const undoRetweet = <ThrowOnError extends boolean = false>(options: OptionsLegacyParser<UndoRetweetData, ThrowOnError>) => {
    return (options?.client ?? client).delete<UndoRetweetResponse, UndoRetweetError, ThrowOnError>({
        ...options,
        url: '/v1/twitter/retweet'
    });
};

/**
 * Bookmark a tweet
 * Bookmark a tweet by ID.
 * Requires the bookmark.write OAuth scope.
 * Rate limit: 50 requests per 15-min window.
 *
 */
export const bookmarkPost = <ThrowOnError extends boolean = false>(options: OptionsLegacyParser<BookmarkPostData, ThrowOnError>) => {
    return (options?.client ?? client).post<BookmarkPostResponse, BookmarkPostError, ThrowOnError>({
        ...options,
        url: '/v1/twitter/bookmark'
    });
};

/**
 * Remove bookmark
 * Remove a bookmark from a tweet.
 *
 */
export const removeBookmark = <ThrowOnError extends boolean = false>(options: OptionsLegacyParser<RemoveBookmarkData, ThrowOnError>) => {
    return (options?.client ?? client).delete<RemoveBookmarkResponse, RemoveBookmarkError, ThrowOnError>({
        ...options,
        url: '/v1/twitter/bookmark'
    });
};

/**
 * Follow a user
 * Follow a user on X/Twitter.
 * Requires the follows.write OAuth scope.
 * For protected accounts, a follow request is sent instead (pending_follow will be true).
 *
 */
export const followUser = <ThrowOnError extends boolean = false>(options: OptionsLegacyParser<FollowUserData, ThrowOnError>) => {
    return (options?.client ?? client).post<FollowUserResponse, FollowUserError, ThrowOnError>({
        ...options,
        url: '/v1/twitter/follow'
    });
};

/**
 * Unfollow a user
 * Unfollow a user on X/Twitter.
 *
 */
export const unfollowUser = <ThrowOnError extends boolean = false>(options: OptionsLegacyParser<UnfollowUserData, ThrowOnError>) => {
    return (options?.client ?? client).delete<UnfollowUserResponse, UnfollowUserError, ThrowOnError>({
        ...options,
        url: '/v1/twitter/follow'
    });
};

/**
 * Search recent tweets
 * Search public tweets from the last 7 days matching an X search query, e.g. to discover tweets to reply to.
 * The query string is passed through to X unchanged and supports X's search operators
 * (`from:user`, `-is:retweet`, `is:reply`, `lang:en`, `"exact phrase"`, `conversation_id:123`, boolean `OR`, ...).
 * Note that standalone operators like `is:` / `has:` / `lang:` must be combined with a keyword or `from:` clause.
 *
 * To reply to a found tweet, pass its `id` as the twitter platform entry's `platformSpecificData.replyToTweetId` when creating a post.
 *
 * Rate limit: 300 requests per 15-min window per connected account.
 *
 */
export const searchTweets = <ThrowOnError extends boolean = false>(options: OptionsLegacyParser<SearchTweetsData, ThrowOnError>) => {
    return (options?.client ?? client).get<SearchTweetsResponse, SearchTweetsError, ThrowOnError>({
        ...options,
        url: '/v1/twitter/search'
    });
};

/**
 * Look up a tweet
 * Resolve a single tweet by ID or URL into its text, author and public metrics.
 *
 * Use this to render a post you are referencing, e.g. the tweet quoted by a quote-style post.
 * Unlike `/v1/twitter/search` this is not limited to the last 7 days and works for any tweet
 * visible to the connected account.
 *
 * Billed as an X posts read ($0.005). Repeat lookups of the same tweet within the same UTC day
 * are charged once.
 *
 */
export const getTweet = <ThrowOnError extends boolean = false>(options: OptionsLegacyParser<GetTweetData, ThrowOnError>) => {
    return (options?.client ?? client).get<GetTweetResponse, GetTweetError, ThrowOnError>({
        ...options,
        url: '/v1/twitter/tweet'
    });
};

/**
 * List mentions
 * Returns mentions of your connected organization accounts, delivered via platform webhooks.
 * Currently supports LinkedIn organization mentions.
 *
 * Requires Inbox addon.
 *
 */
export const listInboxMentions = <ThrowOnError extends boolean = false>(options?: OptionsLegacyParser<ListInboxMentionsData, ThrowOnError>) => {
    return (options?.client ?? client).get<ListInboxMentionsResponse, ListInboxMentionsError, ThrowOnError>({
        ...options,
        url: '/v1/inbox/mentions'
    });
};

/**
 * Reply to a mention
 * Reply to a mention of the connected account. Supported on Instagram only.
 *
 * Two shapes, selected by whether `commentId` is present:
 *
 * - **Comment mention** (someone @mentioned the account inside a comment): pass both
 * `mediaId` and `commentId`. Instagram posts a reply under that comment.
 * - **Caption mention** (someone @mentioned the account in their media caption, so no
 * comment exists): pass `mediaId` only. Instagram posts a comment on their media.
 *
 * Story mentions are not supported by Instagram's API.
 *
 * Note that `GET /v1/inbox/mentions` currently returns LinkedIn mentions only and does
 * not surface Instagram mentions. Source `mediaId` and `commentId` from Instagram's
 * `comments` webhook, which is where mention notifications are delivered for accounts
 * connected through Instagram Login.
 *
 */
export const replyToMention = <ThrowOnError extends boolean = false>(options: OptionsLegacyParser<ReplyToMentionData, ThrowOnError>) => {
    return (options?.client ?? client).post<ReplyToMentionResponse, ReplyToMentionError, ThrowOnError>({
        ...options,
        url: '/v1/inbox/mentions/reply'
    });
};

/**
 * List reviews
 * Fetch reviews from all connected Facebook Pages and Google Business accounts. Aggregates data with filtering and sorting options.
 * Supported platforms: Facebook, Google Business.
 *
 */
export const listInboxReviews = <ThrowOnError extends boolean = false>(options?: OptionsLegacyParser<ListInboxReviewsData, ThrowOnError>) => {
    return (options?.client ?? client).get<ListInboxReviewsResponse, ListInboxReviewsError, ThrowOnError>({
        ...options,
        url: '/v1/inbox/reviews'
    });
};

/**
 * Reply to review
 * Post a reply to a review. Requires accountId in request body.
 */
export const replyToInboxReview = <ThrowOnError extends boolean = false>(options: OptionsLegacyParser<ReplyToInboxReviewData, ThrowOnError>) => {
    return (options?.client ?? client).post<ReplyToInboxReviewResponse, ReplyToInboxReviewError, ThrowOnError>({
        ...options,
        url: '/v1/inbox/reviews/{reviewId}/reply'
    });
};

/**
 * Delete review reply
 * Delete a reply to a review (Google Business only). Requires accountId in request body.
 */
export const deleteInboxReviewReply = <ThrowOnError extends boolean = false>(options: OptionsLegacyParser<DeleteInboxReviewReplyData, ThrowOnError>) => {
    return (options?.client ?? client).delete<DeleteInboxReviewReplyResponse, DeleteInboxReviewReplyError, ThrowOnError>({
        ...options,
        url: '/v1/inbox/reviews/{reviewId}/reply'
    });
};

/**
 * List templates
 * List all message templates for the WhatsApp Business Account (WABA) associated with the given account.
 * Templates are fetched directly from the WhatsApp Cloud API.
 *
 */
export const getWhatsAppTemplates = <ThrowOnError extends boolean = false>(options: OptionsLegacyParser<GetWhatsAppTemplatesData, ThrowOnError>) => {
    return (options?.client ?? client).get<GetWhatsAppTemplatesResponse, GetWhatsAppTemplatesError, ThrowOnError>({
        ...options,
        url: '/v1/whatsapp/templates'
    });
};

/**
 * Create template
 * Create a new message template. Supports two modes:
 *
 * Custom template: Provide components with your own content. Submitted to Meta for review (can take up to 24h).
 *
 * Library template: Provide library_template_name instead of components to use a pre-built template
 * from Meta's template library. Library templates are pre-approved (no review wait). You can optionally
 * customize parameters and buttons via library_template_body_inputs and library_template_button_inputs.
 *
 * Browse available library templates at: https://business.facebook.com/wa/manage/message-templates/
 *
 */
export const createWhatsAppTemplate = <ThrowOnError extends boolean = false>(options: OptionsLegacyParser<CreateWhatsAppTemplateData, ThrowOnError>) => {
    return (options?.client ?? client).post<CreateWhatsAppTemplateResponse, CreateWhatsAppTemplateError, ThrowOnError>({
        ...options,
        url: '/v1/whatsapp/templates'
    });
};

/**
 * Get template
 * Retrieve a single message template by name.
 *
 */
export const getWhatsAppTemplate = <ThrowOnError extends boolean = false>(options: OptionsLegacyParser<GetWhatsAppTemplateData, ThrowOnError>) => {
    return (options?.client ?? client).get<GetWhatsAppTemplateResponse, GetWhatsAppTemplateError, ThrowOnError>({
        ...options,
        url: '/v1/whatsapp/templates/{templateName}'
    });
};

/**
 * Update template
 * Update a message template's components. Only certain fields can be updated depending on
 * the template's current approval state. Approved templates can only have components updated.
 *
 */
export const updateWhatsAppTemplate = <ThrowOnError extends boolean = false>(options: OptionsLegacyParser<UpdateWhatsAppTemplateData, ThrowOnError>) => {
    return (options?.client ?? client).patch<UpdateWhatsAppTemplateResponse, UpdateWhatsAppTemplateError, ThrowOnError>({
        ...options,
        url: '/v1/whatsapp/templates/{templateName}'
    });
};

/**
 * Delete template
 * Permanently delete a message template by name.
 *
 */
export const deleteWhatsAppTemplate = <ThrowOnError extends boolean = false>(options: OptionsLegacyParser<DeleteWhatsAppTemplateData, ThrowOnError>) => {
    return (options?.client ?? client).delete<DeleteWhatsAppTemplateResponse, DeleteWhatsAppTemplateError, ThrowOnError>({
        ...options,
        url: '/v1/whatsapp/templates/{templateName}'
    });
};

/**
 * Get calling config for an account
 * Returns the local calling configuration snapshot for the connected
 * WhatsApp account: whether calling is enabled, the forward-to
 * destination URI, recording opt-in state, the phone number record id
 * (use as `{id}` on the read-write calling sub-resource at
 * /v1/phone-numbers/{id}/whatsapp/calling) and whether SIP digest
 * credentials are stored (the encrypted password itself is never
 * returned). Also carries account-level extras (billing eligibility,
 * current-period spend) that the number-keyed GET does not.
 *
 */
export const getWhatsAppCallingConfig = <ThrowOnError extends boolean = false>(options: OptionsLegacyParser<GetWhatsAppCallingConfigData, ThrowOnError>) => {
    return (options?.client ?? client).get<GetWhatsAppCallingConfigResponse, GetWhatsAppCallingConfigError, ThrowOnError>({
        ...options,
        url: '/v1/whatsapp/calling'
    });
};

/**
 * @deprecated
 * Enable calling on a number
 * Deprecated alias of `/v1/phone-numbers/{id}/whatsapp/calling`; same contract. New
 * integrations should use that path.
 *
 * Enable WhatsApp Business Calling on a connected number. Configures
 * Meta calling.status=ENABLED with our Telnyx SIP endpoint, fetches and
 * stores the Meta-issued SIP password (encrypted), and snapshots the
 * customer's forward-to destination.
 *
 */
export const enableWhatsAppCallingLegacy = <ThrowOnError extends boolean = false>(options: OptionsLegacyParser<EnableWhatsAppCallingLegacyData, ThrowOnError>) => {
    return (options?.client ?? client).post<EnableWhatsAppCallingLegacyResponse, EnableWhatsAppCallingLegacyError, ThrowOnError>({
        ...options,
        url: '/v1/whatsapp/phone-numbers/{id}/calling'
    });
};

/**
 * @deprecated
 * Update calling config
 * Deprecated alias of `/v1/phone-numbers/{id}/whatsapp/calling`; same contract. New
 * integrations should use that path.
 *
 * Update fields on an already-enabled number. Only fields present in
 * the body are written; `undefined` leaves the stored value alone,
 * explicit `null` clears a nullable field. No Meta side effect, this
 * only changes local routing state consumed by the Telnyx webhook
 * handler.
 *
 */
export const updateWhatsAppCallingLegacy = <ThrowOnError extends boolean = false>(options: OptionsLegacyParser<UpdateWhatsAppCallingLegacyData, ThrowOnError>) => {
    return (options?.client ?? client).patch<UpdateWhatsAppCallingLegacyResponse, UpdateWhatsAppCallingLegacyError, ThrowOnError>({
        ...options,
        url: '/v1/whatsapp/phone-numbers/{id}/calling'
    });
};

/**
 * @deprecated
 * Disable calling on a number
 * Deprecated alias of `/v1/phone-numbers/{id}/whatsapp/calling`; same contract. New
 * integrations should use that path.
 *
 * Disable calling. Sends calling.status=DISABLED to Meta (best-effort)
 * and flips the local `callingEnabled` flag off. forwardTo and SIP
 * creds are preserved so a re-enable does not lose the destination.
 *
 */
export const disableWhatsAppCallingLegacy = <ThrowOnError extends boolean = false>(options: OptionsLegacyParser<DisableWhatsAppCallingLegacyData, ThrowOnError>) => {
    return (options?.client ?? client).delete<DisableWhatsAppCallingLegacyResponse, DisableWhatsAppCallingLegacyError, ThrowOnError>({
        ...options,
        url: '/v1/whatsapp/phone-numbers/{id}/calling'
    });
};

/**
 * Check call permission
 * Returns the permission state and the list of available actions for
 * a given consumer wa_id (e.g. `start_call`, `send_call_permission_request`).
 * Use this before placing a call to decide whether to prompt for
 * consent first.
 *
 */
export const getWhatsAppCallPermissions = <ThrowOnError extends boolean = false>(options: OptionsLegacyParser<GetWhatsAppCallPermissionsData, ThrowOnError>) => {
    return (options?.client ?? client).get<GetWhatsAppCallPermissionsResponse, GetWhatsAppCallPermissionsError, ThrowOnError>({
        ...options,
        url: '/v1/whatsapp/call-permissions'
    });
};

/**
 * Initiate outbound call
 * Initiates an outbound Business-Initiated Call. The Telnyx-side SIP
 * leg is originated server-side (Option B: SIP-first). Telnyx INVITEs
 * Meta directly over TLS:5061 with the SIP digest credentials we
 * captured at calling-enablement time). No client-side SDP is
 * required; pass only `accountId` and `to`.
 *
 * To send the consumer the call-consent prompt instead of placing a
 * call, pass `action: "send_call_permission_request"` (+ optional
 * `bodyText`). The consumer must tap Allow in WhatsApp before
 * `start_call` is permitted; Meta limits the prompt to 1 per consumer
 * per 24h (2 per 7 days) and requires an open 24h service window.
 *
 * **Idempotency:** send an `Idempotency-Key` header to make retries
 * safe; same key + same body replays the original response instead of
 * dialing (and billing) a second call.
 *
 */
export const initiateWhatsAppCall = <ThrowOnError extends boolean = false>(options: OptionsLegacyParser<InitiateWhatsAppCallData, ThrowOnError>) => {
    return (options?.client ?? client).post<InitiateWhatsAppCallResponse, InitiateWhatsAppCallError, ThrowOnError>({
        ...options,
        url: '/v1/whatsapp/calls'
    });
};

/**
 * List call history for an account
 * Compact history listing for a single connected account. Results are
 * scoped to the resolved SocialAccount; profile-scoped team members
 * cannot read calls on sibling accounts.
 *
 * Cursor pagination: pass the returned `nextCursor` as `before` to fetch
 * the next page (same scheme as `GET /v1/calls`). `since`/`until` remain
 * as absolute range filters and combine with the cursor.
 *
 */
export const listWhatsAppCalls = <ThrowOnError extends boolean = false>(options: OptionsLegacyParser<ListWhatsAppCallsData, ThrowOnError>) => {
    return (options?.client ?? client).get<ListWhatsAppCallsResponse, ListWhatsAppCallsError, ThrowOnError>({
        ...options,
        url: '/v1/whatsapp/calls'
    });
};

/**
 * Get a single call
 */
export const getWhatsAppCall = <ThrowOnError extends boolean = false>(options: OptionsLegacyParser<GetWhatsAppCallData, ThrowOnError>) => {
    return (options?.client ?? client).get<GetWhatsAppCallResponse, GetWhatsAppCallError, ThrowOnError>({
        ...options,
        url: '/v1/whatsapp/calls/{id}'
    });
};

/**
 * Get a call recording
 * Resolves a fresh, playable MP3 URL for the call's recording.
 * Provider-signed recording URLs expire ~10 minutes after signing, so the
 * `recordingUrl` stored on the call is usually stale by the time it is
 * played; this endpoint re-signs on demand. Default responds `302 Found`
 * redirecting to the fresh URL (point an `<audio>` element or a link
 * straight at this endpoint); pass `as=json` to receive `{ url }` instead.
 *
 */
export const getWhatsAppCallRecording = <ThrowOnError extends boolean = false>(options: OptionsLegacyParser<GetWhatsAppCallRecordingData, ThrowOnError>) => {
    return (options?.client ?? client).get<GetWhatsAppCallRecordingResponse, GetWhatsAppCallRecordingError, ThrowOnError>({
        ...options,
        url: '/v1/whatsapp/calls/{id}/recording'
    });
};

/**
 * Estimate per-minute cost
 * Returns a zero-markup estimated cost for an outbound call to the
 * given destination, broken down by Meta + Telnyx + recording line
 * items. Costs are pass-through, no margin applied.
 *
 */
export const getWhatsAppCallEstimate = <ThrowOnError extends boolean = false>(options: OptionsLegacyParser<GetWhatsAppCallEstimateData, ThrowOnError>) => {
    return (options?.client ?? client).get<GetWhatsAppCallEstimateResponse, GetWhatsAppCallEstimateError, ThrowOnError>({
        ...options,
        url: '/v1/whatsapp/calls/estimate'
    });
};

/**
 * List all calls (unified history)
 * Unified call history across ALL of your numbers: both channels
 * (WhatsApp Business Calling + regular phone/PSTN), inbound and outbound,
 * newest first. Unlike `GET /v1/voice/calls` (PSTN-only) and
 * `GET /v1/whatsapp/calls` (one account at a time), this endpoint needs no
 * `accountId` and never requires fanning out one request per number.
 *
 * Any row can be opened channel-agnostically via `GET /v1/calls/{id}` and
 * `GET /v1/calls/{id}/recording`; no branching on `channel` needed. When
 * the counterparty number matches a CRM contact, `contactId` and
 * `contactName` are set.
 *
 * Cursor pagination: pass the returned `nextCursor` as `before` to fetch
 * the next page. `nextCursor` is null on the last page.
 *
 */
export const listCalls = <ThrowOnError extends boolean = false>(options?: OptionsLegacyParser<ListCallsData, ThrowOnError>) => {
    return (options?.client ?? client).get<ListCallsResponse, ListCallsError, ThrowOnError>({
        ...options,
        url: '/v1/calls'
    });
};

/**
 * Get a call (any channel)
 * Channel-agnostic call detail: works for both WhatsApp and regular
 * phone (PSTN) calls, so any row from `GET /v1/calls` can be opened
 * without branching on `channel`. Returns the full call including
 * transcript segments, with `contactId`/`contactName` set when the
 * counterparty matches a CRM contact.
 *
 */
export const getCall = <ThrowOnError extends boolean = false>(options: OptionsLegacyParser<GetCallData, ThrowOnError>) => {
    return (options?.client ?? client).get<GetCallResponse, GetCallError, ThrowOnError>({
        ...options,
        url: '/v1/calls/{id}'
    });
};

/**
 * Get a call recording
 * Channel-agnostic recording fetch: resolves a fresh, playable MP3 URL
 * for any call regardless of channel (provider-signed URLs expire ~10
 * minutes after signing, so this re-signs on demand). Default responds
 * `302 Found` redirecting to the fresh URL; pass `as=json` to receive
 * `{ url }` instead.
 *
 */
export const getCallRecording = <ThrowOnError extends boolean = false>(options: OptionsLegacyParser<GetCallRecordingData, ThrowOnError>) => {
    return (options?.client ?? client).get<GetCallRecordingResponse, GetCallRecordingError, ThrowOnError>({
        ...options,
        url: '/v1/calls/{id}/recording'
    });
};

/**
 * Place an outbound phone call
 * Dials `to` FROM one of your voice-enabled numbers and, on answer,
 * bridges the callee to the number's stored forward destination, or to
 * the per-call `forwardTo` override. Destinations can be your own AI
 * voice agent (Vapi/Retell), a phone, or a SIP endpoint. An optional
 * `greeting` is spoken to the callee before the bridge.
 *
 * The 200 response means the call is dialing; the lifecycle continues
 * asynchronously (track it via `GET /v1/voice/calls/{id}` or the `call.*`
 * webhooks). Outbound calls are capped per rolling hour (429 when hit).
 *
 * **Idempotency:** send an `Idempotency-Key` header to make retries safe;
 * same key + same body replays the original response instead of dialing
 * (and billing) a second call.
 *
 */
export const createVoiceCall = <ThrowOnError extends boolean = false>(options: OptionsLegacyParser<CreateVoiceCallData, ThrowOnError>) => {
    return (options?.client ?? client).post<CreateVoiceCallResponse, CreateVoiceCallError, ThrowOnError>({
        ...options,
        url: '/v1/voice/calls'
    });
};

/**
 * List phone calls
 * Your PSTN voice calls (inbound + outbound), newest first. Cursor
 * pagination: pass the returned `nextCursor` as `before` for the next
 * page. For a history that also includes WhatsApp calls, use
 * `GET /v1/calls`.
 *
 */
export const listVoiceCalls = <ThrowOnError extends boolean = false>(options?: OptionsLegacyParser<ListVoiceCallsData, ThrowOnError>) => {
    return (options?.client ?? client).get<ListVoiceCallsResponse, ListVoiceCallsError, ThrowOnError>({
        ...options,
        url: '/v1/voice/calls'
    });
};

/**
 * Get a phone call
 * Full call detail, including the transcript segments when transcription was on.
 */
export const getVoiceCall = <ThrowOnError extends boolean = false>(options: OptionsLegacyParser<GetVoiceCallData, ThrowOnError>) => {
    return (options?.client ?? client).get<GetVoiceCallResponse, GetVoiceCallError, ThrowOnError>({
        ...options,
        url: '/v1/voice/calls/{id}'
    });
};

/**
 * Hang up a live call
 * Hangs up a live call on demand. Idempotent: ending a call that already
 * ended (or never connected) returns success with the call's current
 * status. Final duration/cost are written asynchronously when the hangup
 * event lands, so the call doc may briefly still show its prior status.
 *
 */
export const endVoiceCall = <ThrowOnError extends boolean = false>(options: OptionsLegacyParser<EndVoiceCallData, ThrowOnError>) => {
    return (options?.client ?? client).post<EndVoiceCallResponse, EndVoiceCallError, ThrowOnError>({
        ...options,
        url: '/v1/voice/calls/{id}/end'
    });
};

/**
 * Get a call recording
 * Resolves a fresh, playable MP3 URL for the call's recording
 * (provider-signed URLs expire ~10 minutes after signing, so this
 * endpoint re-signs on demand). Default responds `302 Found` redirecting
 * to the fresh URL; pass `as=json` to receive `{ url }` instead.
 *
 */
export const getVoiceCallRecording = <ThrowOnError extends boolean = false>(options: OptionsLegacyParser<GetVoiceCallRecordingData, ThrowOnError>) => {
    return (options?.client ?? client).get<GetVoiceCallRecordingResponse, GetVoiceCallRecordingError, ThrowOnError>({
        ...options,
        url: '/v1/voice/calls/{id}/recording'
    });
};

/**
 * Blind-transfer a live call
 * Moves the call's current leg to a new destination (a phone number or a
 * SIP endpoint). This is a BLIND transfer: control of the leg is handed
 * off and the call ends normally when the transferred leg hangs up. The
 * caller ID presented on the transfer leg is always your own number.
 *
 */
export const transferVoiceCall = <ThrowOnError extends boolean = false>(options: OptionsLegacyParser<TransferVoiceCallData, ThrowOnError>) => {
    return (options?.client ?? client).post<TransferVoiceCallResponse, TransferVoiceCallError, ThrowOnError>({
        ...options,
        url: '/v1/voice/calls/{id}/transfer'
    });
};

/**
 * Estimate call cost
 * Pre-call cost estimate for a PSTN call: the carrier leg plus optional
 * recording and transcription add-ons. Same billing formula as the
 * post-call invoice, so the quote and the final charge can't disagree.
 * The per-minute figure is deliberately conservative (the real cost
 * comes from the settled carrier record after the call), so estimates
 * trend slightly over the actual invoice. Parity endpoint of
 * `GET /v1/whatsapp/calls/estimate`, minus the Meta line (PSTN calls
 * have no separate Meta bill, so `totalCostUSD` equals
 * `billableCostUSD`).
 *
 */
export const getVoiceCallEstimate = <ThrowOnError extends boolean = false>(options: OptionsLegacyParser<GetVoiceCallEstimateData, ThrowOnError>) => {
    return (options?.client ?? client).get<GetVoiceCallEstimateResponse, GetVoiceCallEstimateError, ThrowOnError>({
        ...options,
        url: '/v1/voice/calls/estimate'
    });
};

/**
 * Mint a browser softphone session
 * Step 1 of the two-step browser softphone handshake. Mints a WebRTC
 * session (token + credential) the browser registers with the
 * `@telnyx/webrtc` SDK. Once registered, call
 * `POST /v1/voice/calls/web/dial` with the returned `credentialId` to
 * place the call. The split avoids bridging to a browser that has not
 * finished registering. The token lives ~1 hour (it must outlive the
 * whole call, not just the handshake).
 *
 */
export const createVoiceWebSession = <ThrowOnError extends boolean = false>(options?: OptionsLegacyParser<unknown, ThrowOnError>) => {
    return (options?.client ?? client).post<CreateVoiceWebSessionResponse, CreateVoiceWebSessionError, ThrowOnError>({
        ...options,
        url: '/v1/voice/calls/web'
    });
};

/**
 * Dial from the browser softphone
 * Step 2 of the browser softphone handshake: places an outbound call
 * whose answered leg is bridged to the browser registered with the
 * credential from `POST /v1/voice/calls/web`. The call runs through the
 * normal outbound lane, so it is logged as outbound (from = your number,
 * to = target) and recorded per the number's settings.
 *
 */
export const dialVoiceWebCall = <ThrowOnError extends boolean = false>(options: OptionsLegacyParser<DialVoiceWebCallData, ThrowOnError>) => {
    return (options?.client ?? client).post<DialVoiceWebCallResponse, DialVoiceWebCallError, ThrowOnError>({
        ...options,
        url: '/v1/voice/calls/web/dial'
    });
};

/**
 * Send an SMS/MMS
 * Sends an SMS (or MMS when `mediaUrls` is set) from one of your
 * SMS-enabled numbers. At least one of `text` / `mediaUrls` is required.
 * Both numbers are normalized to E.164, so `from` matches regardless of
 * formatting and replies thread into the same inbox conversation.
 *
 * US numbers must have an approved carrier registration
 * (`/v1/sms/registrations`) before messages deliver.
 *
 * **Replies and delivery status arrive as webhooks**, not by polling:
 * an inbound reply fires `message.received` with `platform: "sms"`, the
 * first message of a new thread also fires `conversation.started`, and
 * this message's own outcome fires `message.delivered` or
 * `message.failed` (the latter carrying the carrier's error code).
 *
 * **Opted-out recipients:** a send to a number that replied STOP is
 * refused with `409`, never silently dropped.
 *
 * **Idempotency:** send an `Idempotency-Key` header to make retries safe:
 * same key + same body replays the original response instead of sending a
 * second message; same key + different body returns 422; a key still in
 * flight returns 409.
 *
 */
export const sendSms = <ThrowOnError extends boolean = false>(options: OptionsLegacyParser<SendSmsData, ThrowOnError>) => {
    return (options?.client ?? client).post<SendSmsResponse, SendSmsError, ThrowOnError>({
        ...options,
        url: '/v1/sms/messages'
    });
};

/**
 * Look up carrier + line type
 * Carrier name and line type (mobile / landline / voip / toll-free) for a
 * number, plus `smsReachable` (landlines can't receive SMS). Use it to
 * validate recipients before sending. Each lookup is billed by the
 * carrier-data provider, so call it explicitly (e.g. pre-validating an
 * opt-in list), not on every send.
 *
 */
export const lookupSmsNumber = <ThrowOnError extends boolean = false>(options: OptionsLegacyParser<LookupSmsNumberData, ThrowOnError>) => {
    return (options?.client ?? client).get<LookupSmsNumberResponse, LookupSmsNumberError, ThrowOnError>({
        ...options,
        url: '/v1/sms/lookup'
    });
};

/**
 * List SMS opt-outs
 * The recipients who opted out of SMS (replied STOP) across your numbers,
 * most recent first. Compliance surface: you must be able to see and
 * export your opt-out list. Read-only: a recipient is re-subscribed only
 * by replying START. Pass `format=csv` to download a CSV instead of JSON.
 *
 */
export const listSmsOptOuts = <ThrowOnError extends boolean = false>(options?: OptionsLegacyParser<ListSmsOptOutsData, ThrowOnError>) => {
    return (options?.client ?? client).get<ListSmsOptOutsResponse, ListSmsOptOutsError, ThrowOnError>({
        ...options,
        url: '/v1/sms/opt-outs'
    });
};

/**
 * Create an alphanumeric sender ID
 * Registers an alphanumeric sender ID (e.g. `ZERNIO`) — a branded `from`
 * for one-way international SMS. No phone number purchase or carrier
 * registration is needed; once created, pass it as `from` on
 * `POST /v1/sms/messages`.
 *
 * Constraints: 3-11 characters (letters, digits, spaces; at least one
 * letter). Sends cannot reach the US, Canada, or Puerto Rico, are
 * text-only, and recipients cannot reply. Sender IDs that impersonate
 * well-known brands or institutions are rejected. Names are not
 * exclusive: the same sender ID can be registered by any number of
 * workspaces. Creating the same sender ID again is a no-op
 * (re-activates it after a delete).
 *
 */
export const createSmsSenderId = <ThrowOnError extends boolean = false>(options: OptionsLegacyParser<CreateSmsSenderIdData, ThrowOnError>) => {
    return (options?.client ?? client).post<CreateSmsSenderIdResponse, CreateSmsSenderIdError, ThrowOnError>({
        ...options,
        url: '/v1/sms/sender-ids'
    });
};

/**
 * List alphanumeric sender IDs
 */
export const listSmsSenderIds = <ThrowOnError extends boolean = false>(options?: OptionsLegacyParser<unknown, ThrowOnError>) => {
    return (options?.client ?? client).get<ListSmsSenderIdsResponse, ListSmsSenderIdsError, ThrowOnError>({
        ...options,
        url: '/v1/sms/sender-ids'
    });
};

/**
 * Request a higher sender ID daily limit
 * Asks support to raise the workspace's daily sender-ID message cap.
 * There is no self-serve raise: the request (desired cap + use case) is
 * reviewed manually, usually within a business day.
 *
 */
export const requestSmsSenderIdLimitIncrease = <ThrowOnError extends boolean = false>(options: OptionsLegacyParser<RequestSmsSenderIdLimitIncreaseData, ThrowOnError>) => {
    return (options?.client ?? client).post<RequestSmsSenderIdLimitIncreaseResponse, RequestSmsSenderIdLimitIncreaseError, ThrowOnError>({
        ...options,
        url: '/v1/sms/sender-ids/limit-request'
    });
};

/**
 * Delete an alphanumeric sender ID
 * Deactivates the sender ID so it can no longer send. Re-creating the
 * same sender ID via `POST /v1/sms/sender-ids` re-activates it.
 *
 */
export const deleteSmsSenderId = <ThrowOnError extends boolean = false>(options: OptionsLegacyParser<DeleteSmsSenderIdData, ThrowOnError>) => {
    return (options?.client ?? client).delete<DeleteSmsSenderIdResponse, DeleteSmsSenderIdError, ThrowOnError>({
        ...options,
        url: '/v1/sms/sender-ids/{id}'
    });
};

/**
 * Start a carrier registration
 * Starts the US carrier registration that a number needs before SMS
 * delivers: 10DLC (standard company or sole-proprietor) or toll-free
 * verification. 10DLC needs `brand` + `campaign`; toll-free needs
 * `tollFree`. Approval is asynchronous; poll
 * `GET /v1/sms/registrations/{id}` (sole-prop registrations first need
 * the OTP step: a code is texted to the brand's mobile number, submit it
 * via `/verify-otp`).
 *
 * Already have an approved registration? Add another number to it with
 * `POST /v1/phone-numbers/{id}/sms/reuse-registration` instead
 * of registering (and paying the carrier brand fee) again.
 *
 * Rather have your client fill in the legal business details? Create a
 * share link with `POST /v1/sms/registrations/share`.
 *
 */
export const startSmsRegistration = <ThrowOnError extends boolean = false>(options: OptionsLegacyParser<StartSmsRegistrationData, ThrowOnError>) => {
    return (options?.client ?? client).post<StartSmsRegistrationResponse, StartSmsRegistrationError, ThrowOnError>({
        ...options,
        url: '/v1/sms/registrations'
    });
};

/**
 * List carrier registrations
 */
export const listSmsRegistrations = <ThrowOnError extends boolean = false>(options?: OptionsLegacyParser<ListSmsRegistrationsData, ThrowOnError>) => {
    return (options?.client ?? client).get<ListSmsRegistrationsResponse, ListSmsRegistrationsError, ThrowOnError>({
        ...options,
        url: '/v1/sms/registrations'
    });
};

/**
 * Pre-check a carrier registration
 * Dry-run of `POST /v1/sms/registrations` for 10DLC: validates and
 * composes the exact brand/campaign payloads a submission would store
 * (branding, disclosures, auto-replies), runs deterministic compliance
 * lints plus an AI reviewer over them, and returns the findings WITHOUT
 * creating anything. Use it to fix issues before submitting; `block`
 * severity findings indicate a near-certain carrier rejection.
 *
 */
export const preflightSmsRegistration = <ThrowOnError extends boolean = false>(options: OptionsLegacyParser<PreflightSmsRegistrationData, ThrowOnError>) => {
    return (options?.client ?? client).post<PreflightSmsRegistrationResponse, PreflightSmsRegistrationError, ThrowOnError>({
        ...options,
        url: '/v1/sms/registrations/preflight'
    });
};

/**
 * Deactivate a brand/campaign registration
 * Terminates the campaign with the carrier registry so the recurring
 * monthly campaign fee stops (carriers bill the first 3 months of a
 * campaign regardless). Numbers covered by it can no longer SEND texts —
 * receiving is unaffected — until they're registered under a new brand.
 * Irreversible: a deactivated campaign cannot be restored; texting again
 * later requires a new registration (new one-time and review fees).
 * Idempotent.
 *
 */
export const deactivateSmsRegistration = <ThrowOnError extends boolean = false>(options: OptionsLegacyParser<DeactivateSmsRegistrationData, ThrowOnError>) => {
    return (options?.client ?? client).delete<DeactivateSmsRegistrationResponse, DeactivateSmsRegistrationError, ThrowOnError>({
        ...options,
        url: '/v1/sms/registrations/{id}'
    });
};

/**
 * Get a carrier registration
 * Poll this for approval progress after starting a registration.
 */
export const getSmsRegistration = <ThrowOnError extends boolean = false>(options: OptionsLegacyParser<GetSmsRegistrationData, ThrowOnError>) => {
    return (options?.client ?? client).get<GetSmsRegistrationResponse, GetSmsRegistrationError, ThrowOnError>({
        ...options,
        url: '/v1/sms/registrations/{id}'
    });
};

/**
 * Submit the sole-prop OTP
 * Completes sole-proprietor 10DLC brand verification by submitting the
 * one-time PIN texted to the brand's mobile number. On success the
 * registration continues to campaign creation automatically.
 *
 */
export const verifySmsRegistrationOtp = <ThrowOnError extends boolean = false>(options: OptionsLegacyParser<VerifySmsRegistrationOtpData, ThrowOnError>) => {
    return (options?.client ?? client).post<VerifySmsRegistrationOtpResponse, VerifySmsRegistrationOtpError, ThrowOnError>({
        ...options,
        url: '/v1/sms/registrations/{id}/verify-otp'
    });
};

/**
 * Re-send the sole-prop OTP
 * Re-sends the sole-proprietor verification PIN to the brand's mobile
 * number — use it when the original code expired or never arrived. Only
 * valid while the registration is pending and awaiting its OTP; rate
 * limited to one send per minute.
 *
 */
export const resendSmsRegistrationOtp = <ThrowOnError extends boolean = false>(options: OptionsLegacyParser<ResendSmsRegistrationOtpData, ThrowOnError>) => {
    return (options?.client ?? client).post<ResendSmsRegistrationOtpResponse, ResendSmsRegistrationOtpError, ThrowOnError>({
        ...options,
        url: '/v1/sms/registrations/{id}/resend-otp'
    });
};

/**
 * Appeal a rejected campaign
 * Appeals a rejected 10DLC campaign with the carrier registry. Only a
 * registration that reached campaign creation can be appealed; a
 * brand-level rejection should be fixed and re-verified instead. On
 * success the registration returns to `pending`.
 *
 * Content rejections (e.g. an opt-in flow without a verifiable form link,
 * or unrealistic samples) should be FIXED in the same call: pass the
 * corrected `messageFlow` / `sample1` / `sample2` and the campaign is
 * updated before the appeal is filed, so the reviewer sees the new
 * content. The current content is on `GET /v1/sms/registrations/{id}`
 * (`campaignContent`).
 *
 */
export const appealSmsRegistration = <ThrowOnError extends boolean = false>(options: OptionsLegacyParser<AppealSmsRegistrationData, ThrowOnError>) => {
    return (options?.client ?? client).post<AppealSmsRegistrationResponse, AppealSmsRegistrationError, ThrowOnError>({
        ...options,
        url: '/v1/sms/registrations/{id}/appeal'
    });
};

/**
 * Reply to a change request
 * Replies to a reviewer change request on a registration in
 * `changes_requested` state: a note, hosted document URLs (from
 * `POST /v1/sms/opt-in-proof`), or both, sent together. The registration
 * returns to `requested` (back in review) — no need to resubmit the
 * whole registration. To change the submitted brand/campaign fields
 * themselves, resubmit via `POST /v1/sms/registrations` with
 * `resubmitRequestId` instead.
 *
 */
export const respondToSmsRegistrationReview = <ThrowOnError extends boolean = false>(options: OptionsLegacyParser<RespondToSmsRegistrationReviewData, ThrowOnError>) => {
    return (options?.client ?? client).post<RespondToSmsRegistrationReviewResponse, RespondToSmsRegistrationReviewError, ThrowOnError>({
        ...options,
        url: '/v1/sms/registrations/{id}/respond'
    });
};

/**
 * Upload opt-in form proof
 * Hosts a screenshot (or PDF) of your SMS opt-in form and returns its
 * public URL. Include that URL in the campaign's `messageFlow` (the
 * opt-in workflow text) — the carrier registry has no attachment field,
 * so reviewers verify consent by opening links in that answer. Works
 * before a registration exists (use it when registering) and for
 * appeals. `/v1/sms/registrations/{id}/opt-in-proof` is an alias.
 *
 */
export const uploadSmsOptInProofFile = <ThrowOnError extends boolean = false>(options: OptionsLegacyParser<UploadSmsOptInProofFileData, ThrowOnError>) => {
    return (options?.client ?? client).post<UploadSmsOptInProofFileResponse, UploadSmsOptInProofFileError, ThrowOnError>({
        ...options,
        ...formDataBodySerializer,
        headers: {
            'Content-Type': null,
            ...options?.headers
        },
        url: '/v1/sms/opt-in-proof'
    });
};

/**
 * Upload opt-in form proof for an appeal
 * Hosts a screenshot (or PDF) of your SMS opt-in form and returns its
 * public URL. Carrier reviewers reject campaigns whose consent can't be
 * verified and ask for a "link/screenshot of the opt-in form" — the
 * registry has no attachment field, so include the returned URL inside
 * the `messageFlow` you submit with the appeal
 * (`POST /v1/sms/registrations/{id}/appeal`).
 *
 */
export const uploadSmsOptInProof = <ThrowOnError extends boolean = false>(options: OptionsLegacyParser<UploadSmsOptInProofData, ThrowOnError>) => {
    return (options?.client ?? client).post<UploadSmsOptInProofResponse, UploadSmsOptInProofError, ThrowOnError>({
        ...options,
        ...formDataBodySerializer,
        headers: {
            'Content-Type': null,
            ...options?.headers
        },
        url: '/v1/sms/registrations/{id}/opt-in-proof'
    });
};

/**
 * Create a registration share link
 * Creates a single-use, expiring link (valid 7 days) that lets someone
 * else (whoever has the legal business details) fill in the carrier
 * registration form for one of your numbers, without a Zernio login. The
 * registration is created under your account once the form is submitted.
 *
 */
export const shareSmsRegistration = <ThrowOnError extends boolean = false>(options: OptionsLegacyParser<ShareSmsRegistrationData, ThrowOnError>) => {
    return (options?.client ?? client).post<ShareSmsRegistrationResponse, ShareSmsRegistrationError, ThrowOnError>({
        ...options,
        url: '/v1/sms/registrations/share'
    });
};

/**
 * Look up a library template
 * Look up a single pre-approved Template Library template by its exact name, to
 * introspect its structure before importing it. Most importantly it returns the
 * template's `buttons`: a library template with `URL` / `PHONE_NUMBER` buttons
 * must be created with a matching `library_template_button_inputs` array (see
 * Create Template), or Meta rejects it. Use this to discover which inputs to collect.
 *
 */
export const getWhatsAppLibraryTemplate = <ThrowOnError extends boolean = false>(options: OptionsLegacyParser<GetWhatsAppLibraryTemplateData, ThrowOnError>) => {
    return (options?.client ?? client).get<GetWhatsAppLibraryTemplateResponse, GetWhatsAppLibraryTemplateError, ThrowOnError>({
        ...options,
        url: '/v1/whatsapp/template-library'
    });
};

/**
 * Get business profile
 * Retrieve the WhatsApp Business profile for the account (about, address, description, email, websites, etc.).
 *
 */
export const getWhatsAppBusinessProfile = <ThrowOnError extends boolean = false>(options: OptionsLegacyParser<GetWhatsAppBusinessProfileData, ThrowOnError>) => {
    return (options?.client ?? client).get<GetWhatsAppBusinessProfileResponse, GetWhatsAppBusinessProfileError, ThrowOnError>({
        ...options,
        url: '/v1/whatsapp/business-profile'
    });
};

/**
 * Update business profile
 * Update the WhatsApp Business profile. All fields are optional; only provided fields will be updated.
 * Constraints: about max 139 chars, description max 512 chars, max 2 websites.
 *
 */
export const updateWhatsAppBusinessProfile = <ThrowOnError extends boolean = false>(options: OptionsLegacyParser<UpdateWhatsAppBusinessProfileData, ThrowOnError>) => {
    return (options?.client ?? client).post<UpdateWhatsAppBusinessProfileResponse, UpdateWhatsAppBusinessProfileError, ThrowOnError>({
        ...options,
        url: '/v1/whatsapp/business-profile'
    });
};

/**
 * Upload profile picture
 * Upload a new profile picture for the WhatsApp Business Profile.
 * Uses Meta's resumable upload API under the hood: creates an upload session,
 * uploads the image bytes, then updates the business profile with the resulting handle.
 *
 * Provide the image either as a binary upload (`multipart/form-data` with `file`)
 * or as a download URL (`application/json` with `url`) — with a URL we fetch the
 * image server-side and upload the bytes for you. Meta's profile-photo API is
 * bytes-only, so there is no direct URL passthrough. JPEG/PNG, max 5MB either way.
 *
 */
export const uploadWhatsAppProfilePhoto = <ThrowOnError extends boolean = false>(options: OptionsLegacyParser<UploadWhatsAppProfilePhotoData, ThrowOnError>) => {
    return (options?.client ?? client).post<UploadWhatsAppProfilePhotoResponse, UploadWhatsAppProfilePhotoError, ThrowOnError>({
        ...options,
        ...formDataBodySerializer,
        headers: {
            'Content-Type': null,
            ...options?.headers
        },
        url: '/v1/whatsapp/business-profile/photo'
    });
};

/**
 * Get display name status
 * Fetch the current display name and its Meta review status for a WhatsApp Business account.
 * Display name changes require Meta approval and can take 1-3 business days.
 *
 */
export const getWhatsAppDisplayName = <ThrowOnError extends boolean = false>(options: OptionsLegacyParser<GetWhatsAppDisplayNameData, ThrowOnError>) => {
    return (options?.client ?? client).get<GetWhatsAppDisplayNameResponse, GetWhatsAppDisplayNameError, ThrowOnError>({
        ...options,
        url: '/v1/whatsapp/business-profile/display-name'
    });
};

/**
 * Request display name change
 * Submit a display name change request for the WhatsApp Business account.
 * The new name must follow WhatsApp naming guidelines (3-512 characters, must represent your business).
 * Changes require Meta review and approval, which typically takes 1-3 business days.
 *
 */
export const updateWhatsAppDisplayName = <ThrowOnError extends boolean = false>(options: OptionsLegacyParser<UpdateWhatsAppDisplayNameData, ThrowOnError>) => {
    return (options?.client ?? client).post<UpdateWhatsAppDisplayNameResponse, UpdateWhatsAppDisplayNameError, ThrowOnError>({
        ...options,
        url: '/v1/whatsapp/business-profile/display-name'
    });
};

/**
 * Get business username
 * Fetch the current WhatsApp Business username and its approval status.
 * Username status can be `approved` (active), `reserved` (pending activation), or `none` (no username set).
 *
 */
export const getWhatsappBusinessUsername = <ThrowOnError extends boolean = false>(options: OptionsLegacyParser<GetWhatsappBusinessUsernameData, ThrowOnError>) => {
    return (options?.client ?? client).get<GetWhatsappBusinessUsernameResponse, GetWhatsappBusinessUsernameError, ThrowOnError>({
        ...options,
        url: '/v1/whatsapp/business-profile/username'
    });
};

/**
 * Set business username
 * Claim or transfer a WhatsApp Business username for the account.
 *
 * Username rules: 3-35 characters, letters/digits/period/underscore only, must contain at least one letter,
 * no leading or trailing periods, no consecutive periods, no `www` prefix, no domain TLD suffix (e.g. `.com`).
 *
 * If the desired username is currently held by another account, pass `transferAction: "force_transfer"` to
 * request a transfer. On failure the API returns a standard error envelope with one of these codes:
 * `whatsapp_username_unavailable` (already taken and transfer not requested),
 * `whatsapp_username_ineligible` (account not eligible to claim a username), or
 * `whatsapp_username_transfer_required` (username is held elsewhere; retry with `force_transfer`).
 *
 */
export const setWhatsappBusinessUsername = <ThrowOnError extends boolean = false>(options: OptionsLegacyParser<SetWhatsappBusinessUsernameData, ThrowOnError>) => {
    return (options?.client ?? client).post<SetWhatsappBusinessUsernameResponse, SetWhatsappBusinessUsernameError, ThrowOnError>({
        ...options,
        url: '/v1/whatsapp/business-profile/username'
    });
};

/**
 * Delete business username
 * Release the currently claimed WhatsApp Business username from the account.
 * After deletion the username becomes available for other accounts to claim.
 *
 */
export const deleteWhatsappBusinessUsername = <ThrowOnError extends boolean = false>(options: OptionsLegacyParser<DeleteWhatsappBusinessUsernameData, ThrowOnError>) => {
    return (options?.client ?? client).delete<DeleteWhatsappBusinessUsernameResponse, DeleteWhatsappBusinessUsernameError, ThrowOnError>({
        ...options,
        url: '/v1/whatsapp/business-profile/username'
    });
};

/**
 * Get username suggestions
 * Retrieve a list of available WhatsApp Business username suggestions based on the account's
 * business profile name. Use these to help users discover valid, unclaimed usernames.
 *
 */
export const getWhatsappBusinessUsernameSuggestions = <ThrowOnError extends boolean = false>(options: OptionsLegacyParser<GetWhatsappBusinessUsernameSuggestionsData, ThrowOnError>) => {
    return (options?.client ?? client).get<GetWhatsappBusinessUsernameSuggestionsResponse, GetWhatsappBusinessUsernameSuggestionsError, ThrowOnError>({
        ...options,
        url: '/v1/whatsapp/business-profile/username/suggestions'
    });
};

/**
 * Get number status
 * Live snapshot of a connected number straight from Meta: the phone-number node
 * (display number, display name + approval, quality rating, messaging-limit tier,
 * throughput, official-business badge, connection status, health_status) and its
 * owning WhatsApp Business Account (name, business verification, timezone,
 * health_status). Fetched live because Meta updates quality/tier/name/health over
 * time; the call also refreshes the cached values shown on the connection card.
 *
 */
export const getWhatsAppNumberInfo = <ThrowOnError extends boolean = false>(options: OptionsLegacyParser<GetWhatsAppNumberInfoData, ThrowOnError>) => {
    return (options?.client ?? client).get<GetWhatsAppNumberInfoResponse, GetWhatsAppNumberInfoError, ThrowOnError>({
        ...options,
        url: '/v1/whatsapp/number-info'
    });
};

/**
 * Check if a user is blocked
 * Definitive blocked-state lookup for a single contact. Meta exposes no
 * membership endpoint, so this reads Zernio's blocklist mirror (kept in
 * sync by the block/unblock endpoints; the first call per account
 * backfills the mirror from Meta's full list). Constant-time regardless
 * of blocklist size.
 *
 */
export const getWhatsAppBlockStatus = <ThrowOnError extends boolean = false>(options: OptionsLegacyParser<GetWhatsAppBlockStatusData, ThrowOnError>) => {
    return (options?.client ?? client).get<GetWhatsAppBlockStatusResponse, GetWhatsAppBlockStatusError, ThrowOnError>({
        ...options,
        url: '/v1/whatsapp/block-users/status'
    });
};

/**
 * List blocked users
 * List the WhatsApp users blocked on this number. Cursor-paginated; pass
 * `nextCursor` back as `after` to fetch the next page. The blocklist holds
 * up to 64,000 users.
 *
 */
export const getWhatsAppBlockedUsers = <ThrowOnError extends boolean = false>(options: OptionsLegacyParser<GetWhatsAppBlockedUsersData, ThrowOnError>) => {
    return (options?.client ?? client).get<GetWhatsAppBlockedUsersResponse, GetWhatsAppBlockedUsersError, ThrowOnError>({
        ...options,
        url: '/v1/whatsapp/block-users'
    });
};

/**
 * Block users
 * Block one or more WhatsApp users on this number. Blocked users cannot
 * message your number or see that you are online, and your sends to them
 * return an error.
 *
 * Meta constraints, surfaced per-user in `failed` (the request itself still
 * succeeds for the rest of the batch):
 * - Only users who messaged your business within the last 24 hours can be
 * blocked (failures outside the window report "Re-engagement required").
 * - Up to 1,000 users per request; the blocklist caps at 64,000.
 * - Other WhatsApp Business accounts cannot be blocked.
 *
 */
export const blockWhatsAppUsers = <ThrowOnError extends boolean = false>(options: OptionsLegacyParser<BlockWhatsAppUsersData, ThrowOnError>) => {
    return (options?.client ?? client).post<BlockWhatsAppUsersResponse, BlockWhatsAppUsersError, ThrowOnError>({
        ...options,
        url: '/v1/whatsapp/block-users'
    });
};

/**
 * Unblock users
 * Unblock one or more previously blocked WhatsApp users on this number.
 * Up to 1,000 users per request; per-user failures are reported in
 * `failed` without failing the rest of the batch.
 *
 */
export const unblockWhatsAppUsers = <ThrowOnError extends boolean = false>(options: OptionsLegacyParser<UnblockWhatsAppUsersData, ThrowOnError>) => {
    return (options?.client ?? client).delete<UnblockWhatsAppUsersResponse, UnblockWhatsAppUsersError, ThrowOnError>({
        ...options,
        url: '/v1/whatsapp/block-users'
    });
};

/**
 * Get CTWA conversions dataset
 * Returns the Meta Click-to-WhatsApp conversions dataset currently linked
 * to the WhatsApp account, if one has been provisioned. Reads only from
 * the stored `metadata.metaCapiDatasetId` — never hits Meta, never
 * creates a dataset. Use this to detect whether `POST /v1/whatsapp/conversions`
 * is configured for an account.
 *
 */
export const getWhatsAppDataset = <ThrowOnError extends boolean = false>(options: OptionsLegacyParser<GetWhatsAppDatasetData, ThrowOnError>) => {
    return (options?.client ?? client).get<GetWhatsAppDatasetResponse, GetWhatsAppDatasetError, ThrowOnError>({
        ...options,
        url: '/v1/whatsapp/dataset'
    });
};

/**
 * Provision CTWA dataset
 * Creates (or fetches, if one already exists) the Meta dataset that
 * Click-to-WhatsApp ad events are reported against via the Conversions
 * API, and persists its ID on the account as `metadata.metaCapiDatasetId`.
 *
 * The call is GET-first idempotent — a WABA can only own one CTWA
 * dataset, so a second call after a successful provision is a safe no-op
 * that returns the same ID with `created: false`.
 *
 * Requires the connected WhatsApp account's token to carry the
 * `whatsapp_business_manage_events` permission. If the permission is
 * missing the endpoint returns 422 with a message asking the user to
 * reconnect the account.
 *
 */
export const createWhatsAppDataset = <ThrowOnError extends boolean = false>(options: OptionsLegacyParser<CreateWhatsAppDatasetData, ThrowOnError>) => {
    return (options?.client ?? client).post<CreateWhatsAppDatasetResponse, CreateWhatsAppDatasetError, ThrowOnError>({
        ...options,
        url: '/v1/whatsapp/dataset'
    });
};

/**
 * List phone numbers
 * List all phone numbers purchased by the authenticated user.
 * By default, released numbers are excluded. Connected (bring-your-own)
 * WhatsApp numbers are returned in the separate `connected` array; they
 * are not billed and have no provisioning lifecycle.
 *
 */
export const listPhoneNumbers = <ThrowOnError extends boolean = false>(options?: OptionsLegacyParser<ListPhoneNumbersData, ThrowOnError>) => {
    return (options?.client ?? client).get<ListPhoneNumbersResponse, ListPhoneNumbersError, ThrowOnError>({
        ...options,
        url: '/v1/phone-numbers'
    });
};

/**
 * Get phone number
 * Retrieve the current status of a purchased phone number. Poll this to
 * track Meta pre-verification (US sync path) and, for regulated (Tier 3/4)
 * numbers, the async lifecycle: pending_regulatory → active (or
 * regulatory_declined). When a regulated number has an Onfido ID step,
 * `onfidoVerificationUrl` appears here once the order is placed — forward
 * it to the end user. (Or subscribe to the whatsapp.number.* webhooks
 * instead of polling.)
 *
 */
export const getPhoneNumber = <ThrowOnError extends boolean = false>(options: OptionsLegacyParser<GetPhoneNumberData, ThrowOnError>) => {
    return (options?.client ?? client).get<GetPhoneNumberResponse, GetPhoneNumberError, ThrowOnError>({
        ...options,
        url: '/v1/phone-numbers/{id}'
    });
};

/**
 * Release phone number
 * Release a purchased phone number. This will:
 * 1. Disconnect any linked WhatsApp social account
 * 2. Decrement the Stripe subscription quantity (or cancel if last number)
 * 3. Release the number from Telnyx
 * 4. Mark the number as released
 *
 */
export const releasePhoneNumber = <ThrowOnError extends boolean = false>(options: OptionsLegacyParser<ReleasePhoneNumberData, ThrowOnError>) => {
    return (options?.client ?? client).delete<ReleasePhoneNumberResponse, ReleasePhoneNumberError, ThrowOnError>({
        ...options,
        url: '/v1/phone-numbers/{id}'
    });
};

/**
 * Purchase phone number
 * Payment-first: you do not pick a specific number, the system provisions one and
 * auto-assigns it. With usage-based billing active and a payment method on file, the
 * number provisions inline and bills per month on your usage-based invoice (there is
 * no checkout redirect). No payment method on file returns `402 PAYMENT_REQUIRED`;
 * a regulated country returns `202` with `status: "kyc_required"` and a `kycUrl`.
 *
 * Requires usage-based billing (the Usage plan). The maximum number of phone numbers
 * is determined by the user's plan.
 *
 */
export const purchasePhoneNumber = <ThrowOnError extends boolean = false>(options: OptionsLegacyParser<PurchasePhoneNumberData, ThrowOnError>) => {
    return (options?.client ?? client).post<PurchasePhoneNumberResponse, PurchasePhoneNumberError, ThrowOnError>({
        ...options,
        url: '/v1/phone-numbers/purchase'
    });
};

/**
 * List offerable number countries
 * The phone number countries available to purchase, each with its flat
 * monthly price (cents), regulatory tier, whether it needs end-user KYC
 * (Tier 3/4), and per-feature availability (PSTN calls, WhatsApp, SMS,
 * and WhatsApp Business Calling outbound). Drives the country picker.
 * Tier-4 countries appear only when enabled.
 *
 */
export const listPhoneNumberCountries = <ThrowOnError extends boolean = false>(options?: OptionsLegacyParser<unknown, ThrowOnError>) => {
    return (options?.client ?? client).get<ListPhoneNumberCountriesResponse, ListPhoneNumberCountriesError, ThrowOnError>({
        ...options,
        url: '/v1/phone-numbers/countries'
    });
};

/**
 * Search available numbers
 * Search the provider's inventory for numbers available to purchase in a
 * country (default US). Optional filters narrow the results. The country
 * must be offerable (see GET /v1/phone-numbers/countries). Voice
 * capability is always required; pass `sms=true` to only see numbers that
 * can also text (SMS support is per-number, not per-country).
 *
 */
export const searchAvailablePhoneNumbers = <ThrowOnError extends boolean = false>(options?: OptionsLegacyParser<SearchAvailablePhoneNumbersData, ThrowOnError>) => {
    return (options?.client ?? client).get<SearchAvailablePhoneNumbersResponse, SearchAvailablePhoneNumbersError, ThrowOnError>({
        ...options,
        url: '/v1/phone-numbers/available'
    });
};

/**
 * Check country availability
 * Pre-purchase check, so you can warn BEFORE a customer invests in KYC
 * (regulated review is async, 1-3 days). Tells you whether we have
 * deliverable inventory, and what address the customer needs:
 * - `addressConstraint: geo`  → the registered address MUST be in one of
 * the returned `areas` (the only place we have stock). A different-area
 * address passes pre-approval but the number can never be assigned.
 * - `addressConstraint: country` → any in-country address works.
 * - `addressConstraint: none` → field-only / instant country, no address.
 * Call this before starting the KYC form for regulated countries.
 *
 */
export const checkPhoneNumberAvailability = <ThrowOnError extends boolean = false>(options: OptionsLegacyParser<CheckPhoneNumberAvailabilityData, ThrowOnError>) => {
    return (options?.client ?? client).get<CheckPhoneNumberAvailabilityResponse, CheckPhoneNumberAvailabilityError, ThrowOnError>({
        ...options,
        url: '/v1/phone-numbers/availability'
    });
};

/**
 * @deprecated
 * List phone numbers
 * Deprecated alias of `/v1/phone-numbers`; same contract. New
 * integrations should use that path.
 *
 * List all WhatsApp phone numbers purchased by the authenticated user.
 * By default, released numbers are excluded. Connected (bring-your-own)
 * numbers are returned in the separate `connected` array — they are not
 * billed and have no provisioning lifecycle.
 *
 */
export const getWhatsAppPhoneNumbers = <ThrowOnError extends boolean = false>(options?: OptionsLegacyParser<GetWhatsAppPhoneNumbersData, ThrowOnError>) => {
    return (options?.client ?? client).get<GetWhatsAppPhoneNumbersResponse, GetWhatsAppPhoneNumbersError, ThrowOnError>({
        ...options,
        url: '/v1/whatsapp/phone-numbers'
    });
};

/**
 * @deprecated
 * Purchase phone number
 * Deprecated alias of `/v1/phone-numbers/purchase`; same contract. New
 * integrations should use that path.
 *
 * Payment-first: you do not pick a specific number, the system provisions one and
 * auto-assigns it. With usage-based billing active and a payment method on file, the
 * number provisions inline and bills per month on your usage-based invoice (there is
 * no checkout redirect). No payment method on file returns `402 PAYMENT_REQUIRED`;
 * a regulated country returns `202` with `status: "kyc_required"` and a `kycUrl`.
 *
 * Requires usage-based billing (the Usage plan). The maximum number of phone numbers
 * is determined by the user's plan.
 *
 */
export const purchaseWhatsAppPhoneNumber = <ThrowOnError extends boolean = false>(options: OptionsLegacyParser<PurchaseWhatsAppPhoneNumberData, ThrowOnError>) => {
    return (options?.client ?? client).post<PurchaseWhatsAppPhoneNumberResponse, PurchaseWhatsAppPhoneNumberError, ThrowOnError>({
        ...options,
        url: '/v1/whatsapp/phone-numbers/purchase'
    });
};

/**
 * @deprecated
 * List offerable number countries
 * Deprecated alias of `/v1/phone-numbers/countries`; same contract. New
 * integrations should use that path.
 *
 * The WhatsApp number countries available to purchase, each with its flat
 * monthly price (cents), regulatory tier, whether it needs end-user KYC
 * (Tier 3/4), and whether outbound calling is available (not BIC-blocked).
 * Drives the country picker. Tier-4 countries appear only when enabled.
 *
 */
export const listWhatsAppNumberCountries = <ThrowOnError extends boolean = false>(options?: OptionsLegacyParser<unknown, ThrowOnError>) => {
    return (options?.client ?? client).get<ListWhatsAppNumberCountriesResponse, ListWhatsAppNumberCountriesError, ThrowOnError>({
        ...options,
        url: '/v1/whatsapp/phone-numbers/countries'
    });
};

/**
 * @deprecated
 * Search available numbers
 * Deprecated alias of `/v1/phone-numbers/available`; same contract. New
 * integrations should use that path.
 *
 * Search the provider's inventory for numbers available to purchase in a
 * country (default US). Optional filters narrow the results. The country
 * must be offerable (see GET /v1/whatsapp/phone-numbers/countries).
 *
 */
export const searchAvailableWhatsAppNumbers = <ThrowOnError extends boolean = false>(options?: OptionsLegacyParser<SearchAvailableWhatsAppNumbersData, ThrowOnError>) => {
    return (options?.client ?? client).get<SearchAvailableWhatsAppNumbersResponse, SearchAvailableWhatsAppNumbersError, ThrowOnError>({
        ...options,
        url: '/v1/whatsapp/phone-numbers/available'
    });
};

/**
 * @deprecated
 * Check country availability
 * Deprecated alias of `/v1/phone-numbers/availability`; same contract. New
 * integrations should use that path.
 *
 * Pre-purchase check, so you can warn BEFORE a customer invests in KYC
 * (regulated review is async, 1-3 days). Tells you whether we have
 * deliverable inventory, and what address the customer needs:
 * - `addressConstraint: geo`  → the registered address MUST be in one of
 * the returned `areas` (the only place we have stock). A different-area
 * address passes pre-approval but the number can never be assigned.
 * - `addressConstraint: country` → any in-country address works.
 * - `addressConstraint: none` → field-only / instant country, no address.
 * Call this before starting the KYC form for regulated countries.
 *
 */
export const checkWhatsAppNumberAvailability = <ThrowOnError extends boolean = false>(options: OptionsLegacyParser<CheckWhatsAppNumberAvailabilityData, ThrowOnError>) => {
    return (options?.client ?? client).get<CheckWhatsAppNumberAvailabilityResponse, CheckWhatsAppNumberAvailabilityError, ThrowOnError>({
        ...options,
        url: '/v1/whatsapp/phone-numbers/availability'
    });
};

/**
 * Get KYC form spec
 * For a Tier 3/4 country, the fields the end customer must provide (Telnyx
 * regulatory requirements) before a number can be ordered: text, date,
 * address, or file (document) per requirement.
 *
 */
export const getPhoneNumberKycForm = <ThrowOnError extends boolean = false>(options: OptionsLegacyParser<GetPhoneNumberKycFormData, ThrowOnError>) => {
    return (options?.client ?? client).get<GetPhoneNumberKycFormResponse, GetPhoneNumberKycFormError, ThrowOnError>({
        ...options,
        url: '/v1/phone-numbers/kyc'
    });
};

/**
 * Submit KYC
 * Submit the end customer's KYC (textual values, uploaded documents,
 * address) for a Tier 3/4 country. Documents are streamed straight to the
 * number provider and are not stored by Zernio. Builds + submits a
 * regulatory requirement group and claims a pending_regulatory slot; the
 * number is ordered + activated once the provider approves (asynchronous).
 * A customer may hold several same-country numbers in review at once; a
 * double-submit of the SAME attempt is deduped via `submissionId`.
 *
 * For an ID-card document requirement, carriers commonly require BOTH sides:
 * combine the front and back into a single file before uploading (the
 * dashboard does this automatically). A one-sided ID is a common decline
 * reason; fix it via POST /v1/phone-numbers/{id}/remediate.
 *
 * Before submitting, call GET /v1/phone-numbers/availability to
 * check the country has deliverable inventory and, for geographic-match
 * countries, which area the address must be in — otherwise the submission
 * can pass review yet never be assignable a number.
 *
 */
export const submitPhoneNumberKyc = <ThrowOnError extends boolean = false>(options: OptionsLegacyParser<SubmitPhoneNumberKycData, ThrowOnError>) => {
    return (options?.client ?? client).post<SubmitPhoneNumberKycResponse, SubmitPhoneNumberKycError, ThrowOnError>({
        ...options,
        url: '/v1/phone-numbers/kyc'
    });
};

/**
 * View a KYC document on file
 * Stream a document backing a reusable verification (the `documentId`
 * values from GET /v1/phone-numbers/kyc `reusable.options[].details[]`), so
 * the account holder can see what's on file before reusing it. Returned
 * inline as `application/pdf` (uploads are normalized to PDF). Auth-scoped:
 * a document is viewable only when its id is referenced by one of the
 * caller's own numbers — otherwise `404`.
 *
 */
export const viewPhoneNumberKycDocument = <ThrowOnError extends boolean = false>(options: OptionsLegacyParser<ViewPhoneNumberKycDocumentData, ThrowOnError>) => {
    return (options?.client ?? client).get<ViewPhoneNumberKycDocumentResponse, ViewPhoneNumberKycDocumentError, ThrowOnError>({
        ...options,
        url: '/v1/phone-numbers/kyc/document/{documentId}'
    });
};

/**
 * Upload a KYC document
 * Upload ONE document and get back its provider document id, to reference
 * from POST /v1/phone-numbers/kyc via `documents[].documentId`.
 * Send the RAW file bytes as the request body (not base64); put the filename
 * in the `X-Filename` header. Uploading documents one-per-request keeps each
 * request under the ~4.5MB body limit. The document streams straight to the
 * number provider and is not stored by Zernio.
 *
 */
export const uploadPhoneNumberKycDocument = <ThrowOnError extends boolean = false>(options: OptionsLegacyParser<UploadPhoneNumberKycDocumentData, ThrowOnError>) => {
    return (options?.client ?? client).post<UploadPhoneNumberKycDocumentResponse, UploadPhoneNumberKycDocumentError, ThrowOnError>({
        ...options,
        url: '/v1/phone-numbers/kyc/upload-document'
    });
};

/**
 * Pre-validate KYC address
 * Optional early check for the address step of a Tier 4 (end-user identity)
 * registration: validates a postal address for deliverability BEFORE the full
 * KYC submit, so it can be corrected before any documents are uploaded. The
 * full submit (POST /v1/phone-numbers/kyc) re-validates the address,
 * so this call is purely a fast feedback path and skipping it is safe. Only
 * the postal address is sent (no documents, no gov-ID fields). A region
 * (`administrative_area`) is required by the validator; when it is omitted the
 * pre-check is skipped and `{ ok: true, skipped: true }` is returned (the
 * final submit still validates).
 *
 */
export const validatePhoneNumberKycAddress = <ThrowOnError extends boolean = false>(options: OptionsLegacyParser<ValidatePhoneNumberKycAddressData, ThrowOnError>) => {
    return (options?.client ?? client).post<ValidatePhoneNumberKycAddressResponse, ValidatePhoneNumberKycAddressError, ThrowOnError>({
        ...options,
        url: '/v1/phone-numbers/kyc/validate-address'
    });
};

/**
 * Create a hosted KYC link
 * Create a single-use, 7-day hosted KYC link that your end customer
 * completes WITHOUT a Zernio login — useful when the person who holds the
 * ID and address is not your team. They fill the regulated verification on
 * a Zernio-hosted page; the number provisions under YOUR account once they
 * submit. Only regulated (KYC) countries are valid: a country that does not
 * require KYC returns 400.
 *
 * White-label the page with `branding` (your company name, logo, brand
 * color). Supply `redirect_url` to send the end customer back to your own
 * site after a successful submit (completion params are appended — see
 * below). Listen for the `whatsapp.number.kyc_submitted` webhook to react
 * when the form is completed.
 *
 */
export const createPhoneNumberKycLink = <ThrowOnError extends boolean = false>(options: OptionsLegacyParser<CreatePhoneNumberKycLinkData, ThrowOnError>) => {
    return (options?.client ?? client).post<CreatePhoneNumberKycLinkResponse, CreatePhoneNumberKycLinkError, ThrowOnError>({
        ...options,
        url: '/v1/phone-numbers/kyc/share'
    });
};

/**
 * Port numbers in
 * Submit a port-in for one or more existing numbers from another carrier.
 * Creates the carrier order(s), attaches the end-user (current account)
 * info plus the LOA and invoice documents, and submits to the losing
 * carrier. The transfer PIN is forwarded to the carrier and never stored.
 * Ported numbers arrive voice-ready (and SMS-ready where the order
 * supports messaging).
 *
 * Run the portability check (POST /v1/phone-numbers/port-in/check) and
 * upload the two documents (POST /v1/phone-numbers/port-in/documents)
 * first — uploaded documents must be attached to an order within 30
 * minutes or the carrier deletes them, so upload right before this call.
 * The carrier may split the numbers into several orders (by country,
 * number type, losing carrier); `orders` carries per-order results, and a
 * partial failure still returns 201 with the failed orders' `error` set
 * (they stay as cancellable drafts).
 *
 * Non-US/CA numbers additionally need the country-specific values from
 * GET /v1/phone-numbers/port-in/requirements, passed via `requirements`,
 * and must be submitted one country per request. When required
 * information is still missing after submission, the order is kept as a
 * resumable draft whose `error` / `declineReason` names the gaps.
 *
 */
export const createPhoneNumberPortIn = <ThrowOnError extends boolean = false>(options: OptionsLegacyParser<CreatePhoneNumberPortInData, ThrowOnError>) => {
    return (options?.client ?? client).post<CreatePhoneNumberPortInResponse, CreatePhoneNumberPortInError, ThrowOnError>({
        ...options,
        url: '/v1/phone-numbers/port-in'
    });
};

/**
 * List port-in orders
 * Your porting orders, newest first (max 50). Poll this for port progress:
 * pending, confirmed FOC date, exception reason, or ported.
 *
 */
export const listPhoneNumberPortIns = <ThrowOnError extends boolean = false>(options?: OptionsLegacyParser<unknown, ThrowOnError>) => {
    return (options?.client ?? client).get<ListPhoneNumberPortInsResponse, ListPhoneNumberPortInsError, ThrowOnError>({
        ...options,
        url: '/v1/phone-numbers/port-in'
    });
};

/**
 * Check portability
 * Pre-flight portability check: whether each number can be ported in and
 * whether it qualifies for FastPort, BEFORE the user commits to a port
 * order (LOA, invoice, service address). Read-only; creates no order and
 * bills nothing.
 *
 */
export const checkPhoneNumberPortability = <ThrowOnError extends boolean = false>(options: OptionsLegacyParser<CheckPhoneNumberPortabilityData, ThrowOnError>) => {
    return (options?.client ?? client).post<CheckPhoneNumberPortabilityResponse, CheckPhoneNumberPortabilityError, ThrowOnError>({
        ...options,
        url: '/v1/phone-numbers/port-in/check'
    });
};

/**
 * Upload a porting document
 * Upload ONE porting document and get back its `documentId`. For the
 * signed LOA / carrier invoice the id goes to `loaDocumentId` /
 * `invoiceDocumentId`; for a country-specific document requirement
 * (international ports) it becomes that requirement's `fieldValue`.
 * Requirement documents are normalized to PDF automatically (regulators
 * reject raw images). PDF, JPEG, or PNG, 10MB max. Uploads must be
 * attached to an order within 30 minutes or the carrier deletes them.
 *
 */
export const uploadPhoneNumberPortInDocument = <ThrowOnError extends boolean = false>(options: OptionsLegacyParser<UploadPhoneNumberPortInDocumentData, ThrowOnError>) => {
    return (options?.client ?? client).post<UploadPhoneNumberPortInDocumentResponse, UploadPhoneNumberPortInDocumentError, ThrowOnError>({
        ...options,
        ...formDataBodySerializer,
        headers: {
            'Content-Type': null,
            ...options?.headers
        },
        url: '/v1/phone-numbers/port-in/documents'
    });
};

/**
 * Country porting requirements
 * The country-specific information a port-in needs BEYOND the LOA,
 * invoice, and account/address details — e.g. an ID copy, proof of
 * address, a tax id, or a porting code. Call it after the portability
 * check (which returns each number's `countryCode` and
 * `phoneNumberType`), render the fields, and pass the collected values as
 * the create request's `requirements`. US/CA return an empty list.
 *
 */
export const getPhoneNumberPortInRequirements = <ThrowOnError extends boolean = false>(options: OptionsLegacyParser<GetPhoneNumberPortInRequirementsData, ThrowOnError>) => {
    return (options?.client ?? client).get<GetPhoneNumberPortInRequirementsResponse, GetPhoneNumberPortInRequirementsError, ThrowOnError>({
        ...options,
        url: '/v1/phone-numbers/port-in/requirements'
    });
};

/**
 * A port-in order's pending requirements
 * The live requirements on an EXISTING porting order: which are filled,
 * which are still pending, and which bounced on review
 * (`requirement-info-exception`). Use it to fix and resubmit a rejected
 * international port. Same field shape as the country-level requirements
 * endpoint, plus per-requirement status.
 *
 */
export const getPhoneNumberPortInOrderRequirements = <ThrowOnError extends boolean = false>(options: OptionsLegacyParser<GetPhoneNumberPortInOrderRequirementsData, ThrowOnError>) => {
    return (options?.client ?? client).get<GetPhoneNumberPortInOrderRequirementsResponse, GetPhoneNumberPortInOrderRequirementsError, ThrowOnError>({
        ...options,
        url: '/v1/phone-numbers/port-in/{id}/requirements'
    });
};

/**
 * Cancel a port-in
 * Cancel an in-flight port (wrong number, staying with the old carrier).
 * Only orders that haven't ported can be cancelled; a completed port is a
 * normal number release instead. The carrier may report `cancel-pending`
 * briefly while the losing carrier acknowledges; it settles to
 * `cancelled`.
 *
 */
export const cancelPhoneNumberPortIn = <ThrowOnError extends boolean = false>(options: OptionsLegacyParser<CancelPhoneNumberPortInData, ThrowOnError>) => {
    return (options?.client ?? client).delete<CancelPhoneNumberPortInResponse, CancelPhoneNumberPortInError, ThrowOnError>({
        ...options,
        url: '/v1/phone-numbers/port-in/{id}'
    });
};

/**
 * Pre-review a KYC packet
 * Advisory dry-run of a regulated-KYC packet before submitting: reviews
 * the exact documents the regulator will see (referenced by the ids from
 * POST /v1/phone-numbers/kyc/upload-document) against the declared values
 * and address, and returns plain-language advisories for likely decline
 * reasons (wrong document type, mismatched address, one-sided ID scans).
 * Non-blocking: advisories are warnings, submitting anyway is always
 * allowed, and any review failure degrades to an empty list.
 *
 */
export const reviewPhoneNumberKycPacket = <ThrowOnError extends boolean = false>(options: OptionsLegacyParser<ReviewPhoneNumberKycPacketData, ThrowOnError>) => {
    return (options?.client ?? client).post<ReviewPhoneNumberKycPacketResponse, ReviewPhoneNumberKycPacketError, ThrowOnError>({
        ...options,
        url: '/v1/phone-numbers/kyc/review-packet'
    });
};

/**
 * Get declined requirements
 * For a number in `regulatory_declined`, returns ONLY the requirements the
 * reviewer flagged declined, as a form spec (same shape as the KYC form GET).
 * The customer fixes just those — Telnyx supports correcting a declined
 * requirement group and re-submitting it (no new number/group). Falls back
 * to the full spec if the provider exposes no per-requirement flags.
 *
 */
export const getPhoneNumberRemediation = <ThrowOnError extends boolean = false>(options: OptionsLegacyParser<GetPhoneNumberRemediationData, ThrowOnError>) => {
    return (options?.client ?? client).get<GetPhoneNumberRemediationResponse, GetPhoneNumberRemediationError, ThrowOnError>({
        ...options,
        url: '/v1/phone-numbers/{id}/remediate'
    });
};

/**
 * Resubmit a declined number
 * Submit corrected values/documents for the declined requirement(s). We
 * PATCH them onto the SAME requirement group and re-submit it for approval;
 * the number goes `regulatory_declined` → `pending_regulatory`. No new
 * number and no new billing. Body shape matches the KYC submit (values /
 * documents / address) — send only the corrected fields.
 *
 */
export const remediatePhoneNumber = <ThrowOnError extends boolean = false>(options: OptionsLegacyParser<RemediatePhoneNumberData, ThrowOnError>) => {
    return (options?.client ?? client).post<RemediatePhoneNumberResponse, RemediatePhoneNumberError, ThrowOnError>({
        ...options,
        url: '/v1/phone-numbers/{id}/remediate'
    });
};

/**
 * Reply to the regulatory reviewer
 * Post a free-text reply (with optional file attachments) to the reviewer
 * on a number awaiting remediation — for asks the structured form can't
 * express (e.g. "is this personal or business?"). Attachments are stored by
 * us and their links are added to the reviewer's comment thread (the
 * carrier's number order takes no loose files). A reply to a comment-style
 * ask moves the number back to "in review"; a reply on a formal decline is
 * supplementary and you must still resubmit the fix. Requires text or at
 * least one attachment.
 *
 */
export const replyToPhoneNumberReviewer = <ThrowOnError extends boolean = false>(options: OptionsLegacyParser<ReplyToPhoneNumberReviewerData, ThrowOnError>) => {
    return (options?.client ?? client).post<ReplyToPhoneNumberReviewerResponse, ReplyToPhoneNumberReviewerError, ThrowOnError>({
        ...options,
        url: '/v1/phone-numbers/{id}/remediate/reply'
    });
};

/**
 * Respond to the regulatory reviewer (message + corrections)
 * Send a single response to the reviewer on a number awaiting remediation:
 * a free-text message and/or corrected requirement documents, in one call.
 * If corrections are present they are PATCHed onto the requirement group and
 * re-submitted (the number goes back to "in review"); if a message or file
 * attachments are present they are posted to the reviewer's comment thread.
 * When both are present, your message is the thread comment and the resubmit
 * drives the state change. At least one of message, corrections, or
 * attachments is required. `documents` correct requirement slots; `attachments`
 * are loose files (their links are added to your message).
 *
 */
export const respondToPhoneNumberReviewer = <ThrowOnError extends boolean = false>(options: OptionsLegacyParser<RespondToPhoneNumberReviewerData, ThrowOnError>) => {
    return (options?.client ?? client).post<RespondToPhoneNumberReviewerResponse, RespondToPhoneNumberReviewerError, ThrowOnError>({
        ...options,
        url: '/v1/phone-numbers/{id}/remediate/respond'
    });
};

/**
 * @deprecated
 * Get KYC form spec
 * Deprecated alias of `/v1/phone-numbers/kyc`; same contract. New
 * integrations should use that path.
 *
 * For a Tier 3/4 country, the fields the end customer must provide (Telnyx
 * regulatory requirements) before a number can be ordered: text, date,
 * address, or file (document) per requirement.
 *
 */
export const getWhatsAppNumberKycForm = <ThrowOnError extends boolean = false>(options: OptionsLegacyParser<GetWhatsAppNumberKycFormData, ThrowOnError>) => {
    return (options?.client ?? client).get<GetWhatsAppNumberKycFormResponse, GetWhatsAppNumberKycFormError, ThrowOnError>({
        ...options,
        url: '/v1/whatsapp/phone-numbers/kyc'
    });
};

/**
 * @deprecated
 * Submit KYC
 * Deprecated alias of `/v1/phone-numbers/kyc`; same contract. New
 * integrations should use that path.
 *
 * Submit the end customer's KYC (textual values, uploaded documents,
 * address) for a Tier 3/4 country. Documents are streamed straight to the
 * number provider and are not stored by Zernio. Builds + submits a
 * regulatory requirement group and claims a pending_regulatory slot; the
 * number is ordered + activated once the provider approves (asynchronous).
 * A customer may hold several same-country numbers in review at once; a
 * double-submit of the SAME attempt is deduped via `submissionId`.
 *
 * For an ID-card document requirement, carriers commonly require BOTH sides:
 * combine the front and back into a single file before uploading (the
 * dashboard does this automatically). A one-sided ID is a common decline
 * reason; fix it via POST /v1/whatsapp/phone-numbers/{id}/remediate.
 *
 * Before submitting, call GET /v1/whatsapp/phone-numbers/availability to
 * check the country has deliverable inventory and, for geographic-match
 * countries, which area the address must be in — otherwise the submission
 * can pass review yet never be assignable a number.
 *
 */
export const submitWhatsAppNumberKyc = <ThrowOnError extends boolean = false>(options: OptionsLegacyParser<SubmitWhatsAppNumberKycData, ThrowOnError>) => {
    return (options?.client ?? client).post<SubmitWhatsAppNumberKycResponse, SubmitWhatsAppNumberKycError, ThrowOnError>({
        ...options,
        url: '/v1/whatsapp/phone-numbers/kyc'
    });
};

/**
 * @deprecated
 * Upload a KYC document
 * Deprecated alias of `/v1/phone-numbers/kyc/upload-document`; same contract. New
 * integrations should use that path.
 *
 * Upload ONE document and get back its provider document id, to reference
 * from POST /v1/whatsapp/phone-numbers/kyc via `documents[].documentId`.
 * Send the RAW file bytes as the request body (not base64); put the filename
 * in the `X-Filename` header. Uploading documents one-per-request keeps each
 * request under the ~4.5MB body limit. The document streams straight to the
 * number provider and is not stored by Zernio.
 *
 */
export const uploadWhatsAppNumberKycDocument = <ThrowOnError extends boolean = false>(options: OptionsLegacyParser<UploadWhatsAppNumberKycDocumentData, ThrowOnError>) => {
    return (options?.client ?? client).post<UploadWhatsAppNumberKycDocumentResponse, UploadWhatsAppNumberKycDocumentError, ThrowOnError>({
        ...options,
        url: '/v1/whatsapp/phone-numbers/kyc/upload-document'
    });
};

/**
 * @deprecated
 * Pre-validate KYC address
 * Deprecated alias of `/v1/phone-numbers/kyc/validate-address`; same contract. New
 * integrations should use that path.
 *
 * Optional early check for the address step of a Tier 4 (end-user identity)
 * registration: validates a postal address for deliverability BEFORE the full
 * KYC submit, so it can be corrected before any documents are uploaded. The
 * full submit (POST /v1/whatsapp/phone-numbers/kyc) re-validates the address,
 * so this call is purely a fast feedback path and skipping it is safe. Only
 * the postal address is sent (no documents, no gov-ID fields). A region
 * (`administrative_area`) is required by the validator; when it is omitted the
 * pre-check is skipped and `{ ok: true, skipped: true }` is returned (the
 * final submit still validates).
 *
 */
export const validateWhatsAppNumberKycAddress = <ThrowOnError extends boolean = false>(options: OptionsLegacyParser<ValidateWhatsAppNumberKycAddressData, ThrowOnError>) => {
    return (options?.client ?? client).post<ValidateWhatsAppNumberKycAddressResponse, ValidateWhatsAppNumberKycAddressError, ThrowOnError>({
        ...options,
        url: '/v1/whatsapp/phone-numbers/kyc/validate-address'
    });
};

/**
 * @deprecated
 * Create a hosted KYC link
 * Deprecated alias of `/v1/phone-numbers/kyc/share`; same contract. New
 * integrations should use that path.
 *
 * Create a single-use, 7-day hosted KYC link that your end customer
 * completes WITHOUT a Zernio login — useful when the person who holds the
 * ID and address is not your team. They fill the regulated verification on
 * a Zernio-hosted page; the number provisions under YOUR account once they
 * submit. Only regulated (KYC) countries are valid: a country that does not
 * require KYC returns 400.
 *
 * White-label the page with `branding` (your company name, logo, brand
 * color). Supply `redirect_url` to send the end customer back to your own
 * site after a successful submit (completion params are appended — see
 * below). Listen for the `whatsapp.number.kyc_submitted` webhook to react
 * when the form is completed.
 *
 */
export const createWhatsAppNumberKycLink = <ThrowOnError extends boolean = false>(options: OptionsLegacyParser<CreateWhatsAppNumberKycLinkData, ThrowOnError>) => {
    return (options?.client ?? client).post<CreateWhatsAppNumberKycLinkResponse, CreateWhatsAppNumberKycLinkError, ThrowOnError>({
        ...options,
        url: '/v1/whatsapp/phone-numbers/kyc/share'
    });
};

/**
 * Move a number to another profile
 * Move a provisioned number to a different profile.
 *
 * A number is not a single record. Alongside the number itself there are
 * hidden telephony owner accounts (platform `phone`, plus `sms` when SMS is
 * enabled) and, once WhatsApp is connected, the `whatsapp` account. They all
 * carry a profileId and this endpoint moves them together.
 *
 * Use this instead of `PATCH /v1/accounts/{accountId}`: that one moves the
 * social account only and leaves the number itself pinned to its original
 * profile, which splits the number across two profiles. Connecting a
 * Zernio-provisioned number from any profile but its own is rejected with a
 * `409` (`WHATSAPP_NUMBER_PINNED_TO_PROFILE`). This endpoint is how you
 * re-home the number first, so it can then be connected from the new profile.
 *
 * `id` is the number record id from `GET /v1/phone-numbers`, not an account id.
 *
 * A profile holds at most one account per platform, so the destination must be
 * free of every platform this number occupies.
 *
 */
export const moveWhatsAppNumberToProfile = <ThrowOnError extends boolean = false>(options: OptionsLegacyParser<MoveWhatsAppNumberToProfileData, ThrowOnError>) => {
    return (options?.client ?? client).patch<MoveWhatsAppNumberToProfileResponse, MoveWhatsAppNumberToProfileError, ThrowOnError>({
        ...options,
        url: '/v1/whatsapp/phone-numbers/{id}/profile'
    });
};

/**
 * @deprecated
 * Get declined requirements
 * Deprecated alias of `/v1/phone-numbers/{id}/remediate`; same contract. New
 * integrations should use that path.
 *
 * For a number in `regulatory_declined`, returns ONLY the requirements the
 * reviewer flagged declined, as a form spec (same shape as the KYC form GET).
 * The customer fixes just those — Telnyx supports correcting a declined
 * requirement group and re-submitting it (no new number/group). Falls back
 * to the full spec if the provider exposes no per-requirement flags.
 *
 */
export const getWhatsAppNumberRemediation = <ThrowOnError extends boolean = false>(options: OptionsLegacyParser<GetWhatsAppNumberRemediationData, ThrowOnError>) => {
    return (options?.client ?? client).get<GetWhatsAppNumberRemediationResponse, GetWhatsAppNumberRemediationError, ThrowOnError>({
        ...options,
        url: '/v1/whatsapp/phone-numbers/{id}/remediate'
    });
};

/**
 * @deprecated
 * Resubmit a declined number
 * Deprecated alias of `/v1/phone-numbers/{id}/remediate`; same contract. New
 * integrations should use that path.
 *
 * Submit corrected values/documents for the declined requirement(s). We
 * PATCH them onto the SAME requirement group and re-submit it for approval;
 * the number goes `regulatory_declined` → `pending_regulatory`. No new
 * number and no new billing. Body shape matches the KYC submit (values /
 * documents / address) — send only the corrected fields.
 *
 */
export const remediateWhatsAppNumber = <ThrowOnError extends boolean = false>(options: OptionsLegacyParser<RemediateWhatsAppNumberData, ThrowOnError>) => {
    return (options?.client ?? client).post<RemediateWhatsAppNumberResponse, RemediateWhatsAppNumberError, ThrowOnError>({
        ...options,
        url: '/v1/whatsapp/phone-numbers/{id}/remediate'
    });
};

/**
 * Enable phone calling on a number
 * Turns on regular phone (PSTN) calling for one of your numbers and
 * configures how inbound calls are handled. Inbound calls route to
 * `forwardTo`: your own AI voice agent (Vapi/Retell), a phone, or a SIP
 * endpoint. Optional extras: voicemail, business-hours windows, an IVR
 * menu, a caller blocklist, recording, and transcription. A number can
 * also be voice-enabled with no forward (outbound-only).
 *
 * Idempotent, and doubles as the settings update: only fields present in
 * the body are written. Omitting `forwardTo` preserves the current
 * destination; sending an empty string clears it.
 *
 */
export const enableVoiceOnNumber = <ThrowOnError extends boolean = false>(options: OptionsLegacyParser<EnableVoiceOnNumberData, ThrowOnError>) => {
    return (options?.client ?? client).post<EnableVoiceOnNumberResponse, EnableVoiceOnNumberError, ThrowOnError>({
        ...options,
        url: '/v1/phone-numbers/{id}/voice'
    });
};

/**
 * Disable phone calling on a number
 * Turns off PSTN calling for the number. The stored forward destination
 * and settings are preserved, so re-enabling restores the prior config.
 *
 */
export const disableVoiceOnNumber = <ThrowOnError extends boolean = false>(options: OptionsLegacyParser<DisableVoiceOnNumberData, ThrowOnError>) => {
    return (options?.client ?? client).delete<DisableVoiceOnNumberResponse, DisableVoiceOnNumberError, ThrowOnError>({
        ...options,
        url: '/v1/phone-numbers/{id}/voice'
    });
};

/**
 * Enable SMS on a number
 * Turns on SMS for one of your numbers. The number's real carrier
 * capability is checked first: some number types can't do SMS at all
 * (`smsCapable: false`), and a number still provisioning at the carrier
 * returns `notReady: true` (try again once provisioning finishes).
 *
 * US numbers additionally need a carrier registration before messages
 * deliver; the response tells you which path applies:
 * - `alreadyRegistered: true`: a prior registration still covers this
 * number; SMS was simply reactivated.
 * - `reusable` set: you have an approved registration this number can
 * join in one click via
 * `POST /v1/phone-numbers/{id}/sms/reuse-registration`
 * (no new brand/campaign, no extra carrier fee).
 * - `needsRegistration: true` and no `reusable`: start one via
 * `POST /v1/sms/registrations`.
 *
 * Idempotent: re-running re-attempts any carrier-side setup that failed.
 *
 */
export const enableSmsOnNumber = <ThrowOnError extends boolean = false>(options: OptionsLegacyParser<EnableSmsOnNumberData, ThrowOnError>) => {
    return (options?.client ?? client).post<EnableSmsOnNumberResponse, EnableSmsOnNumberError, ThrowOnError>({
        ...options,
        url: '/v1/phone-numbers/{id}/sms'
    });
};

/**
 * Disable SMS on a number
 * Turns off SMS for the number (deactivates its SMS account). The carrier
 * registration is untouched, so re-enabling later just reactivates it,
 * with no re-registration.
 *
 */
export const disableSmsOnNumber = <ThrowOnError extends boolean = false>(options: OptionsLegacyParser<DisableSmsOnNumberData, ThrowOnError>) => {
    return (options?.client ?? client).delete<DisableSmsOnNumberResponse, DisableSmsOnNumberError, ThrowOnError>({
        ...options,
        url: '/v1/phone-numbers/{id}/sms'
    });
};

/**
 * Add number to SMS registration
 * Attaches this number to your existing approved 10DLC campaign instead
 * of running a fresh registration: the number inherits the campaign's
 * approval (no new brand or campaign, no extra carrier fee). Enable SMS
 * on the number first (`POST /v1/phone-numbers/{id}/sms`; its response
 * tells you whether a reusable registration exists).
 *
 */
export const reuseSmsRegistrationForNumber = <ThrowOnError extends boolean = false>(options: OptionsLegacyParser<ReuseSmsRegistrationForNumberData, ThrowOnError>) => {
    return (options?.client ?? client).post<ReuseSmsRegistrationForNumberResponse, ReuseSmsRegistrationForNumberError, ThrowOnError>({
        ...options,
        url: '/v1/phone-numbers/{id}/sms/reuse-registration'
    });
};

/**
 * Get calling config for a number
 * The WhatsApp Business Calling configuration of this number, keyed the
 * same way as the POST/PATCH/DELETE below (full read-write on one
 * sub-resource). Encrypted secrets are never returned; only a boolean
 * saying whether a SIP password is stored. The account-scoped read
 * (`GET /v1/whatsapp/calling?accountId=`) remains for callers that only
 * know the social account id, and additionally carries account-level
 * extras (billing eligibility, current-period spend).
 *
 */
export const getWhatsAppCalling = <ThrowOnError extends boolean = false>(options: OptionsLegacyParser<GetWhatsAppCallingData, ThrowOnError>) => {
    return (options?.client ?? client).get<GetWhatsAppCallingResponse, GetWhatsAppCallingError, ThrowOnError>({
        ...options,
        url: '/v1/phone-numbers/{id}/whatsapp/calling'
    });
};

/**
 * Enable calling on a number
 * Enable WhatsApp Business Calling on a connected number. Configures
 * Meta calling.status=ENABLED with our Telnyx SIP endpoint, fetches and
 * stores the Meta-issued SIP password (encrypted), and snapshots the
 * customer's forward-to destination.
 *
 */
export const enableWhatsAppCalling = <ThrowOnError extends boolean = false>(options: OptionsLegacyParser<EnableWhatsAppCallingData, ThrowOnError>) => {
    return (options?.client ?? client).post<EnableWhatsAppCallingResponse, EnableWhatsAppCallingError, ThrowOnError>({
        ...options,
        url: '/v1/phone-numbers/{id}/whatsapp/calling'
    });
};

/**
 * Update calling config
 * Update fields on an already-enabled number. Only fields present in
 * the body are written; `undefined` leaves the stored value alone,
 * explicit `null` clears a nullable field. No Meta side effect, this
 * only changes local routing state consumed by the Telnyx webhook
 * handler.
 *
 */
export const updateWhatsAppCalling = <ThrowOnError extends boolean = false>(options: OptionsLegacyParser<UpdateWhatsAppCallingData, ThrowOnError>) => {
    return (options?.client ?? client).patch<UpdateWhatsAppCallingResponse, UpdateWhatsAppCallingError, ThrowOnError>({
        ...options,
        url: '/v1/phone-numbers/{id}/whatsapp/calling'
    });
};

/**
 * Disable calling on a number
 * Disable calling. Sends calling.status=DISABLED to Meta (best-effort)
 * and flips the local `callingEnabled` flag off. forwardTo and SIP
 * creds are preserved so a re-enable does not lose the destination.
 *
 */
export const disableWhatsAppCalling = <ThrowOnError extends boolean = false>(options: OptionsLegacyParser<DisableWhatsAppCallingData, ThrowOnError>) => {
    return (options?.client ?? client).delete<DisableWhatsAppCallingResponse, DisableWhatsAppCallingError, ThrowOnError>({
        ...options,
        url: '/v1/phone-numbers/{id}/whatsapp/calling'
    });
};

/**
 * Start caller-ID verification for a customer-brought number
 * Customer-brought (BYO) WhatsApp numbers cannot present themselves as
 * caller ID on `tel:` call forwards until verified (carrier
 * anti-spoofing); until then forwarded calls show a Zernio number
 * (`callerIdMode: platform` on the calling config). This sends a
 * one-time code to the number by SMS or voice call. Re-POST to resend.
 * Zernio-purchased numbers never need this and get a 400.
 *
 */
export const startWhatsAppCallerIdVerification = <ThrowOnError extends boolean = false>(options: OptionsLegacyParser<StartWhatsAppCallerIdVerificationData, ThrowOnError>) => {
    return (options?.client ?? client).post<StartWhatsAppCallerIdVerificationResponse, StartWhatsAppCallerIdVerificationError, ThrowOnError>({
        ...options,
        url: '/v1/phone-numbers/{id}/whatsapp/caller-id-verification'
    });
};

/**
 * Confirm the caller-ID verification code
 * Submits the one-time code the number received. On success, `tel:`
 * call forwards present the business number itself as caller ID
 * (`callerIdMode: business`).
 *
 */
export const verifyWhatsAppCallerId = <ThrowOnError extends boolean = false>(options: OptionsLegacyParser<VerifyWhatsAppCallerIdData, ThrowOnError>) => {
    return (options?.client ?? client).post<VerifyWhatsAppCallerIdResponse, VerifyWhatsAppCallerIdError, ThrowOnError>({
        ...options,
        url: '/v1/phone-numbers/{id}/whatsapp/caller-id-verification/verify'
    });
};

/**
 * @deprecated
 * Get phone number
 * Deprecated alias of `/v1/phone-numbers/{id}`; same contract. New
 * integrations should use that path.
 *
 * Retrieve the current status of a purchased phone number. Poll this to
 * track Meta pre-verification (US sync path) and, for regulated (Tier 3/4)
 * numbers, the async lifecycle: pending_regulatory → active (or
 * regulatory_declined). When a regulated number has an Onfido ID step,
 * `onfidoVerificationUrl` appears here once the order is placed — forward
 * it to the end user. (Or subscribe to the whatsapp.number.* webhooks
 * instead of polling.)
 *
 */
export const getWhatsAppPhoneNumber = <ThrowOnError extends boolean = false>(options: OptionsLegacyParser<GetWhatsAppPhoneNumberData, ThrowOnError>) => {
    return (options?.client ?? client).get<GetWhatsAppPhoneNumberResponse, GetWhatsAppPhoneNumberError, ThrowOnError>({
        ...options,
        url: '/v1/whatsapp/phone-numbers/{phoneNumberId}'
    });
};

/**
 * @deprecated
 * Release phone number
 * Deprecated alias of `/v1/phone-numbers/{id}`; same contract. New
 * integrations should use that path.
 *
 * Release a purchased phone number. This will:
 * 1. Disconnect any linked WhatsApp social account
 * 2. Decrement the Stripe subscription quantity (or cancel if last number)
 * 3. Release the number from Telnyx
 * 4. Mark the number as released
 *
 */
export const releaseWhatsAppPhoneNumber = <ThrowOnError extends boolean = false>(options: OptionsLegacyParser<ReleaseWhatsAppPhoneNumberData, ThrowOnError>) => {
    return (options?.client ?? client).delete<ReleaseWhatsAppPhoneNumberResponse, ReleaseWhatsAppPhoneNumberError, ThrowOnError>({
        ...options,
        url: '/v1/whatsapp/phone-numbers/{phoneNumberId}'
    });
};

/**
 * List your sandbox sessions
 * Returns all of the authenticated user's non-expired sandbox sessions
 * (pending + active) plus the sandbox phone number. In practice there
 * is at most one session per user since the sandbox is one-phone-per-user;
 * the array shape is preserved for forward compatibility.
 *
 */
export const listWhatsAppSandboxSessions = <ThrowOnError extends boolean = false>(options?: OptionsLegacyParser<unknown, ThrowOnError>) => {
    return (options?.client ?? client).get<ListWhatsAppSandboxSessionsResponse, ListWhatsAppSandboxSessionsError, ThrowOnError>({
        ...options,
        url: '/v1/whatsapp/sandbox/sessions'
    });
};

/**
 * Start a sandbox activation
 * Creates (or refreshes) a pending sandbox session for the given phone and
 * immediately fires the verified sandbox template from the shared sandbox
 * number to that phone. The session activates when the phone owner replies
 * to that WhatsApp message — the reply itself is proof of ownership.
 *
 * One phone per user: if the caller already has a non-expired session for
 * a DIFFERENT phone, the request is rejected with `invalid_field_value`
 * (the message names the existing phone so it can be revoked first).
 * Re-creating a session for the SAME phone is idempotent and refreshes
 * the verification template.
 *
 * If Meta rejects the template send (not a WhatsApp number, paused WABA,
 * token issue), the pending row is rolled back and the Meta error message
 * is returned in `error` so the caller knows why.
 *
 */
export const createWhatsAppSandboxSession = <ThrowOnError extends boolean = false>(options: OptionsLegacyParser<CreateWhatsAppSandboxSessionData, ThrowOnError>) => {
    return (options?.client ?? client).post<CreateWhatsAppSandboxSessionResponse, CreateWhatsAppSandboxSessionError, ThrowOnError>({
        ...options,
        url: '/v1/whatsapp/sandbox/sessions'
    });
};

/**
 * Revoke a sandbox session
 * Hard-deletes the session. The user loses the ability to send to that
 * phone via the sandbox until they re-activate it. Existing conversations
 * and messages already exchanged with that phone are untouched —
 * revocation only blocks FUTURE sends.
 *
 * Sessions belonging to other users cannot be revoked; the response is
 * the same 400 as "session not found" so existence isn't leaked.
 *
 */
export const deleteWhatsAppSandboxSession = <ThrowOnError extends boolean = false>(options: OptionsLegacyParser<DeleteWhatsAppSandboxSessionData, ThrowOnError>) => {
    return (options?.client ?? client).delete<DeleteWhatsAppSandboxSessionResponse, DeleteWhatsAppSandboxSessionError, ThrowOnError>({
        ...options,
        url: '/v1/whatsapp/sandbox/sessions/{sessionId}'
    });
};

/**
 * List active groups
 * List active WhatsApp group chats for a business phone number.
 * These are actual WhatsApp group conversations on the platform.
 *
 * Not available on [Coexistence](/platforms/whatsapp/connection#whatsapp-business-app-coexistence) numbers. Requires a Cloud API-only number.
 *
 */
export const listWhatsAppGroupChats = <ThrowOnError extends boolean = false>(options: OptionsLegacyParser<ListWhatsAppGroupChatsData, ThrowOnError>) => {
    return (options?.client ?? client).get<ListWhatsAppGroupChatsResponse, ListWhatsAppGroupChatsError, ThrowOnError>({
        ...options,
        url: '/v1/whatsapp/wa-groups'
    });
};

/**
 * Create group
 * Create a new WhatsApp group chat. Returns the group ID and optionally an invite link.
 *
 * Not available on [Coexistence](/platforms/whatsapp/connection#whatsapp-business-app-coexistence) numbers. Requires a Cloud API-only number.
 *
 */
export const createWhatsAppGroupChat = <ThrowOnError extends boolean = false>(options: OptionsLegacyParser<CreateWhatsAppGroupChatData, ThrowOnError>) => {
    return (options?.client ?? client).post<CreateWhatsAppGroupChatResponse, CreateWhatsAppGroupChatError, ThrowOnError>({
        ...options,
        url: '/v1/whatsapp/wa-groups'
    });
};

/**
 * Get group info
 * Retrieve metadata about a WhatsApp group including subject, description,
 * participants, and settings.
 *
 * Not available on [Coexistence](/platforms/whatsapp/connection#whatsapp-business-app-coexistence) numbers. Requires a Cloud API-only number.
 *
 */
export const getWhatsAppGroupChat = <ThrowOnError extends boolean = false>(options: OptionsLegacyParser<GetWhatsAppGroupChatData, ThrowOnError>) => {
    return (options?.client ?? client).get<GetWhatsAppGroupChatResponse, GetWhatsAppGroupChatError, ThrowOnError>({
        ...options,
        url: '/v1/whatsapp/wa-groups/{groupId}'
    });
};

/**
 * Update group settings
 * Update the subject, description, or join approval mode of a WhatsApp group.
 *
 * Not available on [Coexistence](/platforms/whatsapp/connection#whatsapp-business-app-coexistence) numbers. Requires a Cloud API-only number.
 *
 */
export const updateWhatsAppGroupChat = <ThrowOnError extends boolean = false>(options: OptionsLegacyParser<UpdateWhatsAppGroupChatData, ThrowOnError>) => {
    return (options?.client ?? client).post<UpdateWhatsAppGroupChatResponse, UpdateWhatsAppGroupChatError, ThrowOnError>({
        ...options,
        url: '/v1/whatsapp/wa-groups/{groupId}'
    });
};

/**
 * Delete group
 * Delete a WhatsApp group and remove all participants.
 *
 * Not available on [Coexistence](/platforms/whatsapp/connection#whatsapp-business-app-coexistence) numbers. Requires a Cloud API-only number.
 *
 */
export const deleteWhatsAppGroupChat = <ThrowOnError extends boolean = false>(options: OptionsLegacyParser<DeleteWhatsAppGroupChatData, ThrowOnError>) => {
    return (options?.client ?? client).delete<DeleteWhatsAppGroupChatResponse, DeleteWhatsAppGroupChatError, ThrowOnError>({
        ...options,
        url: '/v1/whatsapp/wa-groups/{groupId}'
    });
};

/**
 * Add participants
 * Add participants to a WhatsApp group. Maximum 8 participants per request.
 *
 * Not available on [Coexistence](/platforms/whatsapp/connection#whatsapp-business-app-coexistence) numbers. Requires a Cloud API-only number.
 *
 */
export const addWhatsAppGroupParticipants = <ThrowOnError extends boolean = false>(options: OptionsLegacyParser<AddWhatsAppGroupParticipantsData, ThrowOnError>) => {
    return (options?.client ?? client).post<AddWhatsAppGroupParticipantsResponse, AddWhatsAppGroupParticipantsError, ThrowOnError>({
        ...options,
        url: '/v1/whatsapp/wa-groups/{groupId}/participants'
    });
};

/**
 * Remove participants
 * Remove participants from a WhatsApp group.
 *
 * Not available on [Coexistence](/platforms/whatsapp/connection#whatsapp-business-app-coexistence) numbers. Requires a Cloud API-only number.
 *
 */
export const removeWhatsAppGroupParticipants = <ThrowOnError extends boolean = false>(options: OptionsLegacyParser<RemoveWhatsAppGroupParticipantsData, ThrowOnError>) => {
    return (options?.client ?? client).delete<RemoveWhatsAppGroupParticipantsResponse, RemoveWhatsAppGroupParticipantsError, ThrowOnError>({
        ...options,
        url: '/v1/whatsapp/wa-groups/{groupId}/participants'
    });
};

/**
 * Create invite link
 * Create a new invite link for a WhatsApp group. The previous link is revoked.
 *
 * Not available on [Coexistence](/platforms/whatsapp/connection#whatsapp-business-app-coexistence) numbers. Requires a Cloud API-only number.
 *
 */
export const createWhatsAppGroupInviteLink = <ThrowOnError extends boolean = false>(options: OptionsLegacyParser<CreateWhatsAppGroupInviteLinkData, ThrowOnError>) => {
    return (options?.client ?? client).post<CreateWhatsAppGroupInviteLinkResponse, CreateWhatsAppGroupInviteLinkError, ThrowOnError>({
        ...options,
        url: '/v1/whatsapp/wa-groups/{groupId}/invite-link'
    });
};

/**
 * List join requests
 * List pending join requests for a WhatsApp group (only for groups with approval_required mode).
 *
 * Not available on [Coexistence](/platforms/whatsapp/connection#whatsapp-business-app-coexistence) numbers. Requires a Cloud API-only number.
 *
 */
export const listWhatsAppGroupJoinRequests = <ThrowOnError extends boolean = false>(options: OptionsLegacyParser<ListWhatsAppGroupJoinRequestsData, ThrowOnError>) => {
    return (options?.client ?? client).get<ListWhatsAppGroupJoinRequestsResponse, ListWhatsAppGroupJoinRequestsError, ThrowOnError>({
        ...options,
        url: '/v1/whatsapp/wa-groups/{groupId}/join-requests'
    });
};

/**
 * Approve join requests
 * Approve pending join requests for a WhatsApp group.
 *
 * Not available on [Coexistence](/platforms/whatsapp/connection#whatsapp-business-app-coexistence) numbers. Requires a Cloud API-only number.
 *
 */
export const approveWhatsAppGroupJoinRequests = <ThrowOnError extends boolean = false>(options: OptionsLegacyParser<ApproveWhatsAppGroupJoinRequestsData, ThrowOnError>) => {
    return (options?.client ?? client).post<ApproveWhatsAppGroupJoinRequestsResponse, ApproveWhatsAppGroupJoinRequestsError, ThrowOnError>({
        ...options,
        url: '/v1/whatsapp/wa-groups/{groupId}/join-requests'
    });
};

/**
 * Reject join requests
 * Reject pending join requests for a WhatsApp group.
 *
 * Not available on [Coexistence](/platforms/whatsapp/connection#whatsapp-business-app-coexistence) numbers. Requires a Cloud API-only number.
 *
 */
export const rejectWhatsAppGroupJoinRequests = <ThrowOnError extends boolean = false>(options: OptionsLegacyParser<RejectWhatsAppGroupJoinRequestsData, ThrowOnError>) => {
    return (options?.client ?? client).delete<RejectWhatsAppGroupJoinRequestsResponse, RejectWhatsAppGroupJoinRequestsError, ThrowOnError>({
        ...options,
        url: '/v1/whatsapp/wa-groups/{groupId}/join-requests'
    });
};

/**
 * List flows
 * List all WhatsApp Flows for the Business Account (WABA) associated with the given account.
 *
 */
export const listWhatsAppFlows = <ThrowOnError extends boolean = false>(options: OptionsLegacyParser<ListWhatsAppFlowsData, ThrowOnError>) => {
    return (options?.client ?? client).get<ListWhatsAppFlowsResponse, ListWhatsAppFlowsError, ThrowOnError>({
        ...options,
        url: '/v1/whatsapp/flows'
    });
};

/**
 * Create flow
 * Create a new WhatsApp Flow in DRAFT status. Optionally clone an existing flow.
 * After creating, upload a Flow JSON definition, then publish to make it sendable.
 *
 */
export const createWhatsAppFlow = <ThrowOnError extends boolean = false>(options: OptionsLegacyParser<CreateWhatsAppFlowData, ThrowOnError>) => {
    return (options?.client ?? client).post<CreateWhatsAppFlowResponse, CreateWhatsAppFlowError, ThrowOnError>({
        ...options,
        url: '/v1/whatsapp/flows'
    });
};

/**
 * Get flow
 * Get details for a specific flow, including status, categories, validation errors, and preview URL.
 *
 */
export const getWhatsAppFlow = <ThrowOnError extends boolean = false>(options: OptionsLegacyParser<GetWhatsAppFlowData, ThrowOnError>) => {
    return (options?.client ?? client).get<GetWhatsAppFlowResponse, GetWhatsAppFlowError, ThrowOnError>({
        ...options,
        url: '/v1/whatsapp/flows/{flowId}'
    });
};

/**
 * Update flow
 * Update metadata (name, categories) of a DRAFT flow. Published flows are immutable.
 *
 */
export const updateWhatsAppFlow = <ThrowOnError extends boolean = false>(options: OptionsLegacyParser<UpdateWhatsAppFlowData, ThrowOnError>) => {
    return (options?.client ?? client).patch<UpdateWhatsAppFlowResponse, UpdateWhatsAppFlowError, ThrowOnError>({
        ...options,
        url: '/v1/whatsapp/flows/{flowId}'
    });
};

/**
 * Delete flow
 * Delete a DRAFT flow. This is irreversible. Only flows in DRAFT status can be deleted.
 *
 */
export const deleteWhatsAppFlow = <ThrowOnError extends boolean = false>(options: OptionsLegacyParser<DeleteWhatsAppFlowData, ThrowOnError>) => {
    return (options?.client ?? client).delete<DeleteWhatsAppFlowResponse, DeleteWhatsAppFlowError, ThrowOnError>({
        ...options,
        url: '/v1/whatsapp/flows/{flowId}'
    });
};

/**
 * Get flow JSON asset
 * Get the flow JSON asset metadata, including a temporary download URL for the Flow JSON file.
 *
 */
export const getWhatsAppFlowJson = <ThrowOnError extends boolean = false>(options: OptionsLegacyParser<GetWhatsAppFlowJsonData, ThrowOnError>) => {
    return (options?.client ?? client).get<GetWhatsAppFlowJsonResponse, GetWhatsAppFlowJsonError, ThrowOnError>({
        ...options,
        url: '/v1/whatsapp/flows/{flowId}/json'
    });
};

/**
 * Upload flow JSON
 * Upload or update the Flow JSON for a DRAFT flow. The Flow JSON defines all screens,
 * components (text inputs, dropdowns, date pickers, etc.), and navigation.
 *
 * Meta validates the JSON on upload and returns any validation errors.
 * See: https://developers.facebook.com/docs/whatsapp/flows/reference/flowjson
 *
 */
export const uploadWhatsAppFlowJson = <ThrowOnError extends boolean = false>(options: OptionsLegacyParser<UploadWhatsAppFlowJsonData, ThrowOnError>) => {
    return (options?.client ?? client).put<UploadWhatsAppFlowJsonResponse, UploadWhatsAppFlowJsonError, ThrowOnError>({
        ...options,
        url: '/v1/whatsapp/flows/{flowId}/json'
    });
};

/**
 * Get flow preview URL
 * Get Meta's public web-preview URL for a flow (drafts included), embeddable as an
 * interactive iframe. The link is reused across calls (valid ~30 days); pass
 * invalidate=true to mint a fresh one (the previous link stops working).
 *
 */
export const getWhatsAppFlowPreview = <ThrowOnError extends boolean = false>(options: OptionsLegacyParser<GetWhatsAppFlowPreviewData, ThrowOnError>) => {
    return (options?.client ?? client).get<GetWhatsAppFlowPreviewResponse, GetWhatsAppFlowPreviewError, ThrowOnError>({
        ...options,
        url: '/v1/whatsapp/flows/{flowId}/preview'
    });
};

/**
 * List flow versions
 * List the flow's version history (the clone lineage Zernio tracks, since Meta has no
 * native versioning), newest version first. Each entry is enriched with the version's
 * live name and status from Meta. A flow with no lineage returns just itself as version 1.
 *
 */
export const listWhatsAppFlowVersions = <ThrowOnError extends boolean = false>(options: OptionsLegacyParser<ListWhatsAppFlowVersionsData, ThrowOnError>) => {
    return (options?.client ?? client).get<ListWhatsAppFlowVersionsResponse, ListWhatsAppFlowVersionsError, ThrowOnError>({
        ...options,
        url: '/v1/whatsapp/flows/{flowId}/versions'
    });
};

/**
 * Publish flow
 * Publish a DRAFT flow. This is irreversible. Once published, the flow and its JSON
 * become immutable and the flow can be sent to users. To update a published flow,
 * create a new flow (optionally cloning this one via cloneFlowId).
 *
 */
export const publishWhatsAppFlow = <ThrowOnError extends boolean = false>(options: OptionsLegacyParser<PublishWhatsAppFlowData, ThrowOnError>) => {
    return (options?.client ?? client).post<PublishWhatsAppFlowResponse, PublishWhatsAppFlowError, ThrowOnError>({
        ...options,
        url: '/v1/whatsapp/flows/{flowId}/publish'
    });
};

/**
 * Deprecate flow
 * Deprecate a PUBLISHED flow. This is irreversible. Deprecated flows cannot be sent
 * or opened, but existing active sessions may continue until they complete.
 *
 */
export const deprecateWhatsAppFlow = <ThrowOnError extends boolean = false>(options: OptionsLegacyParser<DeprecateWhatsAppFlowData, ThrowOnError>) => {
    return (options?.client ?? client).post<DeprecateWhatsAppFlowResponse, DeprecateWhatsAppFlowError, ThrowOnError>({
        ...options,
        url: '/v1/whatsapp/flows/{flowId}/deprecate'
    });
};

/**
 * Send flow message
 * Send a published flow as an interactive message with a CTA button.
 * When the recipient taps the button, the flow opens natively in WhatsApp.
 * Flow responses are received via webhooks.
 *
 */
export const sendWhatsAppFlowMessage = <ThrowOnError extends boolean = false>(options: OptionsLegacyParser<SendWhatsAppFlowMessageData, ThrowOnError>) => {
    return (options?.client ?? client).post<SendWhatsAppFlowMessageResponse, SendWhatsAppFlowMessageError, ThrowOnError>({
        ...options,
        url: '/v1/whatsapp/flows/send'
    });
};

/**
 * List flow responses
 * List the responses customers submitted when completing a flow (parsed from the
 * nfm_reply messages received via webhook), newest first. Scope to a single flow
 * with `flowId` — this matches responses whose flow_token carries the `<flowId>:`
 * prefix that Zernio stamps on auto-generated tokens at send time. Responses sent
 * with a custom integrator-supplied flow_token are not attributed to a flow.
 *
 */
export const listWhatsAppFlowResponses = <ThrowOnError extends boolean = false>(options: OptionsLegacyParser<ListWhatsAppFlowResponsesData, ThrowOnError>) => {
    return (options?.client ?? client).get<ListWhatsAppFlowResponsesResponse, ListWhatsAppFlowResponsesError, ThrowOnError>({
        ...options,
        url: '/v1/whatsapp/flow-responses'
    });
};

/**
 * List contacts
 * List and search contacts for a profile. Supports filtering by tags, platform, subscription status, and text search on name, email and company.
 */
export const listContacts = <ThrowOnError extends boolean = false>(options?: OptionsLegacyParser<ListContactsData, ThrowOnError>) => {
    return (options?.client ?? client).get<ListContactsResponse, ListContactsError, ThrowOnError>({
        ...options,
        url: '/v1/contacts'
    });
};

/**
 * Create contact
 * Create a new contact. Optionally create a platform channel in the same request by providing accountId, platform, and platformIdentifier.
 */
export const createContact = <ThrowOnError extends boolean = false>(options: OptionsLegacyParser<CreateContactData, ThrowOnError>) => {
    return (options?.client ?? client).post<CreateContactResponse, CreateContactError, ThrowOnError>({
        ...options,
        url: '/v1/contacts'
    });
};

/**
 * Get contact
 * Returns a contact with all associated messaging channels.
 */
export const getContact = <ThrowOnError extends boolean = false>(options: OptionsLegacyParser<GetContactData, ThrowOnError>) => {
    return (options?.client ?? client).get<GetContactResponse, GetContactError, ThrowOnError>({
        ...options,
        url: '/v1/contacts/{contactId}'
    });
};

/**
 * Update contact
 * Update one or more fields on a contact. Only provided fields are changed.
 */
export const updateContact = <ThrowOnError extends boolean = false>(options: OptionsLegacyParser<UpdateContactData, ThrowOnError>) => {
    return (options?.client ?? client).patch<UpdateContactResponse, UpdateContactError, ThrowOnError>({
        ...options,
        url: '/v1/contacts/{contactId}'
    });
};

/**
 * Delete contact
 * Permanently deletes a contact and all associated channels.
 */
export const deleteContact = <ThrowOnError extends boolean = false>(options: OptionsLegacyParser<DeleteContactData, ThrowOnError>) => {
    return (options?.client ?? client).delete<DeleteContactResponse, DeleteContactError, ThrowOnError>({
        ...options,
        url: '/v1/contacts/{contactId}'
    });
};

/**
 * List channels for a contact
 * Returns all messaging channels linked to a contact (e.g. Instagram DM, Telegram, WhatsApp).
 */
export const getContactChannels = <ThrowOnError extends boolean = false>(options: OptionsLegacyParser<GetContactChannelsData, ThrowOnError>) => {
    return (options?.client ?? client).get<GetContactChannelsResponse, GetContactChannelsError, ThrowOnError>({
        ...options,
        url: '/v1/contacts/{contactId}/channels'
    });
};

/**
 * Bulk create contacts
 * Import up to 1000 contacts at a time. Skips duplicates, merging any new tags onto the existing contact. accountId is required whenever contacts carry a platformIdentifier (or a row-level accountId); platform is always derived from the resolved account, never used to decide whether channels are created, and a mismatched platform 404s as account not found. When accountId is set, each contact must carry a platformIdentifier; a row missing it is rejected individually (reported in errors[], HTTP 200), not a 400 for the whole import. On phone platforms (whatsapp, sms) the platformIdentifier is normalized to digits and a value that is not phone-shaped is rejected per contact and reported in errors[], not imported.
 */
export const bulkCreateContacts = <ThrowOnError extends boolean = false>(options: OptionsLegacyParser<BulkCreateContactsData, ThrowOnError>) => {
    return (options?.client ?? client).post<BulkCreateContactsResponse, BulkCreateContactsError, ThrowOnError>({
        ...options,
        url: '/v1/contacts/bulk'
    });
};

/**
 * Set custom field value
 * Set or overwrite a custom field value on a contact. The value type must match the field definition.
 */
export const setContactFieldValue = <ThrowOnError extends boolean = false>(options: OptionsLegacyParser<SetContactFieldValueData, ThrowOnError>) => {
    return (options?.client ?? client).put<SetContactFieldValueResponse, SetContactFieldValueError, ThrowOnError>({
        ...options,
        url: '/v1/contacts/{contactId}/fields/{slug}'
    });
};

/**
 * Clear custom field value
 * Remove a custom field value from a contact. The field definition is not affected.
 */
export const clearContactFieldValue = <ThrowOnError extends boolean = false>(options: OptionsLegacyParser<ClearContactFieldValueData, ThrowOnError>) => {
    return (options?.client ?? client).delete<ClearContactFieldValueResponse, ClearContactFieldValueError, ThrowOnError>({
        ...options,
        url: '/v1/contacts/{contactId}/fields/{slug}'
    });
};

/**
 * List custom field definitions
 * Returns all custom field definitions. Optionally filter by profile.
 */
export const listCustomFields = <ThrowOnError extends boolean = false>(options?: OptionsLegacyParser<ListCustomFieldsData, ThrowOnError>) => {
    return (options?.client ?? client).get<ListCustomFieldsResponse, ListCustomFieldsError, ThrowOnError>({
        ...options,
        url: '/v1/custom-fields'
    });
};

/**
 * Create custom field
 * Create a new custom field definition. Supported types are text, number, date, boolean, and select.
 */
export const createCustomField = <ThrowOnError extends boolean = false>(options: OptionsLegacyParser<CreateCustomFieldData, ThrowOnError>) => {
    return (options?.client ?? client).post<CreateCustomFieldResponse, CreateCustomFieldError, ThrowOnError>({
        ...options,
        url: '/v1/custom-fields'
    });
};

/**
 * Update custom field
 * Update a custom field definition. The field type cannot be changed after creation.
 */
export const updateCustomField = <ThrowOnError extends boolean = false>(options: OptionsLegacyParser<UpdateCustomFieldData, ThrowOnError>) => {
    return (options?.client ?? client).patch<UpdateCustomFieldResponse, UpdateCustomFieldError, ThrowOnError>({
        ...options,
        url: '/v1/custom-fields/{fieldId}'
    });
};

/**
 * Delete custom field
 * Delete a custom field definition and remove its values from all contacts.
 */
export const deleteCustomField = <ThrowOnError extends boolean = false>(options: OptionsLegacyParser<DeleteCustomFieldData, ThrowOnError>) => {
    return (options?.client ?? client).delete<DeleteCustomFieldResponse, DeleteCustomFieldError, ThrowOnError>({
        ...options,
        url: '/v1/custom-fields/{fieldId}'
    });
};

/**
 * List broadcasts
 * Returns broadcasts with delivery stats. Filter by status, platform, or profile.
 */
export const listBroadcasts = <ThrowOnError extends boolean = false>(options?: OptionsLegacyParser<ListBroadcastsData, ThrowOnError>) => {
    return (options?.client ?? client).get<ListBroadcastsResponse, ListBroadcastsError, ThrowOnError>({
        ...options,
        url: '/v1/broadcasts'
    });
};

/**
 * Create broadcast draft
 * Create a broadcast in draft status. Add recipients and then send or schedule it.
 */
export const createBroadcast = <ThrowOnError extends boolean = false>(options: OptionsLegacyParser<CreateBroadcastData, ThrowOnError>) => {
    return (options?.client ?? client).post<CreateBroadcastResponse, CreateBroadcastError, ThrowOnError>({
        ...options,
        url: '/v1/broadcasts'
    });
};

/**
 * Get broadcast details
 * Returns a broadcast with its full configuration and delivery stats.
 */
export const getBroadcast = <ThrowOnError extends boolean = false>(options: OptionsLegacyParser<GetBroadcastData, ThrowOnError>) => {
    return (options?.client ?? client).get<GetBroadcastResponse, GetBroadcastError, ThrowOnError>({
        ...options,
        url: '/v1/broadcasts/{broadcastId}'
    });
};

/**
 * Update broadcast
 * Update a broadcast's name, message, template, or segment filters. Only draft broadcasts can be updated.
 */
export const updateBroadcast = <ThrowOnError extends boolean = false>(options: OptionsLegacyParser<UpdateBroadcastData, ThrowOnError>) => {
    return (options?.client ?? client).patch<UpdateBroadcastResponse, UpdateBroadcastError, ThrowOnError>({
        ...options,
        url: '/v1/broadcasts/{broadcastId}'
    });
};

/**
 * Delete broadcast
 * Permanently delete a broadcast. Only drafts can be deleted.
 */
export const deleteBroadcast = <ThrowOnError extends boolean = false>(options: OptionsLegacyParser<DeleteBroadcastData, ThrowOnError>) => {
    return (options?.client ?? client).delete<DeleteBroadcastResponse, DeleteBroadcastError, ThrowOnError>({
        ...options,
        url: '/v1/broadcasts/{broadcastId}'
    });
};

/**
 * Send broadcast now
 * Immediately start sending a draft broadcast to its recipients.
 */
export const sendBroadcast = <ThrowOnError extends boolean = false>(options: OptionsLegacyParser<SendBroadcastData, ThrowOnError>) => {
    return (options?.client ?? client).post<SendBroadcastResponse, SendBroadcastError, ThrowOnError>({
        ...options,
        url: '/v1/broadcasts/{broadcastId}/send'
    });
};

/**
 * Schedule broadcast for later
 * Schedule a draft broadcast to be sent at a future date and time.
 */
export const scheduleBroadcast = <ThrowOnError extends boolean = false>(options: OptionsLegacyParser<ScheduleBroadcastData, ThrowOnError>) => {
    return (options?.client ?? client).post<ScheduleBroadcastResponse, ScheduleBroadcastError, ThrowOnError>({
        ...options,
        url: '/v1/broadcasts/{broadcastId}/schedule'
    });
};

/**
 * Cancel broadcast
 * Cancel a scheduled or in-progress broadcast. Already-sent messages are not affected.
 */
export const cancelBroadcast = <ThrowOnError extends boolean = false>(options: OptionsLegacyParser<CancelBroadcastData, ThrowOnError>) => {
    return (options?.client ?? client).post<CancelBroadcastResponse, CancelBroadcastError, ThrowOnError>({
        ...options,
        url: '/v1/broadcasts/{broadcastId}/cancel'
    });
};

/**
 * List broadcast recipients
 * Returns recipients for a broadcast with individual delivery status. Filter by status.
 */
export const listBroadcastRecipients = <ThrowOnError extends boolean = false>(options: OptionsLegacyParser<ListBroadcastRecipientsData, ThrowOnError>) => {
    return (options?.client ?? client).get<ListBroadcastRecipientsResponse, ListBroadcastRecipientsError, ThrowOnError>({
        ...options,
        url: '/v1/broadcasts/{broadcastId}/recipients'
    });
};

/**
 * Add recipients to a broadcast
 * Add recipients by contact IDs, raw phone numbers, or from the broadcast's segment filters.
 */
export const addBroadcastRecipients = <ThrowOnError extends boolean = false>(options: OptionsLegacyParser<AddBroadcastRecipientsData, ThrowOnError>) => {
    return (options?.client ?? client).post<AddBroadcastRecipientsResponse, AddBroadcastRecipientsError, ThrowOnError>({
        ...options,
        url: '/v1/broadcasts/{broadcastId}/recipients'
    });
};

/**
 * List workflows
 * Returns workflows with run stats. Filter by status or profile.
 */
export const listWorkflows = <ThrowOnError extends boolean = false>(options?: OptionsLegacyParser<ListWorkflowsData, ThrowOnError>) => {
    return (options?.client ?? client).get<ListWorkflowsResponse, ListWorkflowsError, ThrowOnError>({
        ...options,
        url: '/v1/workflows'
    });
};

/**
 * Create workflow
 * Create a branching conversation workflow (draft) from a node/edge graph. Created in `draft` status; activate it to start matching inbound messages. The graph is validated structurally; completeness (a trigger node + reachable entry) is required at activation.
 *
 */
export const createWorkflow = <ThrowOnError extends boolean = false>(options: OptionsLegacyParser<CreateWorkflowData, ThrowOnError>) => {
    return (options?.client ?? client).post<CreateWorkflowResponse, CreateWorkflowError, ThrowOnError>({
        ...options,
        url: '/v1/workflows'
    });
};

/**
 * Get workflow with graph
 * Returns a workflow including its full node/edge graph and run stats.
 */
export const getWorkflow = <ThrowOnError extends boolean = false>(options: OptionsLegacyParser<GetWorkflowData, ThrowOnError>) => {
    return (options?.client ?? client).get<GetWorkflowResponse, GetWorkflowError, ThrowOnError>({
        ...options,
        url: '/v1/workflows/{workflowId}'
    });
};

/**
 * Update workflow
 * Update name, description, the graph, or reassign to a different account. The graph can only be modified while the workflow is draft or paused. Account swaps re-validate the graph against the new platform (so e.g. moving from WhatsApp to Facebook surfaces a `start_call` node as an error instead of silently saving an unrunnable graph).
 *
 */
export const updateWorkflow = <ThrowOnError extends boolean = false>(options: OptionsLegacyParser<UpdateWorkflowData, ThrowOnError>) => {
    return (options?.client ?? client).patch<UpdateWorkflowResponse, UpdateWorkflowError, ThrowOnError>({
        ...options,
        url: '/v1/workflows/{workflowId}'
    });
};

/**
 * Delete workflow
 * Permanently delete a workflow and all of its executions.
 */
export const deleteWorkflow = <ThrowOnError extends boolean = false>(options: OptionsLegacyParser<DeleteWorkflowData, ThrowOnError>) => {
    return (options?.client ?? client).delete<DeleteWorkflowResponse, DeleteWorkflowError, ThrowOnError>({
        ...options,
        url: '/v1/workflows/{workflowId}'
    });
};

/**
 * Activate workflow
 * Validate the graph is runnable and set the workflow live. Once active, matching inbound messages start executions. Idempotent.
 */
export const activateWorkflow = <ThrowOnError extends boolean = false>(options: OptionsLegacyParser<ActivateWorkflowData, ThrowOnError>) => {
    return (options?.client ?? client).post<ActivateWorkflowResponse, ActivateWorkflowError, ThrowOnError>({
        ...options,
        url: '/v1/workflows/{workflowId}/activate'
    });
};

/**
 * Pause workflow
 * Stop matching new inbound messages. In-flight executions continue to completion. Idempotent.
 */
export const pauseWorkflow = <ThrowOnError extends boolean = false>(options: OptionsLegacyParser<PauseWorkflowData, ThrowOnError>) => {
    return (options?.client ?? client).post<PauseWorkflowResponse, PauseWorkflowError, ThrowOnError>({
        ...options,
        url: '/v1/workflows/{workflowId}/pause'
    });
};

/**
 * List workflow runs
 * Returns recent executions (runs) with their status, current node, and accumulated variables.
 */
export const listWorkflowExecutions = <ThrowOnError extends boolean = false>(options: OptionsLegacyParser<ListWorkflowExecutionsData, ThrowOnError>) => {
    return (options?.client ?? client).get<ListWorkflowExecutionsResponse, ListWorkflowExecutionsError, ThrowOnError>({
        ...options,
        url: '/v1/workflows/{workflowId}/executions'
    });
};

/**
 * Manually start a workflow run
 * Kick off a run without waiting for an inbound message (useful for testing). Target an existing conversation by `conversationId`, or — WhatsApp only — a phone number via `to` (a conversation is found or created). `text` seeds the run's `lastMessage` variable. The graph must be runnable.
 *
 */
export const triggerWorkflow = <ThrowOnError extends boolean = false>(options: OptionsLegacyParser<TriggerWorkflowData, ThrowOnError>) => {
    return (options?.client ?? client).post<TriggerWorkflowResponse, TriggerWorkflowError, ThrowOnError>({
        ...options,
        url: '/v1/workflows/{workflowId}/executions'
    });
};

/**
 * Get an execution's timeline
 * Returns the per-step run-log for a single workflow execution: trigger fired, each node visited, edge handles taken, errors, and durations. Backed by Tinybird (90-day retention). Used by the Runs UI drawer to render the timeline.
 *
 */
export const listWorkflowExecutionEvents = <ThrowOnError extends boolean = false>(options: OptionsLegacyParser<ListWorkflowExecutionEventsData, ThrowOnError>) => {
    return (options?.client ?? client).get<ListWorkflowExecutionEventsResponse, ListWorkflowExecutionEventsError, ThrowOnError>({
        ...options,
        url: '/v1/workflows/{workflowId}/executions/{executionId}/events'
    });
};

/**
 * Duplicate a workflow
 * Create an independent copy of a workflow's graph, name, description, and account binding. The copy is created in `draft` status with fresh execution counters and a new id — execution history is NOT copied. Useful for branching off a known-good workflow before making experimental edits.
 *
 */
export const duplicateWorkflow = <ThrowOnError extends boolean = false>(options: OptionsLegacyParser<DuplicateWorkflowData, ThrowOnError>) => {
    return (options?.client ?? client).post<DuplicateWorkflowResponse, DuplicateWorkflowError, ThrowOnError>({
        ...options,
        url: '/v1/workflows/{workflowId}/duplicate'
    });
};

/**
 * List a workflow's version history
 * Returns the snapshot history. A new version is recorded automatically before every PATCH to `nodes` / `edges` / `entryNodeId`, and explicitly when a previous version is restored. Lightweight list — call `getWorkflowVersion` for the full snapshot graph.
 *
 */
export const listWorkflowVersions = <ThrowOnError extends boolean = false>(options: OptionsLegacyParser<ListWorkflowVersionsData, ThrowOnError>) => {
    return (options?.client ?? client).get<ListWorkflowVersionsResponse, ListWorkflowVersionsError, ThrowOnError>({
        ...options,
        url: '/v1/workflows/{workflowId}/versions'
    });
};

/**
 * Get a specific workflow version
 * Returns the full snapshot for a single historical version, including the graph.
 */
export const getWorkflowVersion = <ThrowOnError extends boolean = false>(options: OptionsLegacyParser<GetWorkflowVersionData, ThrowOnError>) => {
    return (options?.client ?? client).get<GetWorkflowVersionResponse, GetWorkflowVersionError, ThrowOnError>({
        ...options,
        url: '/v1/workflows/{workflowId}/versions/{version}'
    });
};

/**
 * Restore a workflow version
 * Replace the current graph with the named version's snapshot. Before the swap, the current graph is itself snapshotted as a new version, so a restore is reversible. The workflow must be in `draft` or `paused` status (same gate as a normal graph edit). The returned workflow carries `restoredFromVersion` so the UI can surface which version was rolled back to.
 *
 */
export const restoreWorkflowVersion = <ThrowOnError extends boolean = false>(options: OptionsLegacyParser<RestoreWorkflowVersionData, ThrowOnError>) => {
    return (options?.client ?? client).post<RestoreWorkflowVersionResponse, RestoreWorkflowVersionError, ThrowOnError>({
        ...options,
        url: '/v1/workflows/{workflowId}/versions/{version}/restore'
    });
};

/**
 * List sequences
 * Returns sequences with enrollment stats. Filter by status, platform, or profile.
 */
export const listSequences = <ThrowOnError extends boolean = false>(options?: OptionsLegacyParser<ListSequencesData, ThrowOnError>) => {
    return (options?.client ?? client).get<ListSequencesResponse, ListSequencesError, ThrowOnError>({
        ...options,
        url: '/v1/sequences'
    });
};

/**
 * Create sequence
 * Create a multi-step messaging sequence. Each step has a delay and a message or WhatsApp template.
 */
export const createSequence = <ThrowOnError extends boolean = false>(options: OptionsLegacyParser<CreateSequenceData, ThrowOnError>) => {
    return (options?.client ?? client).post<CreateSequenceResponse, CreateSequenceError, ThrowOnError>({
        ...options,
        url: '/v1/sequences'
    });
};

/**
 * Get sequence with steps
 * Returns a sequence with all its steps and enrollment stats.
 */
export const getSequence = <ThrowOnError extends boolean = false>(options: OptionsLegacyParser<GetSequenceData, ThrowOnError>) => {
    return (options?.client ?? client).get<GetSequenceResponse, GetSequenceError, ThrowOnError>({
        ...options,
        url: '/v1/sequences/{sequenceId}'
    });
};

/**
 * Update sequence
 * Update a sequence's name, steps, or exit conditions. Steps can only be modified while the sequence is draft or paused.
 */
export const updateSequence = <ThrowOnError extends boolean = false>(options: OptionsLegacyParser<UpdateSequenceData, ThrowOnError>) => {
    return (options?.client ?? client).patch<UpdateSequenceResponse, UpdateSequenceError, ThrowOnError>({
        ...options,
        url: '/v1/sequences/{sequenceId}'
    });
};

/**
 * Delete sequence
 * Permanently delete a sequence. Active enrollments are stopped.
 */
export const deleteSequence = <ThrowOnError extends boolean = false>(options: OptionsLegacyParser<DeleteSequenceData, ThrowOnError>) => {
    return (options?.client ?? client).delete<DeleteSequenceResponse, DeleteSequenceError, ThrowOnError>({
        ...options,
        url: '/v1/sequences/{sequenceId}'
    });
};

/**
 * Activate sequence
 * Start a draft or paused sequence. The sequence must have at least one step.
 */
export const activateSequence = <ThrowOnError extends boolean = false>(options: OptionsLegacyParser<ActivateSequenceData, ThrowOnError>) => {
    return (options?.client ?? client).post<ActivateSequenceResponse, ActivateSequenceError, ThrowOnError>({
        ...options,
        url: '/v1/sequences/{sequenceId}/activate'
    });
};

/**
 * Pause sequence
 * Pause an active sequence. Enrolled contacts stop receiving messages until the sequence is reactivated.
 */
export const pauseSequence = <ThrowOnError extends boolean = false>(options: OptionsLegacyParser<PauseSequenceData, ThrowOnError>) => {
    return (options?.client ?? client).post<PauseSequenceResponse, PauseSequenceError, ThrowOnError>({
        ...options,
        url: '/v1/sequences/{sequenceId}/pause'
    });
};

/**
 * Enroll contacts in a sequence
 * Enroll one or more contacts into a sequence. Contacts already enrolled are skipped.
 */
export const enrollContacts = <ThrowOnError extends boolean = false>(options: OptionsLegacyParser<EnrollContactsData, ThrowOnError>) => {
    return (options?.client ?? client).post<EnrollContactsResponse, EnrollContactsError, ThrowOnError>({
        ...options,
        url: '/v1/sequences/{sequenceId}/enroll'
    });
};

/**
 * Unenroll contact
 * Remove a contact from a sequence. No further messages will be sent to this contact.
 */
export const unenrollContact = <ThrowOnError extends boolean = false>(options: OptionsLegacyParser<UnenrollContactData, ThrowOnError>) => {
    return (options?.client ?? client).delete<UnenrollContactResponse, UnenrollContactError, ThrowOnError>({
        ...options,
        url: '/v1/sequences/{sequenceId}/enroll/{contactId}'
    });
};

/**
 * List enrollments for a sequence
 * Returns enrolled contacts with their progress, status, and next scheduled step.
 */
export const listSequenceEnrollments = <ThrowOnError extends boolean = false>(options: OptionsLegacyParser<ListSequenceEnrollmentsData, ThrowOnError>) => {
    return (options?.client ?? client).get<ListSequenceEnrollmentsResponse, ListSequenceEnrollmentsError, ThrowOnError>({
        ...options,
        url: '/v1/sequences/{sequenceId}/enrollments'
    });
};

/**
 * List comment-to-DM automations
 * List all comment-to-DM automations for a profile. Returns automations with their stats.
 */
export const listCommentAutomations = <ThrowOnError extends boolean = false>(options?: OptionsLegacyParser<ListCommentAutomationsData, ThrowOnError>) => {
    return (options?.client ?? client).get<ListCommentAutomationsResponse, ListCommentAutomationsError, ThrowOnError>({
        ...options,
        url: '/v1/comment-automations'
    });
};

/**
 * Create comment-to-DM automation
 * Create a keyword-triggered DM automation on an Instagram or Facebook account.
 * When someone comments a matching keyword (or, with `trigger: story_reply`, replies
 * to your Instagram story with one), they automatically receive a DM.
 *
 * Triggers (`trigger`):
 * * `comment` (default): fires on keyword comments on a post or reel.
 * * `story_reply`: fires when someone replies to your Instagram story with a keyword,
 * and answers them with a DM. Set `platformPostId` to a story media id to scope to
 * one story, or omit it to match replies to any story.
 *
 * Targeting (comment trigger):
 * * Per-post: set `platformPostId` to scope to one specific post (only one active
 * per-post automation is allowed per post).
 * * Account-wide ("any post"): omit `platformPostId` (and `postId`). The automation
 * evaluates every comment on every post on the account. You can stack unlimited
 * account-wide automations, each with its own keyword set, and they all run
 * independently. Per-post automations take priority on their post.
 *
 * Audience (`audience`, Instagram only): restrict the automation to followers or
 * non-followers, and/or to accounts above a follower count. Instagram only reveals the
 * follow relationship for people who have messaged the account, so `audience.whenUnknown`
 * decides what happens for everyone else - including `verify`, which sends a one-tap
 * confirmation DM (`followGate`) and then delivers the real DM automatically. People we
 * already know follow you skip the tap entirely.
 *
 * Set `alsoMatchInDms: true` on a `comment` automation to also answer people who send
 * a keyword as a direct message instead of commenting it. One automation then covers
 * both doors, and each door is deduplicated separately (someone who already got the DM
 * from their comment still gets it if they later DM the keyword). Requires at least one
 * keyword.
 *
 * Links in the DM's buttons can be click-tracked (`linkTracking`, on by default) and
 * clickers optionally tagged (`clickTag`) for segmentation. Stats returned include
 * delivered, read, and link clicks.
 *
 */
export const createCommentAutomation = <ThrowOnError extends boolean = false>(options: OptionsLegacyParser<CreateCommentAutomationData, ThrowOnError>) => {
    return (options?.client ?? client).post<CreateCommentAutomationResponse, CreateCommentAutomationError, ThrowOnError>({
        ...options,
        url: '/v1/comment-automations'
    });
};

/**
 * Get automation details
 * Returns an automation with its configuration, stats, and recent trigger logs.
 */
export const getCommentAutomation = <ThrowOnError extends boolean = false>(options: OptionsLegacyParser<GetCommentAutomationData, ThrowOnError>) => {
    return (options?.client ?? client).get<GetCommentAutomationResponse, GetCommentAutomationError, ThrowOnError>({
        ...options,
        url: '/v1/comment-automations/{automationId}'
    });
};

/**
 * Update automation settings
 * Update an automation's keywords, DM message, inline buttons, comment reply, or active status.
 * Pass `buttons: []` to clear all buttons. When `buttons` is non-empty, `dmMessage` (the new
 * one if you're changing it, otherwise the stored one) must be 640 characters or less.
 *
 */
export const updateCommentAutomation = <ThrowOnError extends boolean = false>(options: OptionsLegacyParser<UpdateCommentAutomationData, ThrowOnError>) => {
    return (options?.client ?? client).patch<UpdateCommentAutomationResponse, UpdateCommentAutomationError, ThrowOnError>({
        ...options,
        url: '/v1/comment-automations/{automationId}'
    });
};

/**
 * Delete automation
 * Permanently delete an automation and all its trigger logs.
 */
export const deleteCommentAutomation = <ThrowOnError extends boolean = false>(options: OptionsLegacyParser<DeleteCommentAutomationData, ThrowOnError>) => {
    return (options?.client ?? client).delete<DeleteCommentAutomationResponse, DeleteCommentAutomationError, ThrowOnError>({
        ...options,
        url: '/v1/comment-automations/{automationId}'
    });
};

/**
 * List automation logs
 * Paginated list of every comment that triggered this automation, with send status and commenter info.
 */
export const listCommentAutomationLogs = <ThrowOnError extends boolean = false>(options: OptionsLegacyParser<ListCommentAutomationLogsData, ThrowOnError>) => {
    return (options?.client ?? client).get<ListCommentAutomationLogsResponse, ListCommentAutomationLogsError, ThrowOnError>({
        ...options,
        url: '/v1/comment-automations/{automationId}/logs'
    });
};

/**
 * List ads
 * Returns a paginated list of ads with metrics computed over an optional date range.
 * Use source=all to include externally-synced ads from platform ad managers.
 * If no date range is provided, defaults to the last 90 days. Date range is capped at 730 days max.
 *
 * To find the Zernio ad behind a comment you see in Meta Business Manager, filter by
 * platformAdId (the Meta ad ID), effectiveObjectStoryId (Facebook), or
 * effectiveInstagramMediaId (Instagram) — those are the post/media the ad's engagement
 * lives on, and are also returned on each ad's `creative` object. Then call
 * GET /v1/ads/{adId}/comments with the returned ad id.
 *
 */
export const listAds = <ThrowOnError extends boolean = false>(options?: OptionsLegacyParser<ListAdsData, ThrowOnError>) => {
    return (options?.client ?? client).get<ListAdsResponse, ListAdsError, ThrowOnError>({
        ...options,
        url: '/v1/ads'
    });
};

/**
 * Google Ads search terms report
 * The actual search queries that triggered your ads, with matched-keyword
 * status and spend metrics — the raw material for wasted-spend analysis and
 * negative-keyword lists. Reads Google's `search_term_view` live; defaults
 * to the last 30 days. Rows are ordered by cost, descending. Draws on the
 * shared Google Ads operations budget.
 */
export const getAdsSearchTerms = <ThrowOnError extends boolean = false>(options: OptionsLegacyParser<GetAdsSearchTermsData, ThrowOnError>) => {
    return (options?.client ?? client).get<GetAdsSearchTermsResponse, GetAdsSearchTermsError, ThrowOnError>({
        ...options,
        url: '/v1/ads/search-terms'
    });
};

/**
 * Google Local Services Ads leads
 * Leads generated by Local Services Ads (phone calls, messages, bookings),
 * read live from Google's `local_services_lead` resource, newest first.
 * No persistence: Google is the source of truth and lead/credit statuses
 * keep changing server-side. Google never returns healthcare-category
 * leads, and `WIPED_OUT` leads arrive with contact erased (`contact` is
 * null). Draws on the shared Google Ads operations budget.
 */
export const listLocalServicesLeads = <ThrowOnError extends boolean = false>(options: OptionsLegacyParser<ListLocalServicesLeadsData, ThrowOnError>) => {
    return (options?.client ?? client).get<ListLocalServicesLeadsResponse, ListLocalServicesLeadsError, ThrowOnError>({
        ...options,
        url: '/v1/ads/local-services/leads'
    });
};

/**
 * Conversations of a Local Services lead
 * Conversation entries of one Local Services lead: phone calls (duration,
 * recording URL) and messages (text, attachment URLs), oldest first. Read
 * live from `local_services_lead_conversation`, always scoped to a single
 * lead. Call-recording URLs require read access on the Google Ads account.
 * Draws on the shared Google Ads operations budget.
 */
export const listLocalServicesLeadConversations = <ThrowOnError extends boolean = false>(options: OptionsLegacyParser<ListLocalServicesLeadConversationsData, ThrowOnError>) => {
    return (options?.client ?? client).get<ListLocalServicesLeadConversationsResponse, ListLocalServicesLeadConversationsError, ThrowOnError>({
        ...options,
        url: '/v1/ads/local-services/leads/{leadId}/conversations'
    });
};

/**
 * List Search keywords
 * Returns the Google Search keyword criteria (positive and negative) synced from
 * connected Google Ads accounts, one row per ad-group keyword. Refreshed about
 * once a week per Google Ads customer (the keyword sweep rides the ads discovery
 * pass on a slower slot, to stay inside Google's shared daily API quota), so
 * keywords added on Google can take several days to appear. A customer synced
 * for the first time is populated on the next discovery pass rather than
 * waiting for its weekly slot, and connecting an account or triggering a
 * manual sync refreshes it immediately.
 * Campaign-level negative keywords are not included; only ad-group-level
 * criteria are.
 *
 */
export const listAdKeywords = <ThrowOnError extends boolean = false>(options?: OptionsLegacyParser<ListAdKeywordsData, ThrowOnError>) => {
    return (options?.client ?? client).get<ListAdKeywordsResponse, ListAdKeywordsError, ThrowOnError>({
        ...options,
        url: '/v1/ads/keywords'
    });
};

/**
 * List campaigns
 * Returns campaigns as virtual aggregations over ad documents grouped by platform campaign ID.
 * Metrics (spend, impressions, clicks, etc.) are summed across all ads in each campaign.
 * Campaign status is derived from child ad statuses (active > pending_review > paused > error > completed > cancelled > rejected).
 *
 */
export const listAdCampaigns = <ThrowOnError extends boolean = false>(options?: OptionsLegacyParser<ListAdCampaignsData, ThrowOnError>) => {
    return (options?.client ?? client).get<ListAdCampaignsResponse, ListAdCampaignsError, ThrowOnError>({
        ...options,
        url: '/v1/ads/campaigns'
    });
};

/**
 * Create a standalone campaign
 * Creates a campaign WITHOUT its first ad set / ad (the ODAX shell only). Ad sets join it
 * later via `existingCampaignId` on the create endpoints. A budget here is campaign-level
 * (CBO) by definition; omit it for ABO (each ad set carries its own budget). Created
 * `PAUSED` unless `status: ACTIVE`. The campaign materializes in `/v1/ads/tree` via the
 * next sync discovery pass.
 *
 * **Idempotency:** send an `Idempotency-Key` header to make retries safe.
 */
export const createAdCampaign = <ThrowOnError extends boolean = false>(options: OptionsLegacyParser<CreateAdCampaignData, ThrowOnError>) => {
    return (options?.client ?? client).post<CreateAdCampaignResponse, CreateAdCampaignError, ThrowOnError>({
        ...options,
        url: '/v1/ads/campaigns'
    });
};

/**
 * Pause or resume a campaign
 * Writes the campaign's own on/off switch, then lets the platform cascade delivery to its ad sets and ads.
 * Makes one platform API call, not one per ad.
 *
 * The switch is always written, whatever delivery status the ads underneath report: an ad still in review
 * does not block resuming its campaign. The echoed `status` is the confirmation that it landed.
 *
 * `updated` / `skipped` describe only the ads whose own stored status CHANGED alongside it, so
 * `updated: 0` is a normal successful response, not a no-op. Ads are skipped when they are in a terminal
 * status (rejected, completed, cancelled), already in the target state, or switched on but not yet
 * delivering — the last group keeps its `pending_review` / `error` status until the platform reports what
 * it became. `skippedReasons` names which case applies.
 *
 * On Meta this flips the campaign only. An ad set paused in its own right stays paused, so pair this with
 * PUT /v1/ads/ad-sets/{adSetId}/status when you also need the ad set switched back on.
 *
 */
export const updateAdCampaignStatus = <ThrowOnError extends boolean = false>(options: OptionsLegacyParser<UpdateAdCampaignStatusData, ThrowOnError>) => {
    return (options?.client ?? client).put<UpdateAdCampaignStatusResponse, UpdateAdCampaignStatusError, ThrowOnError>({
        ...options,
        url: '/v1/ads/campaigns/{campaignId}/status'
    });
};

/**
 * Update a campaign
 * Campaign-level edits. Send at least one of `budget`, `bidStrategy`, `name`
 * or `platformSpecificData`. An unsupported field is always an error, never a
 * silent drop.
 *
 * | Body field | Meta | Google | Others |
 * |---|---|---|---|
 * | `bidStrategy` | Yes | Yes | 501 |
 * | `bidAmount`, `roasAverageFloor` | 400 — ad-set level | Yes | 400 |
 * | `budget` (CBO; ABO returns 409) | Yes | 501 | 501 |
 * | `name` | Yes | 501 | 501 |
 * | `platformSpecificData.spendCap` | Yes | 400 | 400 |
 * | `accountId` (empty campaigns) | Yes | - | - |
 *
 * Google maps the shared enum onto its own strategies: `LOWEST_COST_WITHOUT_CAP`
 * to Maximize Clicks, `LOWEST_COST_WITH_BID_CAP` to Maximize Clicks with a max
 * CPC (`bidAmount`), `COST_CAP` to Target CPA (`bidAmount`),
 * `LOWEST_COST_WITH_MIN_ROAS` to Target ROAS (`roasAverageFloor`). A campaign on
 * a PORTFOLIO bidding strategy is rejected: detach it in Google Ads first, since
 * it is shared across campaigns.
 *
 * `accountId` forwards the update straight to Meta for a campaign with zero ads,
 * which would otherwise 404; the response then carries `updated: 0`.
 *
 */
export const updateAdCampaign = <ThrowOnError extends boolean = false>(options: OptionsLegacyParser<UpdateAdCampaignData, ThrowOnError>) => {
    return (options?.client ?? client).put<UpdateAdCampaignResponse, UpdateAdCampaignError, ThrowOnError>({
        ...options,
        url: '/v1/ads/campaigns/{campaignId}'
    });
};

/**
 * Delete a campaign
 * Deletes the whole campaign on the platform, cascading to its ad sets
 * and ads. Locally, all Ad documents for this campaign are marked
 * `status: cancelled`.
 *
 * **Empty campaigns.** A campaign with zero ads has no local Ad documents
 * to resolve, so it is invisible to `/v1/ads/tree` and this endpoint would
 * 404. That state is produced by the two-step create flow (campaign, then
 * ads via `existingCampaignId`) whenever Meta rejects the ad step. To
 * delete such a shell, send `accountId` in the body: we skip the local
 * lookup entirely and forward the delete to Meta. `accountId` is ignored
 * when the campaign does have ads.
 *
 */
export const deleteAdCampaign = <ThrowOnError extends boolean = false>(options: OptionsLegacyParser<DeleteAdCampaignData, ThrowOnError>) => {
    return (options?.client ?? client).delete<DeleteAdCampaignResponse, DeleteAdCampaignError, ThrowOnError>({
        ...options,
        url: '/v1/ads/campaigns/{campaignId}'
    });
};

/**
 * Pause or resume many campaigns
 * Process up to 50 campaigns in one call. Each campaign is updated
 * concurrently and the response contains a per-campaign result so a
 * single bad row does not fail the whole batch.
 *
 */
export const bulkUpdateAdCampaignStatus = <ThrowOnError extends boolean = false>(options: OptionsLegacyParser<BulkUpdateAdCampaignStatusData, ThrowOnError>) => {
    return (options?.client ?? client).post<BulkUpdateAdCampaignStatusResponse, BulkUpdateAdCampaignStatusError, ThrowOnError>({
        ...options,
        url: '/v1/ads/campaigns/bulk-status'
    });
};

/**
 * Duplicate a campaign
 * Duplicates a campaign, including its ad sets, ads, creatives, and
 * targeting by default (`deepCopy: true`). The copy is created paused
 * so callers can review before launching.
 *
 * Per-platform implementation:
 * - **Meta** uses the native `POST /{campaign-id}/copies` endpoint.
 * - **TikTok** has no native copy primitive; Zernio walks the source
 * graph (`/v2/campaign/get/`, `/v2/adgroup/get/`, `/v2/ad/get/`) and
 * recreates each entity via the corresponding `/create/` endpoints,
 * carrying over budget / targeting / bid_type / bid_price /
 * deep_bid_type / creative fields. Spark Ad linkage (`tiktok_item_id`)
 * is preserved.
 * - **LinkedIn** has no native copy primitive; Zernio walks the source
 * CampaignGroup → Campaigns → Creatives and recreates each entity,
 * carrying over `type` / `costType` / `unitCost` /
 * `optimizationTargetType` / `creativeSelection` / `objectiveType` /
 * `format` / `dailyBudget` / `totalBudget` / `targetingCriteria` /
 * `runSchedule` and every Creative's `content` object verbatim.
 * `statusOption: INHERITED_FROM_SOURCE` is evaluated **per entity**:
 * any Group / Campaign / Creative whose source is `ACTIVE` gets its
 * clone activated too. Duplicating an ACTIVE campaign with
 * `INHERITED_FROM_SOURCE` starts a second front of spend the moment
 * the clone activates — the safe default is `PAUSED`.
 *
 * The new hierarchy is asynchronous to materialize in our DB — we
 * trigger sync discovery automatically. Set `syncAfter: false` to
 * skip and poll `/v1/ads/tree` on your own cadence.
 *
 * Other platforms return 501 Not Implemented.
 *
 */
export const duplicateAdCampaign = <ThrowOnError extends boolean = false>(options: OptionsLegacyParser<DuplicateAdCampaignData, ThrowOnError>) => {
    return (options?.client ?? client).post<DuplicateAdCampaignResponse, DuplicateAdCampaignError, ThrowOnError>({
        ...options,
        url: '/v1/ads/campaigns/{campaignId}/duplicate'
    });
};

/**
 * Duplicate an ad set
 * Duplicates an ad set, including its ads and creatives by default (`deepCopy: true`),
 * via Meta's native `POST /{adset-id}/copies`. The copy is created paused so callers can
 * review before launching. `campaignId` retargets the copy into another campaign; omitted
 * = the source's own campaign. The new hierarchy materializes asynchronously — sync
 * discovery is triggered automatically (`syncAfter: false` to skip).
 */
export const duplicateAdSet = <ThrowOnError extends boolean = false>(options: OptionsLegacyParser<DuplicateAdSetData, ThrowOnError>) => {
    return (options?.client ?? client).post<DuplicateAdSetResponse, DuplicateAdSetError, ThrowOnError>({
        ...options,
        url: '/v1/ads/ad-sets/{adSetId}/duplicate'
    });
};

/**
 * Duplicate an ad
 * Duplicates a single ad via Meta's native `POST /{ad-id}/copies`. The copy is created
 * paused. `adSetId` retargets the copy into another ad set; omitted = the source's own ad
 * set. Accepts the Zernio ad id or the platform ad id. Sync discovery is triggered
 * automatically (`syncAfter: false` to skip).
 */
export const duplicateAd = <ThrowOnError extends boolean = false>(options: OptionsLegacyParser<DuplicateAdData, ThrowOnError>) => {
    return (options?.client ?? client).post<DuplicateAdResponse, DuplicateAdError, ThrowOnError>({
        ...options,
        url: '/v1/ads/{adId}/duplicate'
    });
};

/**
 * Live ad-set details incl. learning phase
 * Reads the ad set live from Meta, returned verbatim. The default projection includes
 * `learning_stage_info` (learning-phase status: LEARNING / SUCCESS / FAIL / WAIVING — Meta
 * omits its `status` key on paused ad sets), delivery settings, budgets, schedule and
 * targeting. `fields` is a raw-passthrough override; unknown fields return Meta's 400
 * verbatim.
 */
export const getAdSetDetails = <ThrowOnError extends boolean = false>(options: OptionsLegacyParser<GetAdSetDetailsData, ThrowOnError>) => {
    return (options?.client ?? client).get<GetAdSetDetailsResponse, GetAdSetDetailsError, ThrowOnError>({
        ...options,
        url: '/v1/ads/ad-sets/{adSetId}'
    });
};

/**
 * Update an ad set
 * Ad-set-level writes. Use this for ABO budget updates, ad-set-scoped
 * pause/resume, bid-strategy edits, Meta value-rule-set attach/detach, and
 * Meta-only post-launch delivery settings via `platformSpecificData`. At
 * least one updatable field is required.
 *
 * Value rule sets (Meta only, see `/v1/ads/value-rule-sets`):
 * - ATTACH or REPLACE: send `valueRuleSetId`. Attachment is driven by the id's
 * presence, so `valueRulesApplied: true` is optional. Sending a different id
 * replaces the previous association; there is no separate replace call.
 * - DETACH: send `valueRulesApplied: false` and OMIT `valueRuleSetId`.
 * - Sending `valueRulesApplied: false` TOGETHER with `valueRuleSetId` returns 400
 * `mutually_exclusive_fields`. This is deliberate: Meta attaches the rule set
 * whenever `value_rule_set_id` is present, even with `value_rules_applied` false,
 * so echoing stored state while asking to detach would silently keep the bid
 * adjustments live.
 * - Eligibility: only ad sets on `LOWEST_COST_WITHOUT_CAP` or `COST_CAP`. Meta
 * rejects the rest server-side.
 * - Read back with `GET /v1/ads/ad-sets/{adSetId}?fields=value_rule_set_id`. Meta
 * does not document `value_rules_applied` as a readable ad-set field, so the
 * boolean cannot be read back.
 *
 * Bid strategy compatibility (per Meta's spec):
 * - `LOWEST_COST_WITHOUT_CAP`: no `bidAmount`, no `roasAverageFloor`.
 * - `LOWEST_COST_WITH_BID_CAP` / `COST_CAP`: `bidAmount` REQUIRED (whole currency units).
 * - `LOWEST_COST_WITH_MIN_ROAS`: `roasAverageFloor` REQUIRED (decimal multiplier, e.g. 2.0 = 2.0x ROAS).
 * - Meta only: send `bidAmount` WITHOUT `bidStrategy` to change the cap amount on an ad set
 * under a COST_CAP / LOWEST_COST_WITH_BID_CAP parent campaign, leaving the strategy itself
 * (inherited from the campaign) untouched. `roasAverageFloor` without `bidStrategy` is
 * rejected (it has no meaning outside LOWEST_COST_WITH_MIN_ROAS).
 *
 * Delivery settings are validated by Meta against the campaign objective;
 * incompatible combinations (e.g. a billingEvent the optimization goal
 * doesn't allow) surface as 400s from Meta.
 *
 * When updating `budget` on an ABO campaign: if the parent campaign is
 * CBO, the response is 409 with code BUDGET_LEVEL_MISMATCH — route to
 * PUT /v1/ads/campaigns/{campaignId} instead.
 *
 */
export const updateAdSet = <ThrowOnError extends boolean = false>(options: OptionsLegacyParser<UpdateAdSetData, ThrowOnError>) => {
    return (options?.client ?? client).put<UpdateAdSetResponse, UpdateAdSetError, ThrowOnError>({
        ...options,
        url: '/v1/ads/ad-sets/{adSetId}'
    });
};

/**
 * Pause or resume a single ad set
 * Ad-set-scoped pause/resume (doesn't touch sibling ad sets). Thin wrapper
 * over PUT /v1/ads/ad-sets/{adSetId} for callers that only want the
 * status toggle and prefer a symmetric URL to
 * /v1/ads/campaigns/{campaignId}/status.
 *
 * On Meta and LinkedIn this writes the ad set's own on/off switch
 * (Meta: `configured_status`), whatever delivery status its ads report —
 * an ad still in review does not block resuming its ad set. The echoed
 * `status` is the confirmation that it landed. Where the platform has no
 * ad-set switch (TikTok and others) the toggle is emulated by flipping the
 * child ads; a call with no actionable ad then writes nothing and returns a
 * `message` with no `status`.
 *
 * `updated` / `skipped` describe only the ads whose own stored status
 * CHANGED alongside the switch, so `updated: 0` is a normal successful
 * response. See `skippedReasons` for which of the three cases applies
 * (terminal, already in the target state, or switched on but not yet
 * delivering).
 *
 * A campaign created paused needs its campaign resumed as well: pair this
 * with PUT /v1/ads/campaigns/{campaignId}/status.
 *
 */
export const updateAdSetStatus = <ThrowOnError extends boolean = false>(options: OptionsLegacyParser<UpdateAdSetStatusData, ThrowOnError>) => {
    return (options?.client ?? client).put<UpdateAdSetStatusResponse, UpdateAdSetStatusError, ThrowOnError>({
        ...options,
        url: '/v1/ads/ad-sets/{adSetId}/status'
    });
};

/**
 * Get campaign tree
 * Returns a nested Campaign > Ad Set > Ad hierarchy with rolled-up metrics at each level.
 * Uses a two-stage aggregation: ads are grouped into ad sets, then ad sets into campaigns.
 * Metrics are computed over an optional date range, then rolled up from ad level to ad set
 * and campaign levels. Pagination is at the campaign level. Ads without a campaign or ad set
 * ID are grouped into synthetic "Ungrouped" buckets.
 * If no date range is provided, defaults to the last 90 days. Date range is capped at 730 days max.
 *
 * Pass `timeIncrement=1` to also get a daily breakdown: each node gains a `daily[]` array of
 * per-day metrics (same fields as the aggregated `metrics`) in the same call. Use `dailyLevel`
 * (`campaign` default, or `adset` / `ad`) to choose which levels carry the series. This replaces
 * calling the tree once per day for per-campaign daily trends.
 *
 * **Deleted objects stay in the tree.** Deleting an ad or a campaign is a soft delete: the Ad
 * documents move to `status: cancelled` and are kept indefinitely, so their historical spend
 * still counts toward the metrics of any date range they fall in. There is no pruning job and
 * no retention window. Filter on `status` if your view should hide them, but do that after
 * reading the totals, not before.
 *
 */
export const getAdTree = <ThrowOnError extends boolean = false>(options?: OptionsLegacyParser<GetAdTreeData, ThrowOnError>) => {
    return (options?.client ?? client).get<GetAdTreeResponse, GetAdTreeError, ThrowOnError>({
        ...options,
        url: '/v1/ads/tree'
    });
};

/**
 * Get daily account metrics
 * Returns daily aggregate metrics across all ads in a SocialAccount as a single
 * time series — one row per calendar day in the requested range. Use this for
 * dashboards that draw a daily-spend or daily-conversions chart, instead of
 * calling `/v1/ads/tree` once per day.
 *
 * `accountId` is required. The lookup is sibling-expanded so passing the `metaads`
 * ID also includes ads under the linked `facebook` / `instagram` posting account
 * (and vice-versa) — same convention as `/v1/ads/tree` and `/v1/ads`.
 *
 * Date range defaults to the last 90 days. Capped at 730 days. Ranges older
 * than the ingested history return a `202` immediately with the covered part
 * and `backfillPending: true` while the rest is backfilled in the background;
 * repeat the request shortly until it returns 200 with full data.
 *
 */
export const getAdsTimeline = <ThrowOnError extends boolean = false>(options: OptionsLegacyParser<GetAdsTimelineData, ThrowOnError>) => {
    return (options?.client ?? client).get<GetAdsTimelineResponse, GetAdsTimelineError, ThrowOnError>({
        ...options,
        url: '/v1/ads/timeline'
    });
};

/**
 * Get ad details
 * Returns an ad with its creative, targeting, status, and performance metrics.
 *
 * The `{adId}` path segment accepts any identifier dialect Zernio indexes for the ad:
 * - the Zernio internal `_id` (24-char hex)
 * - Meta's numeric `platformAdId` (the value shipped in `comment.received` webhooks as `comment.ad.id`)
 * - the creative's `effective_object_story_id` (`{pageId}_{postId}` shape, Facebook side)
 * - the creative's `effective_instagram_media_id` (Instagram side)
 *
 * Any of the four resolve to the same ad. Caller doesn't need a translation step.
 *
 */
export const getAd = <ThrowOnError extends boolean = false>(options: OptionsLegacyParser<GetAdData, ThrowOnError>) => {
    return (options?.client ?? client).get<GetAdResponse, GetAdError, ThrowOnError>({
        ...options,
        url: '/v1/ads/{adId}'
    });
};

/**
 * Update ad
 * Patch one or more fields on an ad. Status, budget, targeting, and creative changes
 * are propagated to the platform.
 *
 * Per-platform support:
 * - **Meta** (Facebook + Instagram): all fields supported.
 * - **TikTok**: status, budget, targeting (via `/v2/adgroup/update/`), and creative
 * (via `/v2/ad/update/` patch-style — `headline` is ignored, `body` becomes `ad_text`).
 * - **Google**: status, budget, and KEYWORD edits via `targeting.keywords` /
 * `targeting.negativeKeywords` — each list you send becomes the FULL new set of its
 * kind on the ad group (criteria not in the list are removed); a kind left out is
 * untouched. Any other `targeting` field returns 400: Google cannot mutate broad
 * targeting post-create without recreating the campaign. `creative` returns 501.
 * - **Pinterest / X / LinkedIn / OpenAI Ads**: status + budget only. Sending
 * `targeting` or `creative` returns 501 with code `unsupported_platform_operation`.
 * OpenAI Ads budget is lifetime-only (see `budget.type` below).
 *
 */
export const updateAd = <ThrowOnError extends boolean = false>(options: OptionsLegacyParser<UpdateAdData, ThrowOnError>) => {
    return (options?.client ?? client).put<UpdateAdResponse, UpdateAdError, ThrowOnError>({
        ...options,
        url: '/v1/ads/{adId}'
    });
};

/**
 * Cancel an ad
 * Cancels the ad on the platform and marks it as cancelled in the database. The ad is preserved for history. OpenAI Ads has no delete API; the ad is archived instead (a terminal state, the closest equivalent).
 */
export const deleteAd = <ThrowOnError extends boolean = false>(options: OptionsLegacyParser<DeleteAdData, ThrowOnError>) => {
    return (options?.client ?? client).delete<DeleteAdResponse, DeleteAdError, ThrowOnError>({
        ...options,
        url: '/v1/ads/{adId}'
    });
};

/**
 * Pause or resume a single ad
 * Ad-scoped pause/resume — touches ONLY this ad, never its parent ad set or
 * campaign (so sibling ads keep running). Thin wrapper over the `status`
 * field of PUT /v1/ads/{adId}, for callers that want a URL symmetric to
 * /v1/ads/campaigns/{campaignId}/status and /v1/ads/ad-sets/{adSetId}/status.
 *
 * `{adId}` accepts the same identifier dialects as GET/PUT /v1/ads/{adId}
 * (Zernio hex `_id`, Meta numeric `platformAdId`, or the creative's
 * effective story/media IDs). `platform` is inferred from the ad, so it's
 * not required in the body. Ads in terminal statuses (rejected, completed,
 * cancelled) and no-op flips (already in the target state) are skipped.
 *
 */
export const updateAdStatus = <ThrowOnError extends boolean = false>(options: OptionsLegacyParser<UpdateAdStatusData, ThrowOnError>) => {
    return (options?.client ?? client).put<UpdateAdStatusResponse, UpdateAdStatusError, ThrowOnError>({
        ...options,
        url: '/v1/ads/{adId}/status'
    });
};

/**
 * Get campaign analytics
 * Returns performance analytics for a whole campaign in one call: summary metrics, a daily
 * timeline over the requested date range (summed across the campaign's ads), and optional
 * demographic breakdowns. Breakdowns are fetched live from Meta at the campaign level (one call
 * per dimension, no per-ad fan-out), so an agency dashboard gets campaign-level age/gender/etc.
 * without summing thousands of per-ad reads. `campaignId` is the platform campaign id; pass
 * `platform` when a campaign id could be ambiguous across platforms. If no date range is provided,
 * defaults to the last 90 days. Date range is capped at 730 days max.
 *
 */
export const getCampaignAnalytics = <ThrowOnError extends boolean = false>(options: OptionsLegacyParser<GetCampaignAnalyticsData, ThrowOnError>) => {
    return (options?.client ?? client).get<GetCampaignAnalyticsResponse, GetCampaignAnalyticsError, ThrowOnError>({
        ...options,
        url: '/v1/ads/campaigns/{campaignId}/analytics'
    });
};

/**
 * Render pre-create ad previews
 * Renders how a creative would look per placement BEFORE any ad exists, via Meta's
 * `/generatepreviews`. Provide exactly one creative source: `existingCreativeId` or `creativeSpec`.
 * Each preview is an HTML `<iframe>` snippet embeddable directly. Unknown `formats` values
 * return Meta's 400 verbatim.
 *
 */
export const generateAdPreviews = <ThrowOnError extends boolean = false>(options: OptionsLegacyParser<GenerateAdPreviewsData, ThrowOnError>) => {
    return (options?.client ?? client).post<GenerateAdPreviewsResponse, GenerateAdPreviewsError, ThrowOnError>({
        ...options,
        url: '/v1/ads/preview'
    });
};

/**
 * Render previews of an existing ad
 * Renders an EXISTING ad per placement via Meta's `/{ad_id}/previews`. Each preview is an HTML
 * `<iframe>` snippet embeddable directly. Unknown `formats` values return Meta's 400 verbatim.
 *
 */
export const getAdPreviews = <ThrowOnError extends boolean = false>(options: OptionsLegacyParser<GetAdPreviewsData, ThrowOnError>) => {
    return (options?.client ?? client).get<GetAdPreviewsResponse, GetAdPreviewsError, ThrowOnError>({
        ...options,
        url: '/v1/ads/{adId}/preview'
    });
};

/**
 * Generate keyword ideas (Google Keyword Planner)
 * Google Ads only. Runs Keyword Planner's generateKeywordIdeas from seed keywords, a seed URL,
 * or both, returning idea rows verbatim (avgMonthlySearches, competition, competitionIndex,
 * top-of-page bid micros, monthlySearchVolumes). Counters are int64s encoded as strings; bid
 * values are micros of the account currency. Omitting `countries` targets worldwide.
 *
 */
export const generateKeywordIdeas = <ThrowOnError extends boolean = false>(options: OptionsLegacyParser<GenerateKeywordIdeasData, ThrowOnError>) => {
    return (options?.client ?? client).post<GenerateKeywordIdeasResponse, GenerateKeywordIdeasError, ThrowOnError>({
        ...options,
        url: '/v1/ads/keywords/ideas'
    });
};

/**
 * Historical keyword metrics (Google Keyword Planner)
 * Google Ads only. Runs Keyword Planner's generateKeywordHistoricalMetrics for up to 1,000
 * exact keywords: historical search volume, competition and top-of-page bid ranges, plus
 * averageCpcMicros when includeAverageCpc is set. Rows come back verbatim; counters are int64s
 * encoded as strings, bid/CPC values are micros of the account currency.
 *
 */
export const generateKeywordHistoricalMetrics = <ThrowOnError extends boolean = false>(options: OptionsLegacyParser<GenerateKeywordHistoricalMetricsData, ThrowOnError>) => {
    return (options?.client ?? client).post<GenerateKeywordHistoricalMetricsResponse, GenerateKeywordHistoricalMetricsError, ThrowOnError>({
        ...options,
        url: '/v1/ads/keywords/historical-metrics'
    });
};

/**
 * Flexible live insights query
 * Live, flexible insights query. The account's platform picks the contract:
 *
 * **Meta (facebook/instagram)**: forwards caller-chosen `fields`, `breakdowns` and `filtering`
 * to any Meta insights node and returns Meta's rows verbatim. `objectId` (required) selects the
 * node; `level` sets row granularity. Semantic validation is Meta's: an unknown field or invalid
 * breakdown combination returns a 400 carrying Meta's message. For long ranges or agency-scale
 * accounts prefer the async variant (POST /v1/ads/insights/reports).
 *
 * **Google Ads (googleads)**: raw GAQL passthrough. Send any read-only GAQL SELECT via `query`
 * (campaign/keyword/search-term/geo/demographic/asset/shopping resources, `change_event`, any
 * `segments.*`) and rows come back verbatim (camelCase, counters as strings). Results are paged
 * at a fixed 10,000 rows; follow `paging.nextPageToken` with `pageToken`. `customerId` is only
 * needed when the connection has several Google Ads accounts. Semantic validation is Google's:
 * an invalid query returns a 400 carrying Google's message (note: selecting `segments.date`
 * requires a finite date filter).
 *
 */
export const queryAdInsights = <ThrowOnError extends boolean = false>(options: OptionsLegacyParser<QueryAdInsightsData, ThrowOnError>) => {
    return (options?.client ?? client).get<QueryAdInsightsResponse, QueryAdInsightsError, ThrowOnError>({
        ...options,
        url: '/v1/ads/insights'
    });
};

/**
 * Submit an async insights report run
 * Submits an asynchronous Meta insights report. Same query surface as GET /v1/ads/insights, but
 * in the JSON body; Meta processes the report server-side, which is the right choice for long
 * ranges or large accounts where the sync query is slow or rate-limited. Returns a `reportRunId`
 * to poll via GET /v1/ads/insights/reports/{reportRunId}.
 *
 */
export const createAdInsightsReport = <ThrowOnError extends boolean = false>(options: OptionsLegacyParser<CreateAdInsightsReportData, ThrowOnError>) => {
    return (options?.client ?? client).post<CreateAdInsightsReportResponse, CreateAdInsightsReportError, ThrowOnError>({
        ...options,
        url: '/v1/ads/insights/reports'
    });
};

/**
 * Poll an async insights report run
 * Status and results for a report run created via POST /v1/ads/insights/reports. While the job
 * runs, returns `status` and `percentCompletion`. Once `status` is "Job Completed" the response
 * also carries a `data` page, cursor-paginated via `limit` / `after`.
 *
 */
export const getAdInsightsReport = <ThrowOnError extends boolean = false>(options: OptionsLegacyParser<GetAdInsightsReportData, ThrowOnError>) => {
    return (options?.client ?? client).get<GetAdInsightsReportResponse, GetAdInsightsReportError, ThrowOnError>({
        ...options,
        url: '/v1/ads/insights/reports/{reportRunId}'
    });
};

/**
 * Get ad analytics
 * Returns detailed performance analytics for an ad. Includes summary metrics, a daily timeline
 * over the requested date range, and optional demographic breakdowns (Meta and TikTok only).
 * If no date range is provided, defaults to the last 90 days. Date range is capped at 730 days max.
 *
 */
export const getAdAnalytics = <ThrowOnError extends boolean = false>(options: OptionsLegacyParser<GetAdAnalyticsData, ThrowOnError>) => {
    return (options?.client ?? client).get<GetAdAnalyticsResponse, GetAdAnalyticsError, ThrowOnError>({
        ...options,
        url: '/v1/ads/{adId}/analytics'
    });
};

/**
 * Get ad tracking tags
 * Unified read of the platform's native click-URL tracking params.
 * - Meta (facebook/instagram): the creative's `url_tags` (and template_url_spec).
 * - Google (googleads): the campaign's `trackingUrlTemplate` + `finalUrlSuffix`.
 * Subject to the Google Ads API access-tier daily quota; bulk audits need Standard access.
 * - LinkedIn (linkedinads): the campaign's Dynamic UTM `dynamicValueParameters` + `customValueParameters`.
 * Returns 405 for platforms without a click-URL tracking surface (TikTok, X, Pinterest).
 *
 * **Not pixels.** Despite the shared path segment, this endpoint has nothing to do with
 * measurement tags. For an ad account's pixels use
 * `GET /v1/accounts/{accountId}/tracking-tags?adAccountId=act_...` (Meta Pixels, with `kind`
 * and `ownerAdAccountId`) or `GET /v1/accounts/{accountId}/conversion-destinations`.
 *
 */
export const getAdTrackingTags = <ThrowOnError extends boolean = false>(options: OptionsLegacyParser<GetAdTrackingTagsData, ThrowOnError>) => {
    return (options?.client ?? client).get<GetAdTrackingTagsResponse, GetAdTrackingTagsError, ThrowOnError>({
        ...options,
        url: '/v1/ads/{adId}/tracking-tags'
    });
};

/**
 * Set ad tracking tags
 * Unified update. Send only the fields for the ad's platform:
 * - Meta: `urlTags` (array of {key,value}). Meta creatives are immutable, so this rebuilds the
 * creative and repoints the ad. By DEFAULT we PRESERVE the existing creative verbatim
 * (re-post its object_story_spec + the new url_tags, reusing the image), so you send `urlTags`
 * ALONE — no need to read back headline/body/CTA. `creative` (headline, body, callToAction,
 * linkUrl, imageUrl) is OPTIONAL and only needed to rebuild explicitly, or for SHARE / page-post
 * / dark / asset_feed creatives whose object_story_spec Meta strips (those return 422 asking for
 * `creative`).
 * - Google: `trackingUrlTemplate` and/or `finalUrlSuffix` (full template strings; account quota applies).
 * - LinkedIn: `dynamicValueParameters` and/or `customValueParameters` (campaign-level Dynamic UTM).
 *
 */
export const updateAdTrackingTags = <ThrowOnError extends boolean = false>(options: OptionsLegacyParser<UpdateAdTrackingTagsData, ThrowOnError>) => {
    return (options?.client ?? client).patch<UpdateAdTrackingTagsResponse, UpdateAdTrackingTagsError, ThrowOnError>({
        ...options,
        url: '/v1/ads/{adId}/tracking-tags'
    });
};

/**
 * List comments on an ad
 * Returns comments on an ad's underlying creative post. Useful for moderating or analyzing
 * engagement on dark posts (ad creatives that never went live organically), which the
 * regular GET /v1/inbox/comments/{postId} endpoint cannot serve because dark posts are
 * not in Zernio's post database.
 *
 * An ad that runs on both Facebook feed and Instagram feed has two separate underlying
 * posts with separate comment threads (the creative's effective_object_story_id and
 * effective_instagram_media_id). Use the `placement` query param to pick one; with no
 * param the Instagram side is returned when it exists, otherwise Facebook. The
 * identifiers are read from the ad record (persisted during sync) with a Marketing-API
 * fallback for ads that predate the field.
 *
 * For Instagram-placed comments, the Instagram account that runs the ad must be connected
 * to Zernio — those comments are read through that account's token. If no connected
 * Instagram account on the profile can read the ad's media, the call returns
 * ads_connection_required (the Facebook side, if any, is still readable via ?placement=facebook).
 *
 * Meta-only. Other ad platforms (TikTok, LinkedIn, Pinterest, Google, X) do not
 * expose a public per-ad comments API and return feature_not_available.
 *
 * Requires the Ads add-on. Response shape matches GET /v1/inbox/comments/{postId}.
 *
 * The `{adId}` path segment accepts any identifier dialect Zernio indexes for the ad:
 * Zernio internal `_id` (24-char hex), Meta's numeric `platformAdId` (the value shipped in
 * `comment.received` webhooks as `comment.ad.id`), or the creative's
 * `effective_object_story_id` / `effective_instagram_media_id`. Caller doesn't need a
 * translation step.
 *
 */
export const getAdComments = <ThrowOnError extends boolean = false>(options: OptionsLegacyParser<GetAdCommentsData, ThrowOnError>) => {
    return (options?.client ?? client).get<GetAdCommentsResponse, GetAdCommentsError, ThrowOnError>({
        ...options,
        url: '/v1/ads/{adId}/comments'
    });
};

/**
 * List TikTok Business Centers
 * Returns the TikTok Business Centers (BCs) the connected `tiktokads` account can read.
 * Each BC reports its advertiser count so callers can build agency-style pickers
 * without re-walking `/v1/ads/accounts` per BC.
 *
 * TikTok-only. Solo advertisers (non-agency tokens) return an empty array.
 *
 */
export const listAdsBusinessCenters = <ThrowOnError extends boolean = false>(options: OptionsLegacyParser<ListAdsBusinessCentersData, ThrowOnError>) => {
    return (options?.client ?? client).get<ListAdsBusinessCentersResponse, ListAdsBusinessCentersError, ThrowOnError>({
        ...options,
        url: '/v1/ads/business-centers'
    });
};

/**
 * Ad account change / audit log
 * Account-level audit log from Meta's `/act_X/activities`: who changed what and when
 * (creates, edits, status flips, budget changes...) with Meta's translated event names and
 * the structured before/after in `extra_data`. Rows are returned verbatim. Meta has no
 * server-side per-object filter on this edge, so `objectId` filters the returned page
 * client-side (combine with paging to walk history for one campaign/ad set/ad).
 */
export const getAdsActivityLog = <ThrowOnError extends boolean = false>(options: OptionsLegacyParser<GetAdsActivityLogData, ThrowOnError>) => {
    return (options?.client ?? client).get<GetAdsActivityLogResponse, GetAdsActivityLogError, ThrowOnError>({
        ...options,
        url: '/v1/ads/activity'
    });
};

/**
 * Create a Reach & Frequency prediction
 * Creates an R&F prediction — a QUOTE, nothing is bought and no ad entities are created.
 * Provide a date range plus exactly one of `budgetAmount` (Meta predicts reach) or `reach`
 * (Meta predicts the budget). The response carries the estimate and its allowed bounds
 * (min/max budget and reach). Predictions expire on their own; to buy, reserve one via
 * POST /v1/ads/rf-predictions/{predictionId}/reserve and pass the RESERVED id to
 * POST /v1/ads/create with `buyingType: "RESERVED"`.
 *
 * Reservation campaigns reject automatic placements. Top-level `placements` wins; when it is
 * omitted, `targeting.placements` is used; when neither is set, placements default to
 * Facebook feed (+ Instagram stream when a linked IG professional account resolves).
 * Instagram placements require that IG account.
 */
export const createRfPrediction = <ThrowOnError extends boolean = false>(options: OptionsLegacyParser<CreateRfPredictionData, ThrowOnError>) => {
    return (options?.client ?? client).post<CreateRfPredictionResponse, CreateRfPredictionError, ThrowOnError>({
        ...options,
        url: '/v1/ads/rf-predictions'
    });
};

/**
 * Read a Reach & Frequency prediction
 */
export const getRfPrediction = <ThrowOnError extends boolean = false>(options: OptionsLegacyParser<GetRfPredictionData, ThrowOnError>) => {
    return (options?.client ?? client).get<GetRfPredictionResponse, GetRfPredictionError, ThrowOnError>({
        ...options,
        url: '/v1/ads/rf-predictions/{predictionId}'
    });
};

/**
 * Cancel a Reach & Frequency reservation
 * Releases a RESERVATION's locked price and inventory. Unreserved predictions expire on their own.
 */
export const cancelRfReservation = <ThrowOnError extends boolean = false>(options: OptionsLegacyParser<CancelRfReservationData, ThrowOnError>) => {
    return (options?.client ?? client).delete<CancelRfReservationResponse, CancelRfReservationError, ThrowOnError>({
        ...options,
        url: '/v1/ads/rf-predictions/{predictionId}'
    });
};

/**
 * Reserve a Reach & Frequency prediction
 * Locks the quoted price + inventory until the returned `expiresAt` and mints a NEW
 * prediction id — pass that RESERVED id (not the original) as `rfPredictionId` on
 * POST /v1/ads/create. Release an unused reservation via DELETE.
 */
export const reserveRfPrediction = <ThrowOnError extends boolean = false>(options: OptionsLegacyParser<ReserveRfPredictionData, ThrowOnError>) => {
    return (options?.client ?? client).post<ReserveRfPredictionResponse, ReserveRfPredictionError, ThrowOnError>({
        ...options,
        url: '/v1/ads/rf-predictions/{predictionId}/reserve'
    });
};

/**
 * A/B tests and lift studies
 * Lists the ad account's A/B tests and lift studies (Meta's `/act_X/ad_studies`), rows
 * returned verbatim. The default projection covers id, name, type, timing and cells with
 * split percentages; `fields` is a raw-passthrough override.
 */
export const listAdStudies = <ThrowOnError extends boolean = false>(options: OptionsLegacyParser<ListAdStudiesData, ThrowOnError>) => {
    return (options?.client ?? client).get<ListAdStudiesResponse, ListAdStudiesError, ThrowOnError>({
        ...options,
        url: '/v1/ads/studies'
    });
};

/**
 * Businesses list
 * Business Manager portfolios the connected Meta user belongs to (Meta's `/me/businesses`),
 * rows returned verbatim (id, name, verification_status, created_time). Token-scoped, so no
 * `adAccountId` is needed. For TikTok Business Centers use
 * `GET /v1/ads/business-centers`.
 */
export const listMetaBusinesses = <ThrowOnError extends boolean = false>(options: OptionsLegacyParser<ListMetaBusinessesData, ThrowOnError>) => {
    return (options?.client ?? client).get<ListMetaBusinessesResponse, ListMetaBusinessesError, ThrowOnError>({
        ...options,
        url: '/v1/ads/businesses'
    });
};

/**
 * Ad labels
 * Lists the ad account's organizational labels (Meta's `/act_X/adlabels`), rows returned
 * verbatim (id, name, created/updated time).
 */
export const listAdLabels = <ThrowOnError extends boolean = false>(options: OptionsLegacyParser<ListAdLabelsData, ThrowOnError>) => {
    return (options?.client ?? client).get<ListAdLabelsResponse, ListAdLabelsError, ThrowOnError>({
        ...options,
        url: '/v1/ads/labels'
    });
};

/**
 * High demand periods / budget schedules
 * Scheduled budget increases (Meta's budget-scheduling API). The Graph edge lives on the
 * campaign and ad-set nodes only, so exactly one of `campaignId` / `adSetId` (platform
 * ids) is required. Rows returned verbatim (budget_value, budget_value_type, time window,
 * recurrence).
 */
export const listHighDemandPeriods = <ThrowOnError extends boolean = false>(options: OptionsLegacyParser<ListHighDemandPeriodsData, ThrowOnError>) => {
    return (options?.client ?? client).get<ListHighDemandPeriodsResponse, ListHighDemandPeriodsError, ThrowOnError>({
        ...options,
        url: '/v1/ads/high-demand-periods'
    });
};

/**
 * Schedule a budget increase
 * Pre-schedule a temporary budget increase (Black Friday, a launch, a sale) instead of
 * editing the budget by hand on the day. Same target rule as the GET: exactly one of
 * `campaignId` / `adSetId`.
 *
 * Two Meta constraints worth knowing before you call it. `timeStart` / `timeEnd` must
 * fall on a 15-minute boundary, and a campaign cannot mix `ABSOLUTE` and `MULTIPLIER`
 * across its schedules — the second type is rejected with "Can't mix your budget scaling
 * selection". Window rules (must sit inside the campaign's run dates, minimum lead time,
 * no overlap) are Meta's and its message is forwarded verbatim.
 */
export const createHighDemandPeriod = <ThrowOnError extends boolean = false>(options: OptionsLegacyParser<CreateHighDemandPeriodData, ThrowOnError>) => {
    return (options?.client ?? client).post<CreateHighDemandPeriodResponse, CreateHighDemandPeriodError, ThrowOnError>({
        ...options,
        url: '/v1/ads/high-demand-periods'
    });
};

/**
 * Creative library
 * Lists the ad account's creative library (Meta's `/act_X/adcreatives`), rows returned
 * verbatim. The default projection covers id, name, status, object type, thumbnail,
 * object_story_spec / asset_feed_spec and url_tags; `fields` is a raw-passthrough
 * override. Any creative id here is reusable on the create endpoints via
 * `existingCreativeId`.
 */
export const listAdCreatives = <ThrowOnError extends boolean = false>(options: OptionsLegacyParser<ListAdCreativesData, ThrowOnError>) => {
    return (options?.client ?? client).get<ListAdCreativesResponse, ListAdCreativesError, ThrowOnError>({
        ...options,
        url: '/v1/ads/creatives'
    });
};

/**
 * Create a standalone creative
 * Creates a creative in the library WITHOUT an ad, reusable on the create endpoints via
 * `existingCreativeId`. Provide exactly one of `imageUrl` (uploaded server-side),
 * `imageHash` (from POST /v1/ads/images or the library list), or `carouselCards` (2-10
 * hand-built cards). The Page (and linked Instagram account, when present) is resolved
 * from `accountId` as the story actor.
 */
export const createAdCreative = <ThrowOnError extends boolean = false>(options: OptionsLegacyParser<CreateAdCreativeData, ThrowOnError>) => {
    return (options?.client ?? client).post<CreateAdCreativeResponse, CreateAdCreativeError, ThrowOnError>({
        ...options,
        url: '/v1/ads/creatives'
    });
};

/**
 * Creative details
 * One creative's details, verbatim from Meta. `fields` is a raw-passthrough override of
 * the default projection.
 */
export const getAdCreative = <ThrowOnError extends boolean = false>(options: OptionsLegacyParser<GetAdCreativeData, ThrowOnError>) => {
    return (options?.client ?? client).get<GetAdCreativeResponse, GetAdCreativeError, ThrowOnError>({
        ...options,
        url: '/v1/ads/creatives/{creativeId}'
    });
};

/**
 * Rename a creative
 * Renames a creative. Creatives are immutable on Meta beyond `name` — for content changes
 * create a new creative (POST /v1/ads/creatives) and swap it onto the ad
 * (PUT /v1/ads/{adId} with `creative`).
 */
export const updateAdCreative = <ThrowOnError extends boolean = false>(options: OptionsLegacyParser<UpdateAdCreativeData, ThrowOnError>) => {
    return (options?.client ?? client).put<UpdateAdCreativeResponse, UpdateAdCreativeError, ThrowOnError>({
        ...options,
        url: '/v1/ads/creatives/{creativeId}'
    });
};

/**
 * Delete a creative
 * Deletes a creative from the library. Meta only allows deleting creatives not referenced
 * by any ad — otherwise its 400 surfaces verbatim.
 */
export const deleteAdCreative = <ThrowOnError extends boolean = false>(options: OptionsLegacyParser<DeleteAdCreativeData, ThrowOnError>) => {
    return (options?.client ?? client).delete<DeleteAdCreativeResponse, DeleteAdCreativeError, ThrowOnError>({
        ...options,
        url: '/v1/ads/creatives/{creativeId}'
    });
};

/**
 * List value rule sets
 * Lists the ad account's value rule sets (Meta's `/act_X/value_rule_set`). A value rule
 * set adjusts the auction bid up or down for audience segments you value differently;
 * attach one to an ad set with `valueRuleSetId` on `POST /v1/ads/create` or
 * `PUT /v1/ads/ad-sets/{adSetId}`.
 *
 * Rows are returned in the same camelCase shape the `PUT` body takes, ids included, so a
 * set round-trips 1:1: **the update is a full replace, not a patch**, so you GET, mutate
 * and send the whole thing back.
 *
 * Limits: 6 rule sets per ad account, 10 rules per set, 4 criteria per rule.
 *
 * **Rule order is semantic.** Rules are evaluated in array order and only the FIRST
 * matching rule adjusts the bid for an overlapping audience. The order you send is the
 * order that is stored and returned.
 *
 * Eligibility: value rule sets apply only to ad sets on the `LOWEST_COST_WITHOUT_CAP`
 * (auto-bid) or `COST_CAP` bid strategies. Meta rejects the rest server-side.
 */
export const listValueRuleSets = <ThrowOnError extends boolean = false>(options: OptionsLegacyParser<ListValueRuleSetsData, ThrowOnError>) => {
    return (options?.client ?? client).get<ListValueRuleSetsResponse, ListValueRuleSetsError, ThrowOnError>({
        ...options,
        url: '/v1/ads/value-rule-sets'
    });
};

/**
 * Create a value rule set
 * Creates a value rule set on the ad account (Meta's `POST /act_X/value_rule_set`).
 * Attach the returned id to an ad set with `valueRuleSetId` on `POST /v1/ads/create` or
 * `PUT /v1/ads/ad-sets/{adSetId}`.
 *
 * **Rule order is semantic**: rules are evaluated in array order and only the first
 * matching rule adjusts the bid for an overlapping audience.
 *
 * `adjustValue` is an unsigned magnitude in percent; the direction lives in `adjustSign`.
 * `INCREASE` accepts 1-1000, `DECREASE` accepts 1-90. There is no signed field and 0 is
 * out of range.
 *
 * `criteriaValueTypes` is positionally paired with `criteriaValues` (same length, same
 * order). Every type is the literal `"NONE"` except on `LOCATION`, which uses
 * `LOCATION_COUNTRY` / `LOCATION_REGION` / `LOCATION_CITY` / `LOCATION_COMSCORE_MARKET`
 * and may mix them within one criterion. Location values are Targeting-Search keys: a
 * two-letter country code for `LOCATION_COUNTRY`, a numeric key for the rest.
 *
 * `LOCATION_DMA` was replaced by `LOCATION_COMSCORE_MARKET` on 2026-06-22 and rules using
 * DMAs are no longer active, so this API rejects it.
 *
 * `AUDIENCE_LABEL` values (e.g. `HIGH_VALUE`) are applied to a Custom Audience in Ads
 * Manager. There is no API to provision them, so label strings are passed through
 * unvalidated and a typo produces a rule that never fires.
 *
 * Ads Manager turns a rule set read-only (this API stays editable) when a rule uses more
 * than 2 criteria, a custom age range, or the placements `FB_MARKETPLACE`, `FB_SEARCH`,
 * `FB_VIDEO` or `IG_EXPLORE`.
 *
 * Limits: 6 rule sets per ad account, 10 rules per set, 4 criteria per rule. The
 * per-account cap is enforced by Meta, not here.
 */
export const createValueRuleSet = <ThrowOnError extends boolean = false>(options: OptionsLegacyParser<CreateValueRuleSetData, ThrowOnError>) => {
    return (options?.client ?? client).post<CreateValueRuleSetResponse, CreateValueRuleSetError, ThrowOnError>({
        ...options,
        url: '/v1/ads/value-rule-sets'
    });
};

/**
 * Read a value rule set
 * Reads one value rule set including every nested rule id and criterion id. This is step
 * one of any edit: `PUT` is a full replace, so you need the ids before you can keep the
 * objects you are not changing.
 *
 * Meta's own read returns `GENDER` values lowercase (`"male"`) while writes require
 * `"MALE"`. Values are passed through untouched, so never case-compare a stored rule
 * against a fetched one.
 */
export const getValueRuleSet = <ThrowOnError extends boolean = false>(options: OptionsLegacyParser<GetValueRuleSetData, ThrowOnError>) => {
    return (options?.client ?? client).get<GetValueRuleSetResponse, GetValueRuleSetError, ThrowOnError>({
        ...options,
        url: '/v1/ads/value-rule-sets/{valueRuleSetId}'
    });
};

/**
 * Replace a value rule set
 * **THIS IS A FULL REPLACE, NOT A PATCH.** Meta's update is declarative: the body you
 * send becomes the rule set.
 *
 * - `GET /v1/ads/value-rule-sets/{valueRuleSetId}` FIRST.
 * - Keep a rule or criterion by echoing its `id`.
 * - Create one by including the object WITHOUT an `id`.
 * - Delete one by OMITTING it from the array. There is no warning and no undo.
 *
 * `name` and `rules` are both required for exactly this reason: a partial body would
 * silently destroy every rule left out.
 *
 * **Rule order is semantic**: the array order you send is the evaluation order, and only
 * the first matching rule adjusts the bid for an overlapping audience.
 *
 * Existing rule sets created elsewhere may contain `LOCATION_DMA` criteria. Those went
 * inert on 2026-06-22 and are rejected here; migrate them to `LOCATION_COMSCORE_MARKET`.
 */
export const updateValueRuleSet = <ThrowOnError extends boolean = false>(options: OptionsLegacyParser<UpdateValueRuleSetData, ThrowOnError>) => {
    return (options?.client ?? client).put<UpdateValueRuleSetResponse, UpdateValueRuleSetError, ThrowOnError>({
        ...options,
        url: '/v1/ads/value-rule-sets/{valueRuleSetId}'
    });
};

/**
 * Delete a value rule set
 * Deletes the rule set (Meta's `POST /{value-rule-set-id}/delete_rule_set`, a custom
 * action edge rather than an HTTP DELETE on its side). Ad sets pointing at it are not
 * modified here; detach them first with `valueRulesApplied: false` on
 * `PUT /v1/ads/ad-sets/{adSetId}`.
 */
export const deleteValueRuleSet = <ThrowOnError extends boolean = false>(options: OptionsLegacyParser<DeleteValueRuleSetData, ThrowOnError>) => {
    return (options?.client ?? client).delete<DeleteValueRuleSetResponse, DeleteValueRuleSetError, ThrowOnError>({
        ...options,
        url: '/v1/ads/value-rule-sets/{valueRuleSetId}'
    });
};

/**
 * Ad account finances
 * Finances of one Meta ad account: prepaid `balance`, lifetime `amountSpent`, account
 * `spendCap` (null = no cap) and the `fundingSource`. Money values are converted from
 * Meta's minor units to whole units of `currency`.
 */
export const getAdAccountFinance = <ThrowOnError extends boolean = false>(options: OptionsLegacyParser<GetAdAccountFinanceData, ThrowOnError>) => {
    return (options?.client ?? client).get<GetAdAccountFinanceResponse, GetAdAccountFinanceError, ThrowOnError>({
        ...options,
        url: '/v1/ads/accounts/finance'
    });
};

/**
 * List ad accounts
 * Returns the platform ad accounts available for the given social account (e.g. Meta ad
 * accounts, TikTok advertiser IDs, Google Ads customer IDs).
 *
 * For TikTok agencies: enumerates every advertiser under every Business Center the token
 * can read (paginated server-side), then chunks the lookup against TikTok's
 * `/advertiser/info/` endpoint (which has a per-call cap of ≤100 IDs). Solo advertisers
 * without a BC fall back to the OAuth-time `advertiser_ids` list. Cached for 1h on the
 * SocialAccount; lazy-refreshed on first call after expiry.
 *
 * For Google Ads: responds `429` when Google's API quota is temporarily exhausted
 * (instead of an empty list). Retry after a delay.
 *
 */
export const listAdAccounts = <ThrowOnError extends boolean = false>(options: OptionsLegacyParser<ListAdAccountsData, ThrowOnError>) => {
    return (options?.client ?? client).get<ListAdAccountsResponse, ListAdAccountsError, ThrowOnError>({
        ...options,
        url: '/v1/ads/accounts'
    });
};

/**
 * Update ad account settings
 * Sets the default DSA beneficiary and payor on a Meta ad account (EU DSA, Article 26).
 * Set them once and every EU-targeted call to `/v1/ads/create`, `/v1/ads/boost` and
 * `/v1/ads/ctwa` on that ad account can omit `dsaBeneficiary`/`dsaPayor`: Meta applies
 * the defaults automatically.
 *
 * The values are written to the ad account on Meta, the same setting Ads Manager edits.
 * Nothing is stored in Zernio, and defaults already set in Ads Manager work identically.
 * Zernio never guesses these values for you. Beneficiary and payor are legal disclosures
 * shown to EU users, so you must provide the entity names explicitly. Use
 * `GET /v1/ads/dsa-recommendations` to offer suggestions in your UI.
 *
 * If `defaultDsaPayor` is omitted, the beneficiary is also set as the payor, which
 * covers the common case where the same entity benefits from and pays for the ads.
 * Read the current values back with `GET /v1/ads/dsa-defaults`.
 *
 * Currently supported for Meta accounts only; other platforms return 400.
 *
 */
export const updateAdAccount = <ThrowOnError extends boolean = false>(options: OptionsLegacyParser<UpdateAdAccountData, ThrowOnError>) => {
    return (options?.client ?? client).patch<UpdateAdAccountResponse, UpdateAdAccountError, ThrowOnError>({
        ...options,
        url: '/v1/ads/accounts'
    });
};

/**
 * Get ad account DSA defaults
 * Returns the default DSA beneficiary and payor currently set on a Meta ad account,
 * whether they were set via `PATCH /v1/ads/accounts` or in Meta Ads Manager. Fields
 * are omitted when no default is configured. Meta accounts only.
 *
 */
export const getDsaDefaults = <ThrowOnError extends boolean = false>(options: OptionsLegacyParser<GetDsaDefaultsData, ThrowOnError>) => {
    return (options?.client ?? client).get<GetDsaDefaultsResponse, GetDsaDefaultsError, ThrowOnError>({
        ...options,
        url: '/v1/ads/dsa-defaults'
    });
};

/**
 * List DSA beneficiary/payor suggestions
 * Returns Meta's suggested beneficiary/payor names for an ad account, derived by Meta
 * from the account's recent activity. Useful for prefilling `dsaBeneficiary`/`dsaPayor`
 * inputs, or the defaults sent to `PATCH /v1/ads/accounts`, in your own UI.
 *
 * Meta returns a single flat list. Entries are not labeled as beneficiary or payor,
 * and since these are legal disclosures Zernio never applies them automatically: let
 * your user pick the right entity. The list may be empty for accounts with little
 * activity. Meta accounts only.
 *
 */
export const getDsaRecommendations = <ThrowOnError extends boolean = false>(options: OptionsLegacyParser<GetDsaRecommendationsData, ThrowOnError>) => {
    return (options?.client ?? client).get<GetDsaRecommendationsResponse, GetDsaRecommendationsError, ThrowOnError>({
        ...options,
        url: '/v1/ads/dsa-recommendations'
    });
};

/**
 * Boost post as ad
 * Creates a paid ad from an existing published post, keeping the post's
 * engagement. By default it provisions the whole hierarchy (campaign, ad
 * set, ad).
 *
 * **Attach shape (Meta).** Send `adSetId` to put the ad under an EXISTING
 * ad set instead, so that ad set keeps its learning phase. It then owns
 * `budget`, `schedule` and `targeting`, and sending any of those alongside
 * `adSetId` is a 400 rather than a silent drop. `budget` is required only
 * without `adSetId`.
 *
 * `instagramAccountId`, `destinationType` and `adSetId` are Meta-only and
 * return 400 on other platforms.
 *
 * **Retries.** Boosts are NOT idempotent and can take minutes when Meta requires re-hosting an
 * Instagram video, so do not retry on client timeout. Send an
 * Idempotency-Key header to make retries safe: same key and body replays
 * the original 201, and distinct keys always create distinct ads.
 * Without the header, an identical request is treated as a retry: while
 * one is in flight it returns 409, and within 10 minutes of a completed
 * boost it returns the already-created ad instead of creating another.
 * To intentionally duplicate an ad, send distinct Idempotency-Keys (or
 * vary the body, e.g. the name).
 *
 */
export const boostPost = <ThrowOnError extends boolean = false>(options: OptionsLegacyParser<BoostPostData, ThrowOnError>) => {
    return (options?.client ?? client).post<BoostPostResponse, BoostPostError, ThrowOnError>({
        ...options,
        url: '/v1/ads/boost'
    });
};

/**
 * Create standalone ad
 * Creates a paid ad with custom creative across Meta, Google Ads, Pinterest, TikTok, X/Twitter, LinkedIn, and OpenAI Ads (ChatGPT Ads). Supports three mutually-exclusive request shapes selected by the body, a legacy single-creative shape (all platforms, default), a Meta-only multi-creative shape via the creatives array (one ad set with N ads sharing budget and targeting), and an attach shape via adSetId that adds one new ad to an existing ad set, inheriting its budget, targeting, and schedule (Meta, TikTok, and LinkedIn; on LinkedIn adSetId is the existing Campaign id, and the budget, schedule, targeting and bidding fields must be omitted). Per-platform required fields, budget minimums, and video-ad rules are documented on each property below. LinkedIn creates a Single Image or Single Video Ad backed by a Direct Sponsored Content "dark post" authored by a Company Page (see `organizationId`); supported goals are engagement, traffic, awareness, and video_views (video ads use the `video` field; video_views requires a video), and traffic ads require `linkUrl`.
 *
 * **Idempotency:** this endpoint is not idempotent at the platform level (a blind retry creates a second campaign/ad set/ad). Send an `Idempotency-Key` header to make retries safe: the first request with a given key creates the ad and we store the response; a retry with the same key replays that exact response (with `Idempotent-Replayed: true`) instead of creating duplicates. Reusing a key with a different body returns 422; a key whose first request is still in flight returns 409 (retry after a short backoff). Keys are scoped to your credential and expire after 24h.
 */
export const createStandaloneAd = <ThrowOnError extends boolean = false>(options: OptionsLegacyParser<CreateStandaloneAdData, ThrowOnError>) => {
    return (options?.client ?? client).post<CreateStandaloneAdResponse, CreateStandaloneAdError, ThrowOnError>({
        ...options,
        url: '/v1/ads/create'
    });
};

/**
 * List submitted leads
 * Returns submitted Lead Gen leads for your team, newest-first, with keyset pagination on `cursor`. For Meta (default) leads are served from the persisted cache, ingested in real time from the `leadgen` webhook. When `accountId` is a LinkedIn ads account, leads are fetched live from LinkedIn's `leadFormResponses` (LinkedIn has no webhook and enforces 90-day retention, so nothing is persisted) and `adAccountId` is required. Reading LinkedIn responses needs the `r_marketing_leadgen_automation` permission; accounts connected before it was added must reconnect. Requires the Ads add-on.
 *
 */
export const listLeads = <ThrowOnError extends boolean = false>(options?: OptionsLegacyParser<ListLeadsData, ThrowOnError>) => {
    return (options?.client ?? client).get<ListLeadsResponse, ListLeadsError, ThrowOnError>({
        ...options,
        url: '/v1/ads/leads'
    });
};

/**
 * List lead forms
 * Lists the Lead Gen forms owned by the account. Meta: forms on the connected Facebook Page. LinkedIn: forms owned by the ad account's Company Page — pass `adAccountId` (LinkedIn forms are org-owned). Requires the Ads add-on.
 *
 */
export const listLeadForms = <ThrowOnError extends boolean = false>(options: OptionsLegacyParser<ListLeadFormsData, ThrowOnError>) => {
    return (options?.client ?? client).get<ListLeadFormsResponse, ListLeadFormsError, ThrowOnError>({
        ...options,
        url: '/v1/ads/lead-forms'
    });
};

/**
 * Create a lead form
 * Creates a Lead Gen form. The form content goes inside `platformSpecificData` for both platforms (the shape is selected by the accountId's platform). Meta: created on the connected Facebook Page (POST /{page-id}/leadgen_forms); the old top-level Meta fields (questions, thankYou*, contextCard, …) are DEPRECATED but still accepted while platformSpecificData is absent — mixing both shapes is a 400. LinkedIn: created on the ad account's Company Page. NOT idempotent — a retry creates a second form. Meta prefilled question types (EMAIL, PHONE, FULL_NAME, …) must omit label/key; CUSTOM questions require both. LinkedIn exposes only free-text and multiple-choice questions via API (prefilled-from-profile fields are Campaign Manager UI-only). Requires the Ads add-on.
 *
 */
export const createLeadForm = <ThrowOnError extends boolean = false>(options: OptionsLegacyParser<CreateLeadFormData, ThrowOnError>) => {
    return (options?.client ?? client).post<CreateLeadFormResponse, CreateLeadFormError, ThrowOnError>({
        ...options,
        url: '/v1/ads/lead-forms'
    });
};

/**
 * Get a lead form
 */
export const getLeadForm = <ThrowOnError extends boolean = false>(options: OptionsLegacyParser<GetLeadFormData, ThrowOnError>) => {
    return (options?.client ?? client).get<GetLeadFormResponse, GetLeadFormError, ThrowOnError>({
        ...options,
        url: '/v1/ads/lead-forms/{formId}'
    });
};

/**
 * Archive a lead form
 * Neither platform hard-deletes a form; this archives it (Meta status=ARCHIVED; LinkedIn state=ARCHIVED via PARTIAL_UPDATE).
 */
export const archiveLeadForm = <ThrowOnError extends boolean = false>(options: OptionsLegacyParser<ArchiveLeadFormData, ThrowOnError>) => {
    return (options?.client ?? client).delete<ArchiveLeadFormResponse, ArchiveLeadFormError, ThrowOnError>({
        ...options,
        url: '/v1/ads/lead-forms/{formId}'
    });
};

/**
 * List leads for a single form
 * Returns leads for one form. Serves persisted leads (ingested via the leadgen webhook) when available, falling back to a live Graph read.
 *
 */
export const listFormLeads = <ThrowOnError extends boolean = false>(options: OptionsLegacyParser<ListFormLeadsData, ThrowOnError>) => {
    return (options?.client ?? client).get<ListFormLeadsResponse, ListFormLeadsError, ThrowOnError>({
        ...options,
        url: '/v1/ads/lead-forms/{formId}/leads'
    });
};

/**
 * Create a test lead
 * Submits a test lead against the form (POST /{form-id}/test_leads) to exercise retrieval without waiting for real ad impressions. Meta allows one test lead per form at a time.
 *
 */
export const createTestLead = <ThrowOnError extends boolean = false>(options: OptionsLegacyParser<CreateTestLeadData, ThrowOnError>) => {
    return (options?.client ?? client).post<CreateTestLeadResponse, CreateTestLeadError, ThrowOnError>({
        ...options,
        url: '/v1/ads/lead-forms/{formId}/test-leads'
    });
};

/**
 * Upload an ad image from base64
 * Uploads raw image bytes to the Meta ad account's image library — for callers whose
 * creatives aren't hosted at a public URL. Returns the image `hash` (Meta's identifier for
 * the asset) and the Meta-hosted `url`, which can be used directly as `imageUrl` on the
 * create endpoints. Max 30 MB decoded.
 */
export const uploadAdImage = <ThrowOnError extends boolean = false>(options: OptionsLegacyParser<UploadAdImageData, ThrowOnError>) => {
    return (options?.client ?? client).post<UploadAdImageResponse, UploadAdImageError, ThrowOnError>({
        ...options,
        url: '/v1/ads/images'
    });
};

/**
 * Ad image library
 * Lists the ad account's image library (Meta's `/act_X/adimages`), rows returned verbatim.
 * The default projection covers hash, url, name, dimensions and status; `fields` is a
 * raw-passthrough override. Any `hash` here is reusable wherever Meta accepts
 * `image_hash` (e.g. `imageHash` on POST /v1/ads/creatives).
 */
export const listAdImages = <ThrowOnError extends boolean = false>(options: OptionsLegacyParser<ListAdImagesData, ThrowOnError>) => {
    return (options?.client ?? client).get<ListAdImagesResponse, ListAdImagesError, ThrowOnError>({
        ...options,
        url: '/v1/ads/images'
    });
};

/**
 * @deprecated
 * Search targeting interests
 * Deprecated alias for `GET /v1/ads/targeting/search?dimension=interest`. Kept for
 * backward compatibility, it returns the legacy `{ interests: [...] }` shape rather
 * than the normalized `{ results: [...] }`. New integrations should use
 * `GET /v1/ads/targeting/search` with `dimension=interest`.
 *
 */
export const searchAdInterests = <ThrowOnError extends boolean = false>(options: OptionsLegacyParser<SearchAdInterestsData, ThrowOnError>) => {
    return (options?.client ?? client).get<SearchAdInterestsResponse, SearchAdInterestsError, ThrowOnError>({
        ...options,
        url: '/v1/ads/interests'
    });
};

/**
 * Search targeting options
 * Resolve a human-readable query into the platform's opaque targeting ids used in
 * the `TargetingSpec` (`countries`/`regions`/`cities`/`zips`/`metros` geo keys, and
 * `interests`/`behaviors` entity ids) on `POST /v1/ads/create`,
 * `POST /v1/ads/targeting/reach-estimate`, and `saved_targeting` audiences.
 *
 * The `dimension` param selects what is searched, `geo` (locations, further scoped
 * by `geoType`), `interest`, `behavior`, or `income`. Availability of each dimension
 * varies by platform (e.g. behaviours are Meta/TikTok only). Results are normalized
 * across platforms into a single shape, so the same client code consumes Meta,
 * TikTok, LinkedIn, X, Pinterest, and Google results.
 *
 * TikTok geo searches return every matching level in one list (`type` is
 * `country`, `region`, `city`, `district`, or `metro` for DMA areas) —
 * `geoType` is not applied. Results are scoped to the advertiser's targetable
 * markets, and every id is usable in `regions`/`cities`/`metros` keys on
 * `POST /v1/ads/create`.
 *
 * LinkedIn geo searches also return every matching level in one list, and
 * neither `geoType` nor `countryCode` is applied: LinkedIn's typeahead only
 * returns a name and a URN per result, with no level or country field to
 * filter on. Every result has `type` set to `location`, and its id is a
 * `urn:li:geo:*` URN usable as a `regions[].key` on `POST /v1/ads/create`,
 * `POST /v1/ads/boost` and `POST /v1/ads/targeting/reach-estimate`.
 *
 * Pinterest resolves against three whole-catalog endpoints (interests, locations,
 * regions) with no server-side query or pagination, so matching, ranking and the
 * `limit` cutoff all happen in Zernio; the catalog is independent of any ad account
 * and results never carry `audienceSize`. Names come back localized to the connected
 * Pinterest account's language (there is no way to force a locale), so match against
 * whatever language that account returns. `geoType` routes to a different catalog:
 * `country` and `metro_area` read the locations catalog (`type` is `country` or
 * `metro`); `region` reads the regions catalog (`type` is `region`, its id a
 * `regions[].key` on `POST /v1/ads/create`); `all` and the default `city` merge both
 * catalogs with honest per-entry `type`s, since Pinterest has no city-level catalog
 * and `city` is an alias for `all`, not a literal city search. `zip`, `subcity`,
 * `neighborhood`, `place` and `geo_market` return a 400: Pinterest exposes no
 * postal-code catalog, pass postal codes directly as `targeting.zips: [{ key }]` on
 * `POST /v1/ads/create`.
 *
 * For geo queries, `q` should contain only the locality name (e.g. `"Amsterdam"`,
 * not `"Amsterdam, NL"`). Use `countryCode` to disambiguate.
 *
 */
export const searchAdTargeting = <ThrowOnError extends boolean = false>(options: OptionsLegacyParser<SearchAdTargetingData, ThrowOnError>) => {
    return (options?.client ?? client).get<SearchAdTargetingResponse, SearchAdTargetingError, ThrowOnError>({
        ...options,
        url: '/v1/ads/targeting/search'
    });
};

/**
 * Estimate audience reach
 * Returns a normalized pre-flight audience-size estimate for a targeting spec,
 * before any campaign is created. Backed by each platform's native reach API
 * (Meta `delivery_estimate`, LinkedIn `audienceCounts`, X `audience_summary`,
 * Pinterest `audience_sizing`).
 *
 * Platforms without a usable pre-flight reach API (Google Search/Display, TikTok)
 * return `available: false` with no bounds, so clients can hide or grey out the
 * estimate rather than treat the absence as an error.
 *
 */
export const estimateAdReach = <ThrowOnError extends boolean = false>(options: OptionsLegacyParser<EstimateAdReachData, ThrowOnError>) => {
    return (options?.client ?? client).post<EstimateAdReachResponse, EstimateAdReachError, ThrowOnError>({
        ...options,
        url: '/v1/ads/targeting/reach-estimate'
    });
};

/**
 * Suggested bid and budget bounds
 * LinkedIn-only. Returns the suggested bid and bid limits for a targeting
 * spec, plus the daily-budget bounds LinkedIn will accept. Use it before
 * creating a campaign to pick a bid inside the allowed range and warn the
 * user if their daily budget is below the minimum. Wraps LinkedIn's
 * `adBudgetPricing` finder.
 *
 * Non-LinkedIn accounts return `available: false` so clients can hide the
 * pricing UI without treating it as a failure.
 *
 */
export const getLinkedInBidPricing = <ThrowOnError extends boolean = false>(options: OptionsLegacyParser<GetLinkedInBidPricingData, ThrowOnError>) => {
    return (options?.client ?? client).post<GetLinkedInBidPricingResponse, GetLinkedInBidPricingError, ThrowOnError>({
        ...options,
        url: '/v1/ads/targeting/bid-pricing'
    });
};

/**
 * Impressions, clicks and spend forecast
 * LinkedIn-only. Forecasted impressions, clicks, spend and ~20 other
 * metrics for a targeting spec over a time range. Wraps LinkedIn's
 * `adSupplyForecasts` finder.
 *
 * Each returned series carries a `metricType` (IMPRESSION, CLICK, SPENDING,
 * MAX_POTENTIAL_BUDGET, COST_PER_MILLION_IMPRESSIONS, ...) and a
 * `granularity` (DAILY, SEVEN_DAY, THIRTY_DAY, CUSTOM). LinkedIn caps the
 * daily spending forecast at 1.2x the daily budget and returns 0 once the
 * total budget is exhausted.
 *
 * Non-LinkedIn accounts return `available: false`.
 *
 */
export const getLinkedInSupplyForecast = <ThrowOnError extends boolean = false>(options: OptionsLegacyParser<GetLinkedInSupplyForecastData, ThrowOnError>) => {
    return (options?.client ?? client).post<GetLinkedInSupplyForecastResponse, GetLinkedInSupplyForecastError, ThrowOnError>({
        ...options,
        url: '/v1/ads/targeting/supply-forecast'
    });
};

/**
 * List Meta product catalogs
 * Lists the Meta product catalogs reachable from an ad account (owned + agency-shared catalogs of the ad account's business), for Advantage+ catalog ads (`goal: catalog_sales` on POST /v1/ads/create — e.g. vehicle inventory catalogs). Read-only; uses scopes customers already granted (no reconnect needed). Catalog contents (items, feeds) are managed in Meta Commerce Manager, not through this API.
 */
export const listAdCatalogs = <ThrowOnError extends boolean = false>(options: OptionsLegacyParser<ListAdCatalogsData, ThrowOnError>) => {
    return (options?.client ?? client).get<ListAdCatalogsResponse, ListAdCatalogsError, ThrowOnError>({
        ...options,
        url: '/v1/ads/catalogs'
    });
};

/**
 * List a catalog's product sets
 * Lists a Meta product catalog's product sets — the unit a catalog ad promotes. Pass the chosen set as `promotedObject.productSetId` on POST /v1/ads/create with `goal: catalog_sales`.
 */
export const listAdCatalogProductSets = <ThrowOnError extends boolean = false>(options: OptionsLegacyParser<ListAdCatalogProductSetsData, ThrowOnError>) => {
    return (options?.client ?? client).get<ListAdCatalogProductSetsResponse, ListAdCatalogProductSetsError, ThrowOnError>({
        ...options,
        url: '/v1/ads/catalogs/{catalogId}/product-sets'
    });
};

/**
 * List custom audiences
 * Returns custom audiences for the given ad account. Supports Meta, Google, TikTok, Pinterest, LinkedIn, and X (Twitter).
 */
export const listAdAudiences = <ThrowOnError extends boolean = false>(options: OptionsLegacyParser<ListAdAudiencesData, ThrowOnError>) => {
    return (options?.client ?? client).get<ListAdAudiencesResponse, ListAdAudiencesError, ThrowOnError>({
        ...options,
        url: '/v1/ads/audiences'
    });
};

/**
 * Create custom audience
 * Create a custom audience. `customer_list` is supported on Meta, Google, X, LinkedIn, TikTok, and Pinterest;
 * `website` and `lookalike` are Meta-only. `saved_targeting` stores a reusable TargetingSpec (no member upload,
 * no adAccountId) that you reference later via `savedTargetingId` on `POST /v1/ads/create`. Upload-backed audiences
 * are created empty, add members via `POST /v1/ads/audiences/{audienceId}/users`. On TikTok and Pinterest the
 * audience is provisioned lazily on the first member upload (until then its status is `pending`). Create is not
 * idempotent, never auto-retry.
 *
 */
export const createAdAudience = <ThrowOnError extends boolean = false>(options: OptionsLegacyParser<CreateAdAudienceData, ThrowOnError>) => {
    return (options?.client ?? client).post<CreateAdAudienceResponse, CreateAdAudienceError, ThrowOnError>({
        ...options,
        url: '/v1/ads/audiences'
    });
};

/**
 * Get audience details
 * Returns the local audience record and fresh data from Meta (if available).
 */
export const getAdAudience = <ThrowOnError extends boolean = false>(options: OptionsLegacyParser<GetAdAudienceData, ThrowOnError>) => {
    return (options?.client ?? client).get<GetAdAudienceResponse, GetAdAudienceError, ThrowOnError>({
        ...options,
        url: '/v1/ads/audiences/{audienceId}'
    });
};

/**
 * Update an audience
 * Update an audience. `saved_targeting` audiences accept `name`, `description`, and `spec`
 * (full replacement, no merge, Zernio-only, no platform call). Platform audiences
 * (uploaded/website/lookalike) accept `name` and `description` only, updated on the
 * platform first and then mirrored locally; their rules are immutable, so `spec` returns
 * 400 for them. Platform audience updates are Meta-only for now (other platforms return
 * 501). Ads already created from a saved_targeting audience are unaffected, they snapshot
 * the targeting at creation.
 *
 */
export const updateAdAudience = <ThrowOnError extends boolean = false>(options: OptionsLegacyParser<UpdateAdAudienceData, ThrowOnError>) => {
    return (options?.client ?? client).put<UpdateAdAudienceResponse, UpdateAdAudienceError, ThrowOnError>({
        ...options,
        url: '/v1/ads/audiences/{audienceId}'
    });
};

/**
 * Delete custom audience
 * Deletes the audience from both the platform and the local database. `saved_targeting` audiences exist only on Zernio, so only the local record is removed.
 */
export const deleteAdAudience = <ThrowOnError extends boolean = false>(options: OptionsLegacyParser<DeleteAdAudienceData, ThrowOnError>) => {
    return (options?.client ?? client).delete<DeleteAdAudienceResponse, DeleteAdAudienceError, ThrowOnError>({
        ...options,
        url: '/v1/ads/audiences/{audienceId}'
    });
};

/**
 * Add users to audience
 * Upload user data to a customer_list audience. Data is SHA256-hashed server-side before sending to the platform.
 * Email is used on every platform; phone is used on Meta only (other platforms ignore it). On TikTok and Pinterest,
 * the first upload also provisions the audience (deferred create). LinkedIn uploads are full-replace. Max 10,000 users per request.
 *
 */
export const addUsersToAdAudience = <ThrowOnError extends boolean = false>(options: OptionsLegacyParser<AddUsersToAdAudienceData, ThrowOnError>) => {
    return (options?.client ?? client).post<AddUsersToAdAudienceResponse, AddUsersToAdAudienceError, ThrowOnError>({
        ...options,
        url: '/v1/ads/audiences/{audienceId}/users'
    });
};

/**
 * Get Event Match Quality
 * Reads Meta Event Match Quality (EMQ) and pixel↔CAPI event coverage for a
 * pixel/dataset, live from Meta's Dataset Quality API. Web events only (a
 * Meta limitation). Meta-only; other platforms return 405. Requires the Ads add-on.
 *
 */
export const getConversionsQuality = <ThrowOnError extends boolean = false>(options: OptionsLegacyParser<GetConversionsQualityData, ThrowOnError>) => {
    return (options?.client ?? client).get<GetConversionsQualityResponse, GetConversionsQualityError, ThrowOnError>({
        ...options,
        url: '/v1/ads/conversions/quality'
    });
};

/**
 * Send conversion events
 * Relay one or more conversion events to the target ad platform's native Conversions API.
 * Platform is inferred from the provided `accountId`. Requires the Ads add-on.
 *
 * Supported platforms:
 *
 * - Meta (`metaads`) via Graph API
 * - Google Ads (`googleads`) via Data Manager API `ingestEvents`
 * - LinkedIn (`linkedinads`) via `/rest/conversionEvents`
 * - TikTok (`tiktokads`) via the Offline Events API `/offline/batch/` — OFFLINE conversions only
 * - OpenAI Ads (`openaiads`) via its Conversions API (a separate host, `bzr.openai.com`)
 *
 * `destinationId` semantics differ per platform:
 *
 * - Meta: pixel (dataset) ID, e.g. `123456789012345`
 * - Google: conversion action resource name, e.g. `customers/1234567890/conversionActions/987654321`
 * - LinkedIn: conversion rule ID or URN, e.g. `104012` or `urn:lla:llaPartnerConversion:104012`
 * - TikTok: Offline Event Set ID, e.g. `7057103914977558530`
 * - OpenAI Ads: pixel wire id (numeric `pixel_id`, distinct from the internal pixel id), as returned by `GET /v1/accounts/{accountId}/conversion-destinations`
 *
 * TikTok notes: this path sends OFFLINE conversions (in-store / CRM / call-center), not web-pixel
 * events. Each event must carry an email or phone (TikTok requires at least one). The connected
 * TikTok ads account must have granted the Offline Events permission; older grants must reconnect.
 *
 * OpenAI Ads notes: requires a tracking tag (pixel) to already exist on the account — returns 422
 * with code `TRACKING_TAG_REQUIRED` if `POST /v1/accounts/{accountId}/tracking-tags` hasn't been
 * called yet.
 *
 * Callers can list valid destinations via `GET /v1/accounts/{accountId}/conversion-destinations`.
 *
 * All PII (email, phone, names, external IDs) is hashed with SHA-256 server-side per each
 * platform's normalization spec, including Google's Gmail-specific dot/plus-suffix stripping.
 * Send plaintext. LinkedIn `externalIds` are passed through as plaintext per LinkedIn's spec;
 * only emails and phones are hashed.
 *
 * For LinkedIn, the connected account must have been authorized after the Conversions API
 * rollout (i.e. the OAuth grant must include `rw_conversions`). Older accounts must reconnect.
 *
 * Batching is handled automatically. Meta caps at 1000 events per request and rejects the
 * entire batch if any event is malformed. Google caps at 2000. LinkedIn caps at 5000 and is
 * also all-or-nothing per chunk. OpenAI Ads caps at 1000 per request; larger submissions are
 * split into 1000-event chunks, each all-or-nothing (a malformed event fails every event in
 * that chunk, not the whole request).
 *
 * Dedup: pass a stable `eventId` on every event. Meta and LinkedIn use it to dedupe against
 * browser-side pixel/Insight Tag events; Google maps it to `transactionId`.
 *
 * Per-platform `eventName` semantics:
 *
 * - Meta: free-form. Standard names (Purchase, Lead, ...) match Meta's built-in events; custom strings are accepted.
 * - Google: ignored. The conversion action's category determines the event type. Send the standard name closest to your action for documentation, but the platform will not branch on it.
 * - LinkedIn: ignored. The conversion rule's `type` (LEAD, PURCHASE, etc.) is locked to the destination at rule-creation time. Send the standard name for documentation; LinkedIn does not branch on it.
 * - OpenAI Ads: a fixed subset of standard names (Purchase, Lead, AddToCart, ViewContent, InitiateCheckout, CompleteRegistration, Subscribe, StartTrial, Schedule) maps 1:1 onto OpenAI's own event-type enum; any other standard name or custom string is sent as `type: custom` with the name preserved.
 *
 */
export const sendConversions = <ThrowOnError extends boolean = false>(options: OptionsLegacyParser<SendConversionsData, ThrowOnError>) => {
    return (options?.client ?? client).post<SendConversionsResponse, SendConversionsError, ThrowOnError>({
        ...options,
        url: '/v1/ads/conversions'
    });
};

/**
 * Adjust uploaded conversions
 * Adjust conversions that were previously uploaded via `POST /v1/ads/conversions` —
 * retract them, restate their value, or enhance them with first-party data. Requires
 * the Ads add-on.
 *
 * **Google Ads only.** Google handles adjustments through the classic Google Ads API
 * (`ConversionAdjustmentUploadService`); the Data Manager `ingestEvents` path used for
 * sending conversions is ingest-only. Meta and LinkedIn have no equivalent, so this
 * endpoint returns `405` for those platforms.
 *
 * Adjustment types:
 *
 * - `RETRACTION` — remove the conversion entirely (refund, chargeback, cancelled order, churn).
 * - `RESTATEMENT` — change the conversion's value (upgrade / downgrade / partial refund). Send the corrected **total** value in `restatementValue` (not a delta).
 * - `ENHANCEMENT` — attach first-party identifiers (hashed email / phone) to an existing conversion (enhanced conversions applied after the fact).
 *
 * Identifying the original conversion (per adjustment):
 *
 * - `orderId` — the transaction ID you sent as `eventId` on the original conversion. Recommended, and **required** for `ENHANCEMENT`.
 * - or `gclid` + `conversionTime` — the click ID and the original conversion's time (unix seconds). Not available for `ENHANCEMENT`.
 *
 * `destinationId` is the conversion action resource name, e.g.
 * `customers/1234567890/conversionActions/987654321` (same value you send to
 * `POST /v1/ads/conversions`). PII in `user` is hashed with SHA-256 server-side
 * (Gmail-specific normalization included). Send plaintext.
 *
 * Times are unix seconds; we convert to Google's required
 * `yyyy-MM-dd HH:mm:ss+00:00` format. Up to 2000 adjustments per request; partial
 * failure is supported (inspect `adjustmentsFailed` / `failures[]`).
 *
 */
export const adjustConversions = <ThrowOnError extends boolean = false>(options: OptionsLegacyParser<AdjustConversionsData, ThrowOnError>) => {
    return (options?.client ?? client).post<AdjustConversionsResponse, AdjustConversionsError, ThrowOnError>({
        ...options,
        url: '/v1/ads/conversions/adjustments'
    });
};

/**
 * List conversion destinations
 * Returns the list of pixels (Meta), conversion actions (Google),
 * conversion rules (LinkedIn), or pixels (OpenAI Ads) accessible to the
 * connected ads account. Use the returned `id` as `destinationId` when
 * posting to `POST /v1/ads/conversions`.
 *
 * For Google and LinkedIn, each destination's `type` reflects the
 * conversion type (PURCHASE, LEAD, SIGN_UP, etc.) — the event type is
 * locked to the destination. For Meta and OpenAI Ads, `type` is absent:
 * pixels accept any event name per request.
 *
 * For LinkedIn, destinations are returned across every sponsored ad
 * account the connected token can access; the `adAccountId` field on
 * each destination identifies the parent ad account and is required for
 * subsequent CRUD calls (update, delete, associations, metrics).
 *
 */
export const listConversionDestinations = <ThrowOnError extends boolean = false>(options: OptionsLegacyParser<ListConversionDestinationsData, ThrowOnError>) => {
    return (options?.client ?? client).get<ListConversionDestinationsResponse, ListConversionDestinationsError, ThrowOnError>({
        ...options,
        url: '/v1/accounts/{accountId}/conversion-destinations'
    });
};

/**
 * Create a conversion destination
 * Create a new conversion destination on the platform. Supported for
 * LinkedIn (conversion rule) and Google Ads (conversion action). Meta
 * and OpenAI Ads pixels are created via their own tracking-tags flow
 * instead (`POST /v1/accounts/{accountId}/tracking-tags`); this endpoint
 * returns 405 for both.
 *
 * **LinkedIn:** creation is NOT idempotent. A retry creates a second
 * destination. Deduplicate before retrying.
 *
 * **Google Ads:** calling with a name that already exists reuses the
 * existing conversion action transparently (the response is identical to
 * a fresh create). Calling with the same name but a different category
 * returns a typed `IDEMPOTENCY_CONFLICT` (409) rather than silently
 * returning the mismatched action.
 *
 * **LinkedIn:** the rule is created with `conversionMethod=CONVERSIONS_API`
 * and (by default) auto-associated with all of the ad account's campaigns
 * via `autoAssociationType=ALL_CAMPAIGNS`. Pass `autoAssociationType: NONE`
 * to opt out and manage associations explicitly via the associations
 * endpoints below.
 *
 * 365-day attribution windows are only valid for `SUBMIT_APPLICATION`,
 * `PURCHASE`, `ADD_TO_CART`, `QUALIFIED_LEAD`, and `LEAD` rule types;
 * the API rejects other combinations locally.
 *
 * **Google Ads:** the conversion action is created with
 * `type=UPLOAD_CLICKS` (required for API-uploaded offline conversions,
 * immutable after creation). The `type` field carries the Google
 * `ConversionActionCategory` enum value, e.g. `PURCHASE`,
 * `SUBSCRIBE_PAID`, `SIGNUP`, `IMPORTED_LEAD`, `BOOK_APPOINTMENT`.
 * Unified standard event names (e.g. `Purchase`, `Subscribe`,
 * `CompleteRegistration`, `Lead`, `Schedule`) are resolved to their
 * Google category equivalents automatically. The action defaults to
 * secondary (non-primary) to avoid immediately steering Smart Bidding;
 * pass `primaryForGoal: true` to opt in.
 *
 */
export const createConversionDestination = <ThrowOnError extends boolean = false>(options: OptionsLegacyParser<CreateConversionDestinationData, ThrowOnError>) => {
    return (options?.client ?? client).post<CreateConversionDestinationResponse, CreateConversionDestinationError, ThrowOnError>({
        ...options,
        url: '/v1/accounts/{accountId}/conversion-destinations'
    });
};

/**
 * Get a conversion destination
 * LinkedIn-only today. Returns the full destination record for one
 * conversion rule. The `adAccountId` query parameter is required because
 * LinkedIn rules are scoped to a sponsored ad account.
 *
 */
export const getConversionDestination = <ThrowOnError extends boolean = false>(options: OptionsLegacyParser<GetConversionDestinationData, ThrowOnError>) => {
    return (options?.client ?? client).get<GetConversionDestinationResponse, GetConversionDestinationError, ThrowOnError>({
        ...options,
        url: '/v1/accounts/{accountId}/conversion-destinations/{destinationId}'
    });
};

/**
 * Update a conversion destination
 * Partial-update a conversion rule. LinkedIn-only today. Whitelisted
 * fields: `name`, `enabled`, attribution windows, `valueType`, `value`,
 * `attributionType`. The rule's `type` and parent ad account are
 * intentionally not exposed for update — recreate the rule if those
 * need to change.
 *
 */
export const updateConversionDestination = <ThrowOnError extends boolean = false>(options: OptionsLegacyParser<UpdateConversionDestinationData, ThrowOnError>) => {
    return (options?.client ?? client).patch<UpdateConversionDestinationResponse, UpdateConversionDestinationError, ThrowOnError>({
        ...options,
        url: '/v1/accounts/{accountId}/conversion-destinations/{destinationId}'
    });
};

/**
 * Delete a conversion destination
 * LinkedIn-only today. LinkedIn does not expose hard-delete on conversion
 * rules — what their UI calls "delete" is the same `enabled: false` flip
 * we apply here. The rule remains fetchable via GET with
 * `status: 'inactive'`; the unified discovery endpoint hides it by
 * default.
 *
 * `adAccountId` may be passed as a query parameter (recommended) or as
 * a JSON body field for clients that can send DELETE bodies.
 *
 */
export const deleteConversionDestination = <ThrowOnError extends boolean = false>(options: OptionsLegacyParser<DeleteConversionDestinationData, ThrowOnError>) => {
    return (options?.client ?? client).delete<DeleteConversionDestinationResponse, DeleteConversionDestinationError, ThrowOnError>({
        ...options,
        url: '/v1/accounts/{accountId}/conversion-destinations/{destinationId}'
    });
};

/**
 * List associated campaigns
 * LinkedIn-only today. Returns the campaigns currently associated with
 * this conversion rule. Note that auto-association on rule creation
 * runs once at create time; campaigns created after the rule still need
 * explicit association.
 *
 */
export const listConversionAssociations = <ThrowOnError extends boolean = false>(options: OptionsLegacyParser<ListConversionAssociationsData, ThrowOnError>) => {
    return (options?.client ?? client).get<ListConversionAssociationsResponse, ListConversionAssociationsError, ThrowOnError>({
        ...options,
        url: '/v1/accounts/{accountId}/conversion-destinations/{destinationId}/associations'
    });
};

/**
 * Associate campaigns
 * Associate one or more campaigns with this conversion rule. Returns a
 * per-campaign success/failure result so callers can retry only the
 * rows that failed (e.g. wrong campaign type for the rule's objective).
 *
 */
export const addConversionAssociations = <ThrowOnError extends boolean = false>(options: OptionsLegacyParser<AddConversionAssociationsData, ThrowOnError>) => {
    return (options?.client ?? client).post<AddConversionAssociationsResponse, AddConversionAssociationsError, ThrowOnError>({
        ...options,
        url: '/v1/accounts/{accountId}/conversion-destinations/{destinationId}/associations'
    });
};

/**
 * Remove associated campaigns
 * Remove one or more campaign associations from this conversion rule.
 * Pass `adAccountId` and `campaignIds` as query parameters
 * (`campaignIds` is comma-separated). The route also accepts a JSON
 * body with the same fields for clients that prefer DELETE-with-body,
 * but the documented surface is query-only because some SDK code
 * generators (e.g. Python) collapse query + body parameters with the
 * same name into a single kwarg.
 *
 */
export const removeConversionAssociations = <ThrowOnError extends boolean = false>(options: OptionsLegacyParser<RemoveConversionAssociationsData, ThrowOnError>) => {
    return (options?.client ?? client).delete<RemoveConversionAssociationsResponse, RemoveConversionAssociationsError, ThrowOnError>({
        ...options,
        url: '/v1/accounts/{accountId}/conversion-destinations/{destinationId}/associations'
    });
};

/**
 * Get attribution metrics
 * LinkedIn-only today. Returns conversion-attribution metrics
 * (`externalWebsiteConversions`, `externalWebsitePostClickConversions`,
 * `externalWebsitePostViewConversions`, `conversionValueInLocalCurrency`,
 * `qualifiedLeads`, `costInLocalCurrency`) bucketed by date.
 *
 * Date-range constraints (passed through from LinkedIn):
 * - `granularity=DAILY` is retained for ~6 months only
 * - `granularity=ALL` with a range > 6 months auto-rounds to month boundaries
 * - `granularity=MONTHLY`/`YEARLY` retains 24 months
 *
 * Throttle: LinkedIn caps adAnalytics at 45M metric values per 5-minute
 * window across the calling token. Single-rule queries are well within
 * that limit; surfaces as 429 if hit.
 *
 */
export const getConversionMetrics = <ThrowOnError extends boolean = false>(options: OptionsLegacyParser<GetConversionMetricsData, ThrowOnError>) => {
    return (options?.client ?? client).get<GetConversionMetricsResponse, GetConversionMetricsError, ThrowOnError>({
        ...options,
        url: '/v1/accounts/{accountId}/conversion-destinations/{destinationId}/metrics'
    });
};

/**
 * List conversion events
 * Returns the most recent conversion events sent through
 * `POST /v1/whatsapp/conversions` for the given WhatsApp account.
 * Sourced from delivery logs (Axiom `late` dataset), so the visible
 * window is bounded by log retention (about 30 days). Useful for
 * rendering a "recent activity" panel on the conversions setup tab
 * without standing up a parallel persistence layer.
 *
 * Per-event payload mirrors the structured log we write on every
 * successful send: `eventName`, `conversationId`, `eventsReceived`,
 * `eventsFailed`, `traceId`, `durationMs`, and the wall-clock
 * `timestamp`.
 *
 */
export const listWhatsAppConversions = <ThrowOnError extends boolean = false>(options: OptionsLegacyParser<ListWhatsAppConversionsData, ThrowOnError>) => {
    return (options?.client ?? client).get<ListWhatsAppConversionsResponse, ListWhatsAppConversionsError, ThrowOnError>({
        ...options,
        url: '/v1/whatsapp/conversions'
    });
};

/**
 * Send WhatsApp conversion event
 * Forward a WhatsApp Business Messaging conversion event (`LeadSubmitted`,
 * `Purchase`, `AddToCart`, `InitiateCheckout`, `ViewContent`) to Meta's
 * Conversions API with `action_source = business_messaging` and
 * `messaging_channel = whatsapp`. The endpoint looks up the originating
 * CTWA click ID (`ctwa_clid`) captured on the first inbound message of
 * the conversation and replays it on every event so Meta can attribute
 * the conversion back to the Click-to-WhatsApp ad that drove the chat.
 *
 * Configuration prerequisite on the WhatsApp account metadata:
 * - `metaCapiDatasetId`: the Meta dataset ID linked to the WABA.
 * Provision one with `POST /v1/whatsapp/dataset`.
 *
 * The WABA ID (already set automatically at connect time) is forwarded as
 * `user_data.whatsapp_business_account_id`, which is the per-channel
 * attribution identifier Meta requires for WhatsApp events. No Facebook
 * Page ID is needed (that field is the Messenger-branch identifier).
 *
 * Identify the conversation by either `conversationId` (preferred) or
 * `phoneE164` (digits only, no `+`). At least one is required. If the
 * conversation has no captured `ctwa_clid`, the request returns 422
 * because there is nothing to attribute.
 *
 * Token and dataset coupling: the WhatsApp account's accessToken must
 * have access to the configured `metaCapiDatasetId`. By default a WABA's
 * system-user token is scoped to the WABA's own Business Manager and
 * cannot post to a pixel owned by a different Business; Meta returns
 * code 100 in that case. Either share the dataset with the WhatsApp
 * app's Business in BM, or use a dataset already in the same Business
 * as the WABA.
 *
 */
export const sendWhatsAppConversion = <ThrowOnError extends boolean = false>(options: OptionsLegacyParser<SendWhatsAppConversionData, ThrowOnError>) => {
    return (options?.client ?? client).post<SendWhatsAppConversionResponse, SendWhatsAppConversionError, ThrowOnError>({
        ...options,
        url: '/v1/whatsapp/conversions'
    });
};

/**
 * Create click-to-message ad (WhatsApp / Messenger / Instagram Direct)
 * Creates a click-to-message ad; `destination` selects where the tapped ad opens a
 * conversation: WhatsApp, the Page's Messenger inbox or the linked Instagram account's Direct inbox.
 * The ad set is created with the matching destination_type and
 * CONVERSATIONS optimization; the campaign objective defaults to OUTCOME_ENGAGEMENT.
 * Supports single-creative and multi-creative shapes. Supersedes POST /v1/ads/ctwa
 * (deprecated, equivalent to `destination: whatsapp`).
 */
export const createMessagingAd = <ThrowOnError extends boolean = false>(options: OptionsLegacyParser<CreateMessagingAdData, ThrowOnError>) => {
    return (options?.client ?? client).post<CreateMessagingAdResponse, CreateMessagingAdError, ThrowOnError>({
        ...options,
        url: '/v1/ads/messaging'
    });
};

/**
 * Create Click-to-Call ad
 * Same shape and flow as POST /v1/ads/ctwa, but the CTA is CALL_NOW dialing `phoneNumber`
 * via a tel: link. The ad set is destination_type PHONE_CALL optimizing QUALITY_CALL
 * and the campaign objective defaults to OUTCOME_LEADS.
 * Supports the same single-creative and multi-creative shapes as CTWA.
 */
export const createCallAd = <ThrowOnError extends boolean = false>(options: OptionsLegacyParser<CreateCallAdData, ThrowOnError>) => {
    return (options?.client ?? client).post<CreateCallAdResponse, CreateCallAdError, ThrowOnError>({
        ...options,
        url: '/v1/ads/call'
    });
};

/**
 * @deprecated
 * Create Click-to-WhatsApp ad (deprecated)
 * Deprecated: use POST /v1/ads/messaging with `destination: whatsapp`. This endpoint stays available for back-compat; no removal planned.
 *
 * Creates one or more Click-to-WhatsApp (CTWA) ads on Meta under a single campaign and ad set. When tapped, each ad opens a WhatsApp conversation with the business attached to the supplied Facebook Page. The full hierarchy (campaign, ad set, creative(s), ad(s)) is created and activated in one call. The CTA is locked to WHATSAPP_MESSAGE and the destination is hard-coded to api.whatsapp.com/send; Meta resolves the actual WhatsApp number from the Page-to-WA pairing configured in Page settings or Business Manager.
 *
 * Supports two mutually-exclusive shapes:
 *
 * - **Single-creative**: supply top-level `headline`, `body`, and one of `imageUrl` / `video`. Creates 1 campaign + 1 ad set + 1 ad.
 *
 * - **Multi-creative**: supply a `creatives[]` array with N entries (each carrying its own headline, body, and image/video). Creates 1 campaign + 1 ad set + N ads sharing budget and targeting so Meta A/Bs the creatives inside a single auction instead of fragmenting budget across N parallel campaigns. Recommended when launching multiple creative variants for the same campaign.
 *
 * **Attach shape.** Send `adSetId` (with either creative shape) to add the ads to an EXISTING messaging ad set instead of building a campaign, so the ad set keeps its learning phase — the way to refresh a CTWA creative without resetting delivery. The ad set then owns budget, targeting and schedule, so `budgetAmount`, `budgetType`, `endDate`, `objective`, `countries`, `interests` and `audienceId` are rejected with a 400 alongside it rather than silently dropped. The target ad set's `destination_type` must match the ad's destination (a WhatsApp ad needs a `WHATSAPP` ad set), otherwise Meta would accept an ad that never delivers.
 *
 * Prerequisites enforced by Meta (surfaced as platform_error on failure): the Facebook Page must be paired with a verified WhatsApp Business number, the WhatsApp Business Account must be business-verified, and the Meta access token must carry ads_management.
 */
export const createCtwaAd = <ThrowOnError extends boolean = false>(options: OptionsLegacyParser<CreateCtwaAdData, ThrowOnError>) => {
    return (options?.client ?? client).post<CreateCtwaAdResponse, CreateCtwaAdError, ThrowOnError>({
        ...options,
        url: '/v1/ads/ctwa'
    });
};

/**
 * List custom conversions
 * The ad account's Meta custom conversions, including archived ones (`isArchived`).
 */
export const listCustomConversions = <ThrowOnError extends boolean = false>(options: OptionsLegacyParser<ListCustomConversionsData, ThrowOnError>) => {
    return (options?.client ?? client).get<ListCustomConversionsResponse, ListCustomConversionsError, ThrowOnError>({
        ...options,
        url: '/v1/accounts/{accountId}/custom-conversions'
    });
};

/**
 * Create or reuse a custom conversion
 * Provision the Meta custom conversion an ads flow optimises toward, and hand back the
 * `customConversionId` for `promotedObject.customConversionId` on POST /v1/ads/create.
 * Removes the manual "create it in Ads Manager first" step.
 *
 * **Reuse is ours, not Meta's.** Meta's create is not idempotent, so a retried request
 * would otherwise mint a duplicate carrying none of the original's optimisation history.
 * A non-archived conversion with the same `name` on the same `pixelId` is returned
 * instead of created, with `reused: true` and a 200 rather than a 201.
 *
 * `rule` is forwarded verbatim in Meta's own grammar (e.g.
 * `{"url": {"i_contains": "thank-you"}}`); Meta validates it and rejects a malformed one
 * with "A conversion rule is required at creation time".
 */
export const createCustomConversion = <ThrowOnError extends boolean = false>(options: OptionsLegacyParser<CreateCustomConversionData, ThrowOnError>) => {
    return (options?.client ?? client).post<CreateCustomConversionResponse, CreateCustomConversionError, ThrowOnError>({
        ...options,
        url: '/v1/accounts/{accountId}/custom-conversions'
    });
};

/**
 * List tracking tags
 * Returns the tracking tags (Meta Pixels, or OpenAI Ads pixels) the
 * connected ads account can see. Pass `?adAccountId=act_...` (Meta only)
 * to scope the list to a single ad account; omit it to list every pixel
 * reachable by the token (the name is then suffixed with the ad account
 * it was discovered on, for disambiguation). The list view omits `code`
 * — call `getTrackingTag` for the install snippet and full detail (Meta
 * only; OpenAI Ads has no get-by-id endpoint).
 *
 * Meta (platform `metaads`) and OpenAI Ads (platform `openaiads`); other
 * platforms return 405. The `accountId` must be the ads SocialAccount
 * created by the Ads add-on connect flow (Meta) or the OpenAI Ads
 * connect flow, not a Facebook/Instagram posting account. Get your Meta
 * `act_...` ids from `GET /v1/ads/accounts`; `adAccountId` is ignored for
 * OpenAI Ads (one API key maps to exactly one ad account).
 *
 */
export const listTrackingTags = <ThrowOnError extends boolean = false>(options: OptionsLegacyParser<ListTrackingTagsData, ThrowOnError>) => {
    return (options?.client ?? client).get<ListTrackingTagsResponse, ListTrackingTagsError, ThrowOnError>({
        ...options,
        url: '/v1/accounts/{accountId}/tracking-tags'
    });
};

/**
 * Create a tracking tag
 * Meta: creates a Meta Pixel on the given ad account (`POST /act_{id}/adspixels`
 * — `name` is the only input). Returns the created tag including its
 * install `code`. The pixel is owned by the Business Manager that owns the
 * ad account; a pixel created on a personal (non-BM) ad account ends up
 * with `ownerBusinessId: null` and can't be shared with other ad accounts.
 *
 * Creating a Meta pixel does NOT install it — install the returned `code`
 * snippet on the site, or send events server-side via
 * `POST /v1/ads/conversions`. The check `installed` is derived from
 * `lastFiredTime`.
 *
 * OpenAI Ads: creates an OpenAI pixel AND provisions a Conversions API
 * key for it in the same call (`adAccountId` is required by this
 * endpoint but ignored — one API key maps to exactly one ad account, so
 * there's nothing to select). Returns 422 (`FEATURE_NOT_AVAILABLE`) if
 * the ad account isn't enabled for pixel management; contact your OpenAI
 * partner representative to enable it. There is no delete API for
 * OpenAI pixels. If the pixel is created but the Conversions API key
 * provisioning then fails, the pixel is left live on OpenAI (it cannot
 * be cleaned up) and the error message names the surviving pixel id and
 * warns against retrying, since a retry would create a second, orphaned
 * pixel.
 *
 * NOT idempotent on either platform: each call creates a new pixel (and,
 * for OpenAI, a new Conversions API key). Do not retry blindly on
 * timeout. Meta (platform `metaads`) and OpenAI Ads (platform
 * `openaiads`); other platforms return 405.
 *
 */
export const createTrackingTag = <ThrowOnError extends boolean = false>(options: OptionsLegacyParser<CreateTrackingTagData, ThrowOnError>) => {
    return (options?.client ?? client).post<CreateTrackingTagResponse, CreateTrackingTagError, ThrowOnError>({
        ...options,
        url: '/v1/accounts/{accountId}/tracking-tags'
    });
};

/**
 * Get a tracking tag
 * Returns the full tag record including the base-code `code` snippet,
 * `lastFiredTime`, `ownerBusinessId`, `isUnavailable`, etc. Meta only
 * (platform `metaads`); other platforms return 405. OpenAI Ads has no
 * get-by-id endpoint, so it 405s here too — use
 * `GET /v1/accounts/{accountId}/tracking-tags` (list) instead.
 *
 */
export const getTrackingTag = <ThrowOnError extends boolean = false>(options: OptionsLegacyParser<GetTrackingTagData, ThrowOnError>) => {
    return (options?.client ?? client).get<GetTrackingTagResponse, GetTrackingTagError, ThrowOnError>({
        ...options,
        url: '/v1/accounts/{accountId}/tracking-tags/{tagId}'
    });
};

/**
 * Update a tracking tag
 * Partial-update a pixel. Whitelisted fields: `name` (rename),
 * `enableAutomaticMatching`, `automaticMatchingFields`,
 * `firstPartyCookieStatus`, `dataUseSetting`. At least one is required.
 * Returns the re-fetched canonical tag. Meta only (platform `metaads`);
 * other platforms return 405.
 *
 * There is no DELETE — Meta has no API to delete a pixel. To stop using
 * one, unshare it from your ad accounts (`DELETE
 * .../tracking-tags/{tagId}/shared-accounts`) or disable it in Events
 * Manager.
 *
 */
export const updateTrackingTag = <ThrowOnError extends boolean = false>(options: OptionsLegacyParser<UpdateTrackingTagData, ThrowOnError>) => {
    return (options?.client ?? client).patch<UpdateTrackingTagResponse, UpdateTrackingTagError, ThrowOnError>({
        ...options,
        url: '/v1/accounts/{accountId}/tracking-tags/{tagId}'
    });
};

/**
 * List accounts it is shared with
 * Meta only (platform `metaads`); other platforms return 405.
 */
export const listTrackingTagSharedAccounts = <ThrowOnError extends boolean = false>(options: OptionsLegacyParser<ListTrackingTagSharedAccountsData, ThrowOnError>) => {
    return (options?.client ?? client).get<ListTrackingTagSharedAccountsResponse, ListTrackingTagSharedAccountsError, ThrowOnError>({
        ...options,
        url: '/v1/accounts/{accountId}/tracking-tags/{tagId}/shared-accounts'
    });
};

/**
 * Share with an ad account
 * Shares the pixel with another ad account so campaigns/audiences in that
 * account can use it. Requires that you administer both the pixel's owning
 * Business Manager and the target ad account; a pixel on a personal
 * (non-BM) ad account can't be shared (Meta will reject the call). Meta
 * only (platform `metaads`); other platforms return 405.
 *
 */
export const addTrackingTagSharedAccount = <ThrowOnError extends boolean = false>(options: OptionsLegacyParser<AddTrackingTagSharedAccountData, ThrowOnError>) => {
    return (options?.client ?? client).post<AddTrackingTagSharedAccountResponse, AddTrackingTagSharedAccountError, ThrowOnError>({
        ...options,
        url: '/v1/accounts/{accountId}/tracking-tags/{tagId}/shared-accounts'
    });
};

/**
 * Stop sharing with an account
 * `adAccountId` may be passed as a query parameter (recommended) or as a
 * JSON body field for clients that can send DELETE bodies. Meta only
 * (platform `metaads`); other platforms return 405.
 *
 */
export const removeTrackingTagSharedAccount = <ThrowOnError extends boolean = false>(options: OptionsLegacyParser<RemoveTrackingTagSharedAccountData, ThrowOnError>) => {
    return (options?.client ?? client).delete<RemoveTrackingTagSharedAccountResponse, RemoveTrackingTagSharedAccountError, ThrowOnError>({
        ...options,
        url: '/v1/accounts/{accountId}/tracking-tags/{tagId}/shared-accounts'
    });
};

/**
 * Get aggregated event stats
 * Returns aggregated event counts for the pixel (`GET /{pixel_id}/stats`).
 * Rows are passed through from Meta as-is — their shape depends on the
 * `aggregation` requested. Meta only (platform `metaads`); other platforms
 * return 405.
 *
 */
export const getTrackingTagStats = <ThrowOnError extends boolean = false>(options: OptionsLegacyParser<GetTrackingTagStatsData, ThrowOnError>) => {
    return (options?.client ?? client).get<GetTrackingTagStatsResponse, GetTrackingTagStatsError, ThrowOnError>({
        ...options,
        url: '/v1/accounts/{accountId}/tracking-tags/{tagId}/stats'
    });
};

/**
 * List blogs
 * Lists the blogs on the connected store, newest-first as the platform
 * returns them. Cursor-paginated: pass `limit` (1-50, default 20) and the
 * `cursor` from a previous response's `nextCursor`; `nextCursor` is null
 * when there are no more pages.
 *
 * Supported on Shopify (platform `shopify`). Accounts on platforms
 * without blogs support return 400; a blogs-capable platform that lacks
 * this specific operation returns 405.
 *
 */
export const listBlogs = <ThrowOnError extends boolean = false>(options: OptionsLegacyParser<ListBlogsData, ThrowOnError>) => {
    return (options?.client ?? client).get<ListBlogsResponse, ListBlogsError, ThrowOnError>({
        ...options,
        url: '/v1/accounts/{accountId}/blogs'
    });
};

/**
 * Create a blog
 * Creates a blog on the connected store. The platform generates the URL
 * `handle` from the title when omitted.
 *
 * Supported on Shopify (platform `shopify`). Accounts on platforms
 * without blogs support return 400; a blogs-capable platform that lacks
 * this specific operation returns 405.
 *
 */
export const createBlog = <ThrowOnError extends boolean = false>(options: OptionsLegacyParser<CreateBlogData, ThrowOnError>) => {
    return (options?.client ?? client).post<CreateBlogResponse, CreateBlogError, ThrowOnError>({
        ...options,
        url: '/v1/accounts/{accountId}/blogs'
    });
};

/**
 * Get a blog
 * Fetches a single blog. `blogId` is the platform's numeric blog id from
 * `GET /v1/accounts/{accountId}/blogs`, not a Zernio id.
 *
 * Supported on Shopify (platform `shopify`). Accounts on platforms
 * without blogs support return 400; a blogs-capable platform that lacks
 * this specific operation returns 405.
 *
 */
export const getBlog = <ThrowOnError extends boolean = false>(options: OptionsLegacyParser<GetBlogData, ThrowOnError>) => {
    return (options?.client ?? client).get<GetBlogResponse, GetBlogError, ThrowOnError>({
        ...options,
        url: '/v1/accounts/{accountId}/blogs/{blogId}'
    });
};

/**
 * Update a blog
 * Partial-updates a blog. Send any subset of `title` and `handle`; at
 * least one field is required (an empty body returns 400).
 *
 * Supported on Shopify (platform `shopify`). Accounts on platforms
 * without blogs support return 400; a blogs-capable platform that lacks
 * this specific operation returns 405.
 *
 */
export const updateBlog = <ThrowOnError extends boolean = false>(options: OptionsLegacyParser<UpdateBlogData, ThrowOnError>) => {
    return (options?.client ?? client).patch<UpdateBlogResponse, UpdateBlogError, ThrowOnError>({
        ...options,
        url: '/v1/accounts/{accountId}/blogs/{blogId}'
    });
};

/**
 * Delete a blog
 * Deletes the blog AND every article in it. The delete happens on the
 * platform and is permanent; Zernio stores nothing to restore it from.
 *
 * Supported on Shopify (platform `shopify`). Accounts on platforms
 * without blogs support return 400; a blogs-capable platform that lacks
 * this specific operation returns 405.
 *
 */
export const deleteBlog = <ThrowOnError extends boolean = false>(options: OptionsLegacyParser<DeleteBlogData, ThrowOnError>) => {
    return (options?.client ?? client).delete<DeleteBlogResponse, DeleteBlogError, ThrowOnError>({
        ...options,
        url: '/v1/accounts/{accountId}/blogs/{blogId}'
    });
};

/**
 * List blog articles
 * Lists the articles of a blog. Cursor-paginated: pass `limit` (1-50,
 * default 20) and the `cursor` from a previous response's `nextCursor`;
 * `nextCursor` is null when there are no more pages.
 *
 * Supported on Shopify (platform `shopify`). Accounts on platforms
 * without blogs support return 400; a blogs-capable platform that lacks
 * this specific operation returns 405.
 *
 */
export const listBlogArticles = <ThrowOnError extends boolean = false>(options: OptionsLegacyParser<ListBlogArticlesData, ThrowOnError>) => {
    return (options?.client ?? client).get<ListBlogArticlesResponse, ListBlogArticlesError, ThrowOnError>({
        ...options,
        url: '/v1/accounts/{accountId}/blogs/{blogId}/articles'
    });
};

/**
 * Create a blog article
 * Creates an article on the blog. Publishing behavior:
 *
 * - `isPublished: false` keeps the article as a draft.
 * - A future `publishDate` schedules publication natively on the
 * platform; the platform publishes it at that time with no Zernio
 * queue involved.
 * - `seo.title` / `seo.description` map to Shopify's global `title_tag`
 * and `description_tag` metafields (the fields Shopify themes read for
 * the page title and meta description).
 *
 * Supported on Shopify (platform `shopify`). Accounts on platforms
 * without blogs support return 400; a blogs-capable platform that lacks
 * this specific operation returns 405.
 *
 */
export const createBlogArticle = <ThrowOnError extends boolean = false>(options: OptionsLegacyParser<CreateBlogArticleData, ThrowOnError>) => {
    return (options?.client ?? client).post<CreateBlogArticleResponse, CreateBlogArticleError, ThrowOnError>({
        ...options,
        url: '/v1/accounts/{accountId}/blogs/{blogId}/articles'
    });
};

/**
 * Get a blog article
 * Fetches a single article. An article addressed through a blog it does
 * not belong to is a 404 (code blog_article_not_found).
 *
 * Supported on Shopify (platform `shopify`). Accounts on platforms
 * without blogs support return 400; a blogs-capable platform that lacks
 * this specific operation returns 405.
 *
 */
export const getBlogArticle = <ThrowOnError extends boolean = false>(options: OptionsLegacyParser<GetBlogArticleData, ThrowOnError>) => {
    return (options?.client ?? client).get<GetBlogArticleResponse, GetBlogArticleError, ThrowOnError>({
        ...options,
        url: '/v1/accounts/{accountId}/blogs/{blogId}/articles/{articleId}'
    });
};

/**
 * Update a blog article
 * Partial-updates an article. Send any subset of the create fields
 * (`title`, `bodyHtml`, `handle`, `tags`, `author`, `excerpt`, `image`,
 * `seo`, `isPublished`, `publishDate`); at least one field is required
 * (an empty body returns 400). `isPublished` and `publishDate` behave as
 * on create: `isPublished: false` unpublishes back to a draft and a
 * future `publishDate` schedules publication natively on the platform.
 *
 * Supported on Shopify (platform `shopify`). Accounts on platforms
 * without blogs support return 400; a blogs-capable platform that lacks
 * this specific operation returns 405.
 *
 */
export const updateBlogArticle = <ThrowOnError extends boolean = false>(options: OptionsLegacyParser<UpdateBlogArticleData, ThrowOnError>) => {
    return (options?.client ?? client).patch<UpdateBlogArticleResponse, UpdateBlogArticleError, ThrowOnError>({
        ...options,
        url: '/v1/accounts/{accountId}/blogs/{blogId}/articles/{articleId}'
    });
};

/**
 * Delete a blog article
 * Deletes the article. The delete happens on the platform and is
 * permanent; Zernio stores nothing to restore it from.
 *
 * Supported on Shopify (platform `shopify`). Accounts on platforms
 * without blogs support return 400; a blogs-capable platform that lacks
 * this specific operation returns 405.
 *
 */
export const deleteBlogArticle = <ThrowOnError extends boolean = false>(options: OptionsLegacyParser<DeleteBlogArticleData, ThrowOnError>) => {
    return (options?.client ?? client).delete<DeleteBlogArticleResponse, DeleteBlogArticleError, ThrowOnError>({
        ...options,
        url: '/v1/accounts/{accountId}/blogs/{blogId}/articles/{articleId}'
    });
};

/**
 * Send a verification code
 * Generate a one-time code, deliver it to the recipient, and store only
 * its hash. Check the user-typed code with
 * POST /v1/verify/verifications/{verificationId}/check.
 *
 * Re-POSTing for the same (channel, to) while a verification is active
 * RESENDS a fresh code on the existing verification (200 with
 * `resend: true`) instead of creating a new one; resends are limited to
 * one per 60 seconds (429 with `retryAfterSeconds` inside the cooldown).
 * The stored brandName/codeLength/ttlMinutes win on a resend.
 *
 * Codes deliver by SMS from a phone number on your account (`from`
 * optional when you own exactly one SMS-enabled number) and the message
 * uses a fixed template. Each accepted send bills one verification fee
 * plus the standard message rate.
 *
 */
export const createVerification = <ThrowOnError extends boolean = false>(options: OptionsLegacyParser<CreateVerificationData, ThrowOnError>) => {
    return (options?.client ?? client).post<CreateVerificationResponse, CreateVerificationError, ThrowOnError>({
        ...options,
        url: '/v1/verify/verifications'
    });
};

/**
 * Get a verification
 * Current state of a verification. `status` is effective (a pending code
 * past its expiry reads as `expired`). Verification records are deleted
 * 24 hours after creation, after which this returns 404.
 *
 */
export const getVerification = <ThrowOnError extends boolean = false>(options: OptionsLegacyParser<GetVerificationData, ThrowOnError>) => {
    return (options?.client ?? client).get<GetVerificationResponse, GetVerificationError, ThrowOnError>({
        ...options,
        url: '/v1/verify/verifications/{verificationId}'
    });
};

/**
 * Check a verification code
 * Verify the code the user typed. Wrong, expired, and exhausted codes
 * answer 200 with `valid: false` and the settled `status` — only an
 * unknown id is a 404. A correct code consumes the verification
 * (single-use, `status: approved`) and fires the `verification.approved`
 * webhook; the 5th wrong attempt settles it as `max_attempts_reached`
 * and fires `verification.failed`.
 *
 */
export const checkVerification = <ThrowOnError extends boolean = false>(options: OptionsLegacyParser<CheckVerificationData, ThrowOnError>) => {
    return (options?.client ?? client).post<CheckVerificationResponse, CheckVerificationError, ThrowOnError>({
        ...options,
        url: '/v1/verify/verifications/{verificationId}/check'
    });
};